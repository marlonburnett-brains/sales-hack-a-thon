{
  "name": "lumenalta-hackathon",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.12.0",
  "pnpm": {
    "patchedDependencies": {
      "mastra": "patches/mastra.patch"
    }
  },
  "dependencies": {
    "@playwright/test": "^1.58.2"
  }
}
