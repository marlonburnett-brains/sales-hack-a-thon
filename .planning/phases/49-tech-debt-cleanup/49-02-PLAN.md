---
phase: 49-tech-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/deals/briefing-chat-panel.tsx
  - apps/web/src/components/touch/touch-stage-content.tsx
  - apps/web/src/components/touch/touch-page-shell.tsx
  - apps/web/src/lib/hooks/use-touch-preferences.ts
  - apps/web/src/app/(authenticated)/deals/[dealId]/touch/[touchNumber]/touch-page-client.tsx
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "BriefingChatPanel component no longer exists in the codebase"
    - "TouchStageContent no longer accepts a displayMode prop"
    - "TouchPageShell no longer renders an empty right panel placeholder"
    - "The web app builds without errors after all removals"
  artifacts:
    - path: "apps/web/src/components/touch/touch-stage-content.tsx"
      provides: "Simplified content renderer without dead display modes"
    - path: "apps/web/src/components/touch/touch-page-shell.tsx"
      provides: "Full-width touch layout without empty split panel"
    - path: "apps/web/src/lib/hooks/use-touch-preferences.ts"
      provides: "Simplified preferences hook without display mode or layout mode"
  key_links:
    - from: "apps/web/src/app/(authenticated)/deals/[dealId]/touch/[touchNumber]/touch-page-client.tsx"
      to: "apps/web/src/components/touch/touch-stage-content.tsx"
      via: "prop passing"
      pattern: "TouchStageContent"
---

<objective>
Remove orphaned UI components, dead display mode branches, and the empty split-panel placeholder from touch pages.

Purpose: Phase 45's PersistentDealChat replaced all deal-scoped chat functionality, making BriefingChatPanel orphaned. The inline-diff/side-by-side display modes were never implemented. The split-mode right panel is empty because chat lives at the layout level. All three are dead code that confuses future developers.
Output: Cleaner codebase with no placeholder toasts, dead type branches, or empty divs
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-tech-debt-cleanup/49-RESEARCH.md

<interfaces>
From apps/web/src/components/touch/touch-stage-content.tsx:
```typescript
interface TouchStageContentProps {
  touchType: string;
  stage: HitlStage;
  content: unknown;
  displayMode: "inline" | "inline-diff" | "side-by-side";  // To be simplified
}
```

From apps/web/src/lib/hooks/use-touch-preferences.ts:
```typescript
export type LayoutMode = "full" | "split";
export type DisplayMode = "inline" | "inline-diff" | "side-by-side";
export function useTouchPreferences(): {
  layoutMode: LayoutMode;
  updateLayoutMode: (mode: LayoutMode) => void;
  displayMode: DisplayMode;
  updateDisplayMode: (mode: DisplayMode) => void;
};
```

From apps/web/src/app/(authenticated)/deals/[dealId]/touch/[touchNumber]/touch-page-client.tsx:
```typescript
// Line 191: const { displayMode } = useTouchPreferences();
// Line 475: displayMode={displayMode}
// Line 515: displayMode={displayMode}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete orphaned BriefingChatPanel and remove dead display modes</name>
  <files>
    apps/web/src/components/deals/briefing-chat-panel.tsx,
    apps/web/src/components/touch/touch-stage-content.tsx,
    apps/web/src/app/(authenticated)/deals/[dealId]/touch/[touchNumber]/touch-page-client.tsx,
    apps/web/src/lib/hooks/use-touch-preferences.ts
  </files>
  <action>
1. **Delete** `apps/web/src/components/deals/briefing-chat-panel.tsx` entirely. This file is orphaned -- no imports exist anywhere in the codebase (confirmed by grep). This resolves the "Chat coming soon" toast and placeholder suggestion buttons.

2. **Simplify TouchStageContent** in `touch-stage-content.tsx`:
   - Remove `displayMode` from the `TouchStageContentProps` interface entirely
   - Remove the TODO comment about display modes (line ~17)
   - The component already renders the same way regardless of displayMode, so no rendering logic changes needed -- just remove the unused prop

3. **Update callers** in `touch-page-client.tsx`:
   - Remove `displayMode` from the destructured return of `useTouchPreferences()` (line ~191). Keep `layoutMode` if still used for anything, but note Task 2 may also remove it.
   - Remove `displayMode={displayMode}` prop from both `<TouchStageContent>` usages (lines ~475 and ~515)

4. **Simplify useTouchPreferences** in `use-touch-preferences.ts`:
   - Remove the `DisplayMode` type export
   - Remove the `displayMode` state and `updateDisplayMode` callback
   - Remove the `STORAGE_KEY_DISPLAY` constant
   - Remove `displayMode` and `updateDisplayMode` from the returned object
   - Keep `LayoutMode`, `layoutMode`, and `updateLayoutMode` for now (Task 2 will evaluate whether to remove them)
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && ! test -f apps/web/src/components/deals/briefing-chat-panel.tsx && ! grep -r "displayMode" apps/web/src/components/touch/touch-stage-content.tsx && ! grep -r "DisplayMode" apps/web/src/lib/hooks/use-touch-preferences.ts && echo "PASS"</automated>
  </verify>
  <done>briefing-chat-panel.tsx deleted. displayMode prop removed from TouchStageContent and all callers. DisplayMode type removed from preferences hook.</done>
</task>

<task type="auto">
  <name>Task 2: Remove empty split-panel placeholder and simplify touch layout</name>
  <files>
    apps/web/src/components/touch/touch-page-shell.tsx,
    apps/web/src/lib/hooks/use-touch-preferences.ts,
    apps/web/src/app/(authenticated)/deals/[dealId]/touch/[touchNumber]/touch-page-client.tsx
  </files>
  <action>
1. **Remove split-mode right panel** from `touch-page-shell.tsx`:
   - Remove the empty right panel placeholder div (lines ~111-115: the `{layoutMode === "split" && (...)}` block)
   - Remove the split/full layout toggle button entirely (the button that calls `updateLayoutMode`)
   - Remove the `layoutMode` prop from the component's props interface
   - Change the grid layout to always render full-width (remove the conditional `grid-cols-[1fr_360px]` class that activates in split mode)
   - Remove the import of icons used only for the toggle button if no longer needed

2. **Remove layout mode from preferences hook** in `use-touch-preferences.ts`:
   - Since split mode serves no purpose without right panel content, remove `LayoutMode` type, `layoutMode` state, `updateLayoutMode` callback, and `STORAGE_KEY_LAYOUT` constant
   - If the hook is now empty (no display mode from Task 1, no layout mode), delete the entire file
   - If deleted, remove the file's import from `touch-page-client.tsx` and `touch-page-shell.tsx`

3. **Update touch-page-client.tsx**:
   - Remove `layoutMode` from the destructured `useTouchPreferences()` call (or remove the call entirely if the hook was deleted)
   - Stop passing `layoutMode` to `<TouchPageShell>` if it was a prop

4. **Verify build**: Run `npx next build` or `npx tsc --noEmit` from `apps/web` to confirm no type errors from the removals.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/web && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>Split-mode placeholder removed. Layout toggle removed. Touch pages always render full-width. Web app compiles without type errors.</done>
</task>

</tasks>

<verification>
- `briefing-chat-panel.tsx` does not exist
- No `displayMode` references in touch components
- No empty split-mode placeholder in touch-page-shell.tsx
- `cd apps/web && npx tsc --noEmit` passes cleanly
</verification>

<success_criteria>
- Zero placeholder toasts ("Chat coming soon", "Coming in a future update") in the codebase
- Zero dead display mode type branches
- Zero empty right-panel divs in touch pages
- Web app builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/49-tech-debt-cleanup/49-02-SUMMARY.md`
</output>
