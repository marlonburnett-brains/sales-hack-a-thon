---
phase: 37-frontend-ui
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/classification/template-classification-controls.tsx
  - apps/web/src/components/classification/__tests__/template-classification-controls.test.tsx
  - apps/web/src/lib/template-utils.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/lib/actions/template-actions.ts
  - apps/agent/src/mastra/index.ts
  - apps/agent/src/mastra/__tests__/template-classify-route.test.ts
autonomous: true
requirements:
  - CLSF-01
  - CLSF-02
must_haves:
  truths:
    - The classify flow can represent a single required artifact choice for Touch 4 examples.
    - Artifact state clears automatically when classification moves away from Touch 4 examples.
    - The classify API persists `artifactType` for Touch 4 examples and clears stale values for every other case.
  artifacts:
    - path: apps/web/src/components/classification/template-classification-controls.tsx
      provides: Shared Touch 4 artifact-aware classify UI used by both classify surfaces.
    - path: apps/web/src/lib/api-client.ts
      provides: Template artifactType contract plus classify payload support.
    - path: apps/agent/src/mastra/index.ts
      provides: /templates/:id/classify persistence and validation for artifactType.
  key_links:
    - from: apps/web/src/components/classification/template-classification-controls.tsx
      to: apps/web/src/lib/actions/template-actions.ts
      via: Shared save payload with touchTypes and artifactType.
      pattern: classifyTemplateAction\(.*artifactType
    - from: apps/web/src/lib/actions/template-actions.ts
      to: apps/agent/src/mastra/index.ts
      via: POST /templates/:id/classify request body.
      pattern: artifactType
    - from: apps/web/src/lib/template-utils.ts
      to: apps/web/src/components/classification/template-classification-controls.tsx
      via: Shared classify control and label helper use the same artifact vocabulary.
      pattern: ARTIFACT_TYPE_LABELS|getClassificationLabel
---

<objective>
Make Touch 4 example classification artifact-aware across every existing classify entry point and persist the selected artifact type end to end.

Purpose: Satisfy CLSF-01/02 without letting the two classify surfaces drift or silently drop the new artifact selection.
Output: Shared classify controls, persisted `artifactType`, and targeted tests for the shared classify contract.

Execution note: Keep this plan tightly scoped to the shared Touch 4 classify contract only. Task 1 defines the shared artifact-aware UI model, and Task 2 carries `artifactType` through the current web/agent classify contract with a focused classify-route test. The final surface wiring happens in a follow-up plan so this plan stays under the file-count warning threshold.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-frontend-ui/37-CONTEXT.md
@.planning/phases/37-frontend-ui/37-RESEARCH.md
@apps/web/src/lib/template-utils.ts
@apps/web/src/lib/actions/template-actions.ts
@apps/web/src/lib/api-client.ts
@apps/agent/src/mastra/index.ts

<interfaces>
From `apps/web/src/lib/api-client.ts`:

```ts
export interface Template {
  id: string;
  touchTypes: string;
  contentClassification: string | null;
}

export async function classifyTemplate(
  id: string,
  data: { classification: "template" | "example"; touchTypes?: string[] },
): Promise<Template>;
```

From `apps/web/src/lib/actions/template-actions.ts`:

```ts
export async function classifyTemplateAction(
  templateId: string,
  classification: "template" | "example",
  touchTypes?: string[],
): Promise<{ success: boolean; error?: string }>;
```

From `apps/web/src/lib/template-utils.ts`:

```ts
export function getClassificationLabel(
  classification: string | null | undefined,
  touchTypes?: string[],
): string;

export const TOUCH_TYPES = [
  { value: "touch_1", label: "Touch 1" },
  { value: "touch_2", label: "Touch 2" },
  { value: "touch_3", label: "Touch 3" },
  { value: "touch_4", label: "Touch 4+" },
] as const;
```

Locked decisions to honor exactly:
- Use inline radio-card artifact options under Example + Touch 4, never a dropdown.
- Examples allow one touch total; templates keep multi-touch behavior.
- Clear `artifactType` immediately when Example leaves Touch 4.
- Block save with inline validation when Touch 4 Example lacks an artifact.
- Surface the saved artifact on the card/panel badge after save.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared artifact-aware classification controls</name>
  <files>apps/web/src/components/classification/template-classification-controls.tsx, apps/web/src/components/classification/__tests__/template-classification-controls.test.tsx, apps/web/src/lib/template-utils.ts</files>
  <behavior>
    - Test 1: Example mode exposes one-touch selection and only shows artifact radio cards after `touch_4` is selected.
    - Test 2: Switching away from `touch_4` or back to Template clears the selected artifact type immediately.
    - Test 3: Badge-label formatting includes `Proposal`, `Talk Track`, or `FAQ` for saved Touch 4 examples.
  </behavior>
  <action>Create a shared client component or helper that owns all Touch 4 classification derivation for both classify surfaces. Import `ARTIFACT_TYPES`, `ArtifactType`, and `ARTIFACT_TYPE_LABELS` from `@lumenalta/schemas` instead of hardcoding values. Keep template mode multi-select, but make example mode single-select and render visible radio-card artifact choices inline under the touch choice only when `classifyType === "example"` and the selected touch is `touch_4`. Add inline validation state and extend `getClassificationLabel()` so Touch 4 example badges include the artifact label.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/classification/__tests__/template-classification-controls.test.tsx</automated>
  </verify>
  <done>Both classify entry points can consume one shared artifact-aware model, and label formatting is ready to show artifact-specific Example badges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist artifact type through the classify contract</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/lib/actions/template-actions.ts, apps/agent/src/mastra/index.ts, apps/agent/src/mastra/__tests__/template-classify-route.test.ts</files>
  <behavior>
    - Test 1: Classify requests can send `artifactType` alongside Example `touchTypes`.
    - Test 2: Touch 4 Example classification rejects missing `artifactType`.
    - Test 3: Template and non-Touch-4 Example saves clear `artifactType` instead of leaving stale values behind.
  </behavior>
  <action>Update the shared web/agent classify contract so `Template` responses expose `artifactType` and `classifyTemplate()` / `classifyTemplateAction()` accept `artifactType?: ArtifactType | null`. Add a focused agent classify-route test file that exercises the persisted payload contract directly: valid Touch 4 Example requests keep the selected artifact, missing-artifact Touch 4 requests fail with validation, and Template/non-Touch-4 Example requests clear stale `artifactType`. In the agent route, validate the locked rule set: Example requires one touch, Touch 4 Example requires artifactType, non-Touch-4 Example and Template clear artifactType. Keep the existing `/templates/:id/classify` route family and current mutation path, but add any needed revalidation for `/settings/deck-structures/touch-4` so counts and confidence update after classification. Do not introduce parallel classify endpoints or new persistence surfaces in this plan.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/mastra/__tests__/template-classify-route.test.ts && pnpm --filter web build && pnpm --filter agent exec tsc --noEmit</automated>
  </verify>
  <done>The artifact choice survives save/reload, invalid Touch 4 Example payloads fail fast, and the classify API stays aligned with the frontend contract.</done>
</task>

</tasks>

<verification>
Run the targeted web Vitest suites, then verify `pnpm --filter web build` and `pnpm --filter agent exec tsc --noEmit` pass with the updated classify contract.
</verification>

<success_criteria>
- Shared classify controls expose Proposal / Talk Track / FAQ radio-card options only for Example + Touch 4.
- The classify contract persists `artifactType` for Touch 4 examples and clears it for every other classification state.
- Focused shared-control and classify-route tests cover the locked validation and clearing rules before surface wiring begins.
</success_criteria>

<output>
After completion, create `.planning/phases/37-frontend-ui/37-01-SUMMARY.md`
</output>
