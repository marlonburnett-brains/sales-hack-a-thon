---
phase: 37-frontend-ui
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/actions/deck-structure-actions.ts
  - apps/web/src/components/settings/touch-4-artifact-tabs.tsx
  - apps/web/src/components/settings/touch-type-detail-view.tsx
  - apps/web/src/components/settings/chat-bar.tsx
  - apps/web/src/components/settings/__tests__/touch-4-artifact-tabs.test.tsx
  - apps/web/src/components/settings/__tests__/touch-type-detail-view.test.tsx
  - apps/web/src/app/(authenticated)/settings/deck-structures/[touchType]/page.tsx
  - apps/web/src/app/(authenticated)/settings/deck-structures/[touchType]/__tests__/page.test.tsx
autonomous: true
requirements:
  - DECK-03
  - DECK-04
must_haves:
  truths:
    - Touch 4 settings renders Proposal, Talk Track, and FAQ as in-page tabs with Proposal selected by default.
    - Each tab exposes its own confidence and example-count context without opening the tab first.
    - Each tab loads and refines the matching Touch 4 artifact structure, even when that artifact has zero examples.
  artifacts:
    - path: apps/web/src/components/settings/touch-4-artifact-tabs.tsx
      provides: Touch 4 tab shell with confidence-aware triggers.
    - path: apps/web/src/components/settings/touch-type-detail-view.tsx
      provides: Artifact-aware detail, empty-state, and chat composition.
    - path: apps/web/src/app/(authenticated)/settings/deck-structures/[touchType]/page.tsx
      provides: Server route split between Touch 1-3 detail and Touch 4 artifact tabs.
  key_links:
    - from: apps/web/src/components/settings/touch-4-artifact-tabs.tsx
      to: apps/web/src/lib/actions/deck-structure-actions.ts
      via: Per-artifact summary/detail loading for `proposal`, `talk_track`, and `faq`.
      pattern: getDeckStructureAction\(.*artifactType
    - from: apps/web/src/components/settings/touch-4-artifact-tabs.tsx
      to: apps/web/src/components/settings/touch-type-detail-view.tsx
      via: Active Proposal / Talk Track / FAQ tab passes the selected artifact context into the detail panel.
      pattern: artifactType
    - from: apps/web/src/components/settings/touch-type-detail-view.tsx
      to: apps/web/src/components/settings/chat-bar.tsx
      via: Detail view forwards the active Touch 4 artifact context so chat refinement stays scoped to the open tab.
      pattern: artifactType
    - from: apps/web/src/components/settings/chat-bar.tsx
      to: apps/web/src/app/api/deck-structures/chat/route.ts
      via: POST body includes `artifactType` for Touch 4 chat.
      pattern: artifactType
    - from: apps/web/src/app/(authenticated)/settings/deck-structures/[touchType]/page.tsx
      to: apps/web/src/components/settings/touch-4-artifact-tabs.tsx
      via: Touch 4 route-only shell handoff.
      pattern: touch-4
---

<objective>
Turn the Touch 4 settings route into one artifact-aware page with tabbed Proposal / Talk Track / FAQ deck structures, confidence-at-a-glance navigation, and scoped chat refinement.

Purpose: Satisfy DECK-03/04 while keeping the existing Settings left nav and Touch 1-3 behavior intact.
Output: A Touch 4 tab shell, artifact-aware detail/chat wiring, and regression tests for tab confidence and empty-state behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-frontend-ui/37-CONTEXT.md
@.planning/phases/37-frontend-ui/37-RESEARCH.md
@apps/web/src/app/(authenticated)/settings/deck-structures/[touchType]/page.tsx
@apps/web/src/components/settings/touch-type-detail-view.tsx
@apps/web/src/components/settings/chat-bar.tsx
@apps/web/src/components/ui/tabs.tsx
@apps/web/src/lib/actions/deck-structure-actions.ts
@apps/web/src/lib/api-client.ts
@apps/web/src/app/api/deck-structures/chat/route.ts

<interfaces>
From `apps/web/src/lib/actions/deck-structure-actions.ts`:

```ts
export async function getDeckStructuresAction(): Promise<DeckStructureSummary[]>;

export async function getDeckStructureAction(
  touchType: string,
): Promise<DeckStructureDetail>;
```

From `apps/web/src/lib/api-client.ts`:

```ts
export interface DeckStructureSummary {
  touchType: string;
  artifactType?: string | null;
  exampleCount: number;
  confidence: number;
  confidenceColor: "green" | "yellow" | "red";
  confidenceLabel: string;
}

export interface DeckStructureDetail {
  touchType: string;
  artifactType?: string | null;
  structure: { sections: DeckSectionData[]; sequenceRationale: string };
  exampleCount: number;
  confidence: number;
  confidenceColor: "green" | "yellow" | "red";
  confidenceLabel: string;
  chatMessages: DeckChatMessageData[];
}
```

From `apps/web/src/components/settings/chat-bar.tsx`:

```ts
interface ChatBarProps {
  touchType: string;
  onStructureUpdate: (...) => void;
  disabled?: boolean;
  initialMessages?: DeckChatMessageData[];
}
```

Locked decisions to honor exactly:
- Keep the existing left nav; no new sidebar entries for artifacts.
- Use in-page top tabs for Proposal, Talk Track, and FAQ, defaulting to Proposal.
- Put confidence context in the tab strip, not only the active panel.
- Give every artifact its own empty state and Templates CTA.
- Keep chat refinement active even when `exampleCount === 0`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an artifact-aware Touch 4 tab shell and tests</name>
  <files>apps/web/src/lib/actions/deck-structure-actions.ts, apps/web/src/components/settings/touch-4-artifact-tabs.tsx, apps/web/src/components/settings/__tests__/touch-4-artifact-tabs.test.tsx</files>
  <behavior>
    - Test 1: Touch 4 settings renders Proposal, Talk Track, and FAQ tabs with Proposal active by default.
    - Test 2: Every tab trigger shows confidence/example context from its own artifact summary.
    - Test 3: Switching tabs requests or reuses the matching artifact detail instead of generic `touch_4` data.
  </behavior>
  <action>Extend the deck-structure server actions so detail/infer reads accept optional `artifactType` and keep using `URLSearchParams` through the existing web client path. Create a dedicated `touch-4-artifact-tabs.tsx` client shell that uses the existing Radix `Tabs` wrapper, loads all three Touch 4 summaries up front, defaults to `proposal`, and renders each trigger with at-a-glance confidence context plus low-confidence wording when appropriate. Reuse backend-provided confidence fields; do not invent client-side heuristics.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/settings/__tests__/touch-4-artifact-tabs.test.tsx</automated>
  </verify>
  <done>The Touch 4 route has a tested artifact-tab shell with Proposal / Talk Track / FAQ triggers and per-tab confidence context.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make detail and chat components artifact-aware, including empty tabs</name>
  <files>apps/web/src/components/settings/touch-type-detail-view.tsx, apps/web/src/components/settings/chat-bar.tsx, apps/web/src/components/settings/__tests__/touch-type-detail-view.test.tsx</files>
  <behavior>
    - Test 1: `TouchTypeDetailView` fetches the correct Touch 4 artifact detail and renders artifact-specific empty-state copy plus a Templates CTA.
    - Test 2: Empty Touch 4 tabs still render an enabled chat surface instead of disabled helper text.
    - Test 3: `ChatBar` sends `artifactType` in Touch 4 requests so refinement stays scoped to the active artifact tab.
  </behavior>
  <action>Add optional `artifactType` and artifact-aware copy props to `TouchTypeDetailView` and `ChatBar`. For Touch 4 tabs, call `getDeckStructureAction(touchType, artifactType)` and keep `ChatBar` active even when `exampleCount === 0`; only preserve the disabled empty-chat behavior for Touch 1-3 if needed. Update the empty-state body so each artifact panel explicitly names Proposal / Talk Track / FAQ and links users back to `/templates` for classification. In `chat-bar.tsx`, include `artifactType` in the `/api/deck-structures/chat` payload whenever the active panel is a Touch 4 artifact tab.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/settings/__tests__/touch-type-detail-view.test.tsx</automated>
  </verify>
  <done>Each Touch 4 artifact panel loads its own structure and confidence state, and chat refinement stays usable and correctly scoped even when no examples exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route Touch 4 to the new tabbed settings experience</name>
  <files>apps/web/src/app/(authenticated)/settings/deck-structures/[touchType]/page.tsx, apps/web/src/app/(authenticated)/settings/deck-structures/[touchType]/__tests__/page.test.tsx</files>
  <behavior>
    - Test 1: `/settings/deck-structures/touch-4` renders the tab shell instead of the generic single-detail view.
    - Test 2: Touch 1, Touch 2, and Touch 3 still render the existing detail view unchanged.
  </behavior>
  <action>Keep the page itself server-side. Branch only on the `touch-4` slug: render the new Touch 4 artifact tab shell there, while leaving Touch 1-3 on the existing `TouchTypeDetailView`. Add or extend a page-level route test that renders the page for `touch-4` and for one representative non-Touch-4 slug so the branch itself is asserted directly, not inferred through component-only suites. Preserve the current heading structure and left-nav route family so Settings navigation does not expand into separate artifact pages.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/app/\(authenticated\)/settings/deck-structures/\[touchType\]/__tests__/page.test.tsx src/components/settings/__tests__/touch-4-artifact-tabs.test.tsx src/components/settings/__tests__/touch-type-detail-view.test.tsx && pnpm --filter web build</automated>
  </verify>
  <done>The Touch 4 settings route becomes one tabbed page with artifact-specific panels, while the rest of Settings keeps the current navigation and route structure.</done>
</task>

</tasks>

<verification>
Run the new settings Vitest suites, then confirm `pnpm --filter web build` succeeds with the route split and artifact-aware chat payloads.
</verification>

<success_criteria>
- `/settings/deck-structures/touch-4` shows three in-page tabs with Proposal selected by default.
- Each tab trigger exposes its own confidence context and example count.
- Proposal, Talk Track, and FAQ tabs each load/chat against their own artifact-specific deck structure, even when the tab has zero examples.
</success_criteria>

<output>
After completion, create `.planning/phases/37-frontend-ui/37-02-SUMMARY.md`
</output>
