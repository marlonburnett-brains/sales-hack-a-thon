---
phase: 37-frontend-ui
plan: "03"
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - apps/web/src/components/template-card.tsx
  - apps/web/src/components/slide-viewer/classification-panel.tsx
  - apps/web/src/components/__tests__/template-card.test.tsx
  - apps/web/src/components/slide-viewer/__tests__/classification-panel.test.tsx
autonomous: true
requirements:
  - CLSF-01
  - CLSF-02
must_haves:
  truths:
    - User can choose exactly one artifact type when classifying an Example as Touch 4 from either existing classify surface.
    - Artifact type controls stay hidden for templates and for non-Touch-4 examples in both classify surfaces.
    - Saved Example badges show the Touch 4 artifact choice without reopening classification UI.
  artifacts:
    - path: apps/web/src/components/template-card.tsx
      provides: Template list classify surface wired to the shared Touch 4 classify control.
    - path: apps/web/src/components/slide-viewer/classification-panel.tsx
      provides: Slide viewer classify surface wired to the shared Touch 4 classify control.
    - path: apps/web/src/components/__tests__/template-card.test.tsx
      provides: Regression coverage for TemplateCard Touch 4 artifact behavior.
  key_links:
    - from: apps/web/src/components/template-card.tsx
      to: apps/web/src/components/classification/template-classification-controls.tsx
      via: Template card delegates Example/Template classify state to the shared control.
      pattern: template-classification-controls
    - from: apps/web/src/components/slide-viewer/classification-panel.tsx
      to: apps/web/src/components/classification/template-classification-controls.tsx
      via: Classification panel reuses the shared Touch 4 control instead of duplicating checkbox logic.
      pattern: template-classification-controls
    - from: apps/web/src/components/template-card.tsx
      to: apps/web/src/lib/template-utils.ts
      via: Saved classification badge formatting includes the selected artifact label.
      pattern: getClassificationLabel\(.*artifactType
---

<objective>
Wire both existing classify surfaces to the shared Touch 4 artifact-aware control and surface the saved artifact label in-place.

Purpose: Finish CLSF-01/02 UI behavior without letting `TemplateCard` and `ClassificationPanel` drift after the shared contract work in 37-01.
Output: Shared-control adoption in both classify surfaces plus regression tests for visibility, validation, single-touch example selection, and saved artifact badges.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-frontend-ui/37-CONTEXT.md
@.planning/phases/37-frontend-ui/37-RESEARCH.md
@.planning/phases/37-frontend-ui/37-01-PLAN.md
@apps/web/src/components/classification/template-classification-controls.tsx
@apps/web/src/components/template-card.tsx
@apps/web/src/components/slide-viewer/classification-panel.tsx
@apps/web/src/lib/template-utils.ts

<interfaces>
From `apps/web/src/components/classification/template-classification-controls.tsx`:

```ts
// Shared classify control created in 37-01. Reuse its props/state contract directly;
// do not recreate checkbox-only logic in the consuming surfaces.
```

Locked decisions to honor exactly:
- Use inline radio-card artifact options under Example + Touch 4, never a dropdown.
- Examples allow one touch total; templates keep multi-touch behavior.
- Clear `artifactType` immediately when Example leaves Touch 4.
- Block save with inline validation when Touch 4 Example lacks an artifact.
- Surface the saved artifact on the card/panel badge after save.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire `TemplateCard` to the shared Touch 4 classify flow</name>
  <files>apps/web/src/components/template-card.tsx, apps/web/src/components/__tests__/template-card.test.tsx</files>
  <behavior>
    - Test 1: `TemplateCard` shows artifact radio cards only for Example + Touch 4, blocks save until one is chosen, and renders the saved artifact in the badge.
    - Test 2: Example editing in `TemplateCard` no longer allows simultaneous `touch_1` + `touch_4` selections.
  </behavior>
  <action>Replace the duplicated checkbox-only example classification logic in `template-card.tsx` with the shared Touch 4-aware control from 37-01. Preserve the existing compact inline classify UX, but honor the locked rules exactly: Example uses one touch only, Touch 4 reveals radio-card artifact options, deselecting Touch 4 clears artifact immediately, inline validation blocks save when artifact is missing, and the saved badge text or adjacent badge visibly includes the artifact label. Update the existing Vitest suite instead of relying on manual verification.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/__tests__/template-card.test.tsx</automated>
  </verify>
  <done>`TemplateCard` uses the shared classify control and visibly reflects the persisted Touch 4 artifact selection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire `ClassificationPanel` to the shared Touch 4 classify flow</name>
  <files>apps/web/src/components/slide-viewer/classification-panel.tsx, apps/web/src/components/slide-viewer/__tests__/classification-panel.test.tsx</files>
  <behavior>
    - Test 1: `ClassificationPanel` follows the same visibility, clearing, and validation rules as `TemplateCard`.
    - Test 2: Saved artifact labeling is visible from the slide viewer classify surface after save.
  </behavior>
  <action>Replace the duplicated checkbox-only example classification logic in `classification-panel.tsx` with the shared Touch 4-aware control from 37-01. Match `TemplateCard` behavior for one-touch Example selection, artifact-only-under-Touch-4 visibility, immediate clearing, inline validation, and visible saved artifact labeling. Keep the current slide viewer layout and classify entry point intact; this task is wiring only, not a new UX surface.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/slide-viewer/__tests__/classification-panel.test.tsx src/components/__tests__/template-card.test.tsx</automated>
  </verify>
  <done>`ClassificationPanel` matches the shared Touch 4 classify behavior and stays aligned with `TemplateCard` under test.</done>
</task>

</tasks>

<verification>
Run the classify-surface Vitest suites and confirm both existing classify entry points now expose the same Touch 4 artifact behavior and saved badge labeling.
</verification>

<success_criteria>
- Touch 4 Example classification exposes Proposal / Talk Track / FAQ radio cards inline in both classify surfaces.
- Artifact controls never appear outside Example + Touch 4.
- Saved Example badges visibly include the chosen artifact type after refresh.
</success_criteria>

<output>
After completion, create `.planning/phases/37-frontend-ui/37-03-SUMMARY.md`
</output>
