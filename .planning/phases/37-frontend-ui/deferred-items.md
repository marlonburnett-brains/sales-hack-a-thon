- 2026-03-07: `pnpm --filter web build` fails in `apps/web/src/components/settings/touch-4-artifact-tabs.tsx:28` with `Cannot find namespace 'JSX'`. This file is unrelated to plan `37-01` and was already modified outside this task, so it was not changed here.
- 2026-03-07: `pnpm --filter agent exec tsc --noEmit` currently fails on multiple pre-existing issues outside plan `37-01`, including `packages/schemas/index.ts` `.ts` extension imports, `apps/agent/src/lib/mcp-client.ts` private method access, and older test typing failures. These were not introduced by this plan, so they were left untouched.
