---
phase: 69-medium-complexity-tutorials-library-settings
plan: 02
type: execute
wave: 2
depends_on: [69-01]
files_modified: []
autonomous: true
requirements: [TUT-08, TUT-09, TUT-10, TUT-11, TUT-12]

must_haves:
  truths:
    - "All 5 tutorials have TTS narration audio files generated from script narration text"
    - "All 5 tutorials have timing manifests with accurate per-step durations"
    - "All 5 tutorials render as complete MP4 videos with synchronized screenshots and audio"
  artifacts:
    - path: "apps/tutorials/audio/template-library/timing.json"
      provides: "Timing manifest for Template Library tutorial"
      contains: "durationMs"
    - path: "apps/tutorials/audio/slide-library/timing.json"
      provides: "Timing manifest for Slide Library tutorial"
      contains: "durationMs"
    - path: "apps/tutorials/audio/deck-structures/timing.json"
      provides: "Timing manifest for Deck Structures tutorial"
      contains: "durationMs"
    - path: "apps/tutorials/audio/agent-prompts/timing.json"
      provides: "Timing manifest for Agent Prompts tutorial"
      contains: "durationMs"
    - path: "apps/tutorials/audio/atlus-integration/timing.json"
      provides: "Timing manifest for AtlusAI Integration tutorial"
      contains: "durationMs"
    - path: "apps/tutorials/videos/template-library.mp4"
      provides: "Rendered Template Library tutorial video"
    - path: "apps/tutorials/videos/slide-library.mp4"
      provides: "Rendered Slide Library tutorial video"
    - path: "apps/tutorials/videos/deck-structures.mp4"
      provides: "Rendered Deck Structures tutorial video"
    - path: "apps/tutorials/videos/agent-prompts.mp4"
      provides: "Rendered Agent Prompts tutorial video"
    - path: "apps/tutorials/videos/atlus-integration.mp4"
      provides: "Rendered AtlusAI Integration tutorial video"
  key_links:
    - from: "apps/tutorials/fixtures/*/script.json"
      to: "apps/tutorials/audio/*/timing.json"
      via: "TTS pipeline reads narration text, produces audio + timing manifest"
      pattern: "durationMs"
    - from: "apps/tutorials/audio/*/timing.json"
      to: "apps/tutorials/videos/*.mp4"
      via: "Remotion render reads timing manifest for frame counts"
      pattern: "timing.json"
---

<objective>
Generate TTS narration audio and render MP4 videos for all 5 medium-complexity tutorials: Template Library, Slide Library, Deck Structures, Agent Prompts, and AtlusAI Integration.

Purpose: Produce final tutorial videos by running the existing TTS and Remotion render pipelines against the scripts and screenshots from Plan 01.
Output: 5 timing manifests, 5 sets of WAV audio files, and 5 MP4 tutorial videos (all gitignored artifacts).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-medium-complexity-tutorials-library-settings/69-CONTEXT.md
@.planning/phases/69-medium-complexity-tutorials-library-settings/69-01-SUMMARY.md
@.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate TTS audio for all 5 tutorials</name>
  <files></files>
  <action>
Run the existing Kokoro TTS pipeline for each tutorial sequentially. The pipeline reads script.json narration text, generates per-step WAV files, and produces timing.json manifests.

```bash
pnpm --filter tutorials tts template-library
pnpm --filter tutorials tts slide-library
pnpm --filter tutorials tts deck-structures
pnpm --filter tutorials tts agent-prompts
pnpm --filter tutorials tts atlus-integration
```

After each TTS run, verify:
1. WAV files exist for each step (count matches script step count)
2. timing.json exists with durationMs entries for each step
3. No zero-duration entries (would cause Remotion render issues)

All outputs go to `apps/tutorials/audio/<tutorial-name>/` (gitignored). No source commits needed.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && for t in template-library slide-library deck-structures agent-prompts atlus-integration; do if [ -f "apps/tutorials/audio/$t/timing.json" ]; then zeros=$(node -e "const t=require('./apps/tutorials/audio/$t/timing.json'); console.log(t.filter(e=>e.durationMs===0).length)"); echo "$t: timing.json exists, zero-duration entries: $zeros"; else echo "$t: MISSING timing.json"; fi; done</automated>
  </verify>
  <done>
    - 5 timing manifests exist with accurate per-step durations
    - All WAV audio files generated (one per script step per tutorial)
    - Zero zero-duration entries across all 5 tutorials
  </done>
</task>

<task type="auto">
  <name>Task 2: Render MP4 videos for all 5 tutorials</name>
  <files></files>
  <action>
Run the existing Remotion render pipeline for each tutorial sequentially. The pipeline reads screenshots from output/, audio from audio/, timing from audio/timing.json, and script metadata to compose MP4 with visual effects (zoom, callout, cursor, transitions, intro/outro slates).

```bash
pnpm --filter tutorials render template-library
pnpm --filter tutorials render slide-library
pnpm --filter tutorials render deck-structures
pnpm --filter tutorials render agent-prompts
pnpm --filter tutorials render atlus-integration
```

Use `--concurrency=2` for M1 Pro memory safety (this is the default in the render pipeline).

After each render, verify the MP4 file exists and has reasonable file size (> 1MB). All outputs go to `apps/tutorials/videos/` (gitignored). No source commits needed.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && for t in template-library slide-library deck-structures agent-prompts atlus-integration; do if [ -f "apps/tutorials/videos/$t.mp4" ]; then size=$(ls -la "apps/tutorials/videos/$t.mp4" | awk '{print $5}'); echo "$t.mp4: $(($size / 1048576))MB"; else echo "$t.mp4: MISSING"; fi; done</automated>
  </verify>
  <done>
    - 5 MP4 tutorial videos exist in apps/tutorials/videos/
    - Each video has reasonable file size (> 1MB, typically 5-15MB)
    - Videos include intro slate, step scenes with narration, visual effects, and outro slate
  </done>
</task>

</tasks>

<verification>
1. All 5 timing manifests exist with non-zero durations
2. All 5 MP4 videos exist with reasonable file sizes
3. No failed TTS or render steps
</verification>

<success_criteria>
- 5 complete MP4 tutorial videos covering Template Library, Slide Library, Deck Structures, Agent Prompts, and AtlusAI Integration
- Each video has synchronized narration audio with screenshots
- All visual effects (zoom, callout, cursor, transitions, slates) render correctly
- Total video content approximately 8-12 minutes across all 5 tutorials
</success_criteria>

<output>
After completion, create `.planning/phases/69-medium-complexity-tutorials-library-settings/69-02-SUMMARY.md`
</output>
