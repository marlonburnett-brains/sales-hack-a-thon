---
phase: 69-medium-complexity-tutorials-library-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tutorials/fixtures/shared/templates.json
  - apps/tutorials/fixtures/shared/slides.json
  - apps/tutorials/scripts/mock-server.ts
  - apps/tutorials/src/helpers/route-mocks.ts
  - apps/tutorials/fixtures/template-library/script.json
  - apps/tutorials/fixtures/template-library/overrides.json
  - apps/tutorials/fixtures/template-library/stages/ingesting.json
  - apps/tutorials/fixtures/template-library/stages/ingested.json
  - apps/tutorials/fixtures/slide-library/script.json
  - apps/tutorials/fixtures/slide-library/overrides.json
  - apps/tutorials/fixtures/deck-structures/script.json
  - apps/tutorials/fixtures/deck-structures/overrides.json
  - apps/tutorials/fixtures/deck-structures/stages/list.json
  - apps/tutorials/fixtures/deck-structures/stages/detail.json
  - apps/tutorials/fixtures/deck-structures/stages/chat-loading.json
  - apps/tutorials/fixtures/deck-structures/stages/chat-refined.json
  - apps/tutorials/fixtures/agent-prompts/script.json
  - apps/tutorials/fixtures/agent-prompts/overrides.json
  - apps/tutorials/fixtures/agent-prompts/stages/list.json
  - apps/tutorials/fixtures/agent-prompts/stages/view-published.json
  - apps/tutorials/fixtures/agent-prompts/stages/draft-created.json
  - apps/tutorials/fixtures/agent-prompts/stages/draft-edited.json
  - apps/tutorials/fixtures/agent-prompts/stages/published.json
  - apps/tutorials/fixtures/agent-prompts/stages/version-history.json
  - apps/tutorials/fixtures/agent-prompts/stages/rolled-back.json
  - apps/tutorials/fixtures/atlus-integration/script.json
  - apps/tutorials/fixtures/atlus-integration/overrides.json
  - apps/tutorials/fixtures/atlus-integration/stages/disconnected.json
  - apps/tutorials/fixtures/atlus-integration/stages/connected.json
  - apps/tutorials/fixtures/atlus-integration/stages/browse.json
  - apps/tutorials/fixtures/atlus-integration/stages/search-results.json
  - apps/tutorials/fixtures/atlus-integration/stages/ingesting.json
  - apps/tutorials/fixtures/atlus-integration/stages/ingested.json
  - apps/tutorials/capture/template-library.spec.ts
  - apps/tutorials/capture/slide-library.spec.ts
  - apps/tutorials/capture/deck-structures.spec.ts
  - apps/tutorials/capture/agent-prompts.spec.ts
  - apps/tutorials/capture/atlus-integration.spec.ts
autonomous: true
requirements: [TUT-08, TUT-09, TUT-10, TUT-11, TUT-12]

must_haves:
  truths:
    - "Template Library tutorial captures screenshots showing template grid, new template registration, ingestion progress, and ingestion results"
    - "Slide Library tutorial captures screenshots showing slide grid, slide detail/metadata, and search results"
    - "Deck Structures tutorial captures screenshots showing touch type list, structure detail with confidence scores, chat refinement with loading state, and updated structure"
    - "Agent Prompts tutorial captures screenshots showing agent list, published prompt, draft creation, edit, publish, version history, and rollback"
    - "AtlusAI Integration tutorial captures screenshots showing disconnected state, connection flow, browse results, search results, ingestion progress, and completion"
  artifacts:
    - path: "apps/tutorials/fixtures/shared/templates.json"
      provides: "Shared content library base with 3-5 pre-ingested templates"
      contains: "ingestionStatus.*completed"
    - path: "apps/tutorials/fixtures/shared/slides.json"
      provides: "Shared slides from ingested templates"
      min_lines: 30
    - path: "apps/tutorials/fixtures/template-library/script.json"
      provides: "Template Library tutorial script"
      contains: "mockStage.*ingesting"
    - path: "apps/tutorials/fixtures/slide-library/script.json"
      provides: "Slide Library tutorial script"
      contains: "narration"
    - path: "apps/tutorials/fixtures/deck-structures/script.json"
      provides: "Deck Structures tutorial script"
      contains: "mockStage.*chat-loading"
    - path: "apps/tutorials/fixtures/agent-prompts/script.json"
      provides: "Agent Prompts tutorial script with 10-12 steps"
      contains: "mockStage.*rolled-back"
    - path: "apps/tutorials/fixtures/atlus-integration/script.json"
      provides: "AtlusAI Integration tutorial script"
      contains: "mockStage.*disconnected"
    - path: "apps/tutorials/capture/template-library.spec.ts"
      provides: "Playwright capture spec for Template Library"
      contains: "TUTORIAL_ID"
    - path: "apps/tutorials/capture/slide-library.spec.ts"
      provides: "Playwright capture spec for Slide Library"
      contains: "TUTORIAL_ID"
    - path: "apps/tutorials/capture/deck-structures.spec.ts"
      provides: "Playwright capture spec for Deck Structures"
      contains: "TUTORIAL_ID"
    - path: "apps/tutorials/capture/agent-prompts.spec.ts"
      provides: "Playwright capture spec for Agent Prompts"
      contains: "TUTORIAL_ID"
    - path: "apps/tutorials/capture/atlus-integration.spec.ts"
      provides: "Playwright capture spec for AtlusAI Integration"
      contains: "TUTORIAL_ID"
  key_links:
    - from: "apps/tutorials/scripts/mock-server.ts"
      to: "stage fixtures"
      via: "loadStageFixtures for templates, deck-structures, agent-configs, discovery routes"
      pattern: "loadStageFixtures.*template|deck|agent|discovery"
    - from: "apps/tutorials/fixtures/shared/templates.json"
      to: "apps/tutorials/fixtures/shared/slides.json"
      via: "templates reference slideCount > 0 for slide library SSR"
      pattern: "slideCount"
    - from: "capture specs"
      to: "script.json files"
      via: "generic capture loop iterates script.steps"
      pattern: "TUTORIAL_ID"
---

<objective>
Author scripts, fixtures, mock server extensions, and capture specs for 5 medium-complexity tutorials: Template Library, Slide Library, Deck Structures, Agent Prompts, and AtlusAI Integration.

Purpose: Create all content and mock infrastructure needed to capture deterministic screenshots for 5 tutorials covering library management, deck intelligence, agent configuration, and external integration features.
Output: 5 tutorial scripts, shared content library fixtures, per-tutorial stage fixtures, 9 stage-aware mock server routes, and 5 Playwright capture specs producing screenshots.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-medium-complexity-tutorials-library-settings/69-CONTEXT.md
@.planning/phases/69-medium-complexity-tutorials-library-settings/69-RESEARCH.md
@.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-01-SUMMARY.md

# Reference files for patterns
@apps/tutorials/capture/deals.spec.ts (capture spec pattern to replicate)
@apps/tutorials/fixtures/deals/script.json (script.json pattern to replicate)
@apps/tutorials/fixtures/deals/overrides.json (overrides pattern)
@apps/tutorials/fixtures/deal-chat/stages/chat-initial.json (stage fixture pattern)
@apps/tutorials/fixtures/briefing/stages/generating.json (async stage pattern)
@apps/tutorials/scripts/mock-server.ts (mock server — routes to make stage-aware)
@apps/tutorials/src/helpers/route-mocks.ts (browser-side mocks — may need extensions)
@apps/tutorials/fixtures/shared/users.json (shared fixture pattern)
@apps/tutorials/fixtures/shared/companies.json (shared fixture data)
@apps/tutorials/fixtures/shared/deals.json (shared fixture data)

# Web app pages for correct SSR patterns and route shapes
@apps/web/src/app/(authenticated)/templates/page.tsx
@apps/web/src/app/(authenticated)/slides/page.tsx
@apps/web/src/app/(authenticated)/settings/deck-structures/page.tsx
@apps/web/src/app/(authenticated)/settings/agents/page.tsx
@apps/web/src/app/(authenticated)/settings/agents/[agentId]/page.tsx
@apps/web/src/app/(authenticated)/settings/integrations/page.tsx
@apps/web/src/app/(authenticated)/discovery/page.tsx

# API client types for fixture shapes
@apps/web/src/lib/api-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared content fixtures, mock server stage-awareness, and all fixture data</name>
  <files>
    apps/tutorials/fixtures/shared/templates.json,
    apps/tutorials/fixtures/shared/slides.json,
    apps/tutorials/scripts/mock-server.ts,
    apps/tutorials/src/helpers/route-mocks.ts,
    apps/tutorials/fixtures/template-library/script.json,
    apps/tutorials/fixtures/template-library/overrides.json,
    apps/tutorials/fixtures/template-library/stages/ingesting.json,
    apps/tutorials/fixtures/template-library/stages/ingested.json,
    apps/tutorials/fixtures/slide-library/script.json,
    apps/tutorials/fixtures/slide-library/overrides.json,
    apps/tutorials/fixtures/deck-structures/script.json,
    apps/tutorials/fixtures/deck-structures/overrides.json,
    apps/tutorials/fixtures/deck-structures/stages/list.json,
    apps/tutorials/fixtures/deck-structures/stages/detail.json,
    apps/tutorials/fixtures/deck-structures/stages/chat-loading.json,
    apps/tutorials/fixtures/deck-structures/stages/chat-refined.json,
    apps/tutorials/fixtures/agent-prompts/script.json,
    apps/tutorials/fixtures/agent-prompts/overrides.json,
    apps/tutorials/fixtures/agent-prompts/stages/list.json,
    apps/tutorials/fixtures/agent-prompts/stages/view-published.json,
    apps/tutorials/fixtures/agent-prompts/stages/draft-created.json,
    apps/tutorials/fixtures/agent-prompts/stages/draft-edited.json,
    apps/tutorials/fixtures/agent-prompts/stages/published.json,
    apps/tutorials/fixtures/agent-prompts/stages/version-history.json,
    apps/tutorials/fixtures/agent-prompts/stages/rolled-back.json,
    apps/tutorials/fixtures/atlus-integration/script.json,
    apps/tutorials/fixtures/atlus-integration/overrides.json,
    apps/tutorials/fixtures/atlus-integration/stages/disconnected.json,
    apps/tutorials/fixtures/atlus-integration/stages/connected.json,
    apps/tutorials/fixtures/atlus-integration/stages/browse.json,
    apps/tutorials/fixtures/atlus-integration/stages/search-results.json,
    apps/tutorials/fixtures/atlus-integration/stages/ingesting.json,
    apps/tutorials/fixtures/atlus-integration/stages/ingested.json
  </files>
  <action>
**Part A: Shared content library fixtures**

Create `fixtures/shared/templates.json` with 3-5 pre-ingested templates:
- Template 1: "Lumenalta Introduction Deck" (touch_1, ingested, slideCount: 12)
- Template 2: "Cloud Transformation Proposal" (touch_3, ingested, slideCount: 18)
- Template 3: "Customer Success Case Studies" (touch_1/touch_2, ingested, slideCount: 8)
- Template 4: "Technical Architecture Overview" (touch_4, ingested, slideCount: 15)
- Template 5: "Q2 Pipeline Review" (internal, not ingested, slideCount: 0)

All ingested templates must have `ingestionStatus: "completed"`, `slideCount > 0`, `lastIngestedAt` set, and realistic `contentClassification`. Match the Template type from `api-client.ts`.

Create `fixtures/shared/slides.json` with 15-20 slides across the ingested templates. Each slide needs: `contentText` (title + body matching slide purpose), `classificationJson` with industry/solutionPillar/persona/funnelStage/contentType fields, mix of `reviewStatus` values ("approved", "unreviewed", "needs_correction"), `slideObjectId` field (can be null). Match the Slide type shapes from `api-client.ts`.

**Part B: Mock server route modifications (9 routes)**

Make these routes stage-aware in `scripts/mock-server.ts` by checking `loadStageFixtures(tutorialName, currentStage)` for domain-specific fields. Follow the exact pattern from RESEARCH.md code examples:

1. `GET /templates` — check `stageFixtures.templates`, else fall back to `fixtures.templates ?? []`
2. `GET /deck-structures` — check `stageFixtures.deckStructures`, else return `[]`
3. `GET /deck-structures/:touchType` — check `stageFixtures.deckStructureDetail`, else return minimal mock
4. `GET /agent-configs` — check `stageFixtures.agentConfigs`, else return `[]`
5. `GET /agent-configs/:agentId` — check `stageFixtures.agentConfigDetail`, else return minimal mock
6. `GET /agent-configs/:agentId/versions` — check `stageFixtures.agentConfigVersions`, else return `[]`
7. `GET /discovery/access-check` — check `stageFixtures.discoveryAccess`, else return `{ hasAccess: true }`
8. `GET /discovery/browse` — check `stageFixtures.discoveryBrowse`, else return `{ documents: [], ingestedHashes: [] }`
9. `POST /discovery/search` — check `stageFixtures.discoverySearch`, else return `{ results: [], ingestedHashes: [] }`

Also check if `route-mocks.ts` needs browser-side mock extensions for deck-structures chat POST (already mocked returning plaintext — verify it works with stage switching for structure re-fetch).

**Part C: All 5 tutorial scripts + overrides + stage fixtures**

Author each script following TutorialScriptSchema. Each step has: id, url, narration, waitFor, actions, and optional zoomTarget/callout/cursorTarget/mockStage/emotion/delayMs. Use conversational tone consistent with Phase 67/68. Zoom ~30-40% of steps, callouts ~20-30%, cursor on all click/hover steps.

**Template Library (TUT-08) — ~12 steps:**
1. Navigate to /templates, show existing 4 ingested templates in grid
2. Point out ingestion status and slide counts
3. Click "Add Template" button
4. Fill Google Slides URL field
5. Select touch type classification
6. Submit form (mock POST)
7. mockStage: "ingesting" — navigate/reload showing progress on new template
8. mockStage: "ingested" — navigate/reload showing completed with results
9. View ingestion results: AI descriptions, element maps, classifications
10. Narrate cross-reference ("Templates you register here will appear in the Slide Library")
11. Return to grid showing 5 templates now
12. Outro narration

Stage fixtures: `ingesting.json` (templates array with new template showing progress), `ingested.json` (templates array with new template completed, showing descriptions/classifications).

**Slide Library (TUT-09) — ~10 steps:**
1. Navigate to /slides, show slide grid with slides from ingested templates
2. Point out classification badges and review status
3. Click a slide to view detail/metadata
4. Show classification breakdown (industry, persona, content type)
5. Navigate to search
6. Enter search query
7. Show search results
8. Click a result to view similar slides
9. Narrate cross-reference ("Slides come from templates you've registered in the Template Library")
10. Outro narration

Overrides: Additional slides or classification overrides if needed.

**Deck Structures (TUT-10) — ~12 steps:**
1. Navigate to /settings/deck-structures (auto-redirects to /settings/deck-structures/touch-1)
2. mockStage: "detail" — show Touch 1 structure with sections, confidence score (0.82, "High")
3. Point out confidence badge and section flow
4. Narrate confidence scores ("Confidence scores show how well each section maps to real templates in your library")
5. Navigate to sidebar link for another touch type (e.g., touch-3)
6. Show Touch 3 structure with different sections and confidence
7. Navigate back to touch-1 for chat refinement demo
8. Type a chat message (e.g., "Move the Executive Summary before the Introduction")
9. mockStage: "chat-loading" with delayMs: 1500 — show loading state
10. mockStage: "chat-refined" — show updated structure with reordered sections
11. Narrate the result ("The AI reordered the sections based on your request")
12. Outro narration

Stage fixtures: `list.json` (deckStructures array for sidebar), `detail.json` (deckStructureDetail for touch-1 with 5 sections, confidence 0.82), `chat-loading.json` (same detail but with loading indicator state), `chat-refined.json` (deckStructureDetail with Executive Summary moved before Introduction).

**Agent Prompts (TUT-11) — ~12 steps:**
1. Navigate to /settings/agents, mockStage: "list" — show 6-8 agents grouped by family
2. Point out agent families (pre-call, deck-generation, etc.)
3. Click Pre-Call Research Agent to drill in, mockStage: "view-published" — show published prompt
4. Read the current prompt text (formal tone)
5. Click "Create Draft", mockStage: "draft-created" — show draft editor with same text
6. Edit the prompt to change tone from formal to conversational, mockStage: "draft-edited" — show meaningful edit
7. Click "Publish", mockStage: "published" — show newly published version
8. Navigate to version history, mockStage: "version-history" — show 3-4 versions
9. Point out version numbers and change summaries
10. Click "Rollback" on a previous version, mockStage: "rolled-back" — show restored version
11. Narrate the value ("Version history lets you safely experiment with prompts knowing you can always roll back")
12. Outro narration

Fixture prompt content should be realistic system prompt instructions. The meaningful edit: change "Use formal, analytical language" to "Use a warm, conversational tone. Speak as a trusted advisor rather than an analyst." This demonstrates WHY prompt management matters.

Stage fixtures: 7 stages as listed in RESEARCH.md (list, view-published, draft-created, draft-edited, published, version-history, rolled-back). Each must include BOTH agentConfigDetail and agentConfigVersions fields (Pitfall 5 from RESEARCH).

**AtlusAI Integration (TUT-12) — ~12 steps:**
1. mockStage: "disconnected" — navigate to /discovery, show "AtlusAI Not Available" with connect button
2. Navigate to /settings/integrations to show integration status
3. Narrate the OAuth flow ("Clicking Connect would redirect you to AtlusAI to authorize access")
4. mockStage: "connected" — navigate back to /discovery, show browse view
5. Show document browse results (5-8 documents with titles and metadata)
6. Click a document to see details
7. Navigate to search
8. Enter search query, mockStage: "search-results" — show search results
9. Select a document for ingestion
10. Click ingest, mockStage: "ingesting" — show progress
11. mockStage: "ingested" — show completion
12. Outro narration

Stage fixtures: disconnected (discoveryAccess: hasAccess false), connected (hasAccess true + discoveryBrowse with 5-8 documents), browse (same as connected), search-results (discoverySearch with results), ingesting (discoveryBrowse with ingest progress), ingested (discoveryBrowse with ingested hash).
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && node -e "
const fs = require('fs');
const shared = ['fixtures/shared/templates.json','fixtures/shared/slides.json'];
const tutorials = ['template-library','slide-library','deck-structures','agent-prompts','atlus-integration'];
const base = 'apps/tutorials/';
let ok = true;
for (const f of shared) { if (!fs.existsSync(base+f)) { console.log('MISSING: '+f); ok=false; } }
for (const t of tutorials) {
  if (!fs.existsSync(base+'fixtures/'+t+'/script.json')) { console.log('MISSING: fixtures/'+t+'/script.json'); ok=false; }
  const script = JSON.parse(fs.readFileSync(base+'fixtures/'+t+'/script.json','utf8'));
  console.log(t+': '+script.steps.length+' steps');
}
const templates = JSON.parse(fs.readFileSync(base+'fixtures/shared/templates.json','utf8'));
const slides = JSON.parse(fs.readFileSync(base+'fixtures/shared/slides.json','utf8'));
console.log('Shared templates: '+templates.length+', slides: '+slides.length);
const ingested = templates.filter(t => t.slideCount > 0);
console.log('Ingested templates (slideCount>0): '+ingested.length);
if (ingested.length < 2) { console.log('FAIL: Need at least 2 ingested templates'); ok=false; }
if (slides.length < 15) { console.log('FAIL: Need at least 15 slides'); ok=false; }
console.log(ok ? 'ALL CHECKS PASSED' : 'SOME CHECKS FAILED');
"</automated>
  </verify>
  <done>
    - fixtures/shared/templates.json has 3-5 templates (at least 3 with slideCount > 0 and ingestionStatus "completed")
    - fixtures/shared/slides.json has 15-20 slides with classification metadata
    - mock-server.ts has 9 routes made stage-aware (templates, deck-structures x2, agent-configs x3, discovery x3)
    - All 5 tutorial scripts exist with correct step counts (10-14 each)
    - All stage fixture directories populated with correct stage files
    - All overrides.json files exist per tutorial
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture specs and end-to-end screenshot verification</name>
  <files>
    apps/tutorials/capture/template-library.spec.ts,
    apps/tutorials/capture/slide-library.spec.ts,
    apps/tutorials/capture/deck-structures.spec.ts,
    apps/tutorials/capture/agent-prompts.spec.ts,
    apps/tutorials/capture/atlus-integration.spec.ts
  </files>
  <action>
Create 5 Playwright capture specs following the exact pattern from `capture/deals.spec.ts`:
- Import generic capture loop helpers
- Set TUTORIAL_ID constant matching fixture directory name
- beforeEach: set auth cookies + call mockBrowserAPIs with stageGetter closure
- Test: iterate script.steps, navigate/wait/act/capture per step
- Each spec is generic — only TUTORIAL_ID changes

Capture spec file names:
- `capture/template-library.spec.ts` (TUTORIAL_ID = "template-library")
- `capture/slide-library.spec.ts` (TUTORIAL_ID = "slide-library")
- `capture/deck-structures.spec.ts` (TUTORIAL_ID = "deck-structures")
- `capture/agent-prompts.spec.ts` (TUTORIAL_ID = "agent-prompts")
- `capture/atlus-integration.spec.ts` (TUTORIAL_ID = "atlus-integration")

After creating specs, run each capture and verify:
1. `pnpm --filter tutorials capture template-library` — verify screenshot count matches script step count
2. `pnpm --filter tutorials capture slide-library` — verify screenshot count
3. `pnpm --filter tutorials capture deck-structures` — verify screenshot count
4. `pnpm --filter tutorials capture agent-prompts` — verify screenshot count
5. `pnpm --filter tutorials capture atlus-integration` — verify screenshot count

If any capture fails, debug and fix the script/fixtures/mock-server as needed (same auto-fix pattern as Phase 68 Plan 01). Common fixes: selector mismatches, missing stage fixture fields, mock server route not returning expected shape.

NOTE: First run may be slow due to cold Next.js compilation (~30s per new route). Second runs will be fast with cached compilations. Use 120s timeout per tutorial capture.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && for t in template-library slide-library deck-structures agent-prompts atlus-integration; do count=$(ls apps/tutorials/output/$t/step-*.png 2>/dev/null | wc -l); echo "$t: $count screenshots"; done</automated>
  </verify>
  <done>
    - All 5 capture specs exist and follow the generic capture loop pattern
    - template-library captures 10-14 screenshots
    - slide-library captures 8-12 screenshots
    - deck-structures captures 10-14 screenshots
    - agent-prompts captures 10-12 screenshots
    - atlus-integration captures 10-14 screenshots
    - Total: ~50-64 screenshots across all 5 tutorials
  </done>
</task>

</tasks>

<verification>
1. All 5 tutorial fixture directories populated with script.json, overrides.json, and stage subdirectories
2. Shared templates.json has 3+ ingested templates, slides.json has 15+ slides
3. Mock server has 9 stage-aware routes (grep for loadStageFixtures in modified routes)
4. All 5 capture specs produce correct screenshot counts
5. No hardcoded fixture data in capture specs — all data in JSON fixture files
</verification>

<success_criteria>
- 5 complete tutorial scripts with narration, visual effects, and stage transitions
- Shared content library fixtures provide realistic "lived-in" feel across all tutorials
- All stage-based workflows function correctly: ingestion 3-stage (TUT-08, TUT-12), chat refinement with loading (TUT-10), agent lifecycle 7-stage (TUT-11), connection toggle (TUT-12)
- All 5 Playwright captures produce deterministic screenshots with correct counts
</success_criteria>

<output>
After completion, create `.planning/phases/69-medium-complexity-tutorials-library-settings/69-01-SUMMARY.md`
</output>
