---
phase: 44-agent-management-ui
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - apps/web/src/app/(authenticated)/settings/agents/[agentId]/page.tsx
  - apps/web/src/components/settings/agent-detail.tsx
  - apps/web/src/components/settings/agent-prompt-editor.tsx
  - apps/web/src/components/settings/publish-dialog.tsx
  - apps/web/src/components/settings/baseline-editor.tsx
  - apps/web/src/app/(authenticated)/settings/agents/baseline/page.tsx
autonomous: true
requirements: [MGMT-02, MGMT-04]

must_haves:
  truths:
    - "User can navigate to an agent detail page and see current baseline (read-only) and role prompt (editable)"
    - "User can edit the role prompt in a plain textarea with monospace font and auto-resize"
    - "Saving creates a draft version without going live"
    - "Sticky publish bar appears when draft exists, with Publish and Discard Draft buttons"
    - "Publishing shows confirmation dialog with diff summary and optional change note"
    - "User can edit the shared baseline from a dedicated page with blast-radius warning"
  artifacts:
    - path: "apps/web/src/app/(authenticated)/settings/agents/[agentId]/page.tsx"
      provides: "Agent detail page with server-side data fetch"
      min_lines: 15
    - path: "apps/web/src/components/settings/agent-detail.tsx"
      provides: "Tabbed detail view (Prompts + History tabs)"
      min_lines: 80
    - path: "apps/web/src/components/settings/agent-prompt-editor.tsx"
      provides: "Textarea prompt editing with save/draft"
      min_lines: 60
    - path: "apps/web/src/components/settings/publish-dialog.tsx"
      provides: "Publish confirmation dialog with diff + change note"
      min_lines: 40
    - path: "apps/web/src/components/settings/baseline-editor.tsx"
      provides: "Baseline prompt editor with blast-radius warning"
      min_lines: 50
    - path: "apps/web/src/app/(authenticated)/settings/agents/baseline/page.tsx"
      provides: "Baseline editing page"
      min_lines: 10
  key_links:
    - from: "apps/web/src/components/settings/agent-prompt-editor.tsx"
      to: "saveDraftAction"
      via: "server action call on Save button"
      pattern: "saveDraftAction"
    - from: "apps/web/src/components/settings/publish-dialog.tsx"
      to: "publishAction"
      via: "server action call on Publish confirm"
      pattern: "publishAction"
    - from: "apps/web/src/components/settings/baseline-editor.tsx"
      to: "publishBaselineAction"
      via: "server action for baseline publish"
      pattern: "publishBaselineAction"
---

<objective>
Build the agent detail page with prompt editing, draft/publish workflow, and baseline editing.

Purpose: Enables direct text editing of agent role prompts with a safe draft/publish lifecycle, and provides a dedicated baseline editing experience with clear blast-radius communication.
Output: Working agent detail page at /settings/agents/[agentId] and baseline editor at /settings/agents/baseline.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-agent-management-ui/44-CONTEXT.md
@.planning/phases/44-agent-management-ui/44-RESEARCH.md
@.planning/phases/44-agent-management-ui/44-01-SUMMARY.md

@apps/web/src/lib/api-client.ts
@apps/web/src/lib/actions/agent-config-actions.ts
@apps/web/src/components/settings/agent-list.tsx

<interfaces>
<!-- Types from Plan 01 api-client additions -->

From apps/web/src/lib/api-client.ts (added in Plan 01):
```typescript
export interface AgentConfigDetail {
  agentId: string;
  name: string;
  responsibility: string;
  family: string;
  isShared: boolean;
  publishedVersion: {
    id: string; version: number; baselinePrompt: string; rolePrompt: string;
    compiledPrompt: string | null; changeSummary: string | null;
    publishedAt: string | null; publishedBy: string | null;
  } | null;
  draft: { id: string; version: number; rolePrompt: string; createdAt: string; } | null;
}
```

From apps/web/src/lib/actions/agent-config-actions.ts (added in Plan 01):
```typescript
export async function getAgentConfigAction(agentId: string): Promise<AgentConfigDetail>;
export async function saveDraftAction(agentId: string, data: { rolePrompt: string; expectedVersion?: number; userId?: string }): Promise<...>;
export async function publishAction(agentId: string, data: { changeSummary?: string; userId?: string }): Promise<...>;
export async function discardDraftAction(agentId: string): Promise<{ success: boolean }>;
export async function saveBaselineDraftAction(data: { baselinePrompt: string; userId?: string }): Promise<...>;
export async function publishBaselineAction(data: { changeSummary?: string; userId?: string }): Promise<{ agentsUpdated: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent detail page with prompt editing and draft workflow</name>
  <files>apps/web/src/app/(authenticated)/settings/agents/[agentId]/page.tsx, apps/web/src/components/settings/agent-detail.tsx, apps/web/src/components/settings/agent-prompt-editor.tsx</files>
  <action>
**[agentId]/page.tsx** -- Server component. Extract `agentId` from params. Call `getAgentConfigAction(agentId)`. Call `getAgentConfigVersionsAction(agentId)`. Pass both to `<AgentDetail config={config} versions={versions} />`. Add a back link to `/settings/agents` at top.

**agent-detail.tsx** -- "use client" component. Props: `{ config: AgentConfigDetail, versions: AgentConfigVersionItem[] }`. Layout:
1. Header: agent name (h2), family badge, responsibility text
2. Draft publish bar (conditionally shown): When `config.draft` exists, render a sticky bar at top with amber background, text "You have unpublished changes", and two buttons: [Publish] (opens PublishDialog) and [Discard Draft] (calls `discardDraftAction` with confirmation via AlertDialog, then `router.refresh()`).
3. shadcn Tabs with two tabs: "Prompts" and "History" (History tab will be populated in Plan 03; for now render a placeholder div with "Version history coming soon").
4. Prompts tab content: renders `<AgentPromptEditor>`.

**agent-prompt-editor.tsx** -- "use client" component. Props: `{ agentId: string, baselinePrompt: string, rolePrompt: string, draftRolePrompt?: string, publishedVersion: number }`.

Layout (stacked, per user decision):
1. **Baseline section (read-only):** Label "Baseline Prompt", monospace pre-formatted text in a read-only div with bg-slate-50 border, truncated to ~10 lines with expand toggle. "Edit Baseline" link (using lucide Edit icon) pointing to `/settings/agents/baseline`.

2. **Role prompt section (editable):** Label "Role Prompt". Plain `<textarea>` with:
   - `font-mono text-sm` styling
   - Auto-resize: use a `useEffect` that sets `textarea.style.height = textarea.scrollHeight + "px"` on value change
   - Initial value: `draftRolePrompt ?? rolePrompt` (show draft content if draft exists)
   - Track `isDirty` state (compare current value to initial)
   - Min height 200px

3. **Save button:** Below textarea. "Save Draft" button, disabled when `!isDirty` or while saving (loading state with spinner). On click: call `saveDraftAction(agentId, { rolePrompt: currentValue, expectedVersion: publishedVersion })`. On success: show sonner toast "Draft saved", call `router.refresh()`. On error: show error toast.

Use `useRouter` for refresh after mutations. Use `useTransition` for pending states.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Agent detail page renders at /settings/agents/[agentId] with baseline (read-only) and role prompt (editable textarea). Save Draft creates an unpublished version. Draft publish bar shows when draft exists with Publish and Discard Draft buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Publish dialog, discard confirmation, and baseline editor page</name>
  <files>apps/web/src/components/settings/publish-dialog.tsx, apps/web/src/components/settings/baseline-editor.tsx, apps/web/src/app/(authenticated)/settings/agents/baseline/page.tsx</files>
  <action>
**publish-dialog.tsx** -- "use client" component using shadcn AlertDialog. Props: `{ agentId: string, currentRolePrompt: string, draftRolePrompt: string, onPublished: () => void }`.

Content:
1. Title: "Publish Agent Update"
2. Body: Simple line-by-line diff display showing changes between `currentRolePrompt` and `draftRolePrompt`. For now, implement a basic diff: split both by newlines, highlight added lines (green bg) and removed lines (red bg). No external library needed here -- Plan 03 will install `diff` for the full history comparison. A simple approach: show "Current" section with removed content highlighted and "Updated" section with added content highlighted, or a unified view comparing lines.
3. Optional change note: text input for `changeSummary` field ("Describe what changed (optional)")
4. Buttons: Cancel and "Publish Changes" (calls `publishAction(agentId, { changeSummary, userId })` then `onPublished()`)
5. Loading state on publish button while in transit

**baseline-editor.tsx** -- "use client" component. Props: `{ baselinePrompt: string }`.

Layout:
1. Warning card at top: amber/yellow background with AlertTriangle icon. Text: "Editing the shared baseline affects all 19 agents. Changes to the baseline will recompile every agent's system prompt when published."
2. Plain textarea (monospace, auto-resize, same pattern as agent-prompt-editor) initialized with current `baselinePrompt`
3. `isDirty` tracking
4. "Save Baseline Draft" button -> calls `saveBaselineDraftAction({ baselinePrompt })`
5. "Publish Baseline" button (only shown when a baseline draft exists) -> opens a confirmation AlertDialog stating "This will update the compiled prompt for all 19 agents. This action cannot be undone." with optional change note field. On confirm: calls `publishBaselineAction({ changeSummary })`, shows toast "Baseline published. All agents updated.", `router.refresh()`.

**baseline/page.tsx** -- Server component. Fetch any agent config to get the current baseline prompt (call `getAgentConfigAction("company-researcher")` or similar, extract `publishedVersion.baselinePrompt`). Render back link to `/settings/agents`, heading "Edit Shared Baseline", and `<BaselineEditor baselinePrompt={baseline} />`.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit -p apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Publish dialog shows diff summary and change note field before publishing. Baseline editor page at /settings/agents/baseline shows blast-radius warning, textarea for editing, and publish confirmation. All draft/publish lifecycle operations work end-to-end.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes for apps/web
2. Navigate to /settings/agents/{any-agent-id} -- detail page loads with baseline (read-only) and role prompt (editable)
3. Edit role prompt, click Save Draft -- draft created, publish bar appears
4. Click Publish -- dialog shows diff summary and change note, confirm publishes and clears draft
5. Click Discard Draft -- confirmation dialog, draft deleted
6. Navigate to /settings/agents/baseline -- baseline editor loads with warning card
7. Edit baseline, save draft, publish -- all 19 agents recompiled
</verification>

<success_criteria>
- Agent detail page renders with tabbed layout (Prompts active, History placeholder)
- Role prompt editable in monospace textarea with auto-resize
- Save creates draft, publish bar appears with Publish + Discard buttons
- Publish confirmation shows diff and accepts change note
- Baseline editor accessible from list page and detail page with blast-radius warning
- All mutations use server actions with sonner toast feedback
</success_criteria>

<output>
After completion, create `.planning/phases/44-agent-management-ui/44-02-SUMMARY.md`
</output>
