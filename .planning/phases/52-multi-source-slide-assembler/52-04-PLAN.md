---
phase: 52-multi-source-slide-assembler
plan: 04
type: execute
wave: 4
depends_on: ['52-03']
files_modified:
  - apps/agent/src/generation/multi-source-assembler.ts
  - apps/agent/src/generation/__tests__/multi-source-assembler.test.ts
autonomous: true
gap_closure: true
requirements:
  - FR-4.4
  - NFR-3
  - NFR-6
must_haves:
  truths:
    - "Secondary-source slides in assembled decks preserve design-bearing content beyond text boxes, including images, tables, and non-text shapes, in the intended order."
    - "Secondary slide theme, backgrounds, and element-level styling are fully preserved."
  artifacts:
    - path: "apps/agent/src/generation/multi-source-assembler.ts"
      provides: "Exhaustive high-fidelity element reconstructor"
    - path: "apps/agent/src/generation/__tests__/multi-source-assembler.test.ts"
      provides: "Tests for mapping of text styling, fills, borders, themes, and backgrounds"
  key_links:
    - from: "apps/agent/src/generation/multi-source-assembler.ts"
      to: "apps/agent/src/generation/__tests__/multi-source-assembler.test.ts"
      via: "UpdateShapePropertiesRequest, UpdateTextStyleRequest, etc."
      pattern: "UpdateShapeProperties|UpdateTextStyle"
---

<objective>
Close the gap from 52-03 by replacing the structurally insufficient element reconstructor with a true high-fidelity cloning mechanism for secondary slides. Since the Google Slides REST API lacks native cross-presentation slide copy and Apps Script adds blocked infrastructure complexity, this requires an exhaustive property mapper that faithfully clones page backgrounds, shape fills/outlines, text formatting, and paragraph styles via batched API operations.

Purpose: 52-VERIFICATION.md failed because text styling, backgrounds, and structure were lost. A complete mapping resolves visual distortion.
Output: Updated multi-source assembler that issues chained create and update commands (CreateShape + UpdateShapeProperties + InsertText + UpdateTextStyle) to replicate elements with full visual fidelity.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/agent/src/generation/multi-source-assembler.ts
@apps/agent/src/generation/__tests__/multi-source-assembler.test.ts
</context>

<tasks>

<task type="auto" gap_closure="true">
  <name>Task 1: Add high-fidelity background mapping</name>
  <files>apps/agent/src/generation/multi-source-assembler.ts, apps/agent/src/generation/__tests__/multi-source-assembler.test.ts</files>
  <action>
    - In `buildSecondarySlideRequests`, generate an `updatePageProperties` request targeting the new `targetSlideId`.
    - Map the `sourceSlide.pageProperties.pageBackgroundFill` (both `solidFill` and `stretchedPictureFill`).
    - Use `fields: "pageBackgroundFill"` and verify the test suite expects it.
    
    Reference existing code: Existing structure uses `buildSecondarySlideRequests` which currently only maps elements, not page backgrounds.
    Gap reason: "missing backgrounds"
  </action>
  <verify>
    <automated>cd apps/agent && npx vitest run src/generation/__tests__/multi-source-assembler.test.ts</automated>
  </verify>
  <done>Page backgrounds are accurately mapped in the API request chain.</done>
</task>

<task type="auto" gap_closure="true">
  <name>Task 2: Add high-fidelity shape and text style mapping</name>
  <files>apps/agent/src/generation/multi-source-assembler.ts, apps/agent/src/generation/__tests__/multi-source-assembler.test.ts</files>
  <action>
    - Expand `buildShapeRequests` to generate `updateShapeProperties` for fills, outlines, and shadows.
    - Rather than just extracting raw text, iterate over `element.shape.text.textElements`.
    - For each text element that contains a `textRun`:
      1. Issue `insertText` at the correct index.
      2. Issue `updateTextStyle` covering that specific run's length, mapping fonts, colors, bold, italic.
    - Generate `updateParagraphStyle` and `createParagraphBullets` for paragraph markers.
    - Use `*` for the `fields` parameter to map all extracted style keys safely, or enumerate them carefully.
    
    Reference existing code: Current `buildShapeRequests` extracts plain text via `extractTextContent` and ignores properties.
    Gap reason: "loses text styling, fills, borders, themes... severely overlapping text"
  </action>
  <verify>
    <automated>cd apps/agent && npx vitest run src/generation/__tests__/multi-source-assembler.test.ts</automated>
  </verify>
  <done>Shapes are accurately recreated with fills, outlines, and exact text formatting ranges.</done>
</task>

<task type="auto" gap_closure="true">
  <name>Task 3: Add high-fidelity table and image style mapping</name>
  <files>apps/agent/src/generation/multi-source-assembler.ts, apps/agent/src/generation/__tests__/multi-source-assembler.test.ts</files>
  <action>
    - Expand `buildTableRequests` to map column widths (`updateTableColumnProperties`), row heights (`updateTableRowProperties`), and cell backgrounds/borders (`updateTableCellProperties`, `updateTableBorderProperties`).
    - Expand `buildImageRequests` to include `updateImageProperties` mapping crop window, outlines, and shadows.
    
    Reference existing code: Current image/table builders only recreate dimensions and raw layout.
    Gap reason: "distorted element types"
  </action>
  <verify>
    <automated>cd apps/agent && npx vitest run src/generation/__tests__/multi-source-assembler.test.ts</automated>
  </verify>
  <done>Images and tables preserve internal layout, borders, crops, and cell properties.</done>
</task>

</tasks>

<verification>
Review the test coverage to confirm `UpdateShapePropertiesRequest`, `UpdateTextStyleRequest`, `UpdatePagePropertiesRequest`, and `UpdateParagraphStyleRequest` exist in the request payloads returned by the assembly logic.
</verification>

<success_criteria>
- Reconstructed slides preserve backgrounds, colors, outlines, text styles, and paragraph layouts
- Test coverage passes for style generation
- No new external NPM dependencies or Apps Script deployments are introduced
</success_criteria>

<output>
After completion, create `.planning/phases/52-multi-source-slide-assembler/52-04-SUMMARY.md`
</output>
