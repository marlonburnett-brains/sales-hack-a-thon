---
phase: 52-multi-source-slide-assembler
plan: 05
type: execute
wave: 5
depends_on: ['52-04']
files_modified:
  - apps/agent/src/generation/__tests__/multi-source-assembler.test.ts
autonomous: true
requirements: [FR-4.4, NFR-3, NFR-6]
gap_closure: true
must_haves:
  truths:
    - "Secondary slide theme, backgrounds, and element-level styling are fully preserved."
    - "Automated tests verify that updateShapeProperties is called when elements have shape properties."
    - "Automated tests verify that updateTextStyle and updateParagraphStyle are called when text elements have formatting."
  artifacts:
    - path: "apps/agent/src/generation/__tests__/multi-source-assembler.test.ts"
      provides: "Style mapping assertions for the multi-source assembler"
  key_links:
    - from: "apps/agent/src/generation/__tests__/multi-source-assembler.test.ts"
      to: "apps/agent/src/generation/multi-source-assembler.ts"
      via: "batchUpdate assertions"
      pattern: "updateShapeProperties|updateTextStyle|updateParagraphStyle"
---

<objective>
Close verification gaps by updating the multi-source assembler test suite to mock and assert styling properties.

Purpose: The comprehensive style property mapper was implemented in Plan 04, but the tests were not updated to mock or assert these properties. This plan adds explicit test expectations to ensure style mapping is fully tested and guards against regressions.
Output: Updated test suite with assertions for `updateShapeProperties`, `updateTextStyle`, and `updateParagraphStyle`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/agent/src/generation/__tests__/multi-source-assembler.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update test helpers with style properties</name>
  <files>apps/agent/src/generation/__tests__/multi-source-assembler.test.ts</files>
  <action>
    Update the element mock generators to include styling data so that the style mapper functions have properties to map.
    - Ensure `shapeProperties` (e.g. `shapeBackgroundFill`) are present in `makeShapeElement` and `makeTextShapeElement`.
    - Add `textStyle` (e.g. `foregroundColor`, `bold`, `fontFamily`) and `paragraphStyle` (e.g. `alignment`, `lineSpacing`) to the `textRun` and `paragraphMarker` in `makeTextShapeElement` and `makeTableElement`.
    - Add `tableColumnProperties` (like `columnWidth`) to `makeTableElement` where appropriate.
    Reference the gap reason: "Mock elements used in the batchUpdate assertion test lack style properties. Consequently, style update requests are never asserted, making it a stub for style generation coverage."
  </action>
  <verify>
    <automated>cd apps/agent && npx vitest run src/generation/__tests__/multi-source-assembler.test.ts -t "groups slides" -x</automated>
  </verify>
  <done>Mock generators produce objects that have the necessary styling fields populated for testing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add assertions for style update requests</name>
  <files>apps/agent/src/generation/__tests__/multi-source-assembler.test.ts</files>
  <action>
    Update the main `assembleMultiSourceDeck` batchUpdate assertion test (e.g., "copies the primary source, rebuilds supported secondary elements...") to explicitly expect the styling requests.
    - Add assertions expecting `updateShapeProperties` requests following `createShape` or `createTable` (or where appropriate based on the mock elements).
    - Add assertions expecting `updateTextStyle` and `updateParagraphStyle` requests following `insertText` operations.
    Reference the missing item: "Add explicit test expectations for updateShapeProperties, updateTextStyle, updateParagraphStyle, etc., to ensure the style mapper works correctly."
  </action>
  <verify>
    <automated>cd apps/agent && npx vitest run src/generation/__tests__/multi-source-assembler.test.ts -x</automated>
  </verify>
  <done>The test suite actively asserts that styling properties are mapped and sent in the batchUpdate requests.</done>
</task>

</tasks>

<verification>
Ensure the test suite passes and that explicit assertions exist for styling endpoints (`updateShapeProperties`, `updateTextStyle`, `updateParagraphStyle`).
</verification>

<success_criteria>
- The test suite successfully runs all multi-source assembler tests.
- `updateShapeProperties`, `updateTextStyle`, and `updateParagraphStyle` assertions are present in the test file.
- The verification gap is completely addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/52-multi-source-slide-assembler/52-05-SUMMARY.md`
</output>
