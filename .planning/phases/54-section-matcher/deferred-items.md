## Deferred Items

- Full `npx vitest run` remains red due to unrelated pre-existing failures in deal chat, auth, Atlus search, and deck-structure route tests.
- Full `npx tsc --noEmit` remains red due to unrelated pre-existing repo-wide TypeScript issues outside Phase 54 scope.
