---
phase: 43-named-agent-architecture
plan: "03"
type: execute
wave: 3
depends_on:
  - 43-02
files_modified:
  - apps/agent/src/mastra/workflows/pre-call-workflow.ts
  - apps/agent/src/mastra/workflows/touch-1-workflow.ts
  - apps/agent/src/mastra/workflows/touch-2-workflow.ts
  - apps/agent/src/mastra/workflows/touch-3-workflow.ts
  - apps/agent/src/mastra/workflows/touch-4-workflow.ts
  - apps/agent/src/lib/slide-selection.ts
  - apps/agent/src/lib/proposal-assembly.ts
  - apps/agent/src/mastra/__tests__/workflow-agent-versioning.test.ts
  - apps/agent/src/lib/__tests__/workflow-agent-coverage.test.ts
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
must_haves:
  truths:
    - "Seller-facing and core workflow LLM calls now run through named agents instead of inline prompt authority."
    - "Suspend/resume workflows keep the same prompt versions after approval boundaries."
    - "Shared job families such as slide selection and proposal assembly still produce the same structured outputs while using the named-agent layer."
  artifacts:
    - path: "apps/agent/src/mastra/workflows/touch-4-workflow.ts"
      provides: "Named-agent execution with prompt-version pinning across resume boundaries"
    - path: "apps/agent/src/mastra/workflows/pre-call-workflow.ts"
      provides: "Pre-call generation steps routed through named agents"
    - path: "apps/agent/src/lib/slide-selection.ts"
      provides: "Shared slide-selection family routed through the named-agent executor"
  key_links:
    - from: "apps/agent/src/mastra/workflows/touch-1-workflow.ts"
      to: "apps/agent/src/lib/agent-executor.ts"
      via: "named pager agent invocation"
      pattern: "invoke.*first-contact|agentVersion"
    - from: "apps/agent/src/mastra/workflows/touch-4-workflow.ts"
      to: "workflow state"
      via: "pinned version ids carried across suspend/resume"
      pattern: "agentVersion|versionId"
---

<objective>
Move the main seller-facing workflows and shared generation helpers onto the named-agent runtime seam.

Purpose: This is the high-value migration that makes the formal agent architecture real in the product flows users rely on today.
Output: Pre-call and Touch 1-4 prompt execution routed through named agents, plus workflow version pinning for long-running approval flows.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-named-agent-architecture/43-CONTEXT.md
@.planning/phases/43-named-agent-architecture/43-RESEARCH.md

<interfaces>
From `apps/agent/src/lib/slide-selection.ts`:
```typescript
export interface SlideSelectionParams {
  touchType: "touch_2" | "touch_3";
  companyName: string;
  industry: string;
  capabilityAreas?: string[];
  context?: string;
  priorTouchOutputs?: string[];
  limit?: number;
}

export interface SlideSelectionResult {
  selectedSlideIds: string[];
  slideOrder: string[];
  personalizationNotes: string;
  capabilityAreas?: string[];
  candidateSlides: SlideSearchResult[];
}
```

From `apps/agent/src/mastra/workflows/touch-1-workflow.ts`:
```typescript
resumeSchema: z.object({
  decision: z.enum(["approved", "edited"]),
  editedContent: PagerContentLlmSchema.optional(),
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route seller-facing workflows through named agents</name>
  <files>apps/agent/src/mastra/workflows/pre-call-workflow.ts, apps/agent/src/mastra/workflows/touch-1-workflow.ts, apps/agent/src/mastra/workflows/touch-2-workflow.ts, apps/agent/src/mastra/workflows/touch-3-workflow.ts, apps/agent/src/mastra/workflows/touch-4-workflow.ts, apps/agent/src/lib/__tests__/workflow-agent-coverage.test.ts</files>
  <behavior>
    - Test 1: Every existing workflow prompt site in pre-call and Touch 1-4 now calls the named-agent executor instead of `ai.models.generateContent` directly.
    - Test 2: The workflow-specific schemas and output parsing remain unchanged from the caller’s perspective.
    - Test 3: Touch 1 and Touch 4 capture prompt version ids before suspension and reuse those ids after resume so publishes do not change an in-flight run.
  </behavior>
  <action>Replace inline prompt authority in the workflow files with named-agent calls that use the roster from Phase 43-01 and the executor from Phase 43-02. Keep the existing workflow step layout, schemas, and user-visible outputs intact; only the prompt source and version bookkeeping should move. Touch 1 and Touch 4 must explicitly persist the selected prompt version ids in workflow state before any suspend boundary and resume by version id, not by latest published lookup. Use separate named agents for the split pre-call jobs and the distinct Touch 4 sub-roles called out in the locked decisions.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/lib/__tests__/workflow-agent-coverage.test.ts src/mastra/__tests__/workflow-agent-versioning.test.ts</automated>
  </verify>
  <done>All main workflow generation steps run through named agents, and long-running approval flows are deterministic across publish boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate shared workflow-adjacent helpers to named agent families</name>
  <files>apps/agent/src/lib/slide-selection.ts, apps/agent/src/lib/proposal-assembly.ts, apps/agent/src/mastra/__tests__/workflow-agent-versioning.test.ts</files>
  <behavior>
    - Test 1: Touch 2/3 slide selection uses one shared job-based agent family rather than duplicating per-touch prompt ownership.
    - Test 2: Proposal assembly uses the named proposal copywriter agent and preserves the current structured output contract.
    - Test 3: The migration does not reintroduce direct inline prompt authority in these helpers.
  </behavior>
  <action>Refactor `slide-selection.ts` and `proposal-assembly.ts` to call the shared named-agent executor instead of constructing prompt authority locally. Keep one shared slide-selector family for Touch 2/3 with touch mode as input, and keep Touch 4 proposal copy generation under its dedicated agent role. Reuse the same response schema/parsing logic already present so downstream assembly code does not change. Do not fork separate per-touch agents where the responsibility is already shared.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/mastra/__tests__/workflow-agent-versioning.test.ts</automated>
  </verify>
  <done>Shared selection and proposal-copy helpers are governed by the same named-agent system as the workflows, while keeping their current contracts stable.</done>
</task>

</tasks>

<verification>
Run the workflow coverage/versioning suites and confirm no seller-facing workflow file still owns its permanent prompt text inline.
</verification>

<success_criteria>
- Pre-call and Touch 1-4 workflows use named agents for every LLM call.
- Touch 1 and Touch 4 pin prompt versions across suspend/resume.
- Shared slide-selection and proposal-assembly helpers preserve their current APIs while using named agent families.
</success_criteria>

<output>
After completion, create `.planning/phases/43-named-agent-architecture/43-03-SUMMARY.md`
</output>
