---
phase: 43-named-agent-architecture
plan: "02"
type: execute
wave: 2
depends_on:
  - 43-01
files_modified:
  - apps/agent/src/lib/agent-prompt-cache.ts
  - apps/agent/src/lib/agent-config.ts
  - apps/agent/src/lib/agent-executor.ts
  - apps/agent/src/mastra/agents/build-agent.ts
  - apps/agent/src/mastra/agents/index.ts
  - apps/agent/src/mastra/index.ts
  - apps/agent/src/lib/__tests__/agent-config.test.ts
  - apps/agent/src/mastra/__tests__/agent-registry.test.ts
autonomous: true
requirements:
  - AGENT-02
must_haves:
  truths:
    - "Named agents always execute with the published prompt version assigned to their role."
    - "Two runs that target the same published agent version resolve the same instructions, even if newer drafts or publishes exist later."
    - "Publishing a new prompt version affects future executions without serving stale instructions from an older publish."
  artifacts:
    - path: "apps/agent/src/lib/agent-config.ts"
      provides: "Prompt lookup, version resolution, and publish-safe accessors"
    - path: "apps/agent/src/lib/agent-prompt-cache.ts"
      provides: "Published-version cache and invalidation helpers"
    - path: "apps/agent/src/mastra/agents/index.ts"
      provides: "Mastra agent registry for the named catalog"
  key_links:
    - from: "apps/agent/src/mastra/agents/index.ts"
      to: "apps/agent/src/lib/agent-config.ts"
      via: "async instructions loader"
      pattern: "instructions: async|getPublished"
    - from: "apps/agent/src/lib/agent-config.ts"
      to: "apps/agent/src/lib/agent-prompt-cache.ts"
      via: "published-version cache key"
      pattern: "agentId:version|publishedVersion"
---

<objective>
Build the runtime prompt-loading layer that turns the new database rows into deterministic named Mastra agents.

Purpose: Downstream callsite migrations need one canonical resolver for prompt lookup, cache semantics, and Mastra registration instead of many ad-hoc wrappers.
Output: Prompt resolver/cache helpers, a reusable named-agent executor surface, and a registered Mastra agent roster sourced from published Prisma versions.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-named-agent-architecture/43-CONTEXT.md
@.planning/phases/43-named-agent-architecture/43-RESEARCH.md
@.planning/phases/40-agent-typecheck-cleanup/40-01-SUMMARY.md

<interfaces>
From `apps/agent/src/mastra/index.ts`:
```typescript
import { Mastra } from "@mastra/core";
import { PostgresStore } from "@mastra/pg";

export const mastra = new Mastra({
  workflows: { touch1Workflow, touch2Workflow, touch3Workflow, touch4Workflow, preCallWorkflow },
  storage: new PostgresStore({ connectionString: env.DATABASE_URL }),
});
```

From research guidance:
```typescript
new Agent({
  id: "sales-brief-strategist",
  name: "Sales Brief Strategist",
  instructions: async () => [
    { role: "system", content: LUMENALTA_BASELINE_PROMPT },
    { role: "system", content: published.rolePrompt },
  ],
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the prompt resolver and published-version cache</name>
  <files>apps/agent/src/lib/agent-prompt-cache.ts, apps/agent/src/lib/agent-config.ts, apps/agent/src/lib/__tests__/agent-config.test.ts</files>
  <behavior>
    - Test 1: Looking up a named agent returns the published version metadata plus compiled baseline+role instructions.
    - Test 2: Cache keys include immutable published version identity so a new publish does not reuse stale prompt text.
    - Test 3: Version-specific lookups can bypass “latest published” reads for long-running workflow pinning.
  </behavior>
  <action>Create a focused runtime prompt layer around Prisma. `agent-config.ts` should expose helpers such as “get published config by agent id”, “get config by version id”, and “compile instructions from baseline + role prompt”, returning both prompt text and version metadata. `agent-prompt-cache.ts` should cache compiled instructions by immutable published-version key and offer explicit invalidation helpers for future publish flows. Keep published Prisma rows as the source of truth, not `@mastra/editor`, and avoid a coarse `Map<agentId, prompt>` cache. The tests should mock Prisma interactions and prove cache invalidation and version-specific resolution behavior.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/lib/__tests__/agent-config.test.ts</automated>
  </verify>
  <done>One helper layer can deterministically resolve current or pinned prompts from Prisma, and cache behavior is version-safe instead of agent-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the named Mastra roster and expose a shared execution seam</name>
  <files>apps/agent/src/lib/agent-executor.ts, apps/agent/src/mastra/agents/build-agent.ts, apps/agent/src/mastra/agents/index.ts, apps/agent/src/mastra/index.ts, apps/agent/src/mastra/__tests__/agent-registry.test.ts</files>
  <behavior>
    - Test 1: Every shared catalog entry has a corresponding registered Mastra agent with the same stable id and plain-language name.
    - Test 2: Registered agents resolve instructions asynchronously from the Prisma-backed prompt loader.
    - Test 3: The shared execution seam returns the resolved prompt version metadata so downstream workflows can pin it.
  </behavior>
  <action>Create a small Mastra agent factory and registry under `apps/agent/src/mastra/agents/`, then wire that registry into `apps/agent/src/mastra/index.ts` without disturbing the existing workflow and route setup. Add `agent-executor.ts` as the shared call surface for non-Mastra helper modules; it should invoke a named agent, pass structured output config when needed, and return prompt version metadata alongside the model response so later plans can pin versions in workflow state. Preserve the current model provider/runtime, keep agent names aligned with the product-readable catalog, and do not hand-roll a second registry map that drifts from Mastra.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/mastra/__tests__/agent-registry.test.ts</automated>
  </verify>
  <done>Mastra exposes the full named roster from Prisma-backed prompts, and both workflow code and helper modules have one typed execution seam to consume.</done>
</task>

</tasks>

<verification>
Run the resolver and registry suites, then confirm the registry remains wired into the existing Mastra instance without changing the deployed storage/runtime pattern from prior phases.
</verification>

<success_criteria>
- Prisma-backed prompt resolution is the only runtime source of truth for named agents.
- Cache keys use immutable published-version identity.
- Mastra registration and helper execution both consume the same shared catalog and prompt loader.
</success_criteria>

<output>
After completion, create `.planning/phases/43-named-agent-architecture/43-02-SUMMARY.md`
</output>
