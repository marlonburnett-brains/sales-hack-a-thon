---
phase: 43-named-agent-architecture
plan: "04"
type: execute
wave: 3
depends_on:
  - 43-02
files_modified:
  - apps/agent/src/lib/atlusai-search.ts
  - apps/agent/src/ingestion/classify-metadata.ts
  - apps/agent/src/ingestion/describe-slide.ts
  - apps/agent/src/ingestion/auto-classify-templates.ts
  - apps/agent/src/ingestion/pilot-ingestion.ts
  - apps/agent/src/validation/validate-schemas.ts
  - apps/agent/src/lib/__tests__/agent-callsite-adoption.test.ts
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
must_haves:
  truths:
    - "Background ingestion, extraction, and maintenance jobs are governed by the same named-agent roster as seller-facing flows."
    - "Internal utility prompts keep their current outputs while moving under published named-agent control."
    - "No hidden non-deck business job remains outside the formal named-agent system."
  artifacts:
    - path: "apps/agent/src/lib/atlusai-search.ts"
      provides: "Named knowledge-result extraction agent with preserved adaptive-cache behavior"
    - path: "apps/agent/src/validation/validate-schemas.ts"
      provides: "Catalog-backed maintenance validation agent"
    - path: "apps/agent/src/lib/__tests__/agent-callsite-adoption.test.ts"
      provides: "Regression proof for adopted internal/background job callsites"
  key_links:
    - from: "apps/agent/src/lib/atlusai-search.ts"
      to: "apps/agent/src/lib/agent-executor.ts"
      via: "knowledge result extractor agent"
      pattern: "invoke.*knowledge-result-extractor"
    - from: "apps/agent/src/ingestion/classify-metadata.ts"
      to: "packages/schemas/agent-catalog.ts"
      via: "named ingestion agent ids"
      pattern: "slide-metadata-classifier|slide-description-writer|template-classification-analyst"
---

<objective>
Finish the architecture by migrating the remaining internal LLM touchpoints and locking the repo against future prompt drift.

Purpose: AGENT-01 is not satisfied unless the hidden/background prompt sites are governed too; this plan closes that gap and leaves a durable regression fence.
Output: Named-agent adoption across Atlus extraction, ingestion, and validation scripts, plus regression coverage for those internal/background jobs.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-named-agent-architecture/43-CONTEXT.md
@.planning/phases/43-named-agent-architecture/43-RESEARCH.md

<interfaces>
From `apps/agent/src/lib/atlusai-search.ts`:
```typescript
export interface SlideSearchResult {
  slideId: string;
  documentTitle: string;
  textContent: string;
  speakerNotes: string;
  metadata: Record<string, unknown>;
  presentationId?: string;
  slideObjectId?: string;
  source?: "mcp" | "drive";
  relevanceScore?: number;
}
```

Current direct-call targets from repo inventory:
```text
apps/agent/src/lib/atlusai-search.ts
apps/agent/src/ingestion/classify-metadata.ts
apps/agent/src/ingestion/describe-slide.ts
apps/agent/src/ingestion/auto-classify-templates.ts
apps/agent/src/ingestion/pilot-ingestion.ts
apps/agent/src/validation/validate-schemas.ts
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate extraction and ingestion prompt sites to named agents</name>
  <files>apps/agent/src/lib/atlusai-search.ts, apps/agent/src/ingestion/classify-metadata.ts, apps/agent/src/ingestion/describe-slide.ts, apps/agent/src/ingestion/auto-classify-templates.ts, apps/agent/src/ingestion/pilot-ingestion.ts, apps/agent/src/lib/__tests__/agent-callsite-adoption.test.ts</files>
  <behavior>
    - Test 1: Atlus extraction and ingestion jobs run through named agents while preserving their current structured output contracts and caches.
    - Test 2: Shared extraction and classification jobs keep touch-agnostic responsibility boundaries instead of fragmenting per caller.
    - Test 3: These internal/background modules no longer own permanent inline prompt authority.
  </behavior>
  <action>Replace direct `GoogleGenAI` prompt authority in `atlusai-search.ts` and the listed ingestion files with the shared named-agent executor. Preserve existing adaptive behavior where it matters: `atlusai-search.ts` should keep its cached extraction template semantics, but the extraction prompt authority must come from the named knowledge-result extractor agent instead of the hardcoded inline text. Treat ingestion/classification/description/template-classification as first-class catalog entries and keep shared job-based families when the responsibility repeats. Add a focused regression test proving these adopted callsites now resolve through named agents.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/lib/__tests__/agent-callsite-adoption.test.ts</automated>
  </verify>
  <done>Atlus extraction and ingestion prompt sites are governed by the named-agent architecture, with no hidden inline prompt ownership left in those modules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the validation script onto the named-agent layer</name>
  <files>apps/agent/src/validation/validate-schemas.ts, apps/agent/src/lib/__tests__/agent-callsite-adoption.test.ts</files>
  <behavior>
    - Test 1: The validation script resolves its prompt through a named maintenance-oriented agent instead of a hidden inline prompt.
    - Test 2: The validation flow still returns the same structured review output expected by the script.
    - Test 3: The shared adoption test covers the validation script alongside the extraction and ingestion callsites.
  </behavior>
  <action>Route `apps/agent/src/validation/validate-schemas.ts` through its own named maintenance/validation agent instead of leaving it as a utility exception. Keep the script's current schema-review contract intact, and extend the shared adoption regression test so it proves the validation script migrated with the rest of the internal/background job set.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/lib/__tests__/agent-callsite-adoption.test.ts</automated>
  </verify>
  <done>The schema validation script is no longer a prompt exception, and regression coverage proves the adopted internal/background set all use named agents.</done>
</task>

</tasks>

<verification>
Run the internal/background adoption suite, then confirm the extraction, ingestion, and validation modules no longer own permanent inline prompt text.
</verification>

<success_criteria>
- Extraction, ingestion, and validation prompt sites use the named-agent architecture too.
- Shared internal job families preserve their current outputs while moving to named-agent governance.
- Regression proof exists for the internal/background modules migrated in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/43-named-agent-architecture/43-04-SUMMARY.md`
</output>
