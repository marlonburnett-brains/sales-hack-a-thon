---
phase: 40-agent-typecheck-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/schemas/index.ts
  - packages/schemas/llm/slide-metadata.ts
  - apps/agent/src/mastra/index.ts
  - apps/agent/src/mastra/workflows/touch-4-workflow.ts
  - apps/agent/src/mastra/__tests__/template-classify-route.test.ts
  - apps/agent/src/mastra/__tests__/token-store-route.test.ts
  - apps/agent/src/mastra/__tests__/workflow-resume-contract.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - Shared schema imports no longer fail the agent compile because the barrel uses compiler-safe local import paths.
    - Mastra workflow resume handlers compile against the installed async `createRun()` plus `resume({ step, resumeData })` API.
    - Zod 4 request parsing in touched agent routes uses current `z.record()` and `err.issues` semantics instead of legacy signatures.
  artifacts:
    - path: packages/schemas/index.ts
      provides: Extensionless shared schema barrel exports compatible with the current TypeScript config.
    - path: packages/schemas/llm/slide-metadata.ts
      provides: Zod schema imports aligned to the same extensionless barrel rule.
    - path: apps/agent/src/mastra/index.ts
      provides: Current Mastra resume calls and Zod 4 route validation in touched handlers.
    - path: apps/agent/src/mastra/workflows/touch-4-workflow.ts
      provides: Zod 4 workflow-local record schema compatible with the installed types.
    - path: apps/agent/src/mastra/__tests__/workflow-resume-contract.test.ts
      provides: Regression coverage for the current resume contract in source.
  key_links:
    - from: apps/agent/src/mastra/index.ts
      to: apps/agent/src/mastra/workflows/touch-4-workflow.ts
      via: Resume handlers and workflow step ids stay aligned to the installed Mastra API.
      pattern: createRun|resume\(|await-brief-approval|await-asset-review
    - from: apps/agent/src/mastra/index.ts
      to: packages/schemas/index.ts
      via: Agent route validation still imports the shared artifact contract from the repaired barrel.
      pattern: @lumenalta/schemas|ARTIFACT_TYPES|ArtifactType
    - from: packages/schemas/index.ts
      to: packages/schemas/llm/slide-metadata.ts
      via: Shared schema exports use extensionless local paths consistently.
      pattern: from "\./|from "\.\./
---

<objective>
Remove the deterministic TypeScript API-drift failures in shared schemas and Mastra/Zod code so the remaining Phase 40 work can focus on the isolated MCP seam.

Purpose: Clear the low-risk compiler blockers first instead of mixing them with the higher-risk MCP refactor.
Output: Repaired shared schema imports, Mastra resume handlers aligned to current APIs, and route/workflow regression coverage for the updated contracts.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RETROSPECTIVE.md
@.planning/phases/40-agent-typecheck-cleanup/40-RESEARCH.md
@.planning/phases/39-artifact-contract-hardening/39-02-SUMMARY.md
@packages/schemas/index.ts
@packages/schemas/llm/slide-metadata.ts
@apps/agent/src/mastra/index.ts
@apps/agent/src/mastra/workflows/touch-4-workflow.ts
@apps/agent/src/mastra/__tests__/template-classify-route.test.ts
@apps/agent/src/mastra/__tests__/token-store-route.test.ts

<interfaces>
From `packages/schemas/index.ts`:

```ts
export {
  ARTIFACT_TYPES,
  type ArtifactType,
} from "./constants.ts";
```

From `apps/agent/src/mastra/index.ts`:

```ts
const run = wf.createRun({ runId: data.runId });
await run.resume({
  stepId: "await-brief-approval",
  resumeData: { ... },
});
```

From `apps/agent/src/mastra/workflows/touch-4-workflow.ts`:

```ts
const fieldSeveritySchema = z.record(z.enum(["error", "warning", "ok"]));
```

Keep these Phase 39 decisions intact while fixing the compile drift:
- Keep `ArtifactType | null` as the trusted agent-side contract after route parsing.
- Keep the existing root-mounted `:touchType` route family and current Touch 4 behavior unchanged.
- Do not loosen tsconfig or add compiler escape hatches; fix source code directly.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove shared schema import-extension drift</name>
  <files>packages/schemas/index.ts, packages/schemas/llm/slide-metadata.ts</files>
  <action>Replace local `./*.ts` and `../*.ts` import specifiers with extensionless paths in the shared schemas barrel and the touched LLM schema module. Preserve the existing public exports exactly, including `ARTIFACT_TYPES`, `ArtifactType`, and every current schema symbol; this is a compiler-rule cleanup, not a package API redesign.</action>
  <verify>
    <automated>pnpm --filter agent exec tsc --noEmit 2&gt;&amp;1 | rg -q "packages/schemas/index.ts|packages/schemas/llm/slide-metadata.ts" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>The shared schema package no longer contributes `.ts` import-extension errors to the agent no-emit baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align Mastra resume handlers and Zod 4 route/workflow usage</name>
  <files>apps/agent/src/mastra/index.ts, apps/agent/src/mastra/workflows/touch-4-workflow.ts, apps/agent/src/mastra/__tests__/template-classify-route.test.ts, apps/agent/src/mastra/__tests__/token-store-route.test.ts, apps/agent/src/mastra/__tests__/workflow-resume-contract.test.ts</files>
  <behavior>
    - Test 1: The source contract for brief approval and asset approval awaits `createRun()` and resumes with `step`, not the old sync plus `stepId` shape.
    - Test 2: Touched route handlers report Zod validation details through `err.issues` and use Zod 4-compatible record schemas.
    - Test 3: Existing Touch 4 classify-route invariants and token-store behavior remain unchanged after the type cleanup.
  </behavior>
  <action>Create a focused source-contract regression test for the workflow resume API, then update `apps/agent/src/mastra/index.ts` and `apps/agent/src/mastra/workflows/touch-4-workflow.ts` to match the installed Mastra 1.8 and Zod 4 signatures. Use `await wf.createRun(...)`, `run.resume({ step, resumeData })`, two-argument `z.record(...)` where needed, and `err.issues` in the touched route handlers. Keep route behavior, Touch 4 artifact validation, and existing endpoint shapes unchanged; this plan fixes compile drift only.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/mastra/__tests__/workflow-resume-contract.test.ts src/mastra/__tests__/template-classify-route.test.ts src/mastra/__tests__/token-store-route.test.ts &amp;&amp; pnpm --filter agent exec tsc --noEmit 2&gt;&amp;1 | rg -q "src/mastra/index.ts|src/mastra/workflows/touch-4-workflow.ts" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>The touched Mastra and Zod paths compile against current library APIs, and focused tests protect the unchanged route behavior.</done>
</task>

</tasks>

<verification>
Use targeted TypeScript filtering plus focused Mastra Vitest suites to prove the shared-schema, workflow, and route drift is gone before touching the MCP surface.
</verification>

<success_criteria>
- No agent `tsc --noEmit` errors remain in `packages/schemas`, `apps/agent/src/mastra/index.ts`, or `apps/agent/src/mastra/workflows/touch-4-workflow.ts`.
- Brief approval and asset approval source paths match the current async Mastra resume contract.
- Touched Zod validations compile under Zod 4 without changing Touch 4 route behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/40-agent-typecheck-cleanup/40-01-SUMMARY.md`
</output>
