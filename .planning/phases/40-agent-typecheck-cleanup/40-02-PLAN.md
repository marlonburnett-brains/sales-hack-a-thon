---
phase: 40-agent-typecheck-cleanup
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/lib/mcp-client.ts
  - apps/agent/src/lib/__tests__/mcp-client.test.ts
  - apps/agent/src/lib/__tests__/atlusai-search.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - The MCP client compiles without typed reach-through to a private `@mastra/mcp` method.
    - MCP source changes and MCP-adjacent tests stay aligned to the current health-check and tool-call behavior.
    - The stale Vitest suites compile under Vitest 4 callable mock typing instead of blocking the agent baseline.
  artifacts:
    - path: apps/agent/src/lib/mcp-client.ts
      provides: Public-API-aligned MCP client seam for connection checks and tool execution.
    - path: apps/agent/src/lib/__tests__/mcp-client.test.ts
      provides: Regression coverage for MCP lifecycle, auth recovery, and the repaired seam.
    - path: apps/agent/src/lib/__tests__/atlusai-search.test.ts
      provides: Regression coverage for MCP-vs-Drive search routing under current Vitest mock typing.
  key_links:
    - from: apps/agent/src/lib/mcp-client.ts
      to: apps/agent/src/lib/__tests__/mcp-client.test.ts
      via: MCP lifecycle tests must assert the exact health-check and tool-call seam used by production code.
      pattern: initMcp|callMcpTool|getMcpClient|disconnect
    - from: apps/agent/src/lib/__tests__/atlusai-search.test.ts
      to: apps/agent/src/lib/mcp-client.ts
      via: Search adapter mocks must match the real MCP wrapper exports and call signatures.
      pattern: callMcpTool|isMcpAvailable|getCachedExtractionPrompt|setCachedExtractionPrompt
---

<objective>
Repair the isolated MCP compile hotspot and its stale Vitest suites without disturbing the already-passing Touch 4 deck-structure paths.

Purpose: Contain the riskiest Phase 40 drift to one MCP-focused slice so the final compile gate has one clear seam to trust.
Output: A public-API-aligned MCP wrapper, refreshed MCP lifecycle tests, and repaired search-adapter tests that compile under Vitest 4.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/RETROSPECTIVE.md
@.planning/phases/40-agent-typecheck-cleanup/40-RESEARCH.md
@.planning/phases/39-artifact-contract-hardening/39-02-SUMMARY.md
@apps/agent/src/lib/mcp-client.ts
@apps/agent/src/lib/__tests__/mcp-client.test.ts
@apps/agent/src/lib/__tests__/atlusai-search.test.ts

<interfaces>
From `apps/agent/src/lib/mcp-client.ts`:

```ts
export async function initMcp(): Promise<void>;
export async function getMcpClient(): Promise<MCPClient | null>;
export async function callMcpTool(
  toolName: string,
  args: Record<string, unknown>,
): Promise<unknown>;
```

Current risky seam in `apps/agent/src/lib/mcp-client.ts`:

```ts
await client.getConnectedClientForServer("atlus");
const internalClient = await c.getConnectedClientForServer("atlus");
```

Keep these constraints from research and history:
- Do not loosen tsconfig or paper over private API usage with broad casts unless one tiny, isolated escape hatch is the only viable option.
- Update MCP source and MCP tests together; Phase 31 mock drift must not repeat here.
- Do not refactor deck-structure or Touch 4 route code in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the private MCP reach-through with a contained supported seam</name>
  <files>apps/agent/src/lib/mcp-client.ts, apps/agent/src/lib/__tests__/mcp-client.test.ts</files>
  <behavior>
    - Test 1: `initMcp()` still marks MCP available only after the repaired health-check seam succeeds.
    - Test 2: `callMcpTool()` still retries once after auth recovery and preserves the current result parsing behavior.
    - Test 3: The source no longer relies on a normal typed call to private `getConnectedClientForServer()`.
  </behavior>
  <action>Update `apps/agent/src/lib/mcp-client.ts` to stop depending on `getConnectedClientForServer()` as a normal typed API. Prefer the smallest supported public path available in the installed package; if none exists, isolate the unavoidable escape hatch behind one tiny helper instead of scattering casts. Refresh `apps/agent/src/lib/__tests__/mcp-client.test.ts` to match the current lifecycle behavior, including the real health-check path, token refresh flow, and `persistAtlusClientId` export expectations where needed. Keep the existing fallback-mode, refresh mutex, and result parsing behavior intact.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/lib/__tests__/mcp-client.test.ts &amp;&amp; pnpm --filter agent exec tsc --noEmit 2&gt;&amp;1 | rg -q "src/lib/mcp-client.ts|src/lib/__tests__/mcp-client.test.ts" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>The MCP wrapper compiles against the installed package surface, and its lifecycle/auth-recovery behavior stays covered by focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Repair stale Vitest callable mocks in the search adapter suite</name>
  <files>apps/agent/src/lib/__tests__/atlusai-search.test.ts</files>
  <behavior>
    - Test 1: MCP and Drive routing assertions still prove the adapter chooses the correct backend for each scenario.
    - Test 2: Prompt-cache and project-scoping assertions still match the current `mcp-client` export surface.
    - Test 3: The suite compiles under Vitest 4 without `Mock<Procedure | Constructable>` call-signature errors.
  </behavior>
  <action>Refactor the callable mock declarations and wrappers in `apps/agent/src/lib/__tests__/atlusai-search.test.ts` so they use Vitest-4-safe callable signatures instead of broad `ReturnType<typeof vi.fn>` calls that TypeScript now treats as constructable-only. Keep the existing behavioral assertions, especially MCP-vs-Drive routing, prompt caching, and project scoping. Align the mocked `mcp-client` exports with the repaired Task 1 source contract rather than historical assumptions.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/lib/__tests__/atlusai-search.test.ts &amp;&amp; pnpm --filter agent exec tsc --noEmit 2&gt;&amp;1 | rg -q "src/lib/__tests__/atlusai-search.test.ts" &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <done>The stale search-adapter suite compiles and still protects the intended MCP/Drive routing behavior.</done>
</task>

</tasks>

<verification>
Run the repaired MCP and search-adapter Vitest suites plus targeted TypeScript filtering to prove the remaining baseline errors are no longer in the MCP/test bucket.
</verification>

<success_criteria>
- `apps/agent/src/lib/mcp-client.ts` no longer raises the private-method TypeScript error.
- `mcp-client.test.ts` and `atlusai-search.test.ts` compile and pass under the current Vitest mock typing.
- MCP lifecycle and search-routing behavior remain regression-covered after the seam repair.
</success_criteria>

<output>
After completion, create `.planning/phases/40-agent-typecheck-cleanup/40-02-SUMMARY.md`
</output>
