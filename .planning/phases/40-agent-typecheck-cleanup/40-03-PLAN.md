---
phase: 40-agent-typecheck-cleanup
plan: "03"
type: execute
wave: 2
depends_on:
  - 40-01
  - 40-02
files_modified:
  - .planning/phases/40-agent-typecheck-cleanup/40-VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - `pnpm --filter agent exec tsc --noEmit` is green for the repository baseline after the Phase 40 repairs land.
    - The previously locked Touch 4 route regressions still pass after the cleanup work.
    - Phase 40 leaves a verification trail showing exactly which commands were run and what passed.
  artifacts:
    - path: .planning/phases/40-agent-typecheck-cleanup/40-VERIFICATION.md
      provides: Final Phase 40 compile and regression proof for v1.6 closeout.
  key_links:
    - from: .planning/phases/40-agent-typecheck-cleanup/40-VERIFICATION.md
      to: apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts
      via: Final verification must include the existing Touch 4 route guardrail suites, not just the compile command.
      pattern: deck-structure-routes.test.ts|template-classify-route.test.ts|tsc --noEmit
---

<objective>
Close Phase 40 with a clean agent no-emit baseline and a durable verification record for the final v1.6 milestone cleanup.

Purpose: Convert the repaired code into an auditable closeout signal instead of stopping at code changes without proof.
Output: A final verification document capturing the passing agent compile and the targeted regression suites that protect Touch 4 and MCP behavior.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-agent-typecheck-cleanup/40-RESEARCH.md
@.planning/phases/39-artifact-contract-hardening/39-02-SUMMARY.md
@.planning/phases/39-artifact-contract-hardening/39-03-SUMMARY.md

<interfaces>
Final verification commands must include:

```bash
pnpm --filter agent exec tsc --noEmit
pnpm --filter agent exec vitest run src/mastra/__tests__/deck-structure-routes.test.ts src/mastra/__tests__/template-classify-route.test.ts
pnpm --filter agent exec vitest run src/lib/__tests__/mcp-client.test.ts src/lib/__tests__/atlusai-search.test.ts
```

Keep the final proof focused on repo-baseline health and Touch 4 guardrails; do not expand into unrelated web or deployment verification.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the final agent compile and locked regression suites</name>
  <files>.planning/phases/40-agent-typecheck-cleanup/40-VERIFICATION.md</files>
  <action>Run the final Phase 40 verification set after Plans 40-01 and 40-02 are complete: full `pnpm --filter agent exec tsc --noEmit`, the already-passing Touch 4 guardrails from research (`deck-structure-routes.test.ts` and `template-classify-route.test.ts`), and the repaired MCP/search suites. Record exact commands, pass/fail status, and any noteworthy observations in `.planning/phases/40-agent-typecheck-cleanup/40-VERIFICATION.md`. If the full compile still fails, stop and document the exact remaining errors instead of broadening scope into unrelated subsystems.</action>
  <verify>
    <automated>pnpm --filter agent exec tsc --noEmit &amp;&amp; pnpm --filter agent exec vitest run src/mastra/__tests__/deck-structure-routes.test.ts src/mastra/__tests__/template-classify-route.test.ts src/lib/__tests__/mcp-client.test.ts src/lib/__tests__/atlusai-search.test.ts</automated>
  </verify>
  <done>The final agent compile and the locked regression suites all pass, and the verification document captures the evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Publish the Phase 40 verification trail</name>
  <files>.planning/phases/40-agent-typecheck-cleanup/40-VERIFICATION.md</files>
  <action>Write a concise verification report with: baseline issue summary, commands run, observed results, and an explicit verdict on whether the Phase 40 cleanup restored the agent compile baseline. Include references to the repaired error buckets from the research file so the closeout trail is auditable later.</action>
  <verify>
    <automated>test -f .planning/phases/40-agent-typecheck-cleanup/40-VERIFICATION.md &amp;&amp; rg -q "tsc --noEmit|deck-structure-routes.test.ts|mcp-client.test.ts|Verdict" .planning/phases/40-agent-typecheck-cleanup/40-VERIFICATION.md</automated>
  </verify>
  <done>Phase 40 has a durable verification artifact that explains what passed and why the v1.6 closeout is complete.</done>
</task>

</tasks>

<verification>
The phase exits only when the full agent no-emit compile is green and the verification file records the same passing commands plus the Touch 4 and MCP regression suites.
</verification>

<success_criteria>
- `pnpm --filter agent exec tsc --noEmit` passes after Phase 40 execution.
- Touch 4 route regressions still pass after the cleanup.
- `.planning/phases/40-agent-typecheck-cleanup/40-VERIFICATION.md` exists with exact command evidence and a final verdict.
</success_criteria>

<output>
After completion, create `.planning/phases/40-agent-typecheck-cleanup/40-03-SUMMARY.md`
</output>
