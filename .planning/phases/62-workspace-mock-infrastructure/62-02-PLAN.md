---
phase: 62-workspace-mock-infrastructure
plan: 02
type: execute
wave: 2
depends_on: ["62-01"]
files_modified:
  - apps/tutorials/scripts/mock-server.ts
  - apps/tutorials/src/helpers/auth.ts
  - apps/tutorials/src/helpers/route-mocks.ts
  - apps/tutorials/src/helpers/determinism.ts
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Mock agent server starts on port 4112 and serves fixture JSON for all api-client.ts routes"
    - "Mock server handles GET /tokens/check/:userId returning hasToken: true (prevents middleware timeout)"
    - "Auth helper creates a valid Supabase session that passes middleware getSession() and getUser() checks"
    - "page.route() helpers intercept all browser-side /api/* calls with fixture responses"
    - "Determinism helpers disable CSS animations, wait for network idle, font ready, and skeleton removal"
  artifacts:
    - path: "apps/tutorials/scripts/mock-server.ts"
      provides: "Express mock agent server with all api-client.ts route handlers"
      exports: ["createMockServer", "startMockServer"]
      min_lines: 80
    - path: "apps/tutorials/src/helpers/auth.ts"
      provides: "Supabase REST login + session injection for Playwright"
      exports: ["ensureAuthState"]
    - path: "apps/tutorials/src/helpers/route-mocks.ts"
      provides: "page.route() factory functions for browser-side API mocking"
      exports: ["mockBrowserAPIs"]
    - path: "apps/tutorials/src/helpers/determinism.ts"
      provides: "Screenshot determinism utilities"
      exports: ["prepareForScreenshot", "disableAnimations"]
  key_links:
    - from: "apps/tutorials/scripts/mock-server.ts"
      to: "apps/tutorials/fixtures/loader.ts"
      via: "loadFixtures() call to serve fixture data"
      pattern: "loadFixtures"
    - from: "apps/tutorials/src/helpers/auth.ts"
      to: "@supabase/supabase-js"
      via: "signInWithPassword for real session tokens"
      pattern: "signInWithPassword"
    - from: "apps/tutorials/src/helpers/route-mocks.ts"
      to: "page.route"
      via: "Playwright route interception"
      pattern: "page\\.route"
---

<objective>
Build the three mocking layers: Express mock agent server (server-side), page.route() helpers (browser-side), and Supabase auth bypass. Plus deterministic screenshot preparation utilities.

Purpose: These are the core infrastructure pieces that enable Playwright to navigate the real Next.js app without any live backend services. The mock server replaces the agent service, auth bypass gets past the login wall, route mocks handle browser-initiated API calls, and determinism helpers ensure repeatable captures.

Output: Four helper modules and one mock server script, all ready for the capture orchestration in Plan 03.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-workspace-mock-infrastructure/62-CONTEXT.md
@.planning/phases/62-workspace-mock-infrastructure/62-RESEARCH.md
@.planning/phases/62-workspace-mock-infrastructure/62-01-SUMMARY.md

@apps/web/src/lib/api-client.ts
@apps/web/src/middleware.ts
@apps/web/src/lib/supabase/server.ts
@apps/web/src/env.ts

<interfaces>
<!-- The executor MUST read api-client.ts to build the complete route catalog -->
<!-- Key routes from RESEARCH.md (executor should verify against actual file): -->

Mock server must handle ALL these routes (from api-client.ts analysis):
- GET/POST /companies
- GET/POST /deals, GET /deals/:id, PATCH /deals/:id/status, PATCH /deals/:id/assign
- GET /users
- GET /deals/:dealId/interactions
- POST /workflows/touch-{1,2,3,4}/start
- GET /workflows/touch-{1,2,3,4}/:runId/status
- GET /templates, POST /templates/register
- GET /slides, GET /slides/:id
- GET/POST /settings/drive
- GET /agent-prompts, GET/PUT /agent-prompts/:agentId
- GET /tokens/check/:userId (CRITICAL -- middleware calls this)
- GET /deck-structures/:touchType
- POST /pre-call-briefing, GET /pre-call-briefing/:dealId
- POST /deals/:dealId/chat, GET/POST /deals/:dealId/chat/bindings
- GET /discovery/search, POST /discovery/ingest
- GET /generation-logs

Browser-side routes to mock via page.route():
- /api/workflows/status* (polling)
- /api/generation-logs* (polling)
- /api/presentations/*/thumbnails (image data)
- Any other /api/* routes the browser calls directly

Auth middleware flow (from middleware.ts):
1. Creates Supabase server client from cookies
2. Calls supabase.auth.getUser() to validate session
3. If no user, redirects to /login (except /login, /auth/**)
4. If user, calls GET /tokens/check/:userId on agent service
5. Sets google-token-status cookie based on response
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Express mock agent server with full route catalog</name>
  <files>apps/tutorials/scripts/mock-server.ts</files>
  <action>
1. Read `apps/web/src/lib/api-client.ts` thoroughly to extract the COMPLETE list of endpoints, HTTP methods, request parameters, and response shapes. Do not rely solely on the research summary -- the actual file is the source of truth.

2. Create `apps/tutorials/scripts/mock-server.ts`:
   - Import express and the loadFixtures function from Plan 01's fixture loader
   - Export `createMockServer(tutorialName: string, port?: number)` function
   - Default port: 4112 (different from real agent on 4111)
   - Use express.json() middleware
   - Skip all auth validation -- mock server trusts all requests

3. Register route handlers for EVERY endpoint from api-client.ts:
   - Each handler returns the appropriate fixture data from the loaded fixtures
   - For parameterized routes (e.g., GET /deals/:id), look up the fixture by ID
   - For POST routes that create resources, return a fixture object (not actually creating anything)
   - For PATCH routes, return the updated fixture
   - For routes that accept query params (e.g., GET /deals?ownerId=...&status=...), return filtered fixtures or the full list

4. CRITICAL routes that MUST be implemented:
   - `GET /tokens/check/:userId` -> returns `{ hasToken: true }` (prevents 3-second middleware timeout and Google re-auth banners)
   - All workflow status routes -> return `{ status: "completed" }` or appropriate fixture
   - Chat routes -> return fixture chat responses

5. Export `startMockServer(tutorialName: string, port?: number): Promise<{ server: Server; port: number }>` that:
   - Creates the mock server
   - Returns a promise that resolves when the server is listening
   - Includes a `close()` method for cleanup

6. Add a catch-all 404 handler that logs unhandled routes (helps identify missing mock routes during development):
   ```typescript
   app.use((req, res) => {
     console.warn(`[mock-server] Unhandled: ${req.method} ${req.url}`);
     res.status(404).json({ error: "Not mocked", path: req.url });
   });
   ```

7. Add a standalone run mode at the bottom:
   ```typescript
   if (require.main === module || process.argv[1]?.endsWith('mock-server.ts')) {
     startMockServer(process.argv[2] || 'getting-started').then(({ port }) => {
       console.log(`Mock agent server running on http://localhost:${port}`);
     });
   }
   ```
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && pnpm --filter tutorials exec -- npx tsx -e "
const { startMockServer } = require('./scripts/mock-server');
async function test() {
  const { server, port } = await startMockServer('getting-started', 0);
  const res = await fetch(\`http://localhost:\${port}/tokens/check/test-user\`);
  const data = await res.json();
  if (!data.hasToken) throw new Error('tokens/check failed');
  const companies = await fetch(\`http://localhost:\${port}/companies\`);
  if (!companies.ok) throw new Error('companies route failed');
  server.close();
  console.log('PASS - mock server serves fixtures');
}
test();
"</automated>
  </verify>
  <done>Mock agent server starts, serves fixture JSON for all api-client.ts routes, handles /tokens/check/:userId, logs unhandled routes as warnings</done>
</task>

<task type="auto">
  <name>Task 2: Build auth bypass, page.route() helpers, and determinism utilities</name>
  <files>apps/tutorials/src/helpers/auth.ts, apps/tutorials/src/helpers/route-mocks.ts, apps/tutorials/src/helpers/determinism.ts</files>
  <action>
1. Create `apps/tutorials/src/helpers/auth.ts`:
   - Read `apps/web/src/middleware.ts` and `apps/web/src/lib/supabase/server.ts` to understand the exact auth check flow
   - Export `ensureAuthState(page: Page)` function following RESEARCH.md Pattern 2:
     a. Create a Supabase client using NEXT_PUBLIC_SUPABASE_URL and NEXT_PUBLIC_SUPABASE_ANON_KEY env vars
     b. Call `supabase.auth.signInWithPassword()` with TUTORIAL_USER_EMAIL and TUTORIAL_USER_PASSWORD env vars
     c. Throw descriptive error if sign-in fails (include hint about needing password auth enabled in Supabase)
     d. Inject the session into the browser via `page.addInitScript()` setting the localStorage key `sb-{ref}-auth-token` (extract ref from Supabase URL hostname)
     e. Also set the Supabase auth cookies that the middleware expects (the SSR client reads cookies, not just localStorage)
   - Export `getStorageStatePath()` returning the path for caching auth state
   - Consider caching: If session is still valid, skip re-login

2. Create `apps/tutorials/src/helpers/route-mocks.ts`:
   - Read `apps/web/src/app/api/` directory to identify all browser-callable API routes
   - Export `mockBrowserAPIs(page: Page, fixtures: FixtureSet)` function following RESEARCH.md Pattern 4
   - Register page.route() handlers for all `/api/*` routes the browser calls directly:
     - `/api/workflows/status*` (workflow polling)
     - `/api/generation-logs*` (generation log polling)
     - `/api/presentations/*/thumbnails` (presentation thumbnails)
     - Any other `/api/*` routes found in the browser-side code
   - Each handler returns the appropriate fixture data
   - Add a catch-all for `**/api/**` that warns about unhandled browser API calls (helps catch missing mocks)

3. Create `apps/tutorials/src/helpers/determinism.ts`:
   - Export `disableAnimations(page: Page)`: Adds CSS style tag disabling all animations/transitions with `*` selector and `!important` (per RESEARCH.md Pattern 3)
   - Export `waitForStableState(page: Page)`: Waits for networkidle + document.fonts.ready + skeleton/spinner removal
   - Export `prepareForScreenshot(page: Page)`: Calls disableAnimations + waitForStableState as a combined convenience function
   - Skeleton detection query: `[class*="skeleton"], [class*="Skeleton"], [class*="spinner"], [class*="Spinner"], [class*="loading"], [role="progressbar"]`
   - Timeout for skeleton wait: 10 seconds with graceful catch (some pages have no skeletons)
   - Export `waitForElement(page: Page, selector: string, timeout?: number)`: Utility for waiting for specific elements before capture
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && pnpm --filter tutorials exec -- npx tsx -e "
const auth = require('./src/helpers/auth');
const mocks = require('./src/helpers/route-mocks');
const det = require('./src/helpers/determinism');
if (typeof auth.ensureAuthState !== 'function') throw new Error('auth missing');
if (typeof mocks.mockBrowserAPIs !== 'function') throw new Error('route-mocks missing');
if (typeof det.prepareForScreenshot !== 'function') throw new Error('determinism missing');
if (typeof det.disableAnimations !== 'function') throw new Error('disableAnimations missing');
console.log('PASS - all helpers export expected functions');
"</automated>
  </verify>
  <done>Auth helper creates valid Supabase sessions via REST login. page.route() helpers intercept all browser-side /api/* calls. Determinism utilities disable animations and wait for stable state before screenshots.</done>
</task>

</tasks>

<verification>
- Mock server starts on a random port and responds to GET /tokens/check/:userId with `{ hasToken: true }`
- Mock server responds to GET /companies, GET /deals, and other core routes with fixture JSON
- Mock server logs warnings for unhandled routes (catch-all 404)
- Auth helper exports ensureAuthState function
- Route mocks helper exports mockBrowserAPIs function
- Determinism helper exports prepareForScreenshot, disableAnimations, waitForStableState
</verification>

<success_criteria>
- Express mock server handles all routes from api-client.ts with fixture data
- Auth bypass uses real Supabase signInWithPassword for valid session tokens
- page.route() helpers mock all browser-initiated /api/* calls
- Determinism utilities disable CSS animations and wait for network idle + font ready + skeleton removal
- All modules importable and export expected functions
</success_criteria>

<output>
After completion, create `.planning/phases/62-workspace-mock-infrastructure/62-02-SUMMARY.md`
</output>
