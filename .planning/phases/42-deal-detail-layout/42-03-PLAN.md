---
phase: 42-deal-detail-layout
plan: 03
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - apps/web/src/app/(authenticated)/deals/[dealId]/briefing/page.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/briefing/loading.tsx
  - apps/web/src/components/deals/briefing-chat-panel.tsx
  - apps/web/src/components/deals/prior-briefings-list.tsx
autonomous: true
requirements:
  - BRIEF-01
  - BRIEF-02

must_haves:
  truths:
    - "User sees a conversational AI chat panel at the top of the briefing page with suggestion buttons"
    - "User sees prior briefings displayed below the chat panel, newest first, expandable/collapsible"
    - "User can trigger 'Generate full briefing' from a suggestion button and it calls the existing pre-call workflow"
    - "All prep/context material (pre-call briefings, research data) is accessible from the single briefing page"
  artifacts:
    - path: "apps/web/src/app/(authenticated)/deals/[dealId]/briefing/page.tsx"
      provides: "Briefing page composing chat panel + prior briefings"
      min_lines: 40
    - path: "apps/web/src/components/deals/briefing-chat-panel.tsx"
      provides: "AI chat shell with suggestion buttons"
      exports: ["BriefingChatPanel"]
    - path: "apps/web/src/components/deals/prior-briefings-list.tsx"
      provides: "Expandable list of prior briefings"
      exports: ["PriorBriefingsList"]
  key_links:
    - from: "briefing/page.tsx"
      to: "getDealAction"
      via: "server action import"
      pattern: "getDealAction"
    - from: "briefing-chat-panel.tsx"
      to: "pre-call workflow"
      via: "server action or API call"
      pattern: "generateBrief|createPreCall|startPreCallAction"
    - from: "prior-briefings-list.tsx"
      to: "InteractionRecord"
      via: "type import"
      pattern: "InteractionRecord"
---

<objective>
Build the deal Briefing page with a conversational AI chat panel (UI shell with functional suggestion buttons) and a prior briefings list.

Purpose: Replaces the rigid PreCallForm with a more AI-driven, conversational meeting prep experience. The chat panel provides suggestions and can trigger briefing generation. Prior briefings are displayed in an expandable list.
Output: Functional Briefing page at `/deals/[id]/briefing` with working "Generate full briefing" action and prior briefings display.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-deal-detail-layout/42-CONTEXT.md
@.planning/phases/42-deal-detail-layout/42-RESEARCH.md
@.planning/phases/42-deal-detail-layout/42-01-SUMMARY.md
@apps/web/src/components/pre-call/pre-call-section.tsx
@apps/web/src/components/pre-call/pre-call-form.tsx

<interfaces>
<!-- Existing pre-call components and actions to understand -->

From apps/web/src/components/pre-call/pre-call-section.tsx:
```typescript
// Current section with PreCallForm + prior briefings list.
// Props: dealId, companyName, industry, interactions
export function PreCallSection(props: {
  dealId: string;
  companyName: string;
  industry: string;
  interactions: InteractionRecord[];
});
```

From apps/web/src/components/pre-call/pre-call-form.tsx:
```typescript
// Current rigid form for pre-call briefing. Has fields for meeting agenda,
// research notes, etc. Submits to create a pre_call interaction.
export function PreCallForm(props: {
  dealId: string;
  companyName: string;
  industry: string;
});
```

From apps/web/src/lib/api-client.ts:
```typescript
export interface InteractionRecord {
  id: string;
  dealId: string;
  touchType: string; // "pre_call" for briefings
  status: string;
  inputs: string; // JSON string with form data
  decision: string | null;
  generatedContent: string | null; // JSON string with briefing content
  outputRefs: string | null;
  createdAt: string;
  updatedAt: string;
  brief?: BriefRecord | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build BriefingChatPanel and PriorBriefingsList components</name>
  <files>
    apps/web/src/components/deals/briefing-chat-panel.tsx,
    apps/web/src/components/deals/prior-briefings-list.tsx
  </files>
  <action>
1. Create `apps/web/src/components/deals/briefing-chat-panel.tsx` ("use client"):
   - Props: `dealId: string`, `companyName: string`, `industry: string`.
   - This is the UI shell for the AI-driven briefing experience. Full persistent chat (CHAT-01-05) is Phase 45. This phase builds the visual panel with functional suggestion buttons.
   - Layout: Card-style container with rounded-lg border bg-white shadow-sm.
   - Header: AI assistant greeting -- "I can help you prepare for your meeting with {companyName}. Here are some things I can do:" (or similar warm, assistant-like copy).
   - Suggestion buttons (grid or flex-wrap layout):
     - "Dig deeper on their tech stack" -- placeholder action (show toast "Coming in a future update" via sonner)
     - "Suggest discovery questions" -- placeholder action (show toast)
     - "Generate full briefing" -- FUNCTIONAL: triggers the existing pre-call workflow. Read the PreCallForm component to understand how it submits. Use the same server action / API call but with default inputs (empty meeting agenda, no notes -- just trigger generation). Show a loading state while generating. On success, show toast "Briefing generated" and call `router.refresh()` to update the prior briefings list.
   - Below the suggestions: a text input area styled like a chat input (rounded, with send button). For Phase 42, this is visual only -- typing and sending shows a toast "Chat coming soon". This establishes the visual pattern for Phase 45.
   - Use lucide icons: Sparkles for the AI header, Send for the input button, Loader2 for loading state.

2. Create `apps/web/src/components/deals/prior-briefings-list.tsx` ("use client"):
   - Props: `interactions: InteractionRecord[]`, `dealId: string`.
   - Filters interactions to only `touchType === "pre_call"` entries.
   - Sorts newest first by `createdAt`.
   - Each briefing is an expandable/collapsible card:
     - Collapsed: shows date (formatted with date-fns `format`), status badge, brief summary (first ~100 chars of generated content if available).
     - Expanded: shows full generated content (parsed from JSON `generatedContent` field), inputs used, and a link to review if applicable.
     - Use a simple disclosure pattern: click header to toggle, with ChevronDown/ChevronUp icon rotation.
   - Empty state: friendly message "No briefings yet. Use the AI assistant above to generate your first briefing."
   - Style: space-y-3 for the list, each card with border rounded-lg bg-white.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -40</automated>
  </verify>
  <done>BriefingChatPanel renders with AI greeting, suggestion buttons (Generate full briefing is functional), and chat input shell. PriorBriefingsList renders expandable cards sorted newest first with empty state. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire up the Briefing page with chat panel and prior briefings</name>
  <files>
    apps/web/src/app/(authenticated)/deals/[dealId]/briefing/page.tsx,
    apps/web/src/app/(authenticated)/deals/[dealId]/briefing/loading.tsx
  </files>
  <action>
1. Replace the placeholder `briefing/page.tsx` with the full Briefing page. Server component.
   - `export const dynamic = "force-dynamic";`
   - Fetch deal: `const { dealId } = await params; const deal = await getDealAction(dealId); if (!deal) notFound();`
   - Extract interactions, companyName, industry from deal.

2. Page layout (stacked sections):
   - Page heading: "Meeting Prep" or "Briefing" (text-xl font-semibold text-slate-900) with a brief description (text-sm text-slate-500): "AI-powered meeting preparation for {companyName}".
   - `BriefingChatPanel` component at the top (pass dealId, companyName, industry).
   - Separator or spacing.
   - Section heading "Previous Briefings" (text-lg font-semibold).
   - `PriorBriefingsList` component (pass interactions and dealId).

3. Create `briefing/loading.tsx`:
   - Skeleton: chat panel area skeleton (card with shimmer), then 2-3 collapsed briefing card skeletons.
   - Use shadcn Skeleton component.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -40 && echo "---" && cd apps/web && npx next build 2>&1 | tail -30</automated>
  </verify>
  <done>Briefing page at /deals/[id]/briefing shows AI chat panel with suggestion buttons and prior briefings list. "Generate full briefing" button triggers the pre-call workflow. Prior briefings are expandable/collapsible. Loading skeleton renders. Full build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project apps/web/tsconfig.json` passes
2. `cd apps/web && npx next build` completes successfully
3. Navigate to `/deals/[id]/briefing` -- AI chat panel visible with greeting and suggestions
4. Click "Generate full briefing" -- triggers pre-call workflow, shows loading state, refreshes page on completion
5. Other suggestion buttons show "coming soon" toast
6. Chat input is visible but shows "coming soon" toast on send
7. Prior briefings section shows existing pre_call interactions, expandable/collapsible
8. Empty state renders when no prior briefings exist
</verification>

<success_criteria>
- Briefing page consolidates all prep/context material in one place (BRIEF-01, BRIEF-02)
- AI chat panel replaces rigid PreCallForm with conversational UI
- "Generate full briefing" is functional (not just a placeholder)
- Prior briefings are displayed newest first, expandable/collapsible
- Full Phase 45 chat is NOT implemented (no streaming, no message history, no cross-page persistence)
- TypeScript compiles clean, Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/42-deal-detail-layout/42-03-SUMMARY.md`
</output>
