---
phase: 45-persistent-ai-chat-bar
plan: 05
type: execute
wave: 4
depends_on: ["45-02"]
files_modified:
  - apps/web/src/app/api/deals/[dealId]/chat/route.ts
  - apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts
  - apps/web/src/app/api/deals/[dealId]/chat/__tests__/route.test.ts
  - apps/web/src/app/api/deals/[dealId]/chat/bindings/__tests__/route.test.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/lib/actions/deal-chat-actions.ts
  - apps/web/src/lib/__tests__/api-client.deal-chat.test.ts
autonomous: true
requirements:
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05

must_haves:
  truths:
    - "Web clients can load existing deal chat history and continue the same thread"
    - "Web clients can stream chat replies without losing the final suggestions, knowledge matches, or confirmation metadata"
    - "A seller-confirmed note or transcript binding can travel from the UI to the agent and back as a typed save result"
  artifacts:
    - path: "apps/web/src/app/api/deals/[dealId]/chat/route.ts"
      provides: "Streaming Next.js proxy for deal chat"
      exports: ["GET", "POST"]
    - path: "apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts"
      provides: "Binding confirmation/correction proxy"
      exports: ["POST"]
    - path: "apps/web/src/lib/actions/deal-chat-actions.ts"
      provides: "Server-action boundary for chat bootstrap and binding mutations"
      exports: ["getDealChatBootstrap", "confirmDealChatBinding"]
  key_links:
    - from: "apps/web/src/app/api/deals/[dealId]/chat/route.ts"
      to: "apps/agent/src/mastra/index.ts"
      via: "streaming proxy to /deals/:dealId/chat"
      pattern: "AGENT_SERVICE_URL.*\/deals\/"
    - from: "apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts"
      to: "apps/agent/src/mastra/index.ts"
      via: "binding confirmation proxy"
      pattern: "bindings"
    - from: "apps/web/src/lib/actions/deal-chat-actions.ts"
      to: "apps/web/src/lib/api-client.ts"
      via: "server actions delegate to typed client helpers"
      pattern: "apiClient"
---

<objective>
Mirror the verified agent runtime through a typed web proxy and server-action bridge for the deal chat UI.

Purpose: The persistent chat UI should consume typed helpers instead of raw fetch details, and binding confirmation needs a stable web-to-agent path before the UI plan wires it into components.
Output: Next.js proxy routes, typed api-client helpers, server actions, and web regression coverage for history load, streaming, and binding confirmation.

Execution order inside this plan is strict: build the proxy routes first, then mirror them through api-client and server actions with focused tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-persistent-ai-chat-bar/45-CONTEXT.md
@.planning/phases/45-persistent-ai-chat-bar/45-02-PLAN.md
@apps/web/src/app/api/deck-structures/chat/route.ts
@apps/web/src/lib/api-client.ts

<interfaces>
From `apps/agent/src/mastra/index.ts`:
```typescript
// Registered in Plan 45-02
GET /deals/:dealId/chat
POST /deals/:dealId/chat
POST /deals/:dealId/chat/bindings
```

From `apps/web/src/lib/api-client.ts`:
```typescript
export interface Deal {
  id: string;
  company?: Company;
  interactions?: InteractionRecord[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the typed Next.js proxy routes for chat streaming and binding confirmation</name>
  <files>
    apps/web/src/app/api/deals/[dealId]/chat/route.ts,
    apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts,
    apps/web/src/app/api/deals/[dealId]/chat/__tests__/route.test.ts,
    apps/web/src/app/api/deals/[dealId]/chat/bindings/__tests__/route.test.ts
  </files>
  <behavior>
    - Test 1: the Next.js proxy forwards `dealId`, route context, and message body to the agent route and preserves the stream.
    - Test 2: the binding proxy forwards confirmation or correction payloads and returns the saved source metadata.
    - Test 3: messy-input turns preserve the runtime's `refineBeforeSave` metadata through the proxy layer.
  </behavior>
  <action>
    Create `apps/web/src/app/api/deals/[dealId]/chat/route.ts` as the Phase 45 equivalent of the deck-structure streaming proxy. Mirror the proven pattern from `apps/web/src/app/api/deck-structures/chat/route.ts`, but target the new agent route family and validate bodies with the shared deal-chat Zod contracts.

    Create `apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts` for confirmation/correction requests. Preserve auth headers, forward typed JSON to the agent, and return structured save results.

    Add focused Vitest coverage for both routes. Keep the route family under `/api/deals/[dealId]/chat`; do not invent a separate `/api/chat` namespace.
  </action>
  <verify>
    <automated>pnpm --filter web exec vitest run "src/app/api/deals/[dealId]/chat/__tests__/route.test.ts" "src/app/api/deals/[dealId]/chat/bindings/__tests__/route.test.ts"</automated>
  </verify>
  <done>The web layer has typed, tested route proxies for chat history/streaming and binding confirmation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose typed api-client helpers and server actions for the deal chat UI</name>
  <files>
    apps/web/src/lib/api-client.ts,
    apps/web/src/lib/actions/deal-chat-actions.ts,
    apps/web/src/lib/__tests__/api-client.deal-chat.test.ts
  </files>
  <behavior>
    - Test 1: the web api-client exposes typed helpers for history load, message send, and binding confirmation without leaking raw fetch details into components.
    - Test 2: server actions mirror the runtime contract from Plan 45-02 exactly, including `refineBeforeSave` metadata and saved-source payloads.
    - Test 3: confirmed saves trigger the revalidation behavior needed by the deal layout and chat UI.
  </behavior>
  <action>
    Extend `apps/web/src/lib/api-client.ts` with typed helpers for loading chat bootstrap/history, posting chat turns, and confirming or correcting pending bindings. Preserve the assistant's `refineBeforeSave` metadata without stripping the suggested refined text or cleanup rationale.

    Create `apps/web/src/lib/actions/deal-chat-actions.ts` as the server-action boundary for Phase 45. Include read helpers callable from the deal layout and mutation helpers that revalidate the current deal route after confirmed saves.

    Mirror the runtime contract from Plan 45-02 exactly rather than redefining local response shapes in the web layer.
  </action>
  <verify>
    <automated>pnpm --filter web exec vitest run "src/lib/__tests__/api-client.deal-chat.test.ts" && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <done>The web app has a typed bridge for chat history, streaming turns, and binding confirmation that components can consume directly.</done>
</task>

</tasks>

<verification>
1. Web proxy tests prove request validation and route forwarding for both chat turns and binding confirmation.
2. The api-client and server actions preserve `refineBeforeSave`, suggestions, and saved-source payloads from the runtime.
3. The route family is `/api/deals/[dealId]/chat` on the web and mirrors the verified agent endpoints from Plan 45-02.
</verification>

<success_criteria>
- The web app has a typed, tested bridge for chat history, streaming turns, and binding confirmation.
- Binding confirmation or correction can flow end to end through stable web contracts.
- The UI plan can consume deal-chat helpers without hand-rolling fetch logic or local payload types.
</success_criteria>

<output>
After completion, create `.planning/phases/45-persistent-ai-chat-bar/45-05-SUMMARY.md`
</output>
