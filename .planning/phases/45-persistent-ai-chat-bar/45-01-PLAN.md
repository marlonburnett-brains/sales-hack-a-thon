---
phase: 45-persistent-ai-chat-bar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/schemas/agent-catalog.ts
  - packages/schemas/deal-chat.ts
  - packages/schemas/index.ts
  - apps/agent/src/lib/agent-catalog-defaults.ts
  - apps/agent/src/lib/__tests__/agent-catalog.test.ts
  - apps/agent/src/lib/__tests__/agent-catalog-defaults.test.ts
autonomous: true
requirements:
  - CHAT-04
  - CHAT-05

must_haves:
  truths:
    - "A brand-new deal chat can open with a short greeting and a few suggested next actions"
    - "The seller sees one consistent response format for direct answers, supporting bullets, knowledge matches, and missing-info callouts"
    - "Transcript or note saves, confirmation chips, and knowledge-result cards all share one typed contract across agent and web layers"
  artifacts:
    - path: "packages/schemas/deal-chat.ts"
      provides: "Shared deal-chat request, route-context, binding, and metadata contracts"
      exports: ["dealChatRouteContextSchema", "dealChatMetaSchema", "dealContextSourceSchema"]
    - path: "packages/schemas/agent-catalog.ts"
      provides: "Governed named-agent identity for deal chat"
      contains: "deal-chat-assistant"
    - path: "apps/agent/src/lib/agent-catalog-defaults.ts"
      provides: "Seeded default prompt and behavior contract for the deal chat assistant"
      contains: "deal-chat-assistant"
  key_links:
    - from: "packages/schemas/index.ts"
      to: "packages/schemas/deal-chat.ts"
      via: "shared schema re-export"
      pattern: "deal-chat"
    - from: "apps/agent/src/lib/agent-catalog-defaults.ts"
      to: "packages/schemas/agent-catalog.ts"
      via: "deal-chat-assistant seeded default"
      pattern: "deal-chat-assistant"
---

<objective>
Define the shared contracts and governed named-agent identity for deal chat before persistence, routes, or UI work begin.

Purpose: Phase 45 only works if every later plan shares one contract surface and one governed assistant identity. This plan locks those interfaces first so persistence, routes, and UI do not invent parallel payloads or prompt behavior.
Output: Shared deal-chat contracts plus a seeded `deal-chat-assistant` named-agent entry with regression coverage.

Execution order inside this plan is straightforward: publish the contracts and named-agent identity here, then let downstream plans build storage, routes, and UI against that exact surface.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-persistent-ai-chat-bar/45-CONTEXT.md
@.planning/phases/45-persistent-ai-chat-bar/45-RESEARCH.md
@.planning/phases/42-deal-detail-layout/42-01-SUMMARY.md
@.planning/phases/42-deal-detail-layout/42-03-SUMMARY.md
@.planning/phases/43-named-agent-architecture/43-01-SUMMARY.md
@.planning/phases/43-named-agent-architecture/43-02-SUMMARY.md
@packages/schemas/agent-catalog.ts

<interfaces>
From `packages/schemas/agent-catalog.ts`:
```typescript
export type AgentId =
  | "company-researcher"
  | "knowledge-result-extractor"
  | "deck-structure-refinement-assistant"
  | ...;

export interface AgentCatalogEntry {
  agentId: AgentId;
  name: string;
  responsibility: string;
  family: AgentFamily;
  isShared: boolean;
  touchTypes: string[];
  sourceSites: string[];
  sourceNotes: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the shared deal-chat contracts and named-agent identity</name>
  <files>
    packages/schemas/deal-chat.ts,
    packages/schemas/index.ts,
    packages/schemas/agent-catalog.ts,
    apps/agent/src/lib/agent-catalog-defaults.ts,
    apps/agent/src/lib/__tests__/agent-catalog.test.ts,
    apps/agent/src/lib/__tests__/agent-catalog-defaults.test.ts
  </files>
  <action>
    Create `packages/schemas/deal-chat.ts` as the interface-first contract for Phase 45. Export Zod schemas and inferred types for:
    - deal route context (`section: "overview" | "briefing" | "touch"`, `touchType`, `pathname`, `pageLabel`)
    - chat send requests and final streamed meta payloads
    - assistant suggestions, binding confirmation state, refine-before-save metadata, inline confirmation chips, and knowledge-match cards
    - deal context source payloads that distinguish `note` vs `transcript` and preserve nullable `touchType` for general deal notes

    Update `packages/schemas/index.ts` to re-export the new surface.

    Extend `packages/schemas/agent-catalog.ts` and `apps/agent/src/lib/agent-catalog-defaults.ts` with a new `deal-chat-assistant` entry. Use one orchestrator agent per the research recommendation; do not add separate transcript-cleanup or per-page agents in this phase. The catalog/default prompt should explicitly encode the locked answer style: short direct answer first, concise bullets, missing-info callouts, top knowledge matches with why-fit explanations, and optional next steps.

    Update the existing catalog/default tests so the new agent is required by the shared governance surface.
  </action>
  <verify>
    <automated>pnpm --filter agent exec vitest run "src/lib/__tests__/agent-catalog.test.ts" "src/lib/__tests__/agent-catalog-defaults.test.ts"</automated>
  </verify>
  <done>`packages/schemas/deal-chat.ts` exists and the repo has a first-class `deal-chat-assistant` named-agent entry with seeded defaults and regression coverage.</done>
</task>

</tasks>

<verification>
1. `deal-chat-assistant` appears in the shared catalog/default prompt tests.
2. `packages/schemas/deal-chat.ts` exports the route, message, binding, and metadata contracts used by later plans.
3. The new agent identity formalizes the locked response style and knowledge-match framing before runtime wiring begins.
</verification>

<success_criteria>
- The repo has a shared contract for Phase 45 request/response/meta payloads.
- The new prompt-bearing behavior is formalized as a named agent before runtime wiring begins.
- Later plans can import one canonical deal-chat schema surface instead of redefining local payloads.
</success_criteria>

<output>
After completion, create `.planning/phases/45-persistent-ai-chat-bar/45-01-SUMMARY.md`
</output>
