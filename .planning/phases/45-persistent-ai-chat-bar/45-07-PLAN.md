---
phase: 45-persistent-ai-chat-bar
plan: 07
type: execute
wave: 7
depends_on:
  - 45-05
  - 45-06
files_modified:
  - apps/web/src/app/api/deals/[dealId]/chat/route.ts
  - apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts
  - apps/web/src/app/api/deals/[dealId]/chat/__tests__/route.test.ts
  - apps/web/src/app/api/deals/[dealId]/chat/bindings/__tests__/route.test.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/lib/__tests__/api-client.deal-chat.test.ts
  - apps/web/src/lib/__tests__/api-client-actions.test.ts
  - apps/web/src/lib/__tests__/slide-api-client.test.ts
autonomous: true
gap_closure: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
must_haves:
  truths:
    - "Users can bootstrap the persistent deal chat from any deal sub-page because the protected web-to-agent bridge authenticates with the declared X-API-Key contract"
    - "Users can send note, transcript, deal-history, and knowledge-query turns through the same protected runtime without bearer-header mismatches on streamed chat requests or binding saves"
    - "Deal-chat server actions use the same X-API-Key contract as the route proxies, so persistent chat flows stay wired end to end across bootstrap, send, and binding confirmation"
  artifacts:
    - path: "apps/web/src/app/api/deals/[dealId]/chat/route.ts"
      provides: "Deal-chat bootstrap and streamed send proxy sends X-API-Key plus passthrough Google/user headers"
      exports: ["GET", "POST"]
    - path: "apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts"
      provides: "Deal-chat binding proxy sends X-API-Key on confirmation and correction requests"
      exports: ["POST"]
    - path: "apps/web/src/lib/api-client.ts"
      provides: "Server-side agent fetch helper and deal-chat bootstrap/binding helpers match Mastra SimpleAuth header contract"
      exports: ["getDealChatBootstrap", "sendDealChatMessage", "confirmDealChatBinding"]
    - path: "apps/web/src/lib/__tests__/api-client.deal-chat.test.ts"
      provides: "Regression coverage for X-API-Key on server-side deal-chat bootstrap, send, and binding helpers"
      contains: "X-API-Key"
  key_links:
    - from: "apps/web/src/app/api/deals/[dealId]/chat/route.ts"
      to: "apps/agent/src/mastra/index.ts"
      via: "protected GET/POST /deals/:dealId/chat proxy headers"
      pattern: "X-API-Key"
    - from: "apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts"
      to: "apps/agent/src/mastra/index.ts"
      via: "protected POST /deals/:dealId/chat/bindings proxy headers"
      pattern: "X-API-Key"
    - from: "apps/web/src/lib/api-client.ts"
      to: "apps/agent/src/mastra/index.ts"
      via: "shared fetchAgent auth header for bootstrap and binding server actions"
      pattern: "X-API-Key"
---

<objective>
Close the final Phase 45 verification blocker by aligning every deal-chat web-to-agent request with the agent runtime's required `X-API-Key` auth contract.

Purpose: Phase 45 is still blocked even though the persistent UI, transcript flow, and grounded assistant logic are shipped, because the browser and server-side bridges still authenticate with bearer headers that the protected agent runtime does not accept.
Output: One focused gap-closure pass that updates the deal-chat route proxies, shared server-side api-client auth seam, and regression coverage for bootstrap, streamed turns, and binding confirmations.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-persistent-ai-chat-bar/45-CONTEXT.md
@.planning/phases/45-persistent-ai-chat-bar/45-RESEARCH.md
@.planning/phases/45-persistent-ai-chat-bar/45-VERIFICATION.md
@.planning/phases/45-persistent-ai-chat-bar/45-05-SUMMARY.md
@.planning/phases/45-persistent-ai-chat-bar/45-06-SUMMARY.md
@apps/web/src/app/api/deals/[dealId]/chat/route.ts
@apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts
@apps/web/src/lib/api-client.ts
@apps/agent/src/mastra/index.ts

<interfaces>
From `apps/agent/src/mastra/index.ts`:
```typescript
const auth = new SimpleAuth({
  headers: ["X-API-Key"],
  tokens: {
    [env.AGENT_API_KEY]: { id: "web-app", role: "service" },
  },
});
```

From `apps/web/src/app/api/deals/[dealId]/chat/route.ts`:
```typescript
function buildProxyHeaders(request: NextRequest): HeadersInit {
  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    Authorization: `Bearer ${env.AGENT_API_KEY}`,
  };
```

From `apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts`:
```typescript
function buildProxyHeaders(request: NextRequest): HeadersInit {
  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    Authorization: `Bearer ${env.AGENT_API_KEY}`,
  };
```

From `apps/web/src/lib/api-client.ts`:
```typescript
async function fetchAgent(path: string, init?: RequestInit): Promise<Response> {
  return await fetch(`${BASE_URL}${path}`, {
    ...init,
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${env.AGENT_API_KEY}`,
      ...init?.headers,
    },
  });
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align the deal-chat web proxy routes with the agent runtime header contract</name>
  <files>
    apps/web/src/app/api/deals/[dealId]/chat/route.ts,
    apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts,
    apps/web/src/app/api/deals/[dealId]/chat/__tests__/route.test.ts,
    apps/web/src/app/api/deals/[dealId]/chat/bindings/__tests__/route.test.ts
  </files>
  <behavior>
    - Test 1: bootstrap GET and streamed POST proxy requests send `X-API-Key` instead of `Authorization` while still passing through `X-Google-Access-Token` and `X-User-Id`.
    - Test 2: binding confirmation and correction requests use the same `X-API-Key` contract and keep the existing request body and response shape unchanged.
    - Test 3: route tests fail if a bearer header is reintroduced on the deal-chat proxy path.
  </behavior>
  <action>
    Update `apps/web/src/app/api/deals/[dealId]/chat/route.ts` and `apps/web/src/app/api/deals/[dealId]/chat/bindings/route.ts` so `buildProxyHeaders()` sends `X-API-Key: ${env.AGENT_API_KEY}` on every request to the protected agent deal-chat routes. Preserve `Content-Type`, `X-Google-Access-Token`, and `X-User-Id` passthrough behavior exactly; the change is the auth header contract, not the payload or stream contract.

    Keep the route family and payload shapes exactly as Phase 45 already established: `/api/deals/[dealId]/chat` still handles bootstrap and streaming turns, `/api/deals/[dealId]/chat/bindings` still handles confirmation/correction saves, and the final `---DEAL_CHAT_META---` delimiter must stay untouched. Do not add an `Authorization` fallback because the agent runtime is explicitly configured for `X-API-Key` only.

    Update the focused route regression tests to assert the protected calls now carry `X-API-Key`, preserve the Google/user passthrough headers, and do not regress upload-aware payload forwarding or binding result handling.
  </action>
  <verify>
    <automated>pnpm --filter web exec vitest run "src/app/api/deals/[dealId]/chat/__tests__/route.test.ts" "src/app/api/deals/[dealId]/chat/bindings/__tests__/route.test.ts"</automated>
  </verify>
  <done>The Next.js deal-chat proxy routes authenticate with `X-API-Key` for bootstrap, streamed turns, and binding saves, and the route tests lock that contract in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align the shared server-side api-client seam and auth regressions</name>
  <files>
    apps/web/src/lib/api-client.ts,
    apps/web/src/lib/__tests__/api-client.deal-chat.test.ts,
    apps/web/src/lib/__tests__/api-client-actions.test.ts,
    apps/web/src/lib/__tests__/slide-api-client.test.ts
  </files>
  <behavior>
    - Test 1: `getDealChatBootstrap()`, `sendDealChatMessage()`, and `confirmDealChatBinding()` call the protected agent runtime with `X-API-Key` plus Google/user passthrough headers.
    - Test 2: the shared `fetchAgent()` helper no longer emits `Authorization` bearer headers, so server-side deal-chat actions match the Mastra `SimpleAuth` contract.
    - Test 3: existing shared api-client tests that assert the common service auth header are updated to the new `X-API-Key` contract so the helper change is covered beyond deal chat only.
  </behavior>
  <action>
    Update `apps/web/src/lib/api-client.ts` so the shared `fetchAgent()` helper uses `X-API-Key` instead of `Authorization`. Keep this as one shared fix rather than introducing a deal-chat-only override, because the verification gap explicitly calls out the server-side api-client seam and the agent runtime contract is global. Preserve all current request URLs, JSON bodies, and Google auth passthrough logic.

    Refresh `apps/web/src/lib/__tests__/api-client.deal-chat.test.ts` to assert `X-API-Key` on bootstrap, send, and binding helper calls, and add explicit coverage that `X-Google-Access-Token` and `X-User-Id` still survive the helper path. Because this change updates the common fetch helper, also align `apps/web/src/lib/__tests__/api-client-actions.test.ts` and `apps/web/src/lib/__tests__/slide-api-client.test.ts` anywhere they still lock in the old bearer header contract. Do not change unrelated route handlers like settings, deck-structures, or agent-management chat in this gap-closure pass.

    Treat this as a regression-hardening step for the deal-chat bridge: the executor should leave no remaining `Authorization` assertions in the touched api-client tests, and the deal-chat server-action seam must continue returning the same typed bootstrap, stream, and binding data as before.
  </action>
  <verify>
    <automated>pnpm --filter web exec vitest run "src/lib/__tests__/api-client.deal-chat.test.ts" "src/lib/__tests__/api-client-actions.test.ts" "src/lib/__tests__/slide-api-client.test.ts"</automated>
  </verify>
  <done>The shared server-side api-client uses `X-API-Key`, deal-chat helpers still return the same typed data, and updated tests prevent the old bearer-header regression from returning.</done>
</task>

</tasks>

<verification>
1. Run the targeted web vitest suites for the deal-chat proxy routes and shared api-client helpers; all affected tests should now assert `X-API-Key` rather than `Authorization`.
2. Re-run the persistent deal-chat bootstrap, streamed send, and binding confirmation flows through the web bridge after the header change to confirm the protected Mastra routes are wired end to end.
3. Confirm transcript upload, note binding, grounded Q&amp;A, and knowledge-query flows still rely on the existing `---DEAL_CHAT_META---` response contract after the auth fix.
</verification>

<success_criteria>
- The deal-chat web proxy routes send `X-API-Key` on bootstrap, streamed turns, and binding saves.
- The shared server-side api-client seam used by deal-chat server actions also sends `X-API-Key` and preserves Google/user passthrough headers.
- Focused regressions fail on `Authorization` and pass on `X-API-Key`, closing the remaining Phase 45 verification blocker without changing the shipped persistent chat UX or response contract.
</success_criteria>

<output>
After completion, create `.planning/phases/45-persistent-ai-chat-bar/45-07-SUMMARY.md`
</output>
