---
phase: 45-persistent-ai-chat-bar
plan: 04
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - apps/agent/prisma/schema.prisma
  - apps/agent/prisma/migrations/20260308223000_persistent_ai_chat_bar/migration.sql
  - apps/agent/src/deal-chat/persistence.ts
  - apps/agent/src/deal-chat/bindings.ts
  - apps/agent/src/deal-chat/__tests__/persistence.test.ts
  - apps/agent/src/deal-chat/__tests__/bindings.test.ts
autonomous: true
requirements:
  - CHAT-02
  - CHAT-03

must_haves:
  truths:
    - "A deal keeps one persistent chat thread that can survive page changes and later return visits"
    - "A seller can save pasted notes or transcript text either to a specific touch or as general deal notes when they are on Overview or Briefing"
    - "Saved context keeps its original page or touch visible when the assistant reuses it later"
    - "Ambiguous note or transcript bindings require seller confirmation before anything is saved"
  artifacts:
    - path: "apps/agent/prisma/schema.prisma"
      provides: "Deal chat thread, message, and generic deal context source models"
      contains: "model DealChatThread"
    - path: "apps/agent/src/deal-chat/persistence.ts"
      provides: "Persistence helpers for threads, messages, summaries, and source saves"
      exports: ["getOrCreateDealChatThread", "appendDealChatMessage", "saveDealContextSource"]
    - path: "apps/agent/src/deal-chat/bindings.ts"
      provides: "Best-guess touch binding and ambiguity detection helpers"
      exports: ["inferDealContextBinding", "detectDealContextNeedsReview"]
  key_links:
    - from: "apps/agent/prisma/schema.prisma"
      to: "Deal"
      via: "unique dealId relation"
      pattern: "dealId.*@unique"
    - from: "apps/agent/src/deal-chat/persistence.ts"
      to: "DealChatThread"
      via: "Prisma reads and writes"
      pattern: "prisma\\.dealChatThread\\.(find|upsert|update)"
    - from: "apps/agent/src/deal-chat/bindings.ts"
      to: "packages/schemas/deal-chat.ts"
      via: "shared binding/result types"
      pattern: "@lumenalta/schemas"
---

<objective>
Add the durable storage layer and confirmation-aware binding heuristics behind the deal chat contracts.

Purpose: Phase 45 only feels persistent if one deal thread, saved sources, and seller-confirmed note/transcript bindings all live in durable storage instead of ephemeral route state.
Output: Prisma models and migration for deal chat, persistence helpers, binding heuristics, and focused regression coverage.

Execution order inside this plan is strict: define the Prisma schema and migration first, then implement helpers and tests against that finalized storage shape.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-persistent-ai-chat-bar/45-CONTEXT.md
@.planning/phases/45-persistent-ai-chat-bar/45-RESEARCH.md
@.planning/phases/45-persistent-ai-chat-bar/45-01-PLAN.md
@apps/agent/prisma/schema.prisma

<interfaces>
From `packages/schemas/deal-chat.ts`:
```typescript
export const dealChatRouteContextSchema = z.object({
  section: z.enum(["overview", "briefing", "touch"]),
  touchType: z.string().nullable().optional(),
  pathname: z.string(),
  pageLabel: z.string(),
});

export const dealContextSourceSchema = z.object({
  sourceType: z.enum(["note", "transcript"]),
  touchType: z.string().nullable(),
  originPage: z.string(),
  rawText: z.string(),
});
```

From `apps/agent/prisma/schema.prisma`:
```prisma
model Deal {
  id           String              @id @default(cuid())
  interactions InteractionRecord[]
}

model InteractionRecord {
  id        String @id @default(cuid())
  dealId    String
  touchType String
  status    String @default("pending")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the forward-only Prisma models and migration for persistent deal chat</name>
  <files>
    apps/agent/prisma/schema.prisma,
    apps/agent/prisma/migrations/20260308223000_persistent_ai_chat_bar/migration.sql
  </files>
  <action>
    Update `apps/agent/prisma/schema.prisma` with the Phase 45 storage models:
    - `DealChatThread` with unique `dealId`, nullable `promptSummary`, and timestamps
    - `DealChatMessage` linked to the thread, storing role, content, active route context, and optional `metaJson`
    - `DealContextSource` linked to `dealId`, with `sourceType`, nullable `touchType`, nullable `interactionId`, `originPage`, `rawText`, optional `refinedText`, `status`, and optional `bindingMetaJson`

    Create `apps/agent/prisma/migrations/20260308223000_persistent_ai_chat_bar/migration.sql` using repo-safe migration discipline. Start with `pnpm --filter agent exec prisma migrate dev --name persistent_ai_chat_bar --create-only`, inspect the SQL, and if shared-db drift blocks generation, keep the migration forward-only and manual instead of resetting or using `db push`.

    Do not reuse `Transcript` as the primary storage model for this phase.
  </action>
  <verify>
    <automated>pnpm --filter agent exec prisma validate</automated>
  </verify>
  <done>`DealChatThread`, `DealChatMessage`, and `DealContextSource` exist in Prisma with a committed forward-only migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement persistence helpers and confirmation-aware binding heuristics</name>
  <files>
    apps/agent/src/deal-chat/persistence.ts,
    apps/agent/src/deal-chat/bindings.ts,
    apps/agent/src/deal-chat/__tests__/persistence.test.ts,
    apps/agent/src/deal-chat/__tests__/bindings.test.ts
  </files>
  <behavior>
    - Test 1: `getOrCreateDealChatThread(dealId)` always returns the same thread for repeated calls on one deal.
    - Test 2: saved chat messages keep full seller-visible history while `promptSummary` stays separate for compaction.
    - Test 3: pasted content on `overview` or `briefing` can resolve to either a specific touch or a general deal note, but ambiguous input requires confirmation.
    - Test 4: generic note/transcript intake keeps origin page or touch metadata without requiring fake `InteractionRecord`, `Transcript`, or `subsector` data.
  </behavior>
  <action>
    Implement `apps/agent/src/deal-chat/persistence.ts` with explicit helpers for thread creation/loading, message append/read, prompt-summary updates, and context-source save/confirm flows.

    Implement `apps/agent/src/deal-chat/bindings.ts` with deterministic heuristics that honor the locked decisions:
    - use current route/touch first, then recent chat context, then matching `InteractionRecord` touch history
    - on `overview` or `briefing`, allow `touchType = null` to mean general deal notes
    - return a best guess plus `requiresConfirmation`; do not auto-save uncertain bindings
    - flag obviously partial, messy, or ambiguous transcript-like input for review/refinement before save

    Consume the `packages/schemas/deal-chat.ts` contract surface from Plan 45-01 instead of inventing local payload types.
  </action>
  <verify>
    <automated>pnpm --filter agent exec vitest run "src/deal-chat/__tests__/persistence.test.ts" "src/deal-chat/__tests__/bindings.test.ts" && pnpm --filter agent exec prisma validate</automated>
  </verify>
  <done>One thread per deal, generic note/transcript storage, and confirmation-aware binding heuristics are implemented and regression-tested.</done>
</task>

</tasks>

<verification>
1. Prisma schema contains `DealChatThread`, `DealChatMessage`, and `DealContextSource`.
2. Persistence helpers keep one thread per deal and preserve origin metadata for saved sources.
3. Binding helpers can represent both touch-bound and general deal notes, and ambiguous cases stay pending until confirmation.
4. No task reuses `Transcript` as the primary Phase 45 storage model.
</verification>

<success_criteria>
- The backend has durable storage for one deal-wide thread and generic context sources.
- Sellers can save notes or transcript text without creating fake touch artifacts.
- The storage layer preserves where saved context came from when the assistant references it later.
- Binding heuristics implement best-guess plus confirmation, not silent autosave.
</success_criteria>

<output>
After completion, create `.planning/phases/45-persistent-ai-chat-bar/45-04-SUMMARY.md`
</output>
