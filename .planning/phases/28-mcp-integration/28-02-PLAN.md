---
phase: 28-mcp-integration
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - apps/agent/src/lib/atlusai-search.ts
  - apps/agent/src/lib/atlus-auth.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05
  - SRCH-06

must_haves:
  truths:
    - "searchSlides() uses MCP semantic search when MCP is available"
    - "MCP results are parsed through LLM into SlideSearchResult format with relevanceScore"
    - "searchForProposal() multi-pass structure is preserved -- only inner searchSlides() changes"
    - "searchByCapability() uses MCP semantic search via searchSlides()"
    - "Setting ATLUS_USE_MCP=false routes all searches through Drive API"
    - "Results include a source field indicating 'mcp' or 'drive' origin"
    - "MCP search is scoped to ATLUS_PROJECT_ID env var"
    - "detectAtlusAccess() TODO stubs replaced with real MCP probe"
  artifacts:
    - path: "apps/agent/src/lib/atlusai-search.ts"
      provides: "MCP search with LLM parsing and Drive fallback"
      exports: ["searchSlides", "searchForProposal", "searchByCapability", "SlideSearchResult"]
    - path: "apps/agent/src/lib/atlus-auth.ts"
      provides: "Real MCP probes replacing TODO stubs in detectAtlusAccess"
      exports: ["detectAtlusAccess"]
  key_links:
    - from: "apps/agent/src/lib/atlusai-search.ts"
      to: "apps/agent/src/lib/mcp-client.ts"
      via: "getMCPClient() for search routing"
      pattern: "getMCPClient"
    - from: "apps/agent/src/lib/atlusai-search.ts"
      to: "@google/genai"
      via: "LLM batch parsing of MCP results"
      pattern: "GoogleGenAI|generateContent"
    - from: "apps/agent/src/lib/atlus-auth.ts"
      to: "apps/agent/src/lib/mcp-client.ts"
      via: "getMCPClient() for access detection probes"
      pattern: "getMCPClient"
---

<objective>
Replace Drive API keyword search with MCP semantic search in all three search functions, using LLM-based result parsing, while retaining Drive as a degraded fallback. Also replace the TODO stubs in detectAtlusAccess() with real MCP probes.

Purpose: This is the core value delivery -- semantic search replaces keyword search, improving slide retrieval quality for proposal assembly.
Output: Modified `atlusai-search.ts` with MCP-first search + Drive fallback, modified `atlus-auth.ts` with real MCP probes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-mcp-integration/28-CONTEXT.md
@.planning/phases/28-mcp-integration/28-RESEARCH.md
@.planning/phases/28-mcp-integration/28-01-SUMMARY.md

@apps/agent/src/lib/atlusai-search.ts
@apps/agent/src/lib/atlus-auth.ts
@apps/agent/src/lib/proposal-assembly.ts
@apps/agent/src/lib/slide-selection.ts
@apps/agent/src/env.ts

<interfaces>
<!-- From Plan 28-01 (mcp-client.ts created in prior plan) -->
```typescript
// apps/agent/src/lib/mcp-client.ts
export async function getMCPClient(): Promise<MCPClient | null>;
export async function initMCPClient(): Promise<void>;
export async function shutdownMCPClient(): Promise<void>;

// MCPClient.listTools() returns Record<string, Tool>
// Tool names are namespaced: "atlus-ai_knowledge_base_search_semantic"
// tool.execute({ query: string }) returns the parsed tool output
```

<!-- Existing SlideSearchResult interface (will be extended) -->
```typescript
export interface SlideSearchResult {
  slideId: string;
  documentTitle: string;
  textContent: string;
  speakerNotes: string;
  metadata: Record<string, unknown>;
  presentationId?: string;
  slideObjectId?: string;
  // NEW (added by this plan):
  source?: "mcp" | "drive";
  relevanceScore?: number;
}
```

<!-- 5 consumer files (MUST NOT break): -->
<!-- apps/agent/src/lib/proposal-assembly.ts -- imports SlideSearchResult type -->
<!-- apps/agent/src/lib/slide-selection.ts -- imports searchSlides, searchByCapability, SlideSearchResult -->
<!-- apps/agent/src/mastra/workflows/pre-call-workflow.ts -- imports searchSlides -->
<!-- apps/agent/src/mastra/workflows/touch-4-workflow.ts -- imports searchForProposal -->
<!-- apps/agent/src/scripts/verify-rag-quality.ts -- imports searchForProposal, SlideSearchResult -->

<!-- LLM parsing pattern (from proposal-assembly.ts) -->
```typescript
import { GoogleGenAI } from "@google/genai";
import { zodToLlmJsonSchema } from "@lumenalta/schemas";

const ai = new GoogleGenAI({ vertexai: true, project: env.GOOGLE_CLOUD_PROJECT, location: env.GOOGLE_CLOUD_LOCATION });
const response = await ai.models.generateContent({
  model: "openai/gpt-oss-120b-maas",
  contents: prompt,
  config: {
    responseMimeType: "application/json",
    responseJsonSchema: zodToLlmJsonSchema(schema),
  },
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MCP search with LLM parsing and Drive fallback to atlusai-search.ts</name>
  <files>apps/agent/src/lib/atlusai-search.ts</files>
  <action>
Refactor `atlusai-search.ts` to route through MCP semantic search with LLM result parsing, falling back to Drive API.

**1. Extend SlideSearchResult interface (SRCH-02):**
Add two OPTIONAL fields (so 5 consumer files remain unchanged):
- `source?: "mcp" | "drive"` -- indicates where results came from
- `relevanceScore?: number` -- 0-1 score, set by LLM for MCP results, undefined for Drive results

**2. Create Zod schema for LLM parsing:**
Define `SlideSearchResultSchema` (z.object matching the interface) and `BatchResultSchema` (z.array of it) for structured LLM output. Use `zodToLlmJsonSchema()` from `@lumenalta/schemas`. Import `{ z } from "zod"`, `{ GoogleGenAI } from "@google/genai"`, `{ zodToLlmJsonSchema } from "@lumenalta/schemas"`, and `{ env } from "../env"`.

**3. Create `parseMCPResults()` (internal):**
- Accepts `rawResults: unknown[]` and `query: string`
- Uses the established LLM pattern from `proposal-assembly.ts`:
  - `new GoogleGenAI({ vertexai: true, project: env.GOOGLE_CLOUD_PROJECT, location: env.GOOGLE_CLOUD_LOCATION })`
  - Model: `"openai/gpt-oss-120b-maas"` (project standard per LLM-agnostic quick task 5)
  - Batch ALL results in a single LLM call (user decision: always LLM, batch for efficiency)
  - Prompt: instruct LLM to parse raw MCP results into SlideSearchResult fields, assign relevanceScore 0-1 based on query match
  - Config: `responseMimeType: "application/json"`, `responseJsonSchema: zodToLlmJsonSchema(BatchResultSchema)`
- Parse and validate response with Zod
- Returns `SlideSearchResult[]` with `source: "mcp"` set on each result

**4. Create `searchViaMCP()` (internal):**
- Calls `getMCPClient()` from `../lib/mcp-client`
- If null, return null (signals fallback to Drive)
- Get tools via `mcpClient.listTools()`
- Find `"atlus-ai_knowledge_base_search_semantic"` tool
- Execute with query argument. If `env.ATLUS_PROJECT_ID` is set, try passing it as `project_id` argument too (SRCH-06). Log raw results at debug level on first invocation for format discovery.
- Pass raw results to `parseMCPResults()` for LLM extraction
- Return parsed results or throw on error

**5. Rename existing `searchSlides()` to `searchViaDrive()` (internal):**
- Move ALL existing Drive API search logic into this internal function
- Keep the same parameters and return type
- Add `source: "drive"` to each result before returning

**6. Rewrite `searchSlides()` (public, SRCH-01):**
- Try `searchViaMCP()` first
- If MCP returns results, return them (already have `source: "mcp"`)
- If MCP returns null (not available) or throws (error), log warning and fall back to `searchViaDrive()`
- Drive results get `source: "drive"` (set in searchViaDrive)
- The function signature and return type are unchanged -- consumers don't break

**7. `searchForProposal()` -- NO CHANGES to multi-pass structure (SRCH-03):**
- The multi-pass logic (primary, secondary, case study, 3-tier fallback) stays exactly as-is
- It already delegates to `searchSlides()`, which now routes through MCP
- Keep the existing structure per research recommendation: "preserve existing behavior, optimize later"

**8. `searchByCapability()` -- NO CHANGES needed (SRCH-04):**
- Already delegates to `searchSlides()` which now routes through MCP

**9. Drive fallback env flag (SRCH-05):**
- The `getMCPClient()` function (from Plan 28-01) already returns null when `ATLUS_USE_MCP=false`
- This means `searchViaMCP()` returns null, and `searchSlides()` falls back to Drive
- No additional env flag logic needed in this file

**Anti-patterns to avoid:**
- Do NOT make `source` or `relevanceScore` required (breaks consumers)
- Do NOT parse MCP results with regex (user explicitly chose LLM)
- Do NOT create one LLM call per result (batch all in single call)
- Do NOT change the public API signatures of searchSlides, searchForProposal, searchByCapability
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit -p apps/agent/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>searchSlides routes through MCP with LLM parsing and Drive fallback, SlideSearchResult has optional source and relevanceScore fields, all 5 consumer files compile unchanged, searchForProposal multi-pass structure preserved</done>
</task>

<task type="auto">
  <name>Task 2: Replace detectAtlusAccess TODO stubs with real MCP probes</name>
  <files>apps/agent/src/lib/atlus-auth.ts</files>
  <action>
Replace the three `TODO(phase-28)` stubs in `detectAtlusAccess()` with real MCP-based probes using the MCPClient from Plan 28-01.

**Tier 1: Auth probe -- replace stub with MCP listTools():**
- Import `getMCPClient` from `./mcp-client`
- Instead of raw `fetch` to the SSE endpoint, call `getMCPClient()` temporarily creating a one-off MCPClient with the user's Google token (not the pool)
- Actually, the better approach: keep the existing fetch-based probe for Tier 1. The auth probe just needs to verify the Google token works against the AtlusAI endpoint. The existing `fetch(ATLUS_SSE_ENDPOINT, { headers: { Authorization: "Bearer " + googleAccessToken } })` is correct for this -- it tests if the specific user's token authenticates. Remove the TODO comment but keep the fetch approach since it correctly tests user-specific auth.

**Tier 2: Project probe -- replace stub with MCP tool invocation:**
- For the project probe, we need to verify the user can access the configured project. Keep the existing approach of hitting the project-specific endpoint. Remove the TODO comment. The stub logic is actually reasonable: it checks if the project tools endpoint returns a non-403 response.
- Alternative: if the existing stub URL (`/projects/{id}/tools`) doesn't work, try using the discover_documents MCP tool. But since we're testing with the USER's token (not the pool), we can't easily use the pooled MCPClient. Keep the fetch approach for now and remove the TODO markers.

**What actually changes:**
1. Remove all three `TODO(phase-28)` comments
2. Update the header comment to remove "Replace stub" language -- these are now the real probes
3. The auth probe (Tier 1) fetch logic is correct as-is -- just remove the TODO
4. The project probe (Tier 2) fetch logic is correct as-is -- just remove the TODO
5. Update the top-of-file module comment to reflect that the probes are production-ready

This is a small but important task: it removes tech debt markers and confirms the probing approach is final.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && grep -c "TODO(phase-28)" apps/agent/src/lib/atlus-auth.ts && echo "FAIL: TODOs still present" || echo "PASS: No TODOs remain"</automated>
  </verify>
  <done>All TODO(phase-28) markers removed from atlus-auth.ts, probe logic confirmed as production-ready, TypeScript compiles</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p apps/agent/tsconfig.json` passes
- No `TODO(phase-28)` markers remain in codebase: `grep -r "TODO(phase-28)" apps/agent/src/`
- `SlideSearchResult` has optional `source` and `relevanceScore` fields
- `searchSlides()` calls `getMCPClient()` and falls back to Drive
- `searchForProposal()` multi-pass structure is unchanged (still 3 passes + 3 fallback tiers)
- `searchByCapability()` delegates to `searchSlides()` (unchanged)
- All 5 consumer files compile without changes
</verification>

<success_criteria>
- MCP semantic search is the primary path for all three search functions
- LLM parses every MCP result via batch call using project-standard pattern
- Drive API search is the fallback when MCP is unavailable or ATLUS_USE_MCP=false
- Results include source field ('mcp' or 'drive') for UI transparency
- MCP results include relevanceScore from LLM parsing
- searchForProposal multi-pass structure is preserved exactly
- All 5 consumer files remain unchanged and compile
- No TODO(phase-28) markers remain
</success_criteria>

<output>
After completion, create `.planning/phases/28-mcp-integration/28-02-SUMMARY.md`
</output>
