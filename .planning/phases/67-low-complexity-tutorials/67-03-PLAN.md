---
phase: 67-low-complexity-tutorials
plan: 03
type: execute
wave: 1
depends_on: ["67-02"]
files_modified: []
autonomous: true
gap_closure: true
requirements: [TUT-02, TUT-03]

must_haves:
  truths:
    - "Google Drive Settings tutorial has TTS audio output (WAV files + timing.json)"
    - "Action Center tutorial has TTS audio output (WAV files + timing.json)"
    - "Google Drive Settings tutorial rendered as MP4 video"
    - "Action Center tutorial rendered as MP4 video"
  artifacts:
    - path: "apps/tutorials/audio/google-drive-settings/"
      provides: "TTS audio files for Google Drive Settings tutorial"
    - path: "apps/tutorials/audio/action-center/"
      provides: "TTS audio files for Action Center tutorial"
    - path: "apps/tutorials/videos/google-drive-settings.mp4"
      provides: "Rendered MP4 for Google Drive Settings tutorial"
    - path: "apps/tutorials/videos/action-center.mp4"
      provides: "Rendered MP4 for Action Center tutorial"
  key_links:
    - from: "fixtures/google-drive-settings/script.json"
      to: "audio/google-drive-settings/"
      via: "TTS pipeline reads narration text from script steps"
      pattern: "pnpm tts google-drive-settings"
    - from: "fixtures/action-center/script.json"
      to: "audio/action-center/"
      via: "TTS pipeline reads narration text from script steps"
      pattern: "pnpm tts action-center"
    - from: "audio/google-drive-settings/timing.json"
      to: "videos/google-drive-settings.mp4"
      via: "Remotion render uses timing manifest for frame counts"
      pattern: "pnpm render google-drive-settings"
    - from: "audio/action-center/timing.json"
      to: "videos/action-center.mp4"
      via: "Remotion render uses timing manifest for frame counts"
      pattern: "pnpm render action-center"
---

<objective>
Run TTS narration and Remotion render pipelines for Google Drive Settings and Action Center tutorials to produce final MP4 videos.

Purpose: Phase 67 verification found that while all 3 tutorials were captured successfully (screenshots exist), only Getting Started was run through TTS and render. Google Drive Settings and Action Center are missing audio output and MP4 renders.

Output: 2 sets of WAV audio files + timing manifests, 2 MP4 video files
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-low-complexity-tutorials/67-02-SUMMARY.md
@.planning/phases/67-low-complexity-tutorials/67-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate TTS audio for Google Drive Settings and Action Center tutorials</name>
  <files></files>
  <action>
Run the TTS pipeline for both tutorials that are missing audio output. The pipeline is already proven by Getting Started (audio/getting-started/ has 8 WAVs + timing.json).

1. Run TTS for Google Drive Settings:
   ```
   cd apps/tutorials && pnpm tts google-drive-settings
   ```
   This reads fixtures/google-drive-settings/script.json (5 steps), generates WAV files per step, and produces audio/google-drive-settings/timing.json with duration data.

2. Run TTS for Action Center:
   ```
   cd apps/tutorials && pnpm tts action-center
   ```
   This reads fixtures/action-center/script.json (7 steps), generates WAV files per step, and produces audio/action-center/timing.json with duration data.

3. Verify output:
   - audio/google-drive-settings/ should contain 5 WAV files + timing.json
   - audio/action-center/ should contain 7 WAV files + timing.json

Use the default draft engine (Kokoro) unless the user specifies --engine chatterbox. The draft engine runs on CPU with no Python dependency.

If TTS fails with module resolution errors, check that the tutorials workspace has been built first: `pnpm --filter tutorials build` or run from the monorepo root.
  </action>
  <verify>
    <automated>ls -la apps/tutorials/audio/google-drive-settings/*.wav apps/tutorials/audio/google-drive-settings/timing.json apps/tutorials/audio/action-center/*.wav apps/tutorials/audio/action-center/timing.json 2>/dev/null | wc -l</automated>
  </verify>
  <done>audio/google-drive-settings/ contains 5 WAV files + timing.json (6 files total), audio/action-center/ contains 7 WAV files + timing.json (8 files total) — 14 files total</done>
</task>

<task type="auto">
  <name>Task 2: Render MP4 videos for Google Drive Settings and Action Center tutorials</name>
  <files></files>
  <action>
Run the Remotion render pipeline for both tutorials. The pipeline is already proven by Getting Started (videos/getting-started.mp4 exists at 8.1MB).

1. Render Google Drive Settings:
   ```
   cd apps/tutorials && pnpm render google-drive-settings
   ```
   This reads output/google-drive-settings/ screenshots (5 PNGs) + audio/google-drive-settings/timing.json, composes a Remotion video with visual effects (zoom, callout, cursor, transitions, intro/outro slates), and outputs videos/google-drive-settings.mp4.

2. Render Action Center:
   ```
   cd apps/tutorials && pnpm render action-center
   ```
   This reads output/action-center/ screenshots (7 PNGs) + audio/action-center/timing.json, composes a Remotion video with visual effects, and outputs videos/action-center.mp4.

Use --concurrency=2 for M1 Pro memory safety (should be the default from Phase 65 config).

If render fails with missing audio/timing data, Task 1 must be re-verified first.
  </action>
  <verify>
    <automated>ls -la apps/tutorials/videos/google-drive-settings.mp4 apps/tutorials/videos/action-center.mp4 2>/dev/null | wc -l</automated>
  </verify>
  <done>videos/google-drive-settings.mp4 and videos/action-center.mp4 both exist as non-zero-byte files. All 3 low-complexity tutorial MP4s now exist.</done>
</task>

</tasks>

<verification>
All three tutorial MP4 videos exist in apps/tutorials/videos/:
- getting-started.mp4 (already existed)
- google-drive-settings.mp4 (new)
- action-center.mp4 (new)

All three tutorial audio directories exist with correct file counts:
- audio/getting-started/ (8 WAVs + timing.json — already existed)
- audio/google-drive-settings/ (5 WAVs + timing.json — new)
- audio/action-center/ (7 WAVs + timing.json — new)
</verification>

<success_criteria>
- google-drive-settings.mp4 exists and is non-zero bytes
- action-center.mp4 exists and is non-zero bytes
- audio/google-drive-settings/ has 5 WAV files + timing.json
- audio/action-center/ has 7 WAV files + timing.json
- Phase 67 verification score moves from 5/7 to 7/7
</success_criteria>

<output>
After completion, create `.planning/phases/67-low-complexity-tutorials/67-03-SUMMARY.md`
</output>
