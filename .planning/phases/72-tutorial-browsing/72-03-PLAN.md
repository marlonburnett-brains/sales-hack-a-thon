---
phase: 72-tutorial-browsing
plan: 03
type: execute
wave: 3
depends_on:
  - 72-02
files_modified:
  - apps/web/src/app/(authenticated)/tutorials/page.tsx
  - apps/web/src/app/(authenticated)/tutorials/loading.tsx
  - apps/web/src/app/(authenticated)/tutorials/[slug]/page.tsx
  - apps/web/src/app/(authenticated)/tutorials/tutorials-browse-view.tsx
  - apps/web/src/components/tutorials/tutorial-card.tsx
  - apps/web/src/components/tutorials/tutorial-category-section.tsx
  - apps/web/src/components/tutorials/tutorials-page-header.tsx
  - apps/web/src/components/tutorials/__tests__/tutorials-browse-view.test.tsx
autonomous: true
requirements:
  - BROWSE-02
  - BROWSE-03
  - BROWSE-04

must_haves:
  truths:
    - "Users can open /tutorials and see all tutorials grouped into the six locked category sections"
    - "Each category header shows X of Y progress plus an inline progress bar"
    - "The page header shows overall completion progress, including the locked all-complete success state"
    - "Each card shows thumbnail, title, description, duration, watched checkmark, and a clickable path to /tutorials/[slug]"
  artifacts:
    - path: "apps/web/src/app/(authenticated)/tutorials/page.tsx"
      provides: "authenticated tutorials browse page"
      contains: "listTutorialsAction"
    - path: "apps/web/src/components/tutorials/tutorial-card.tsx"
      provides: "clickable compact tutorial card"
      contains: "next/image"
    - path: "apps/web/src/components/tutorials/tutorials-page-header.tsx"
      provides: "overall progress header with all-complete variant"
      contains: "All 17 tutorials completed"
  key_links:
    - from: "apps/web/src/app/(authenticated)/tutorials/page.tsx"
      to: "apps/web/src/lib/actions/tutorial-actions.ts"
      via: "server component data fetch"
      pattern: "listTutorialsAction"
    - from: "apps/web/src/components/tutorials/tutorial-card.tsx"
      to: "apps/web/src/app/(authenticated)/tutorials/[slug]/page.tsx"
      via: "next/link href=/tutorials/${slug}"
      pattern: "href={`/tutorials/"
    - from: "apps/web/src/app/(authenticated)/tutorials/loading.tsx"
      to: "apps/web/src/components/tutorials/tutorial-card.tsx"
      via: "skeleton reserves aspect-video card layout"
      pattern: "aspect-video"
---

<objective>
Ship the authenticated tutorials browse UI with locked grouping, compact cards, progress display, and non-broken tutorial links.

Purpose: This is the user-visible outcome of Phase 72 — a self-service tutorial index that matches the decided card design and lets users discover every tutorial before Phase 73 adds full playback.

Output: `/tutorials` page, browse components, loading skeleton, and a minimal `/tutorials/[slug]` placeholder route.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-tutorial-browsing/72-CONTEXT.md
@.planning/phases/72-tutorial-browsing/72-RESEARCH.md
@apps/web/src/app/(authenticated)/templates/page.tsx
@apps/web/src/app/(authenticated)/templates/loading.tsx
@apps/web/src/components/template-card.tsx
@apps/web/src/components/ui/card.tsx
@apps/web/src/components/ui/progress.tsx
@apps/web/src/components/ui/skeleton.tsx
@apps/web/src/lib/actions/tutorial-actions.ts

<interfaces>
Pattern from `apps/web/src/app/(authenticated)/templates/page.tsx`:
```tsx
export const dynamic = "force-dynamic";

export default async function Page() {
  let data = ...;
  try {
    data = await someAction();
  } catch (err) {
    console.error("[page] Failed to fetch:", err);
  }
  return <ClientOrView initialData={data} />;
}
```

Contract created by `72-02`:
```ts
export interface TutorialBrowseResponse {
  overall: { completedCount: number; totalCount: number; completionPercent: number };
  categories: Array<{
    key: string;
    label: string;
    tutorialCount: number;
    watchedCount: number;
    completionPercent: number;
    tutorials: Array<{
      id: string;
      slug: string;
      title: string;
      description: string;
      durationSec: number;
      thumbnailUrl: string | null;
      watched: boolean;
    }>;
  }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the browse view components and lock their states with UI tests</name>
  <files>apps/web/src/app/(authenticated)/tutorials/tutorials-browse-view.tsx, apps/web/src/components/tutorials/tutorial-card.tsx, apps/web/src/components/tutorials/tutorial-category-section.tsx, apps/web/src/components/tutorials/tutorials-page-header.tsx, apps/web/src/components/tutorials/__tests__/tutorials-browse-view.test.tsx</files>
  <behavior>
    - Test 1: category sections render in locked order with icon, X of Y copy, and inline progress bar.
    - Test 2: cards render title, description, human-readable duration, watched checkmark only when watched, and a thumbnail fallback when `thumbnailUrl` is null.
    - Test 3: the page header renders overall progress and switches to the green all-complete state copy when completedCount === totalCount > 0.
    - Test 4: empty browse data renders the locked empty state copy instead of blank content.
  </behavior>
  <action>
Create a presentational `tutorials-browse-view.tsx` that accepts the normalized browse DTO from Plan 72-02 and composes three focused components:

1. `tutorials-page-header.tsx`
   - Render `Tutorials` title.
   - Show `X of 17 completed` style copy when incomplete.
   - Show `All 17 tutorials completed!` with a check icon and green progress styling when fully complete.
   - Ship this now per locked user decision even though the roadmap maps the overall progress display to Phase 73.

2. `tutorial-category-section.tsx`
   - Use a fixed icon map for the six locked categories (choose explicit Lucide icons now: Rocket, Briefcase, Hand, Layers3, ClipboardCheck, Settings2).
   - Render section header, `X of Y` text, inline progress, and a subtle completed-state checkmark/muted accent when 100% complete.

3. `tutorial-card.tsx`
   - Wrap the whole card in `next/link` with `cursor-pointer`, hover/focus styles, and no layout-shifting scale transforms.
   - Use `next/image` in an `aspect-video` container when `thumbnailUrl` exists; otherwise render a neutral fallback thumbnail shell with the centered semi-transparent play icon.
   - Overlay the duration pill at bottom-right of the thumbnail and the green watched checkmark in the card corner only when watched.
   - Show compact title + description copy under the media area.

Keep styling Tailwind + existing shadcn primitives only. Do not add search/filter UI, playback controls, resume state, or sidebar badge behavior.
  </action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/tutorials/__tests__/tutorials-browse-view.test.tsx</automated>
  </verify>
  <done>The browse components are test-covered and render the locked card, category, all-complete, and empty-state designs without introducing out-of-scope interactivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the authenticated tutorials routes, loading state, and placeholder slug page</name>
  <files>apps/web/src/app/(authenticated)/tutorials/page.tsx, apps/web/src/app/(authenticated)/tutorials/loading.tsx, apps/web/src/app/(authenticated)/tutorials/[slug]/page.tsx</files>
  <behavior>
    - `/tutorials` server-renders browse data through `listTutorialsAction()` using the standard authenticated page pattern.
    - The route has a card-grid skeleton loading state that preserves aspect ratios and page structure.
    - Tutorial cards navigate to a non-broken `/tutorials/[slug]` route even before Phase 73 adds the full player.
  </behavior>
  <action>
Create the authenticated page wiring:

- `apps/web/src/app/(authenticated)/tutorials/page.tsx`
  - `export const dynamic = "force-dynamic"`.
  - Fetch with `listTutorialsAction()` inside try/catch using the same error logging pattern as templates/discovery pages.
  - Render `tutorials-browse-view.tsx` with the fetched DTO.

- `apps/web/src/app/(authenticated)/tutorials/loading.tsx`
  - Build a route-level skeleton matching the final layout: page header skeleton, section header skeletons, and aspect-video card placeholders.

- `apps/web/src/app/(authenticated)/tutorials/[slug]/page.tsx`
  - Add a minimal placeholder route so locked card navigation is not broken before Phase 73.
  - Validate the slug against the fetched tutorial dataset; use `notFound()` for unknown slugs.
  - For known slugs, render a lightweight placeholder with tutorial title, description, back-to-tutorials link, and copy that playback ships in the next phase. Keep it intentionally simple so Phase 73 can replace it cleanly.

Do not add sidebar nav work, playback, or watched-state mutations in this plan.
  </action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/tutorials/__tests__/tutorials-browse-view.test.tsx && pnpm --filter web exec tsc --noEmit</automated>
  </verify>
  <done>`/tutorials` is a working authenticated browse page with locked grouping/progress UI, route-level loading state, and safe clickable links to a placeholder tutorial detail route.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter web exec vitest run src/components/tutorials/__tests__/tutorials-browse-view.test.tsx`
2. `pnpm --filter web exec tsc --noEmit`
</verification>

<success_criteria>
- `/tutorials` shows category-grouped cards in the locked order
- Category and overall progress states render from the browse DTO
- Cards show title, description, duration, thumbnail/fallback, and watched indicator
- Card navigation lands on a valid placeholder route instead of a 404
</success_criteria>

<output>
After completion, create `.planning/phases/72-tutorial-browsing/72-03-SUMMARY.md`
</output>
