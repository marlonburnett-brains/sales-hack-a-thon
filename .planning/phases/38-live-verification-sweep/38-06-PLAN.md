---
phase: 38-live-verification-sweep
plan: "06"
type: execute
wave: 5
depends_on:
  - 38-05
files_modified:
  - .planning/phases/38-live-verification-sweep/38-UAT.md
  - .planning/phases/38-live-verification-sweep/38-VERIFICATION.md
autonomous: false
requirements:
  - DECK-05
gap_closure: true
must_haves:
  truths:
    - Scenario 2 in `38-UAT.md` is rerun against the same deployed artifact-qualified chat fix proven in `38-BACKEND-EVIDENCE.md`.
    - Human browser validation confirms Touch 4 settings tabs and chat stay artifact-scoped end-to-end, or records the exact remaining failure.
    - The overall phase verdict in `38-VERIFICATION.md` moves to passing only if the production browser rerun and backend proof both succeed.
  artifacts:
    - path: .planning/phases/38-live-verification-sweep/38-UAT.md
      provides: Final passing result or precise new diagnosed failure for the Touch 4 settings-tab chat scenario.
    - path: .planning/phases/38-live-verification-sweep/38-VERIFICATION.md
      provides: Updated 4/4 verification verdict or truthful new blocker trail for Phase 38.
  key_links:
    - from: .planning/phases/38-live-verification-sweep/38-UAT.md
      to: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      via: Human browser pass or diagnosed failure references the same artifact key and successful production chat proof set captured in backend evidence.
      pattern: Status: `pass`|artifactType|timestamp
    - from: .planning/phases/38-live-verification-sweep/38-VERIFICATION.md
      to: .planning/phases/38-live-verification-sweep/38-UAT.md
      via: Phase verdict must cite the final Scenario 2 browser outcome instead of the old diagnosed failure.
      pattern: 4/4 must-haves verified|Touch 4 settings tab and chat behavior stay artifact-scoped end-to-end
---

<objective>
Re-run the final browser scenario against the deployed chat fix and close Phase 38 with a truthful UAT plus verification verdict.

Purpose: Keep the human browser rerun and verification write-up in their own plan so the final approval or remaining blocker is evaluated with focused context after the backend rerun is complete.
Output: Updated `38-UAT.md` and `38-VERIFICATION.md` reflecting either full Phase 38 closure or an exact new blocker trail.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-verification-sweep/38-RUNBOOK.md
@.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
@.planning/phases/38-live-verification-sweep/38-UAT.md
@.planning/phases/38-live-verification-sweep/38-VERIFICATION.md
@.planning/phases/38-live-verification-sweep/38-05-SUMMARY.md

<interfaces>
Production proof must stay on the locked origins:
- Web: `https://lumenalta-hackathon.vercel.app`
- Agent: `https://lumenalta-agent-production.up.railway.app`

Successful chat proof already captured in `38-BACKEND-EVIDENCE.md` uses this request shape:

```json
{
  "touchType": "touch_4",
  "artifactType": "proposal",
  "message": "Refine the proposal structure into 5 concise sales-oriented sections with a stronger narrative arc."
}
```

Evidence rules to preserve:
- Production only; no localhost, preview URLs, or ad hoc tunnels.
- Use the same artifact key and timestamp trail already recorded in `38-BACKEND-EVIDENCE.md`.
- Keep the exact artifact key in every note; do not collapse back to generic Touch 4 language.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human re-run of the full Touch 4 settings-tab browser scenario</name>
  <files>.planning/phases/38-live-verification-sweep/38-UAT.md</files>
  <action>Pause after `38-05` finishes and ask the user to rerun Scenario 2 from `38-UAT.md` exactly as written against the same locked production environment and artifact-qualified chat fix already proven in `38-BACKEND-EVIDENCE.md`. The checkpoint must cover the full scenario, not just one artifact tab: default tab selection, all three trigger summaries before switching, per-tab panel scoping for `Proposal`, `Talk Track`, and `FAQ`, one artifact-scoped chat refinement from an active tab, and the zero-example-tab chat check if a zero-example tab is available. Do not substitute localhost evidence, preview URLs, or inferred approval. Resume only after the user explicitly reports the outcome of the full Scenario 2 checklist or provides precise failure notes for the exact step/tab/timestamp.</action>
  <what-built>A deployed artifact-scoped chat fix plus a matching successful backend proof set for the same production route, ready for a full Scenario 2 rerun.</what-built>
  <how-to-verify>
    1. Open `https://lumenalta-hackathon.vercel.app/settings/deck-structures/touch-4`.
    2. Confirm `Proposal` is selected by default on first load.
    3. Before switching tabs, inspect all three tab triggers and confirm `Proposal`, `Talk Track`, and `FAQ` each show their own confidence/example context.
    4. Switch to `Proposal`, `Talk Track`, and `FAQ` one by one, and for each tab confirm the visible panel matches the active artifact tab.
    5. From one active artifact tab, send a chat refinement message; use the same artifact key and message captured in `.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md` when that tab is part of the scenario.
    6. Confirm assistant text streams instead of returning `Chat failed: 404`, and confirm the visible structure update stays scoped to the active artifact tab rather than jumping artifacts.
    7. If a zero-example tab is available, use that zero-example tab for the chat refinement check and confirm artifact-scoped chat still works there.
    8. Reply with `approved` only if the entire Scenario 2 checklist passes, or describe the exact failing step, artifact tab, timestamp, and rendered error if any part still fails.
  </how-to-verify>
  <resume-signal>Type `approved` only after the full Scenario 2 checklist passes, or describe the remaining failure exactly.</resume-signal>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</automated>
  </verify>
  <done>The plan is paused until the user confirms the full Scenario 2 browser checklist passes or supplies a precise new failure record.</done>
</task>

<task type="auto">
  <name>Task 2: Close the phase verification artifacts</name>
  <files>.planning/phases/38-live-verification-sweep/38-UAT.md, .planning/phases/38-live-verification-sweep/38-VERIFICATION.md</files>
  <action>After the checkpoint response arrives, update `38-UAT.md` and `38-VERIFICATION.md` to reflect the final live result. If the user approved the full Scenario 2 rerun, mark Scenario 2 as `pass`, change the UAT final outcome from `diagnosed` to `approved`, and rewrite `38-VERIFICATION.md` to `status: verified` with `score: 4/4 must-haves verified`, citing both the deployment-confirmed backend evidence and the browser pass. If the user reports another issue, keep both files truthful, capture the exact failing step/tab/timestamp/error from the full Scenario 2 checklist, and leave an explicit new blocker trail rather than claiming closure.</action>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-UAT.md &amp;&amp; test -s .planning/phases/38-live-verification-sweep/38-VERIFICATION.md &amp;&amp; rg -n "approved|Status: `pass`|status: verified|4/4 must-haves verified|artifact-scoped end-to-end" .planning/phases/38-live-verification-sweep/38-UAT.md .planning/phases/38-live-verification-sweep/38-VERIFICATION.md</automated>
  </verify>
  <done>The browser UAT and phase verification files both reflect the final live outcome with no unresolved implicit state.</done>
</task>

</tasks>

<verification>
The final evidence set must connect one successful production artifact-qualified chat request to both backend proof and a passing browser rerun of Scenario 2, or leave a precise new blocker if the browser rerun still fails.
</verification>

<success_criteria>
- `38-UAT.md` marks Scenario 2 passed and the final outcome approved, or records a precise new failure if closure still fails.
- `38-VERIFICATION.md` moves to a 4/4 verified verdict when the live proof and browser pass are both present.
- The final browser and verification closure stays tied to the same artifact-qualified production proof captured in `38-BACKEND-EVIDENCE.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/38-live-verification-sweep/38-06-SUMMARY.md`
</output>
