---
phase: 38-live-verification-sweep
plan: "02"
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
autonomous: true
requirements:
  - DECK-05
must_haves:
  truths:
    - Live chat streaming is exercised against the reachable environment for an exact `(touch_4, artifactType)` pair, and progressive response behavior is captured.
    - Backend proof ties the same streaming request to persisted or logged artifact-qualified deck-structure updates.
    - Cron behavior is re-confirmed for an artifact-qualified Touch 4 key with pre/post evidence, including explicit handling of active-chat skip behavior when encountered.
  artifacts:
    - path: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      provides: Live streaming and cron evidence with request details, timestamps, artifact keys, and paired system proof.
  key_links:
    - from: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      to: apps/web/src/app/api/deck-structures/chat/route.ts
      via: Live verification uses the deployed `/api/deck-structures/chat` proxy with `touchType` plus `artifactType` in the request.
      pattern: /api/deck-structures/chat|artifactType|touch_4
    - from: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      to: apps/agent/prisma/schema.prisma
      via: Evidence cites persisted `DeckStructure` or `DeckChatMessage` fields such as `dataHash`, `inferredAt`, `lastChatAt`, or chat rows.
      pattern: inferredAt|lastChatAt|dataHash|DeckChatMessage
---

<objective>
Capture the backend proof that Phase 36's artifact-qualified runtime paths work in a real reachable environment.

Purpose: Close the remaining live-environment debt for streaming chat and cron without changing product code.
Output: A `38-BACKEND-EVIDENCE.md` file documenting paired live proof for streaming and cron on a specific Touch 4 artifact key.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-verification-sweep/38-RESEARCH.md
@.planning/phases/38-live-verification-sweep/38-RUNBOOK.md
@.planning/milestones/v1.6-phases/36-backend-engine-api-routes/36-VERIFICATION.md
@DEPLOY.md
@apps/web/src/app/api/deck-structures/chat/route.ts
@apps/agent/prisma/schema.prisma

<interfaces>
From `apps/web/src/app/api/deck-structures/chat/route.ts`:

```ts
export async function POST(request: NextRequest) {
  const body = z.object({
    touchType: z.string().min(1),
    artifactType: z.enum(ARTIFACT_TYPES).optional(),
    message: z.string().min(1),
  });
}
```

From `apps/agent/prisma/schema.prisma`:

```prisma
model DeckStructure {
  touchType       String
  artifactType    String?
  chatContextJson String?
  dataHash        String?
  inferredAt      DateTime @default(now())
  lastChatAt      DateTime?
}

model DeckChatMessage {
  deckStructureId String
  role            String
  content         String
}
```

Locked verification rules to honor:
- Use the deployed proxy/agent path already in production; no temporary endpoints.
- Every proof item must name the exact artifact key (`proposal`, `talk_track`, or `faq`).
- If live access fails because CLI auth is missing, capture the auth blocker rather than fabricating evidence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture live artifact-qualified streaming evidence</name>
  <files>.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</files>
  <action>Using the locked environment and scenario from `38-RUNBOOK.md`, send a real Touch 4 chat refinement request through the deployed web proxy (`/api/deck-structures/chat`) with the exact `artifactType` under test. Capture proof that the response is progressive rather than fully buffered by recording chunked `curl -N` output, browser network timing, or another raw transport trace tied to the same timestamp. Pair that with backend proof for the same request: matching Railway/Vercel logs, a persisted `DeckStructure` update, and/or a `DeckChatMessage` row showing the exact artifact-qualified conversation path. Document request URL, artifact key, timestamps, proof source, and outcome in `38-BACKEND-EVIDENCE.md`. Do not rely on localhost, screenshots alone, or generic Touch 4 wording.</action>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md &amp;&amp; rg -n "Live streaming verification|/api/deck-structures/chat|touch_4|artifactType|DeckStructure|DeckChatMessage" .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</automated>
  </verify>
  <done>`38-BACKEND-EVIDENCE.md` contains one artifact-qualified live streaming run with both transport proof and matching backend persistence/log proof.</done>
</task>

<task type="auto">
  <name>Task 2: Re-confirm cron behavior with artifact-qualified row evidence</name>
  <files>.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</files>
  <action>For one artifact-qualified Touch 4 key from the runbook, record the pre-run `DeckStructure` state (`dataHash`, `inferredAt`, `lastChatAt`, `exampleCount`, and any other useful fields), wait through a real cron interval in the reachable environment, then capture the post-run state plus the matching Railway log lines. If cron skips the key because `lastChatAt` is within the active-session window, record that skip as expected behavior and verify one non-chat-blocked artifact-qualified key as well so the phase still proves live cron execution. Document the chosen key, timestamps, whether the loop inferred or skipped, and the exact persisted evidence in `38-BACKEND-EVIDENCE.md`.</action>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md &amp;&amp; rg -n "Cron verification|dataHash|inferredAt|lastChatAt|skip|re-infer|touch_4" .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</automated>
  </verify>
  <done>`38-BACKEND-EVIDENCE.md` shows live cron proof for an artifact-qualified Touch 4 row, including explicit handling of active-chat skip semantics if they occur.</done>
</task>

</tasks>

<verification>
The backend evidence file must show both a live streaming run and a live cron observation, each tied to a specific artifact-qualified Touch 4 key.
</verification>

<success_criteria>
- `38-BACKEND-EVIDENCE.md` includes one live streaming proof set for `/api/deck-structures/chat` with `touch_4` plus `artifactType`.
- `38-BACKEND-EVIDENCE.md` includes one live cron proof set with pre/post row evidence and matching logs.
- Every backend proof item is artifact-qualified and tied to the same reachable environment locked in the runbook.
</success_criteria>

<output>
After completion, create `.planning/phases/38-live-verification-sweep/38-02-SUMMARY.md`
</output>
