---
phase: 38-live-verification-sweep
plan: "03"
type: execute
wave: 2
depends_on:
  - 38-01
files_modified:
  - .planning/phases/38-live-verification-sweep/38-UAT.md
autonomous: false
requirements:
  - DECK-05
must_haves:
  truths:
    - Human browser validation confirms the saved Touch 4 artifact badge survives reload from both classify surfaces.
    - Human browser validation confirms Proposal is the default settings tab and each artifact tab keeps its own confidence/example context and chat scope.
    - The browser verification outcome is recorded with pass/fail evidence and any follow-up issues are diagnosed instead of left implicit.
  artifacts:
    - path: .planning/phases/38-live-verification-sweep/38-UAT.md
      provides: Reachable-environment browser checklist plus recorded approval or diagnosed issues.
  key_links:
    - from: .planning/phases/38-live-verification-sweep/38-UAT.md
      to: .planning/phases/37-frontend-ui/37-VERIFICATION.md
      via: Reuses the two pending human-verification scenarios without inventing new browser scope.
      pattern: Cross-surface Touch 4 classification|Touch 4 settings tab behavior
    - from: .planning/phases/38-live-verification-sweep/38-UAT.md
      to: apps/web/src/components/settings/touch-4-artifact-tabs.tsx
      via: Browser steps explicitly verify Proposal default tab, per-tab confidence, and artifact-scoped settings behavior.
      pattern: proposal|Talk Track|FAQ
---

<objective>
Run the remaining human browser checks on the reachable environment and record the result in a phase-owned UAT file.

Purpose: Close the exact Phase 37 browser debt for classification reload behavior and Touch 4 settings/chat scoping.
Output: A `38-UAT.md` checklist with the final approved result or diagnosed issues from human verification.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-verification-sweep/38-RESEARCH.md
@.planning/phases/38-live-verification-sweep/38-RUNBOOK.md
@.planning/phases/37-frontend-ui/37-VERIFICATION.md
@.planning/phases/37-frontend-ui/37-02-SUMMARY.md
@.planning/phases/37-frontend-ui/37-04-SUMMARY.md
@apps/web/src/components/settings/touch-4-artifact-tabs.tsx
@apps/web/src/components/slide-viewer/classification-panel.tsx

<interfaces>
From `apps/web/src/components/settings/touch-4-artifact-tabs.tsx`:

```ts
const [activeArtifact, setActiveArtifact] = useState<ArtifactType>("proposal");

ARTIFACT_TYPES.map((artifactType) => ({
  artifactType,
  label: ARTIFACT_TYPE_LABELS[artifactType],
  summary: summaries[artifactType],
}));
```

From `apps/web/src/components/slide-viewer/classification-panel.tsx`:

```ts
interface ClassificationPanelProps {
  contentClassification?: string | null;
  touchTypes?: string[];
  artifactType?: ArtifactType | null;
}
```

Browser scope to keep unchanged from prior verification:
- Cross-surface Touch 4 classification reload from Templates and Slide Viewer.
- `/settings/deck-structures/touch-4` Proposal default tab, per-tab confidence/example context, and artifact-scoped chat behavior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the reachable-environment browser checklist</name>
  <files>.planning/phases/38-live-verification-sweep/38-UAT.md</files>
  <action>Create `38-UAT.md` with the exact human-verification script for this phase: the target URLs from `38-RUNBOOK.md`, the template/presentation to use, the expected badge copy for `Proposal`, `Talk Track`, and `FAQ`, and the exact settings-tab checks to perform. Keep the checklist limited to the two pending Phase 37 scenarios. Add a results template with explicit pass/fail fields and evidence placeholders so the post-checkpoint update is mechanical instead of interpretive.</action>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-UAT.md &amp;&amp; rg -n "Cross-surface Touch 4 classification|Touch 4 settings tab behavior|Proposal|Talk Track|FAQ" .planning/phases/38-live-verification-sweep/38-UAT.md</automated>
  </verify>
  <done>`38-UAT.md` contains a concrete browser checklist and results template for the exact pending Phase 37 scenarios.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human browser verification on the reachable environment</name>
  <files>.planning/phases/38-live-verification-sweep/38-UAT.md</files>
  <action>Pause after the checklist is written and ask the user to run the browser script in `38-UAT.md` against the locked reachable environment. Do not substitute browser automation, screenshots from localhost, or inferred approval. Resume only after the user explicitly reports `approved` or provides concrete failure notes.</action>
  <what-built>A concrete UAT checklist for the two remaining Touch 4 browser scenarios, tied to the locked environment and exact artifact-qualified expectations.</what-built>
  <how-to-verify>
    1. Use the URLs and template identifier recorded in `.planning/phases/38-live-verification-sweep/38-UAT.md`.
    2. From the Templates surface, classify the chosen item as `Example + Touch 4`, pick one artifact, save, then refresh and confirm the saved badge still includes `Proposal`, `Talk Track`, or `FAQ`.
    3. Open the same template in the Slide Viewer, repeat the save + refresh flow, and confirm the same saved badge behavior there.
    4. Open `/settings/deck-structures/touch-4`, confirm Proposal is selected by default, and check that each tab trigger shows its own confidence/example context before the tab is opened.
    5. Switch across Proposal, Talk Track, and FAQ, then send a chat refinement from one artifact tab and confirm the behavior stays scoped to that active artifact tab.
    6. Reply with `approved` if all checks pass, or describe the exact failing step and observed behavior.
  </how-to-verify>
  <resume-signal>Type `approved` or describe the issues found during the browser run.</resume-signal>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-UAT.md</automated>
  </verify>
  <done>The plan is paused pending an explicit human verification result for the two browser scenarios.</done>
</task>

<task type="auto">
  <name>Task 3: Record the human-verification outcome</name>
  <files>.planning/phases/38-live-verification-sweep/38-UAT.md</files>
  <action>After the user responds to the checkpoint, update `38-UAT.md` with the final result for each browser scenario. If the user approved the checks, mark both scenarios passed and include any supporting notes or evidence references. If the user reported issues, mark the file as diagnosed, capture the failing step exactly, and name the artifact/tab/surface involved so follow-up work can become a precise gap-closure plan.</action>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-UAT.md &amp;&amp; rg -n "approved|diagnosed|Cross-surface Touch 4 classification|Touch 4 settings tab behavior" .planning/phases/38-live-verification-sweep/38-UAT.md</automated>
  </verify>
  <done>`38-UAT.md` records a final approved or diagnosed outcome for both pending browser scenarios, with no implicit verification state left behind.</done>
</task>

</tasks>

<verification>
The UAT file must contain the exact checklist before the checkpoint and the recorded final outcome after the user responds.
</verification>

<success_criteria>
- `38-UAT.md` contains the exact reachable-environment browser checklist for the two pending Phase 37 scenarios.
- Human verification confirms or diagnoses cross-surface badge reload behavior and Touch 4 settings/chat scoping.
- The final browser outcome is written back into `38-UAT.md` with explicit pass/fail state.
</success_criteria>

<output>
After completion, create `.planning/phases/38-live-verification-sweep/38-03-SUMMARY.md`
</output>
