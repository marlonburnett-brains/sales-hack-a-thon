---
phase: 38-live-verification-sweep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/38-live-verification-sweep/38-RUNBOOK.md
autonomous: true
requirements: []
must_haves:
  truths:
    - One reachable web origin and one reachable agent origin are locked for Phase 38; localhost is explicitly excluded.
    - The live verification runbook names the exact artifact-qualified Touch 4 scenarios, evidence sources, and auth/log/DB prerequisites before execution starts.
    - Targeted Phase 36 and Phase 37 regression suites are rerun first so live failures are not confused with existing code regressions.
  artifacts:
    - path: .planning/phases/38-live-verification-sweep/38-RUNBOOK.md
      provides: Reachable-environment runbook with URLs, candidate artifact keys, evidence pairing rules, and preflight results.
  key_links:
    - from: .planning/phases/38-live-verification-sweep/38-RUNBOOK.md
      to: DEPLOY.md
      via: Deployment and auth contract for Vercel web plus Railway agent origins.
      pattern: WEB_APP_URL|AGENT_SERVICE_URL|Vercel|Railway
    - from: .planning/phases/38-live-verification-sweep/38-RUNBOOK.md
      to: .planning/phases/37-frontend-ui/37-VERIFICATION.md
      via: Browser checks are copied unchanged from the pending human-verification scenarios.
      pattern: Cross-surface Touch 4 classification|Touch 4 settings tab behavior
---

<objective>
Lock the real verification target and write the single Phase 38 runbook before any live checks begin.

Purpose: Remove the known reachable-environment ambiguity so backend evidence capture and browser UAT both run against the same deploy with the same artifact-qualified Touch 4 examples.
Output: A `38-RUNBOOK.md` file containing the chosen environment, prerequisite access, exact scenarios, and preflight regression results.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-verification-sweep/38-RESEARCH.md
@.planning/milestones/v1.6-phases/36-backend-engine-api-routes/36-VERIFICATION.md
@.planning/phases/37-frontend-ui/37-VERIFICATION.md
@.planning/v1.6-v1.6-MILESTONE-AUDIT.md
@DEPLOY.md

Locked scope to honor exactly:
- Verification closure only for v1.6 Touch 4 artifact workflows; do not add product features or debug routes.
- Use one reachable environment, not localhost.
- Keep evidence artifact-qualified (`proposal`, `talk_track`, `faq`), not generic Touch 4 notes.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the reachable-environment verification runbook</name>
  <files>.planning/phases/38-live-verification-sweep/38-RUNBOOK.md</files>
  <action>Read the deployment contract, prior verification debt, and Phase 38 research, then create a runbook that locks one reachable web origin and one agent origin for the whole phase. Record the required auth state, where logs and persisted row evidence will be inspected, which Touch 4 artifact-qualified template or deck-structure key will be used for live checks, and the exact proof pairing rule for each scenario (UI/network proof plus system proof). Explicitly ban localhost, screenshots without artifactType, and any temporary debug code or admin pages.</action>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-RUNBOOK.md &amp;&amp; rg -n "reachable environment|artifactType|stream|cron|browser|localhost" .planning/phases/38-live-verification-sweep/38-RUNBOOK.md</automated>
  </verify>
  <done>`38-RUNBOOK.md` names one reachable environment, one evidence strategy, and one artifact-qualified scenario set that both downstream plans can execute without guessing.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the focused regression suites and append preflight results</name>
  <files>.planning/phases/38-live-verification-sweep/38-RUNBOOK.md</files>
  <action>Run the targeted Phase 36 and Phase 37 Vitest suites called out in `38-RESEARCH.md` before any live verification. Append a short preflight section to the runbook with the commands run, the pass/fail outcome, and any relevant note needed to separate code regressions from environment-only failures. If a targeted suite fails, stop the live sweep and document the failure instead of collecting misleading runtime evidence.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/deck-intelligence/__tests__/auto-infer-cron.test.ts src/mastra/__tests__/deck-structure-routes.test.ts &amp;&amp; pnpm --filter web exec vitest run src/components/settings/__tests__/touch-4-artifact-tabs.test.tsx src/components/slide-viewer/__tests__/classification-panel.test.tsx src/app/api/deck-structures/chat/__tests__/route.test.ts &amp;&amp; rg -n "Preflight|vitest run|passed|failed" .planning/phases/38-live-verification-sweep/38-RUNBOOK.md</automated>
  </verify>
  <done>The runbook includes fresh preflight results showing the artifact-aware backend and frontend regression suites passed immediately before the live sweep.</done>
</task>

</tasks>

<verification>
The runbook exists, names one reachable deployment, and includes a fresh preflight section from the focused backend/frontend suites.
</verification>

<success_criteria>
- `38-RUNBOOK.md` locks one reachable web URL and one reachable agent URL for Phase 38.
- `38-RUNBOOK.md` names the exact artifact-qualified Touch 4 scenarios and evidence sources for streaming, cron, and browser checks.
- The targeted Phase 36/37 regression suites are rerun and logged before any live validation.
</success_criteria>

<output>
After completion, create `.planning/phases/38-live-verification-sweep/38-01-SUMMARY.md`
</output>
