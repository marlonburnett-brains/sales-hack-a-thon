---
phase: 38-live-verification-sweep
plan: "04"
type: execute
wave: 1
depends_on:
  - 38-02
  - 38-03
files_modified:
  - apps/agent/src/mastra/index.ts
  - apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts
  - apps/web/src/app/api/deck-structures/chat/route.ts
  - apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts
  - .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - Touch 4 artifact-scoped chat no longer dies at the web-proxy-to-agent hop with a production 404.
    - The checked-in web proxy and agent route still share one `:touchType` chat contract with `artifactType` carried as a query param.
    - Regression coverage fails if the artifact-scoped chat route or proxy target drifts away from the deployed contract again.
  artifacts:
    - path: apps/web/src/app/api/deck-structures/chat/route.ts
      provides: Next.js proxy for artifact-qualified deck-structure chat.
    - path: apps/agent/src/mastra/index.ts
      provides: Agent-side `POST /api/deck-structures/:touchType/chat` handler.
    - path: apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts
      provides: Proxy regression coverage for artifact-qualified chat forwarding.
    - path: apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts
      provides: Agent route-contract regression coverage for artifact-qualified chat resolution.
    - path: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      provides: Root-cause and shipped-resolution note for the prior production 404.
  key_links:
    - from: apps/web/src/app/api/deck-structures/chat/route.ts
      to: apps/agent/src/mastra/index.ts
      via: Proxy builds the agent chat URL for the existing `:touchType` route family and appends `artifactType` with `URLSearchParams`.
      pattern: /api/deck-structures/.*/chat|artifactType|URLSearchParams
    - from: apps/agent/src/mastra/index.ts
      to: apps/agent/src/deck-intelligence/chat-refinement.ts
      via: Agent route resolves the `{ touchType, artifactType }` key before calling the streaming refinement flow.
      pattern: resolveDeckStructureKey|streamChatRefinement
---

<objective>
Eliminate the production `404 Not Found` blocker on artifact-scoped Touch 4 settings chat and lock the route contract into regression coverage.

Purpose: Close the concrete production blocker carried forward from `38-BACKEND-EVIDENCE.md` and `38-UAT.md` without changing the approved Touch 4 artifact contract.
Output: Fixed web/agent chat route parity, targeted regression coverage, and a recorded root-cause note for the prior production failure.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-verification-sweep/38-RESEARCH.md
@.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
@.planning/phases/38-live-verification-sweep/38-UAT.md
@.planning/phases/38-live-verification-sweep/38-02-SUMMARY.md
@.planning/phases/38-live-verification-sweep/38-03-SUMMARY.md
@apps/web/src/app/api/deck-structures/chat/route.ts
@apps/agent/src/mastra/index.ts
@apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts
@apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts

<interfaces>
From `apps/web/src/app/api/deck-structures/chat/route.ts`:

```ts
const query = new URLSearchParams();
if (body.data.artifactType) {
  query.set("artifactType", body.data.artifactType);
}

const agentUrl = `${env.AGENT_SERVICE_URL}/api/deck-structures/${encodeURIComponent(body.data.touchType)}/chat${suffix}`;
```

From `apps/agent/src/mastra/index.ts`:

```ts
registerApiRoute("/deck-structures/:touchType/chat", {
  method: "POST",
  handler: async (c) => {
    const touchType = c.req.param("touchType");
    const query = z.object({ artifactType: z.string().nullable().optional() }).parse(c.req.query());
    const key = resolveDeckStructureKey(touchType, query.artifactType ?? null);
  },
});
```

Locked decisions to preserve:
- Keep the existing `:touchType` route family and validate Touch 4 artifacts through query params.
- Keep `URLSearchParams` on the web side; do not introduce a second artifact-specific endpoint family.
- Keep chat refinement scoped through `resolveDeckStructureKey()`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression coverage for the production chat-route blocker</name>
  <files>apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts, apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts</files>
  <behavior>
    - Test 1: Touch 4 proxy requests still require `artifactType` and forward the exact artifact-qualified chat URL to the agent.
    - Test 2: The agent source still exposes `POST /deck-structures/:touchType/chat` and resolves `artifactType` through `resolveDeckStructureKey()` before streaming.
    - Test 3: The regression fails if the proxy target or agent route registration drifts away from the deployed chat path that produced the Phase 38 `404`.
  </behavior>
  <action>Update the two targeted route-contract suites so they explicitly encode the production blocker diagnosed in `.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md`: the checked-in contract says `/api/deck-structures/:touchType/chat` with `artifactType` query threading, but production returned `404`. Write the tests so they fail before the fix or fail fast if a future refactor changes the route target, route registration, or artifact-query handling. Do not add a second endpoint family, do not move artifact scope into the request body, and do not relax Touch 4 artifact validation.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/mastra/__tests__/deck-structure-routes.test.ts &amp;&amp; pnpm --filter web exec vitest run src/app/api/deck-structures/chat/__tests__/route.test.ts</automated>
  </verify>
  <done>The route-contract tests encode the production blocker precisely enough that future proxy/agent drift will fail locally before deploy.</done>
</task>

<task type="auto">
  <name>Task 2: Restore production parity for artifact-scoped settings chat</name>
  <files>apps/agent/src/mastra/index.ts, apps/web/src/app/api/deck-structures/chat/route.ts, .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</files>
  <action>Fix the root cause behind the production `404 Not Found` so Touch 4 `proposal`, `talk_track`, and `faq` requests sent from the settings chat can reach the deployed agent handler and return the existing text stream plus `---STRUCTURE_UPDATE---` delimiter payload. Keep the approved contract exactly as-is: existing `:touchType` route family, `artifactType` in `URLSearchParams`, and `resolveDeckStructureKey()` inside the agent handler. If the blocker turns out to be deployment-path drift rather than a logic bug, make the smallest code-level change that codifies the expected path/handler parity and then append a short root-cause + shipped-resolution note to `38-BACKEND-EVIDENCE.md` so the live failure record points to the fix that replaced it. Finish by rerunning the two targeted Vitest suites.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/mastra/__tests__/deck-structure-routes.test.ts &amp;&amp; pnpm --filter web exec vitest run src/app/api/deck-structures/chat/__tests__/route.test.ts &amp;&amp; rg -n "root cause|resolution|404 Not Found|artifactType|/api/deck-structures/.*/chat" .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</automated>
  </verify>
  <done>The checked-in web and agent code once again describe one working artifact-scoped chat path, and the Phase 38 evidence file names the shipped fix that replaces the old `404` diagnosis.</done>
</task>

</tasks>

<verification>
Both targeted Vitest suites must pass after the fix, and `38-BACKEND-EVIDENCE.md` must name the root cause plus the shipped resolution for the prior production `404`.
</verification>

<success_criteria>
- Touch 4 artifact chat keeps the approved route contract with no alternate endpoint family.
- Regression coverage guards both proxy forwarding and agent route registration for artifact-qualified chat.
- The prior production `404` now has a concrete shipped-resolution note in `38-BACKEND-EVIDENCE.md`.
</success_criteria>

<output>
After completion, create `.planning/phases/38-live-verification-sweep/38-04-SUMMARY.md`
</output>
