---
phase: 38-live-verification-sweep
plan: "05"
type: execute
wave: 4
depends_on:
  - 38-04
files_modified:
  - .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
autonomous: false
requirements:
  - DECK-05
gap_closure: true
must_haves:
  truths:
    - The live rerun does not start until the 38-04 fix is confirmed on the locked Vercel and Railway production deployments.
    - One successful production Touch 4 settings-chat run is documented for an exact artifact key, not generic Touch 4 behavior.
    - The same successful production chat run has paired system proof from Railway logs or persisted deck-structure/chat state.
  artifacts:
    - path: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      provides: Successful production stream proof plus paired backend evidence for one artifact-qualified chat request.
  key_links:
    - from: .planning/phases/38-live-verification-sweep/38-04-SUMMARY.md
      to: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      via: Deployment confirmation records the exact Vercel and Railway production rollout evidence before any live proof is rerun.
      pattern: deployed|deployment|Vercel|Railway|production
    - from: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      to: apps/web/src/app/api/deck-structures/chat/route.ts
      via: Live proof uses the production `/api/deck-structures/chat` proxy with the fixed artifact-qualified contract.
      pattern: /api/deck-structures/chat|artifactType|proposal|talk_track|faq
    - from: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      to: .planning/phases/38-live-verification-sweep/38-04-SUMMARY.md
      via: Successful proof is explicitly tied to the deployed Vercel and Railway builds confirmed after the 38-04 route fix shipped.
      pattern: deployment|Vercel|Railway|production|artifactType
---

<objective>
Confirm the Phase 38 route fix is live in production and re-capture the successful backend proof set for artifact-scoped settings chat.

Purpose: Keep the production deploy gate and backend rerun together so the same execution context proves the shipped 38-04 fix is what produced the new live artifact-qualified chat evidence.
Output: Updated `38-BACKEND-EVIDENCE.md` with deploy confirmation and one successful production proof set for the fixed Touch 4 settings-chat path.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-verification-sweep/38-RUNBOOK.md
@.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
@.planning/phases/38-live-verification-sweep/38-04-SUMMARY.md
@apps/web/src/app/api/deck-structures/chat/route.ts
@apps/agent/src/mastra/index.ts

<interfaces>
Production proof must stay on the locked origins:
- Web: `https://lumenalta-hackathon.vercel.app`
- Agent: `https://lumenalta-agent-production.up.railway.app`

Successful chat proof must still use this request shape:

```json
{
  "touchType": "touch_4",
  "artifactType": "proposal",
  "message": "Refine the proposal structure into 5 concise sales-oriented sections with a stronger narrative arc."
}
```

Agent stream contract to preserve:

```ts
controller.enqueue(encoder.encode("\n---STRUCTURE_UPDATE---\n"));
controller.enqueue(encoder.encode(JSON.stringify({ updatedStructure, diff })));
```

Evidence rules to preserve:
- Production only; no localhost, preview URLs, or ad hoc tunnels.
- Pair UI or transport proof with matching backend proof for the same artifact key and timestamp.
- Keep the exact artifact key in every note; do not collapse back to generic Touch 4 language.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm the 38-04 fix is deployed on both locked production origins</name>
  <files>.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</files>
  <action>Before any live rerun begins, pause for deployment confirmation tied to the exact production environments in `38-RUNBOOK.md`: `https://lumenalta-hackathon.vercel.app` on Vercel and `https://lumenalta-agent-production.up.railway.app` on Railway. Require explicit confirmation that the 38-04 route-contract fix has been deployed to both production services, then record the confirming evidence in `.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md` alongside the later proof set (for example: deployment timestamp, build/deployment ID, commit SHA, or dashboard confirmation note). Do not rerun backend proof against stale production deployments, preview URLs, or inferred deploy state.</action>
  <what-built>A blocking deploy gate that ties the live verification sweep to the actual Vercel and Railway production rollouts carrying the 38-04 fix.</what-built>
  <how-to-verify>
    1. Confirm the web production origin `https://lumenalta-hackathon.vercel.app` is serving the deployment that includes the 38-04 fix.
    2. Confirm the agent production origin `https://lumenalta-agent-production.up.railway.app` is serving the deployment that includes the same 38-04 fix.
    3. Provide the exact deployment evidence for both services (deployment/build ID, commit SHA, rollout timestamp, or equivalent production-dashboard confirmation).
    4. Reply only when both production services are confirmed deployed; otherwise report which origin is still stale or pending.
  </how-to-verify>
  <resume-signal>Type `deployed` with the Vercel and Railway deployment identifiers/timestamps, or report which production origin is not updated yet.</resume-signal>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-04-SUMMARY.md</automated>
  </verify>
  <done>The plan is blocked until both locked production services are explicitly confirmed to be running the 38-04 fix.</done>
</task>

<task type="auto">
  <name>Task 2: Capture a successful production artifact-qualified chat proof set</name>
  <files>.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</files>
  <action>Using the locked production Vercel and Railway origins from `38-RUNBOOK.md`, rerun one exact Touch 4 settings-chat request through `/api/deck-structures/chat` only after Task 1 confirms the 38-04 fix is deployed on both services. Capture both halves of the proof for the same artifact-qualified request: user-visible or raw transport proof that the response streamed progressively, and backend proof showing the same successful request reached the agent and persisted state (`DeckChatMessage`, `DeckStructure.lastChatAt`, or equivalent Railway logs tied to the same timestamp/artifact key). Record the deployment confirmation from Task 1 in the same evidence file so the successful proof is explicitly tied to the deployed Vercel/Railway production builds. Replace the old blocker-only conclusion in `38-BACKEND-EVIDENCE.md` with the successful proof set while retaining the original failed attempt as historical context.</action>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md &amp;&amp; rg -n "successful|progressive|DeckChatMessage|lastChatAt|Railway|artifactType|/api/deck-structures/chat|deployment|Vercel|production" .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</automated>
  </verify>
  <done>`38-BACKEND-EVIDENCE.md` contains one successful production artifact-scoped chat proof set with matching system evidence for the same request.</done>
</task>

</tasks>

<verification>
The final evidence set must connect one successful production artifact-qualified chat request to both the confirmed 38-04 deployment and matching backend proof.
</verification>

<success_criteria>
- `38-BACKEND-EVIDENCE.md` contains a successful production artifact-scoped chat proof set, not just the original failure record.
- `38-BACKEND-EVIDENCE.md` records the Vercel and Railway deployment evidence showing the 38-04 fix was live before the rerun.
- The backend rerun stays artifact-qualified end-to-end and is ready for the separate final UAT/verification closure plan.
</success_criteria>

<output>
After completion, create `.planning/phases/38-live-verification-sweep/38-05-SUMMARY.md`
</output>
