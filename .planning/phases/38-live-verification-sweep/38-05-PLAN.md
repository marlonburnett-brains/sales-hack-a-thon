---
phase: 38-live-verification-sweep
plan: "05"
type: execute
wave: 2
depends_on:
  - 38-04
files_modified:
  - .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
  - .planning/phases/38-live-verification-sweep/38-UAT.md
  - .planning/phases/38-live-verification-sweep/38-VERIFICATION.md
autonomous: false
requirements: []
gap_closure: true
must_haves:
  truths:
    - One successful production Touch 4 settings-chat run is documented for an exact artifact key, not generic Touch 4 behavior.
    - The same successful production chat run has paired system proof from Railway logs or persisted deck-structure/chat state.
    - Scenario 2 in `38-UAT.md` and the overall phase verdict in `38-VERIFICATION.md` move from diagnosed failure to passing verification.
  artifacts:
    - path: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      provides: Successful production stream proof plus paired backend evidence for one artifact-qualified chat request.
    - path: .planning/phases/38-live-verification-sweep/38-UAT.md
      provides: Final passing result for the Touch 4 settings-tab chat scenario.
    - path: .planning/phases/38-live-verification-sweep/38-VERIFICATION.md
      provides: Updated 4/4 verification verdict for Phase 38.
  key_links:
    - from: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      to: apps/web/src/app/api/deck-structures/chat/route.ts
      via: Live proof uses the production `/api/deck-structures/chat` proxy with the fixed artifact-qualified contract.
      pattern: /api/deck-structures/chat|artifactType|proposal|talk_track|faq
    - from: .planning/phases/38-live-verification-sweep/38-UAT.md
      to: .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
      via: Human browser pass references the same artifact key and successful chat proof set captured in backend evidence.
      pattern: Status: `pass`|artifactType|timestamp
    - from: .planning/phases/38-live-verification-sweep/38-VERIFICATION.md
      to: .planning/phases/38-live-verification-sweep/38-UAT.md
      via: Phase verdict must cite the now-passing Scenario 2 browser evidence instead of the old diagnosed failure.
      pattern: 4/4 must-haves verified|Touch 4 settings tab and chat behavior stay artifact-scoped end-to-end
---

<objective>
Re-run the live evidence sweep after the chat-route fix and close Phase 38 with a passing backend proof set, a passing UAT scenario, and an updated verification report.

Purpose: Turn the diagnosed production blocker into verified live evidence for the same artifact-scoped settings-chat workflow.
Output: Updated backend evidence, passing browser UAT for Scenario 2, and a final `38-VERIFICATION.md` showing 4/4 truths verified.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-live-verification-sweep/38-RUNBOOK.md
@.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md
@.planning/phases/38-live-verification-sweep/38-UAT.md
@.planning/phases/38-live-verification-sweep/38-VERIFICATION.md
@.planning/phases/38-live-verification-sweep/38-04-SUMMARY.md
@apps/web/src/app/api/deck-structures/chat/route.ts
@apps/agent/src/mastra/index.ts

<interfaces>
Production proof must stay on the locked origins:
- Web: `https://lumenalta-hackathon.vercel.app`
- Agent: `https://lumenalta-agent-production.up.railway.app`

Successful chat proof must still use this request shape:

```json
{
  "touchType": "touch_4",
  "artifactType": "proposal",
  "message": "Refine the proposal structure into 5 concise sales-oriented sections with a stronger narrative arc."
}
```

Agent stream contract to preserve:

```ts
controller.enqueue(encoder.encode("\n---STRUCTURE_UPDATE---\n"));
controller.enqueue(encoder.encode(JSON.stringify({ updatedStructure, diff })));
```

Evidence rules to preserve:
- Production only; no localhost, preview URLs, or ad hoc tunnels.
- Pair UI or transport proof with matching backend proof for the same artifact key and timestamp.
- Keep the exact artifact key in every note; do not collapse back to generic Touch 4 language.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture a successful production artifact-qualified chat proof set</name>
  <files>.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</files>
  <action>Using the locked production Vercel and Railway origins from `38-RUNBOOK.md`, rerun one exact Touch 4 settings-chat request through `/api/deck-structures/chat` after the `38-04` fix is deployed. Capture both halves of the proof for the same artifact-qualified request: user-visible or raw transport proof that the response streamed progressively, and backend proof showing the same successful request reached the agent and persisted state (`DeckChatMessage`, `DeckStructure.lastChatAt`, or equivalent Railway logs tied to the same timestamp/artifact key). Replace the old blocker-only conclusion in `38-BACKEND-EVIDENCE.md` with the successful proof set while retaining the original failed attempt as historical context.</action>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md &amp;&amp; rg -n "successful|progressive|DeckChatMessage|lastChatAt|Railway|artifactType|/api/deck-structures/chat" .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</automated>
  </verify>
  <done>`38-BACKEND-EVIDENCE.md` contains one successful production artifact-scoped chat proof set with matching system evidence for the same request.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human re-run of the Touch 4 settings-tab browser scenario</name>
  <files>.planning/phases/38-live-verification-sweep/38-UAT.md</files>
  <action>Pause after the successful backend proof is recorded and ask the user to rerun Scenario 2 from `38-UAT.md` against the same locked production environment. Do not substitute localhost evidence, preview URLs, or inferred approval. Resume only after the user explicitly reports that the active artifact tab now chats successfully or provides precise failure notes.</action>
  <what-built>A deployed artifact-scoped chat fix plus a matching successful backend proof set for the same production route.</what-built>
  <how-to-verify>
    1. Open `https://lumenalta-hackathon.vercel.app/settings/deck-structures/touch-4`.
    2. Confirm `Proposal` still loads as the default tab and that each tab trigger still shows its own confidence/example context.
    3. Switch to the artifact tab named in `.planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md` and send the same refinement message used in the successful proof set.
    4. Confirm assistant text streams instead of returning `Chat failed: 404`, and confirm the visible structure update stays scoped to the active artifact tab.
    5. Reply with `approved` if Scenario 2 now passes, or describe the exact failing step, artifact tab, timestamp, and rendered error if it still fails.
  </how-to-verify>
  <resume-signal>Type `approved` or describe the remaining failure exactly.</resume-signal>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-BACKEND-EVIDENCE.md</automated>
  </verify>
  <done>The plan is paused until the user confirms the settings-tab browser scenario passes or supplies a precise new failure record.</done>
</task>

<task type="auto">
  <name>Task 3: Close the phase verification artifacts</name>
  <files>.planning/phases/38-live-verification-sweep/38-UAT.md, .planning/phases/38-live-verification-sweep/38-VERIFICATION.md</files>
  <action>After the checkpoint response arrives, update `38-UAT.md` and `38-VERIFICATION.md` to reflect the final live result. If the user approved the rerun, mark Scenario 2 as `pass`, change the UAT final outcome from `diagnosed` to `approved`, and rewrite `38-VERIFICATION.md` to `status: verified` with `score: 4/4 must-haves verified`, citing the successful backend evidence plus the browser pass. If the user reports another issue, keep both files truthful, capture the exact artifact/tab/timestamp/error, and leave an explicit new blocker trail rather than claiming closure.</action>
  <verify>
    <automated>test -s .planning/phases/38-live-verification-sweep/38-UAT.md &amp;&amp; test -s .planning/phases/38-live-verification-sweep/38-VERIFICATION.md &amp;&amp; rg -n "approved|Status: `pass`|status: verified|4/4 must-haves verified|artifact-scoped end-to-end" .planning/phases/38-live-verification-sweep/38-UAT.md .planning/phases/38-live-verification-sweep/38-VERIFICATION.md</automated>
  </verify>
  <done>The browser UAT and phase verification files both reflect the final live outcome with no unresolved implicit state.</done>
</task>

</tasks>

<verification>
The final evidence set must connect one successful production artifact-qualified chat request to both backend proof and a passing browser rerun of Scenario 2.
</verification>

<success_criteria>
- `38-BACKEND-EVIDENCE.md` contains a successful production artifact-scoped chat proof set, not just the original failure record.
- `38-UAT.md` marks Scenario 2 passed and the final outcome approved, or records a precise new failure if closure still fails.
- `38-VERIFICATION.md` moves to a 4/4 verified verdict when the live proof and browser pass are both present.
</success_criteria>

<output>
After completion, create `.planning/phases/38-live-verification-sweep/38-05-SUMMARY.md`
</output>
