---
phase: 25-integration-verification-cutover
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - DEPLOY.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]

must_haves:
  truths:
    - "Deploy checklist documents all v1.3 env vars and Supabase OAuth configuration"
    - "REQUIREMENTS.md shows INTG-01, INTG-02, INTG-03 as complete"
    - "PROJECT.md reflects v1.3 milestone as shipped"
    - "ROADMAP.md shows Phase 25 as complete and v1.3 as shipped"
  artifacts:
    - path: "DEPLOY.md"
      provides: "Deploy checklist with v1.3 env vars and Supabase config"
      contains: "GOOGLE_TOKEN_ENCRYPTION_KEY"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability with INTG-01/02/03 complete"
      contains: "INTG-01.*Complete"
    - path: ".planning/PROJECT.md"
      provides: "v1.3 shipped status"
      contains: "v1.3"
  key_links:
    - from: "DEPLOY.md"
      to: "apps/agent/src/env.ts"
      via: "documents env vars defined in env.ts"
      pattern: "GOOGLE_CLIENT_ID|GOOGLE_CLIENT_SECRET|GOOGLE_TOKEN_ENCRYPTION_KEY"
---

<objective>
Create deploy checklist and update project documentation to mark v1.3 as shipped. This plan runs after all smoke tests pass (Plan 25-01).

Purpose: Complete the v1.3 milestone with operational documentation and accurate traceability records.
Output: DEPLOY.md at project root, updated REQUIREMENTS.md, PROJECT.md, ROADMAP.md, STATE.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-integration-verification-cutover/25-CONTEXT.md

@apps/agent/src/env.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DEPLOY.md with v1.3 environment setup guide</name>
  <files>DEPLOY.md</files>
  <action>
Create `DEPLOY.md` at the project root. This is a practical "new environment setup" reference (per user's specific idea), not just a list of env vars.

Structure:
1. **Overview** -- Brief description of the two-service architecture (Vercel web + Railway agent)
2. **Prerequisites** -- Google Cloud project, Supabase project, service accounts
3. **Environment Variables** section organized by service:
   - **Agent Service (apps/agent)** -- list ALL env vars from `apps/agent/src/env.ts` with:
     - Variable name
     - Description (from env.ts comments)
     - Source (where to get the value: Google Cloud Console, Supabase Dashboard, etc.)
     - Required/Optional status
   - Highlight the v1.3 additions: `GOOGLE_TOKEN_ENCRYPTION_KEY`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`
   - Include generation commands where applicable (e.g., `openssl rand -hex 32` for encryption key)
   - **Web App (apps/web)** -- list web-side env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, NEXT_PUBLIC_AGENT_URL, AGENT_API_KEY)
4. **Supabase Configuration** section:
   - Google OAuth provider setup in Authentication > Providers > Google
   - Required scopes: `openid email profile https://www.googleapis.com/auth/drive.readonly https://www.googleapis.com/auth/presentations.readonly https://www.googleapis.com/auth/documents.readonly`
   - Must enable "offline access" / "access_type: offline" in provider config
   - Callback URL format: `https://<supabase-project>.supabase.co/auth/v1/callback`
5. **Database Setup** -- Prisma migration commands (using `prisma migrate deploy` for production, NOT `db push` per CLAUDE.md)
6. **Deployment** -- Brief notes on Vercel and Railway deployment

Keep it concise and actionable. No filler prose.
  </action>
  <verify>
    <automated>test -f /Users/marlonburnett/source/lumenalta-hackathon/DEPLOY.md && grep -q "GOOGLE_TOKEN_ENCRYPTION_KEY" /Users/marlonburnett/source/lumenalta-hackathon/DEPLOY.md && grep -q "GOOGLE_CLIENT_ID" /Users/marlonburnett/source/lumenalta-hackathon/DEPLOY.md && grep -q "GOOGLE_CLIENT_SECRET" /Users/marlonburnett/source/lumenalta-hackathon/DEPLOY.md && grep -q "Supabase" /Users/marlonburnett/source/lumenalta-hackathon/DEPLOY.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>DEPLOY.md exists at project root with all v1.3 env vars documented, Supabase OAuth scope configuration documented, and practical setup instructions</done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md, PROJECT.md, ROADMAP.md, and STATE.md for v1.3 shipped</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
**REQUIREMENTS.md:**
- Change INTG-01 from `- [ ]` to `- [x]` with status "Complete"
- Change INTG-02 from `- [ ]` to `- [x]` with status "Complete"
- Change INTG-03 from `- [ ]` to `- [x]` with status "Complete"
- Update the Traceability table: INTG-01, INTG-02, INTG-03 status from "Pending" to "Complete"
- Update Coverage line: all 28 requirements now complete

**PROJECT.md:**
- Add v1.3 milestone to the shipped milestones description
- Add key decision entries for Phase 25 (test approach, deploy checklist)
- Update the "What This Is" section to mention user-delegated Google credentials if not already there

**ROADMAP.md:**
- Mark Phase 25 as complete: `- [x] Phase 25: Integration Verification & Cutover (completed 2026-03-06)`
- Update Phase 25 Plans section: `**Plans:** 2/2 plans complete` with plan list checked off
- Mark v1.3 milestone as shipped in the Milestones section with date
- Update Progress table: Phase 25 row with 2/2 complete, status Complete, date 2026-03-06
- Move v1.3 phases into a collapsed `<details>` block like v1.0/v1.1/v1.2

**STATE.md:**
- Update milestone status to "complete"
- Update progress: total_phases 4, completed_phases 4, total_plans 9, completed_plans 9, percent 100
- Update current position to Phase 25 complete
- Update last_activity to reflect v1.3 shipped
- Clear session continuity next action (milestone complete)
  </action>
  <verify>
    <automated>grep -c "\- \[x\] \*\*INTG-0" /Users/marlonburnett/source/lumenalta-hackathon/.planning/REQUIREMENTS.md | grep -q "3" && grep -q "Complete.*2026-03-06" /Users/marlonburnett/source/lumenalta-hackathon/.planning/ROADMAP.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All three INTG requirements marked complete in REQUIREMENTS.md, PROJECT.md updated with v1.3 shipped, ROADMAP.md shows Phase 25 complete and v1.3 shipped, STATE.md reflects 100% progress</done>
</task>

</tasks>

<verification>
Verify all documentation is consistent:

```bash
# INTG requirements all marked complete
grep "INTG-0" .planning/REQUIREMENTS.md

# Phase 25 marked complete in roadmap
grep "Phase 25" .planning/ROADMAP.md

# State shows 100%
grep "percent" .planning/STATE.md

# Deploy checklist exists with key vars
grep -c "GOOGLE_" DEPLOY.md
```
</verification>

<success_criteria>
- DEPLOY.md exists at project root with all env vars and Supabase OAuth instructions
- REQUIREMENTS.md has INTG-01, INTG-02, INTG-03 checked off as Complete
- PROJECT.md reflects v1.3 as shipped
- ROADMAP.md shows Phase 25 complete and v1.3 milestone shipped
- STATE.md shows 100% progress for v1.3
</success_criteria>

<output>
After completion, create `.planning/phases/25-integration-verification-cutover/25-02-SUMMARY.md`
</output>
