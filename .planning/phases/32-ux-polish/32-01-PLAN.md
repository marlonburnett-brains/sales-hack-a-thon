---
phase: 32-ux-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/lib/gcs-thumbnails.ts
  - apps/agent/src/mastra/index.ts
  - apps/web/src/lib/document-types.ts
  - apps/web/src/components/document-type-icon.tsx
  - apps/web/src/components/ingestion-status.tsx
  - apps/web/src/components/ingestion-progress.tsx
  - apps/web/src/lib/api-client.ts
  - apps/web/next.config.ts
autonomous: true
requirements: [UXP-01, UXP-02, UXP-03, UXP-05]

must_haves:
  truths:
    - "Agent browse endpoint returns thumbnailUrl and templateData for each Discovery document"
    - "Agent browse endpoint caches cover thumbnails to GCS for Google Slides documents"
    - "Agent ingest endpoint rejects documents already in 'ingesting' or 'queued' state"
    - "Shared IngestionStatusBadge renders all 7 template statuses identically to TemplateStatusBadge"
    - "Shared IngestionProgress renders progress bar with 'Slide N of M' text"
    - "DocumentTypeIcon renders correct lucide icon with Google-branded color per MIME type"
    - "next/image accepts storage.googleapis.com URLs without error"
  artifacts:
    - path: "apps/web/src/lib/document-types.ts"
      provides: "MIME-to-icon/color mapping constants and getDocumentTypeConfig utility"
      exports: ["DOCUMENT_TYPE_CONFIG", "getDocumentTypeConfig"]
    - path: "apps/web/src/components/document-type-icon.tsx"
      provides: "DocumentTypeIcon component with size variants"
      exports: ["DocumentTypeIcon"]
    - path: "apps/web/src/components/ingestion-status.tsx"
      provides: "Shared status badge replacing TemplateStatusBadge usage"
      exports: ["IngestionStatusBadge"]
    - path: "apps/web/src/components/ingestion-progress.tsx"
      provides: "Shared progress bar with slide count"
      exports: ["IngestionProgress"]
  key_links:
    - from: "apps/agent/src/mastra/index.ts (browse handler)"
      to: "apps/agent/src/lib/gcs-thumbnails.ts"
      via: "cacheDocumentCover function call"
      pattern: "cacheDocumentCover"
    - from: "apps/agent/src/mastra/index.ts (browse handler)"
      to: "prisma.template"
      via: "cross-reference by presentationId"
      pattern: "prisma\\.template\\.findMany.*presentationId.*in"
    - from: "apps/agent/src/mastra/index.ts (ingest handler)"
      to: "prisma.template"
      via: "duplicate guard check"
      pattern: "ingestionStatus.*ingesting.*queued"
---

<objective>
Backend thumbnail pipeline, shared UI components, and server-side duplicate guard for Discovery UX polish.

Purpose: Establish the data layer (cover thumbnails, template cross-reference) and shared UI primitives (status badge, progress bar, file-type icon) that Plan 02 consumes when redesigning Discovery cards.

Output: Agent browse returns thumbnailUrl + templateData per document, shared components ready for both Discovery and Templates pages, server-side duplicate ingestion guard active.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-ux-polish/32-CONTEXT.md
@.planning/phases/32-ux-polish/32-RESEARCH.md

@apps/agent/src/lib/gcs-thumbnails.ts
@apps/agent/src/mastra/index.ts
@apps/web/src/lib/template-utils.ts
@apps/web/src/components/template-status-badge.tsx
@apps/web/src/lib/api-client.ts
@apps/web/next.config.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/web/src/lib/template-utils.ts:
```typescript
export type TemplateStatus = "ready" | "no_access" | "not_ingested" | "stale" | "ingesting" | "queued" | "failed";
export function getTemplateStatus(template: { accessStatus: string; lastIngestedAt: string | null; sourceModifiedAt: string | null; ingestionStatus?: string; slideCount?: number; }): TemplateStatus;
export const STATUS_CONFIG: Record<TemplateStatus, { label: string; className: string }>;
```

From apps/web/src/lib/api-client.ts:
```typescript
export interface DiscoveryDocument {
  slideId: string;
  documentTitle: string;
  textContent: string;
  speakerNotes: string;
  metadata: Record<string, unknown>;
  presentationId?: string;
  slideObjectId?: string;
  source?: "mcp" | "drive";
  relevanceScore?: number;
  mimeType?: string;
  isGoogleSlides?: boolean;
  googleSlidesUrl?: string;
}

export interface BrowseResult {
  documents: DiscoveryDocument[];
  nextCursor?: string;
  totalDocuments?: number;
  ingestedHashes: string[];
}
```

From apps/agent/src/lib/gcs-thumbnails.ts:
```typescript
export async function uploadThumbnailToGCS(bucket: string, key: string, imageBuffer: Buffer, contentType: string): Promise<string>;
export async function cacheThumbnailsForTemplate(templateId: string, presentationId: string, authOptions?: GoogleAuthOptions): Promise<number>;
```

From apps/web/src/components/template-status-badge.tsx:
```typescript
export function TemplateStatusBadge({ status }: { status: TemplateStatus }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared UI primitives and type utilities</name>
  <files>
    apps/web/src/lib/document-types.ts
    apps/web/src/components/document-type-icon.tsx
    apps/web/src/components/ingestion-status.tsx
    apps/web/src/components/ingestion-progress.tsx
    apps/web/next.config.ts
    apps/web/src/lib/api-client.ts
  </files>
  <action>
    1. Create `apps/web/src/lib/document-types.ts`:
       - Export `DOCUMENT_TYPE_CONFIG` constant mapping MIME types to `{ icon, label, color, bgColor }`:
         - `application/vnd.google-apps.presentation` -> FileSliders, "Google Slides", "text-yellow-600", "bg-yellow-50"
         - `application/vnd.google-apps.document` -> FileText, "Google Docs", "text-blue-600", "bg-blue-50"
         - `application/vnd.google-apps.spreadsheet` -> FileSpreadsheet, "Google Sheets", "text-green-600", "bg-green-50"
         - `application/pdf` -> File, "PDF", "text-red-600", "bg-red-50"
       - Export `getDocumentTypeConfig(mimeType?: string)` that returns config with fallback to PDF config for unknown types, Slides config for undefined
       - Export `isIngestible(mimeType?: string): boolean` returning true only for Slides MIME type

    2. Create `apps/web/src/components/document-type-icon.tsx`:
       - Props: `{ mimeType?: string; size?: "sm" | "md" | "lg"; className?: string }`
       - "sm" = 16px icon in 24px rounded container with bgColor
       - "md" = 24px icon (no container, just icon with color)
       - "lg" = 48px icon centered (for empty thumbnail placeholder)
       - Uses `getDocumentTypeConfig` for icon and colors
       - Per user decision: Google-branded colors per file type

    3. Create `apps/web/src/components/ingestion-status.tsx`:
       - Export `IngestionStatusBadge` with same props/rendering as existing `TemplateStatusBadge` (import STATUS_CONFIG and TemplateStatus from template-utils)
       - This is a re-export/wrapper that can be imported from a neutral path (not template-specific)
       - Mark it as "use client"

    4. Create `apps/web/src/components/ingestion-progress.tsx`:
       - Export `IngestionProgress` component with props `{ current: number; total: number; className?: string }`
       - Renders shadcn `<Progress>` bar with value=(current/total)*100, height h-1.5
       - Below progress bar: `<p className="text-xs text-indigo-600">Slide {current} of {total}</p>`
       - Mark it as "use client"

    5. Update `apps/web/next.config.ts`:
       - Add `{ protocol: "https", hostname: "storage.googleapis.com" }` to `images.remotePatterns` array (required for GCS-hosted thumbnail URLs in next/image)

    6. Update `apps/web/src/lib/api-client.ts`:
       - Add `thumbnailUrl?: string` field to `DiscoveryDocument` interface
       - Add `templateData?: { ingestionStatus: string; lastIngestedAt: string | null; sourceModifiedAt: string | null; slideCount: number | null; ingestionProgress?: number; accessStatus: string; }` field to `DiscoveryDocument` interface
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - document-types.ts exports DOCUMENT_TYPE_CONFIG with 4 MIME types and getDocumentTypeConfig utility
    - DocumentTypeIcon renders lucide icons with Google-branded colors in 3 size variants
    - IngestionStatusBadge is importable from neutral path (not template-specific)
    - IngestionProgress renders shadcn Progress bar with "Slide N of M" text
    - next.config.ts allows storage.googleapis.com in remotePatterns
    - DiscoveryDocument has thumbnailUrl and templateData fields
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Agent browse enrichment (thumbnails + template cross-reference) and duplicate guard</name>
  <files>
    apps/agent/src/lib/gcs-thumbnails.ts
    apps/agent/src/mastra/index.ts
  </files>
  <action>
    1. Add `cacheDocumentCover` function to `apps/agent/src/lib/gcs-thumbnails.ts`:
       - Signature: `async function cacheDocumentCover(presentationId: string, mimeType: string, authOptions?: GoogleAuthOptions): Promise<string | null>`
       - For Google Slides (`application/vnd.google-apps.presentation`):
         - Use Slides API `presentations.pages.getThumbnail` for the first page (get page list via `presentations.get`, take first pageObjectId)
         - Fetch image bytes, upload to GCS at `document-covers/{presentationId}.png`
         - Return GCS URL
       - For other types (Docs, Sheets, PDF):
         - Use Drive API `files.get` with `fields: "thumbnailLink"` and `supportsAllDrives: true`
         - If thumbnailLink exists, fetch it (append `=s800` for higher quality), upload to GCS at `document-covers/{presentationId}.png`
         - Return GCS URL
       - On any error, log warning and return null (non-blocking)
       - Check GCS for existing cover first -- if `document-covers/{presentationId}.png` exists and is fresh (< TTL), skip re-fetch and return existing URL
       - Export the function

    2. Update browse handler in `apps/agent/src/mastra/index.ts` (the `/discovery/browse` GET endpoint):
       - After `enrichDocsWithDriveMetadata` call, add two enrichment steps:

       a) **Template cross-reference:** Collect all `presentationId` values from documents. Query `prisma.template.findMany` with `where: { presentationId: { in: presentationIds } }` selecting `presentationId, ingestionStatus, lastIngestedAt, sourceModifiedAt, slideCount, ingestionProgress, accessStatus`. Create a Map by presentationId. For each document with a matching template, attach `templateData` object.

       b) **Cover thumbnail caching (async, non-blocking):** For each document that has a presentationId and no existing thumbnailUrl in templateData:
         - Fire `cacheDocumentCover` in background (do NOT await in the request path -- use `void cacheDocumentCover(...)`)
         - On subsequent browse calls, check if GCS URL exists at `document-covers/{presentationId}.png` and return it as `thumbnailUrl`
         - To check for existing cached covers: use GCS `objects.get` with the key, catch 404. If found, set `doc.thumbnailUrl` to the public URL.
         - This means first browse shows no thumbnails (skeleton), second browse shows cached thumbnails.

    3. Update the search handler in `apps/agent/src/mastra/index.ts` (the `/discovery/search` POST endpoint):
       - Add the same template cross-reference enrichment (same logic as browse, after documents are collected)
       - Add the same thumbnail URL check (check GCS for existing covers, set thumbnailUrl)

    4. Update the ingest handler in `apps/agent/src/mastra/index.ts` (the `/discovery/ingest` POST endpoint):
       - Currently line 2121-2128 checks if template exists and skips with "done" status. Change this to:
         - If template exists AND `ingestionStatus` is 'ingesting' or 'queued', set batch item status to `{ status: "skipped", error: "Already ingesting" }` and continue
         - If template exists AND `ingestionStatus` is 'idle', allow re-ingestion (existing behavior for re-ingest)
         - This is the server-side duplicate guard per user decision (UXP-05)
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit --project apps/agent/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - Browse endpoint returns documents with thumbnailUrl (GCS-cached, populated on second load) and templateData (ingestionStatus, slideCount, etc.)
    - Search endpoint returns same enrichment
    - Cover thumbnails cached to GCS at document-covers/{presentationId}.png
    - Ingest endpoint rejects items already in 'ingesting' or 'queued' state with descriptive status
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes in both apps/web and apps/agent
2. Shared components (IngestionStatusBadge, IngestionProgress, DocumentTypeIcon) are importable and render correctly
3. Agent browse endpoint returns templateData when documents have matching templates
4. Agent ingest endpoint returns "skipped" status for documents already being ingested
</verification>

<success_criteria>
- All shared UI primitives created and TypeScript-clean
- Agent browse enriches documents with template cross-reference data
- Agent browse/search async-caches cover thumbnails to GCS
- Server-side duplicate ingestion guard active
- next/image configured to accept GCS URLs
</success_criteria>

<output>
After completion, create `.planning/phases/32-ux-polish/32-01-SUMMARY.md`
</output>
