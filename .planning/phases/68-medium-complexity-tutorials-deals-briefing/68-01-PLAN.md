---
phase: 68-medium-complexity-tutorials-deals-briefing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tutorials/scripts/mock-server.ts
  - apps/tutorials/src/helpers/route-mocks.ts
  - apps/tutorials/fixtures/deals/script.json
  - apps/tutorials/fixtures/deals/overrides.json
  - apps/tutorials/fixtures/deal-overview/script.json
  - apps/tutorials/fixtures/deal-overview/overrides.json
  - apps/tutorials/fixtures/deal-chat/script.json
  - apps/tutorials/fixtures/deal-chat/overrides.json
  - apps/tutorials/fixtures/deal-chat/stages/chat-initial.json
  - apps/tutorials/fixtures/deal-chat/stages/chat-exchange-1.json
  - apps/tutorials/fixtures/deal-chat/stages/chat-exchange-2.json
  - apps/tutorials/fixtures/deal-chat/stages/chat-note-saved.json
  - apps/tutorials/fixtures/deal-chat/stages/transcript-uploaded.json
  - apps/tutorials/fixtures/briefing/script.json
  - apps/tutorials/fixtures/briefing/overrides.json
  - apps/tutorials/fixtures/briefing/stages/idle.json
  - apps/tutorials/fixtures/briefing/stages/generating.json
  - apps/tutorials/fixtures/briefing/stages/complete.json
  - apps/tutorials/fixtures/briefing/stages/history.json
  - apps/tutorials/capture/deals.spec.ts
  - apps/tutorials/capture/deal-overview.spec.ts
  - apps/tutorials/capture/deal-chat.spec.ts
  - apps/tutorials/capture/briefing.spec.ts
autonomous: true
requirements:
  - TUT-04
  - TUT-05
  - TUT-06
  - TUT-07

must_haves:
  truths:
    - "Deals grid tutorial captures 12-15 screenshots showing create, assign, status change, grid/table toggle, and filtering"
    - "Deal Overview tutorial captures 8-12 screenshots showing metrics, timeline, and collaborators"
    - "Deal Chat tutorial captures 10-14 screenshots showing chat exchanges, transcript upload, and note-to-touch binding"
    - "Pre-Call Briefing tutorial captures 10-14 screenshots showing idle/generating/complete stages and history"
    - "All 4 tutorials use the same primary deal (deal-001) for narrative continuity"
  artifacts:
    - path: "apps/tutorials/fixtures/deals/script.json"
      provides: "12-15 step Deals grid tutorial script"
      contains: "steps"
    - path: "apps/tutorials/fixtures/deal-chat/stages/chat-exchange-1.json"
      provides: "Stage-aware chat messages after first AI response"
      contains: "chatBootstrap"
    - path: "apps/tutorials/fixtures/briefing/stages/complete.json"
      provides: "Completed briefing with company-specific research"
      contains: "interactions"
    - path: "apps/tutorials/capture/deals.spec.ts"
      provides: "Playwright capture spec for deals tutorial"
      contains: "TUTORIAL_ID"
    - path: "apps/tutorials/capture/deal-chat.spec.ts"
      provides: "Playwright capture spec for deal chat tutorial"
      contains: "TUTORIAL_ID"
    - path: "apps/tutorials/capture/briefing.spec.ts"
      provides: "Playwright capture spec for briefing tutorial"
      contains: "TUTORIAL_ID"
  key_links:
    - from: "apps/tutorials/scripts/mock-server.ts"
      to: "apps/tutorials/fixtures/deal-chat/stages/*.json"
      via: "loadStageFixtures reading chatBootstrap field"
      pattern: "chatBootstrap"
    - from: "apps/tutorials/src/helpers/route-mocks.ts"
      to: "apps/tutorials/scripts/mock-server.ts"
      via: "browser-side chat GET proxied to mock server"
      pattern: "fetch.*localhost.*chat"
    - from: "apps/tutorials/capture/*.spec.ts"
      to: "apps/tutorials/fixtures/*/script.json"
      via: "TutorialScriptSchema.parse loading step definitions"
      pattern: "TutorialScriptSchema.parse"
---

<objective>
Author scripts, fixtures, capture specs, and mock server extensions for all four Phase 68 tutorials: Creating & Managing Deals (TUT-04), Deal Overview (TUT-05), Deal Chat (TUT-06), and Pre-Call Briefing (TUT-07).

Purpose: Create all content and infrastructure modifications needed to capture deterministic screenshots for these four medium-complexity tutorials. The mock server needs stage-aware chat bootstrap for Deal Chat, and browser-side route mocks need chat GET proxy for stage awareness.

Output: 4 validated tutorial scripts, fixture data with stage files, 4 capture specs, mock server chat route enhancement, browser-side chat proxy.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-CONTEXT.md
@.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-RESEARCH.md
@.planning/phases/67-low-complexity-tutorials/67-01-SUMMARY.md
@.planning/phases/67-low-complexity-tutorials/67-02-SUMMARY.md

@apps/tutorials/scripts/mock-server.ts
@apps/tutorials/src/helpers/route-mocks.ts
@apps/tutorials/fixtures/loader.ts
@apps/tutorials/capture/action-center.spec.ts
@apps/tutorials/fixtures/action-center/script.json
@apps/tutorials/fixtures/shared/deals.json
@apps/tutorials/fixtures/shared/companies.json
@apps/tutorials/fixtures/shared/users.json
@apps/tutorials/src/types/tutorial-script.ts

<interfaces>
<!-- Executor needs these patterns from existing code -->

Capture spec pattern (from action-center.spec.ts):
- TUTORIAL_ID constant, load script.json via TutorialScriptSchema.parse
- beforeEach: ensureAuthState + mockBrowserAPIs with stageGetter closure
- Generic loop: set mockStage -> navigate -> waitFor -> actions -> capture

Stage fixture pattern (from action-center stages):
- JSON files named by stage (e.g., errors.json, resolved.json)
- Loaded by mock server via loadStageFixtures(tutorialName, currentStage)
- Fields checked by route handlers (e.g., "actions" field for actions routes)

Script JSON structure:
- TutorialScriptSchema: { id, title, description, steps: TutorialStep[] }
- TutorialStep: { id, url, narration, waitFor, actions, zoomTarget, callout, cursorTarget, mockStage, emotion, delayMs, waitForText, resetSequences }

Mock server stage-aware pattern:
- POST /mock/set-stage sets currentStage variable
- Route handlers call loadStageFixtures(tutorialName, currentStage)
- Check for specific fields in stage fixture data
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mock server chat route + browser proxy + all fixture data</name>
  <files>
    apps/tutorials/scripts/mock-server.ts
    apps/tutorials/src/helpers/route-mocks.ts
    apps/tutorials/fixtures/deals/script.json
    apps/tutorials/fixtures/deals/overrides.json
    apps/tutorials/fixtures/deal-overview/script.json
    apps/tutorials/fixtures/deal-overview/overrides.json
    apps/tutorials/fixtures/deal-chat/script.json
    apps/tutorials/fixtures/deal-chat/overrides.json
    apps/tutorials/fixtures/deal-chat/stages/chat-initial.json
    apps/tutorials/fixtures/deal-chat/stages/chat-exchange-1.json
    apps/tutorials/fixtures/deal-chat/stages/chat-exchange-2.json
    apps/tutorials/fixtures/deal-chat/stages/chat-note-saved.json
    apps/tutorials/fixtures/deal-chat/stages/transcript-uploaded.json
    apps/tutorials/fixtures/briefing/script.json
    apps/tutorials/fixtures/briefing/overrides.json
    apps/tutorials/fixtures/briefing/stages/idle.json
    apps/tutorials/fixtures/briefing/stages/generating.json
    apps/tutorials/fixtures/briefing/stages/complete.json
    apps/tutorials/fixtures/briefing/stages/history.json
  </files>
  <action>
    **Mock server modification (mock-server.ts):**
    Make the `GET /deals/:dealId/chat` route stage-aware. Currently returns hardcoded empty messages. Change to:
    1. Call `loadStageFixtures(tutorialName, currentStage)`
    2. Check for `chatBootstrap` field in stage fixtures
    3. If present, return the chatBootstrap object (messages, greeting, suggestions)
    4. If absent, fall back to current hardcoded response (empty messages + generic greeting)
    Follow the exact pattern from the research Example 1 in 68-RESEARCH.md.

    **Browser-side route mock modification (route-mocks.ts):**
    Change the browser-side `**/api/deals/*/chat` handler to proxy GET requests to the mock server (same pattern as the `actions/count` handler). POST requests continue to return the mock plaintext response.
    Follow the exact pattern from the research Example 2 in 68-RESEARCH.md.

    **Deals tutorial (TUT-04) fixtures:**
    - `fixtures/deals/script.json`: 12-15 step script. Steps: (1) navigate to /deals, show grid view with 7 deals, (2) click "New Deal" button, (3) fill deal form (name: "Cloud Migration Assessment", company: Meridian Dynamics), (4) submit and see new deal in grid, (5) click deal-001 to open deal overview, (6) navigate back to /deals, (7) click "Assign" on deal-001, select team member, (8) change status of deal-001 from open to won, (9) toggle from grid to table view, (10) show table view, (11) use status filter to show only "won" deals, (12) clear filter showing all deals. Use zoom on ~30-40% of steps, callouts on ~20-30%, cursorTarget on all click/hover steps. Conversational narration, professional emotion.
    - `fixtures/deals/overrides.json`: 7 deals with varied statuses (3 open, 2 won, 1 lost, 1 abandoned). Include company references from shared fixtures where possible. Add 2-3 new company entries for variety. Include 3-5 interactions for deal-001 to support overview metrics. All deals reference the shared user as owner.

    **Deal Overview tutorial (TUT-05) fixtures:**
    - `fixtures/deal-overview/script.json`: 8-12 step script. Steps: (1) navigate to /deals/deal-001/overview showing deal header, (2) zoom on metrics cards (touches completed, days in pipeline, last activity), (3) scroll to activity timeline showing recent interactions, (4) zoom on a specific timeline entry, (5) click collaborator management section, (6) add a collaborator, (7) zoom on updated collaborator list, (8) scroll to deal status area, (9) show status action options. Use zoom/callouts/cursor per Phase 67 ratios.
    - `fixtures/deal-overview/overrides.json`: Interactions array for deal-001 with 4-5 entries (mix of touch types: pre_call completed, touch_1 completed, touch_2 in_progress) to populate metrics and timeline. Include a knownUsers array with 3-4 users for collaborator picker.

    **Deal Chat tutorial (TUT-06) fixtures:**
    - `fixtures/deal-chat/script.json`: 10-14 step script. Steps: (1) navigate to /deals/deal-001/overview with mockStage "chat-initial" showing 1-2 prior messages in chat panel, (2) open/expand chat panel if collapsed, (3) zoom on prior conversation, (4) type context-aware question about deal pain points with mockStage "chat-exchange-1" to show AI response, (5) zoom on AI response with structured pain points, (6) type knowledge base query with mockStage "chat-exchange-2" to show KB response, (7) zoom on KB response, (8) click transcript upload button with mockStage "transcript-uploaded" to show uploaded state, (9) narrate transcript parsing result, (10) type note to save with mockStage "chat-note-saved", (11) zoom on saved note, (12) click bind-to-touch showing Touch 1 selection, (13) zoom on binding confirmation. Use zoom/callouts/cursor per ratios.
    - `fixtures/deal-chat/overrides.json`: Empty overrides (stage fixtures handle all chat state).
    - `fixtures/deal-chat/stages/chat-initial.json`: chatBootstrap with 1-2 prior messages (user asked about deal status, assistant responded with summary). greeting mentions Meridian Dynamics. Suggestions: "Draft meeting prep", "Summarize activity".
    - `fixtures/deal-chat/stages/chat-exchange-1.json`: chatBootstrap with prior messages + new user question about key pain points + assistant response listing 3 pain points (legacy integration, data silos, scalability). Follow shape from research Example 3.
    - `fixtures/deal-chat/stages/chat-exchange-2.json`: chatBootstrap with all prior messages + new user KB query ("What proposals have we sent similar companies?") + assistant response with KB results.
    - `fixtures/deal-chat/stages/chat-note-saved.json`: chatBootstrap with all messages + a note-saved confirmation message from assistant.
    - `fixtures/deal-chat/stages/transcript-uploaded.json`: chatBootstrap with all messages + transcript upload confirmation ("Transcript uploaded and parsed. I found 12 key discussion points...").

    **Pre-Call Briefing tutorial (TUT-07) fixtures:**
    - `fixtures/briefing/script.json`: 10-14 step script. Steps: (1) navigate to /deals/deal-001/briefing with mockStage "idle" showing empty briefing state, (2) zoom on "Generate Briefing" area, (3) click generate with mockStage "generating" to show loading/progress state, (4) narrate generation in progress, (5) mockStage "complete" showing finished briefing, (6) zoom on company research section, (7) scroll to value hypotheses section, (8) zoom on value hypotheses, (9) scroll to discovery questions, (10) zoom on discovery questions, (11) navigate to briefing history with mockStage "history", (12) zoom on history list showing 2-3 past briefings with different dates.
    - `fixtures/briefing/overrides.json`: Empty overrides (stage fixtures handle all state).
    - `fixtures/briefing/stages/idle.json`: interactions array with NO pre_call entries (empty or only non-pre_call interactions). Triggers "no briefing" empty state.
    - `fixtures/briefing/stages/generating.json`: interactions array with one pre_call entry having status: "running". Shows progress/loading UI.
    - `fixtures/briefing/stages/complete.json`: interactions array with one completed pre_call entry. generatedContent JSON string containing companyResearch, valueHypotheses (3 items), discoveryQuestions (3 items) all specific to Meridian Dynamics. Follow shape from research Example 4.
    - `fixtures/briefing/stages/history.json`: interactions array with 2-3 completed pre_call entries at different dates (March 10, March 15, March 19). Each has different buyerRole in inputs. Brief generatedContent per entry.

    **Shared narrative:** All 4 tutorials use deal-001 (Meridian Dynamics "Q2 Digital Transformation Initiative"). The CONTEXT specifies this creates a "follow one deal's journey" narrative across the series.

    **CRITICAL constraints per CONTEXT.md locked decisions:**
    - AI responses appear instantly via stage switching -- NO typing animation or loading indicators
    - Transcript upload uses stage switching, OS file picker skipped. Narration explains what happened.
    - Briefing three-stage pattern: idle -> generating -> complete
    - Deal Chat starts with 1-2 prior chat messages already visible
    - Conversational tone, no warm intro (that was Getting Started only)
    - Standalone outro slates -- "Tutorial Complete" only, no next-tutorial direction
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && node -e "
      const fs = require('fs');
      const path = require('path');
      const dirs = ['deals', 'deal-overview', 'deal-chat', 'briefing'];
      let ok = true;
      for (const d of dirs) {
        const sp = path.join('fixtures', d, 'script.json');
        if (!fs.existsSync(sp)) { console.error('MISSING: ' + sp); ok = false; continue; }
        const s = JSON.parse(fs.readFileSync(sp, 'utf-8'));
        console.log(d + ': ' + s.steps.length + ' steps');
      }
      // Check stage files
      const chatStages = ['chat-initial','chat-exchange-1','chat-exchange-2','chat-note-saved','transcript-uploaded'];
      for (const st of chatStages) {
        const p = 'fixtures/deal-chat/stages/' + st + '.json';
        if (!fs.existsSync(p)) { console.error('MISSING: ' + p); ok = false; }
      }
      const briefStages = ['idle','generating','complete','history'];
      for (const st of briefStages) {
        const p = 'fixtures/briefing/stages/' + st + '.json';
        if (!fs.existsSync(p)) { console.error('MISSING: ' + p); ok = false; }
      }
      if (!ok) process.exit(1);
      console.log('All fixture files present');
    "</automated>
  </verify>
  <done>
    - Mock server GET /deals/:dealId/chat is stage-aware (returns chatBootstrap from stage fixtures when present)
    - Browser-side chat GET handler proxies to mock server for stage awareness
    - 4 script.json files created with correct step counts: deals 12-15, deal-overview 8-12, deal-chat 10-14, briefing 10-14
    - 4 overrides.json files created (deals has 7 deals with varied statuses)
    - 5 deal-chat stage fixtures and 4 briefing stage fixtures created
    - All scripts use deal-001 (Meridian Dynamics) as primary deal
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture specs and end-to-end verification for all 4 tutorials</name>
  <files>
    apps/tutorials/capture/deals.spec.ts
    apps/tutorials/capture/deal-overview.spec.ts
    apps/tutorials/capture/deal-chat.spec.ts
    apps/tutorials/capture/briefing.spec.ts
  </files>
  <action>
    Create 4 Playwright capture specs following the exact pattern from `capture/action-center.spec.ts`:

    For each of deals, deal-overview, deal-chat, briefing:
    1. Set TUTORIAL_ID to the tutorial name (e.g., "deals", "deal-overview", "deal-chat", "briefing")
    2. Load and parse script.json via TutorialScriptSchema
    3. beforeEach: ensureAuthState + mockBrowserAPIs with stageGetter closure (mutable currentStageRef)
    4. Generic capture loop: iterate steps, set mockStage, navigate, wait, execute actions, capture screenshot
    5. Verify screenshot count matches step count

    The capture specs are intentionally identical in structure -- only TUTORIAL_ID and the describe/test names change. This is the established pattern from Phase 67.

    After creating all 4 specs, run each capture to verify screenshots are produced:
    ```
    pnpm --filter tutorials capture deals
    pnpm --filter tutorials capture deal-overview
    pnpm --filter tutorials capture deal-chat
    pnpm --filter tutorials capture briefing
    ```

    If any capture fails, diagnose and fix the issue in the relevant fixture or mock server code. Common issues:
    - waitFor selector not found: Adjust the selector in script.json to match actual page elements
    - Stage fixture not loading: Verify the stage name in script.json matches the fixture filename
    - Chat bootstrap returning empty: Verify mock server chat route reads chatBootstrap from stage fixtures
    - Page navigation timeout: Ensure URL paths match actual Next.js routes
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && ls capture/deals.spec.ts capture/deal-overview.spec.ts capture/deal-chat.spec.ts capture/briefing.spec.ts && echo "All 4 capture specs exist" && for t in deals deal-overview deal-chat briefing; do count=$(ls output/$t/*.png 2>/dev/null | wc -l); echo "$t: $count screenshots"; done</automated>
  </verify>
  <done>
    - 4 capture specs exist following the generic loop pattern
    - `pnpm --filter tutorials capture deals` produces 12-15 PNGs
    - `pnpm --filter tutorials capture deal-overview` produces 8-12 PNGs
    - `pnpm --filter tutorials capture deal-chat` produces 10-14 PNGs
    - `pnpm --filter tutorials capture briefing` produces 10-14 PNGs
    - All screenshots are visually deterministic (no animation artifacts, no loading states)
  </done>
</task>

</tasks>

<verification>
All 4 tutorials capture successfully with correct screenshot counts:
- deals: 12-15 screenshots in output/deals/
- deal-overview: 8-12 screenshots in output/deal-overview/
- deal-chat: 10-14 screenshots in output/deal-chat/
- briefing: 10-14 screenshots in output/briefing/

Mock server chat route is stage-aware and browser-side proxy works.
</verification>

<success_criteria>
- 4 script.json files with narration, visual effects, and stage transitions
- 4 overrides.json files with tutorial-specific fixture data
- 5 deal-chat stage fixtures + 4 briefing stage fixtures
- Mock server GET /deals/:dealId/chat is stage-aware
- Browser-side chat GET proxies to mock server
- 4 capture specs produce correct screenshot counts
- All tutorials use deal-001 (Meridian Dynamics) for narrative continuity
</success_criteria>

<output>
After completion, create `.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-01-SUMMARY.md`
</output>
