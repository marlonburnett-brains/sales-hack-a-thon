---
phase: 70-high-complexity-tutorials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tutorials/scripts/mock-server.ts
  - apps/tutorials/fixtures/touch-1-pager/script.json
  - apps/tutorials/fixtures/touch-1-pager/overrides.json
  - apps/tutorials/fixtures/touch-1-pager/stages/idle.json
  - apps/tutorials/fixtures/touch-1-pager/stages/generating.json
  - apps/tutorials/fixtures/touch-1-pager/stages/skeleton.json
  - apps/tutorials/fixtures/touch-1-pager/stages/lowfi.json
  - apps/tutorials/fixtures/touch-1-pager/stages/lowfi-refining.json
  - apps/tutorials/fixtures/touch-1-pager/stages/lowfi-refined.json
  - apps/tutorials/fixtures/touch-1-pager/stages/hifi.json
  - apps/tutorials/fixtures/touch-1-pager/stages/completed.json
  - apps/tutorials/fixtures/touch-1-pager/stages/manual-upload.json
  - apps/tutorials/fixtures/touch-2-intro-deck/script.json
  - apps/tutorials/fixtures/touch-2-intro-deck/overrides.json
  - apps/tutorials/fixtures/touch-2-intro-deck/stages/idle.json
  - apps/tutorials/fixtures/touch-2-intro-deck/stages/generating.json
  - apps/tutorials/fixtures/touch-2-intro-deck/stages/skeleton.json
  - apps/tutorials/fixtures/touch-2-intro-deck/stages/skeleton-refining.json
  - apps/tutorials/fixtures/touch-2-intro-deck/stages/skeleton-refined.json
  - apps/tutorials/fixtures/touch-2-intro-deck/stages/lowfi.json
  - apps/tutorials/fixtures/touch-2-intro-deck/stages/hifi.json
  - apps/tutorials/fixtures/touch-2-intro-deck/stages/completed.json
  - apps/tutorials/fixtures/touch-3-capability-deck/script.json
  - apps/tutorials/fixtures/touch-3-capability-deck/overrides.json
  - apps/tutorials/fixtures/touch-3-capability-deck/stages/idle.json
  - apps/tutorials/fixtures/touch-3-capability-deck/stages/generating.json
  - apps/tutorials/fixtures/touch-3-capability-deck/stages/skeleton.json
  - apps/tutorials/fixtures/touch-3-capability-deck/stages/lowfi.json
  - apps/tutorials/fixtures/touch-3-capability-deck/stages/lowfi-refining.json
  - apps/tutorials/fixtures/touch-3-capability-deck/stages/lowfi-refined.json
  - apps/tutorials/fixtures/touch-3-capability-deck/stages/hifi.json
  - apps/tutorials/fixtures/touch-3-capability-deck/stages/completed.json
  - apps/tutorials/capture/touch-1-pager.spec.ts
  - apps/tutorials/capture/touch-2-intro-deck.spec.ts
  - apps/tutorials/capture/touch-3-capability-deck.spec.ts
autonomous: true
requirements:
  - TUT-13
  - TUT-14
  - TUT-15

must_haves:
  truths:
    - "Touch 1 tutorial captures 12-18 screenshots showing full 3-gate HITL (skeleton/lowfi/hifi), one refine demo, and manual upload override"
    - "Touch 2 tutorial captures 12-18 screenshots showing strategy resolution, slide selection, reordering, and full 3-gate HITL with one refine"
    - "Touch 3 tutorial captures 12-18 screenshots showing multi-capability area selection, structure-driven assembly, and full 3-gate HITL with one refine"
    - "All 3 tutorials use deal-001 (Meridian Dynamics) for narrative continuity with Phase 68 tutorials"
    - "Mock server asset-review route is stage-aware (reads from stage fixtures instead of hardcoded response)"
  artifacts:
    - path: "apps/tutorials/fixtures/touch-1-pager/script.json"
      provides: "Touch 1 HITL tutorial script with 12-18 steps"
      contains: "steps"
    - path: "apps/tutorials/fixtures/touch-1-pager/stages/manual-upload.json"
      provides: "Manual upload override stage for Touch 1"
      contains: "interactions"
    - path: "apps/tutorials/fixtures/touch-2-intro-deck/script.json"
      provides: "Touch 2 HITL tutorial script with slide selection emphasis"
      contains: "steps"
    - path: "apps/tutorials/fixtures/touch-3-capability-deck/script.json"
      provides: "Touch 3 HITL tutorial script with capability area selection emphasis"
      contains: "steps"
    - path: "apps/tutorials/capture/touch-1-pager.spec.ts"
      provides: "Playwright capture spec for Touch 1"
      contains: "TUTORIAL_ID"
    - path: "apps/tutorials/capture/touch-2-intro-deck.spec.ts"
      provides: "Playwright capture spec for Touch 2"
      contains: "TUTORIAL_ID"
    - path: "apps/tutorials/capture/touch-3-capability-deck.spec.ts"
      provides: "Playwright capture spec for Touch 3"
      contains: "TUTORIAL_ID"
  key_links:
    - from: "apps/tutorials/scripts/mock-server.ts"
      to: "apps/tutorials/fixtures/*/stages/*.json"
      via: "loadStageFixtures reading assetReview field for stage-aware responses"
      pattern: "assetReview"
    - from: "apps/tutorials/capture/*.spec.ts"
      to: "apps/tutorials/fixtures/*/script.json"
      via: "TutorialScriptSchema.parse loading step definitions"
      pattern: "TutorialScriptSchema.parse"
    - from: "apps/tutorials/fixtures/touch-*/stages/*.json"
      to: "apps/tutorials/scripts/mock-server.ts"
      via: "Stage fixtures loaded by mock server for HITL state transitions"
      pattern: "loadStageFixtures"
---

<objective>
Author scripts, fixtures, mock server extensions, and capture specs for Touch 1 (TUT-13), Touch 2 (TUT-14), and Touch 3 (TUT-15) tutorials, plus make the asset-review mock route stage-aware for Plan 02.

Purpose: Create all content and infrastructure modifications needed to capture deterministic screenshots for the first three high-complexity HITL tutorials. Each tutorial shows the full 3-gate HITL workflow (skeleton/lowfi/hifi) with one refine demo per touch, adapted to what makes each touch unique. The mock server asset-review extension is done here so Plan 02 (Touch 4 + Asset Review) can use it immediately.

Output: 3 validated tutorial scripts with HITL stage fixtures, 3 capture specs producing screenshots, stage-aware asset-review mock route.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-high-complexity-tutorials/70-CONTEXT.md
@.planning/phases/70-high-complexity-tutorials/70-RESEARCH.md
@.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-01-SUMMARY.md

@apps/tutorials/scripts/mock-server.ts
@apps/tutorials/src/helpers/route-mocks.ts
@apps/tutorials/fixtures/loader.ts
@apps/tutorials/fixtures/types.ts
@apps/tutorials/capture/touch-4-hitl.spec.ts
@apps/tutorials/capture/action-center.spec.ts
@apps/tutorials/fixtures/touch-4-hitl/script.json
@apps/tutorials/fixtures/touch-4-hitl/stages/idle.json
@apps/tutorials/fixtures/touch-4-hitl/stages/skeleton.json
@apps/tutorials/fixtures/touch-4-hitl/stages/lowfi.json
@apps/tutorials/fixtures/touch-4-hitl/stages/hifi.json
@apps/tutorials/fixtures/touch-4-hitl/stages/completed.json
@apps/tutorials/fixtures/touch-4-hitl/overrides.json
@apps/tutorials/fixtures/shared/deals.json
@apps/tutorials/fixtures/shared/companies.json
@apps/tutorials/fixtures/shared/users.json
@apps/tutorials/src/types/tutorial-script.ts
@apps/web/src/components/touch/hitl-stage-stepper.tsx

<interfaces>
<!-- Key types and patterns the executor needs -->

Capture spec pattern (from touch-4-hitl.spec.ts -- IDENTICAL boilerplate):
- Only TUTORIAL_ID and test.describe string change per tutorial
- beforeEach: ensureAuthState + mockBrowserAPIs with stageGetter closure
- Generic loop: set mockStage -> navigate/reload -> waitFor -> actions -> capture

Stage fixture pattern (from touch-4-hitl/stages/*.json):
- JSON files named by stage (idle.json, skeleton.json, etc.)
- Each contains "interactions" array with one entry for the touch
- interaction.hitlStage matches the stage name
- interaction.stageContent is a JSON STRING (stringified object)
- Loaded by mock server via loadStageFixtures(tutorialName, currentStage)

HITL stages from hitl-stage-stepper.tsx:
- HITL_STAGES: skeleton, lowfi, highfi (note: "highfi" not "hifi" in the UI)
- Each gate shows content + approve/reject actions

Script JSON structure:
- TutorialScriptSchema: { id, title, description, steps: TutorialStep[] }
- TutorialStep: { id, url, narration, waitFor, actions, zoomTarget, callout, cursorTarget, mockStage, emotion, delayMs, waitForText, resetSequences }

touchType format:
- In fixture interactions: "touch_1", "touch_2", "touch_3" (underscore)
- In script URLs: "/deals/deal-001/touch/1", "/deals/deal-001/touch/2" (slash + number)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mock server asset-review extension + Touch 1-3 fixture data</name>
  <files>
    apps/tutorials/scripts/mock-server.ts
    apps/tutorials/fixtures/touch-1-pager/script.json
    apps/tutorials/fixtures/touch-1-pager/overrides.json
    apps/tutorials/fixtures/touch-1-pager/stages/idle.json
    apps/tutorials/fixtures/touch-1-pager/stages/generating.json
    apps/tutorials/fixtures/touch-1-pager/stages/skeleton.json
    apps/tutorials/fixtures/touch-1-pager/stages/lowfi.json
    apps/tutorials/fixtures/touch-1-pager/stages/lowfi-refining.json
    apps/tutorials/fixtures/touch-1-pager/stages/lowfi-refined.json
    apps/tutorials/fixtures/touch-1-pager/stages/hifi.json
    apps/tutorials/fixtures/touch-1-pager/stages/completed.json
    apps/tutorials/fixtures/touch-1-pager/stages/manual-upload.json
    apps/tutorials/fixtures/touch-2-intro-deck/script.json
    apps/tutorials/fixtures/touch-2-intro-deck/overrides.json
    apps/tutorials/fixtures/touch-2-intro-deck/stages/idle.json
    apps/tutorials/fixtures/touch-2-intro-deck/stages/generating.json
    apps/tutorials/fixtures/touch-2-intro-deck/stages/skeleton.json
    apps/tutorials/fixtures/touch-2-intro-deck/stages/skeleton-refining.json
    apps/tutorials/fixtures/touch-2-intro-deck/stages/skeleton-refined.json
    apps/tutorials/fixtures/touch-2-intro-deck/stages/lowfi.json
    apps/tutorials/fixtures/touch-2-intro-deck/stages/hifi.json
    apps/tutorials/fixtures/touch-2-intro-deck/stages/completed.json
    apps/tutorials/fixtures/touch-3-capability-deck/script.json
    apps/tutorials/fixtures/touch-3-capability-deck/overrides.json
    apps/tutorials/fixtures/touch-3-capability-deck/stages/idle.json
    apps/tutorials/fixtures/touch-3-capability-deck/stages/generating.json
    apps/tutorials/fixtures/touch-3-capability-deck/stages/skeleton.json
    apps/tutorials/fixtures/touch-3-capability-deck/stages/lowfi.json
    apps/tutorials/fixtures/touch-3-capability-deck/stages/lowfi-refining.json
    apps/tutorials/fixtures/touch-3-capability-deck/stages/lowfi-refined.json
    apps/tutorials/fixtures/touch-3-capability-deck/stages/hifi.json
    apps/tutorials/fixtures/touch-3-capability-deck/stages/completed.json
  </files>
  <action>
    **Mock server asset-review extension (mock-server.ts):**
    Modify the `GET /interactions/:id/asset-review` route (line ~321) to be stage-aware. Before returning the hardcoded fallback, check `loadStageFixtures(tutorialName, currentStage)` for an `assetReview` field. If present, return it. If absent, fall back to the existing hardcoded response. This is ~10 lines of code following the exact pattern from 70-RESEARCH.md "Pattern 5: Asset Review Stage-Aware Mock Extension" code example.

    **Touch 1: First-Contact Pager (TUT-13) fixtures:**
    - `script.json`: ~15 step script. Adapted to pager generation focus per CONTEXT.md:
      (1) Navigate to /deals/deal-001/touch/1, mockStage "idle", show empty pager state
      (2) Zoom on "Start Pager Generation" area, callout explaining the feature
      (3) Click generate, mockStage "generating", show progress indicator
      (4) mockStage "skeleton", show extracted outline sections (Challenge, Solution, Value Prop)
      (5) Zoom on outline content, callout "Review outline before approving"
      (6) Click Approve, mockStage "lowfi", show draft pager content with full sections
      (7) Zoom on draft content showing expanded sections
      (8) Click "Request Changes" for refine demo, mockStage "lowfi-refining" showing feedback form/loading
      (9) mockStage "lowfi-refined" showing regenerated draft with updated content
      (10) Zoom on refined content, callout "Notice the updated sections"
      (11) Click Approve on refined draft, mockStage "hifi", show final polished pager
      (12) Zoom on final pager with formatting and branding
      (13) Click Approve, mockStage "completed", show "Saved to Drive" confirmation with link
      (14) mockStage "manual-upload", show manual upload alternative (navigate back to idle-like state with upload option visible)
      (15) Zoom on manual upload area, callout "Upload your own document instead"
    - Visual effects: zoom ~30-40% of steps, callouts ~20-30%, cursorTarget on all click/hover steps
    - Narration: conversational, emphasize pager is a one-page overview document, refine demo at lowfi gate
    - `overrides.json`: Interaction entry for deal-001 with touchType "touch_1", initial status "idle". Keep minimal -- stages handle all state.
    - Stage fixtures (9 files): Each contains `interactions` array with one entry for int-touch1-001/deal-001/touch_1. The hitlStage and stageContent vary per stage:
      - `idle.json`: status "idle", no stageContent
      - `generating.json`: status "in_progress", hitlStage "generating"
      - `skeleton.json`: hitlStage "skeleton", stageContent JSON string with sections array [{title:"Challenge",content:"..."},{title:"Proposed Solution",content:"..."},{title:"Value Proposition",content:"..."}]
      - `lowfi.json`: hitlStage "lowfi", stageContent with full draft sections (longer content)
      - `lowfi-refining.json`: hitlStage "lowfi", status "in_progress" (shows regenerating state)
      - `lowfi-refined.json`: hitlStage "lowfi", stageContent with updated/improved draft content (different from lowfi.json)
      - `hifi.json`: hitlStage "highfi", stageContent with polished final pager content including formatting hints
      - `completed.json`: status "completed", hitlStage "ready", outputRefs with Google Docs URL
      - `manual-upload.json`: status "idle" with a flag indicating manual upload mode is available

    **Touch 2: Intro Deck (TUT-14) fixtures:**
    - `script.json`: ~15 step script. Adapted to strategy resolution + slide selection focus:
      (1) Navigate to /deals/deal-001/touch/2, mockStage "idle", show empty intro deck state
      (2) Zoom on strategy resolution area, callout explaining deck strategy
      (3) Click generate, mockStage "generating"
      (4) mockStage "skeleton", show proposed deck structure/outline with slide titles
      (5) Zoom on deck outline, callout "Review the proposed slide order"
      (6) Click "Request Changes" for refine demo at skeleton gate, mockStage "skeleton-refining"
      (7) mockStage "skeleton-refined" showing updated slide order/structure
      (8) Click Approve skeleton, mockStage "lowfi", show slide previews with draft content
      (9) Zoom on slide selection/reordering UI, callout "Drag to reorder slides"
      (10) Zoom on a specific slide preview showing draft visuals
      (11) Click Approve, mockStage "hifi", show final polished slides
      (12) Zoom on final slide quality with branding
      (13) Click Approve, mockStage "completed", show "Saved to Google Slides" with Drive link
    - Refine demo at skeleton gate (most meaningful for deck strategy)
    - `overrides.json`: Interaction entry for deal-001 with touchType "touch_2"
    - Stage fixtures (8 files): Same pattern as Touch 1 but stageContent reflects deck/slide data:
      - `idle.json`, `generating.json`: standard HITL start states
      - `skeleton.json`: stageContent with slide outline array [{slideNumber:1,title:"Company Overview",purpose:"..."},{slideNumber:2,...}]
      - `skeleton-refining.json`: status "in_progress" for regeneration
      - `skeleton-refined.json`: updated slide outline with reordered/modified slides
      - `lowfi.json`: stageContent with slide previews (imageUrls or content blocks per slide)
      - `hifi.json`: stageContent with polished slide data
      - `completed.json`: outputRefs with Google Slides URL

    **Touch 3: Capability Deck (TUT-15) fixtures:**
    - `script.json`: ~15 step script. Adapted to capability area selection + structure-driven assembly:
      (1) Navigate to /deals/deal-001/touch/3, mockStage "idle"
      (2) Zoom on capability area selection, callout "Select areas relevant to this deal"
      (3) Select 2-3 capability areas (e.g., "Cloud Infrastructure", "Data Analytics", "Security"), mockStage "generating"
      (4) mockStage "skeleton", show structure-driven deck outline with sections per capability area
      (5) Zoom on structure showing how capabilities map to deck sections
      (6) Click Approve skeleton, mockStage "lowfi", show draft capability slides
      (7) Zoom on draft content for a specific capability area
      (8) Click "Request Changes" for refine demo at lowfi gate, mockStage "lowfi-refining"
      (9) mockStage "lowfi-refined" showing improved capability content
      (10) Zoom on refined content, callout noting improvements
      (11) Click Approve, mockStage "hifi", show final polished capability deck
      (12) Zoom on final quality
      (13) Click Approve, mockStage "completed", show Drive link
    - Refine demo at lowfi gate (most meaningful for capability content quality)
    - `overrides.json`: Interaction entry for deal-001 with touchType "touch_3"
    - Stage fixtures (8 files): Same pattern, stageContent reflects capability areas and deck structures

    **CRITICAL constraints per CONTEXT.md locked decisions:**
    - Full HITL gates at EVERY stage (skeleton, lowfi, hifi) -- each gate gets its own step with content + approve
    - ONE refine demo per touch: Touch 1 at lowfi, Touch 2 at skeleton, Touch 3 at lowfi
    - All other gates are review + approve only
    - Touch 1 MUST include manual upload override after HITL completes (adds 2-3 steps)
    - All tutorials use deal-001 (Meridian Dynamics / "Q2 Digital Transformation Initiative")
    - stageContent MUST be a JSON string (stringified JSON), not a nested object
    - touchType in fixtures uses underscore format: "touch_1", "touch_2", "touch_3"
    - Conversational narration tone, standalone outro slates
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && node -e "
      const fs = require('fs');
      const path = require('path');
      const tutorials = ['touch-1-pager', 'touch-2-intro-deck', 'touch-3-capability-deck'];
      let ok = true;
      for (const t of tutorials) {
        const sp = path.join('fixtures', t, 'script.json');
        if (!fs.existsSync(sp)) { console.error('MISSING: ' + sp); ok = false; continue; }
        const s = JSON.parse(fs.readFileSync(sp, 'utf-8'));
        console.log(t + ': ' + s.steps.length + ' steps');
        const stagesDir = path.join('fixtures', t, 'stages');
        if (fs.existsSync(stagesDir)) {
          const stages = fs.readdirSync(stagesDir);
          console.log('  stages: ' + stages.join(', '));
        }
      }
      // Verify mock server has assetReview stage-aware code
      const ms = fs.readFileSync('scripts/mock-server.ts', 'utf-8');
      if (!ms.includes('assetReview')) { console.error('MISSING: assetReview stage-aware code in mock-server.ts'); ok = false; }
      else { console.log('mock-server.ts: assetReview stage-aware OK'); }
      if (!ok) process.exit(1);
      console.log('All fixture files present');
    "</automated>
  </verify>
  <done>
    - Mock server GET /interactions/:id/asset-review is stage-aware (checks loadStageFixtures for assetReview field)
    - 3 script.json files with 12-18 steps each, full HITL gate sequences, one refine demo per touch
    - Touch 1 script includes manual upload override steps after HITL completion
    - Touch 2 script emphasizes strategy/slide selection/reordering with refine at skeleton gate
    - Touch 3 script emphasizes capability area selection (2-3 areas) with refine at lowfi gate
    - 25 stage fixture files across 3 tutorials with correct stageContent JSON strings
    - All tutorials use deal-001 (Meridian Dynamics) for narrative continuity
  </done>
</task>

<task type="auto">
  <name>Task 2: Capture specs and end-to-end verification for Touch 1-3</name>
  <files>
    apps/tutorials/capture/touch-1-pager.spec.ts
    apps/tutorials/capture/touch-2-intro-deck.spec.ts
    apps/tutorials/capture/touch-3-capability-deck.spec.ts
  </files>
  <action>
    Create 3 Playwright capture specs following the EXACT pattern from `capture/touch-4-hitl.spec.ts`. The specs are byte-for-byte identical except for TUTORIAL_ID and the test.describe string.

    For each tutorial (touch-1-pager, touch-2-intro-deck, touch-3-capability-deck):
    1. Copy the structure from touch-4-hitl.spec.ts
    2. Set TUTORIAL_ID to the tutorial directory name (e.g., "touch-1-pager")
    3. Update test.describe string to match tutorial name
    4. Everything else (imports, beforeEach, generic capture loop) stays identical

    After creating all 3 specs, run each capture to verify screenshots are produced:
    ```
    pnpm --filter tutorials capture touch-1-pager
    pnpm --filter tutorials capture touch-2-intro-deck
    pnpm --filter tutorials capture touch-3-capability-deck
    ```

    If any capture fails, diagnose and fix:
    - waitForText not found: Check actual page text vs script expectation, adjust script.json
    - Stage fixture not loading: Verify mockStage name in script matches stage filename (minus .json)
    - Navigation timeout: First cold run may take 30-60s for Next.js compilation; retry once
    - Same-URL reload: The generic capture loop already handles this (page.reload when URL matches current)
    - stageContent parse error: Verify stageContent is a JSON string, not a nested object

    Run each capture at least once to verify. If first run times out due to cold compilation, retry.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && ls capture/touch-1-pager.spec.ts capture/touch-2-intro-deck.spec.ts capture/touch-3-capability-deck.spec.ts && echo "All 3 capture specs exist" && for t in touch-1-pager touch-2-intro-deck touch-3-capability-deck; do count=$(ls output/$t/*.png 2>/dev/null | wc -l); echo "$t: $count screenshots"; done</automated>
  </verify>
  <done>
    - 3 capture specs exist following the generic loop pattern (identical to touch-4-hitl.spec.ts)
    - `pnpm --filter tutorials capture touch-1-pager` produces 12-18 PNGs
    - `pnpm --filter tutorials capture touch-2-intro-deck` produces 12-18 PNGs
    - `pnpm --filter tutorials capture touch-3-capability-deck` produces 12-18 PNGs
    - All screenshots are visually deterministic (no animation artifacts, no loading spinners stuck)
  </done>
</task>

</tasks>

<verification>
All 3 HITL tutorials capture successfully:
- touch-1-pager: 12-18 screenshots showing full HITL + manual upload
- touch-2-intro-deck: 12-18 screenshots showing strategy/slides + full HITL
- touch-3-capability-deck: 12-18 screenshots showing capability selection + full HITL
Mock server asset-review route is stage-aware for Plan 02.
</verification>

<success_criteria>
- 3 script.json files with HITL gate sequences adapted per touch focus area
- 25 stage fixture files with correct stageContent JSON strings
- Mock server asset-review route checks stage fixtures before hardcoded fallback
- 3 capture specs produce correct screenshot counts
- Touch 1 includes manual upload override demonstration
- All tutorials use deal-001 (Meridian Dynamics) for narrative continuity
</success_criteria>

<output>
After completion, create `.planning/phases/70-high-complexity-tutorials/70-01-SUMMARY.md`
</output>
