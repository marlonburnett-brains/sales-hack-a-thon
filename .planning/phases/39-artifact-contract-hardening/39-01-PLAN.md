---
phase: 39-artifact-contract-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/api-client.ts
  - apps/web/src/lib/actions/deck-structure-actions.ts
  - apps/web/src/lib/__tests__/api-client.deck-structures.test.ts
  - apps/web/src/components/settings/chat-bar.tsx
  - apps/web/src/components/settings/__tests__/touch-type-detail-view.test.tsx
  - apps/web/src/app/api/deck-structures/chat/route.ts
  - apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts
autonomous: true
requirements:
  - CLSF-01
  - CLSF-02
  - DECK-05
must_haves:
  truths:
    - Web deck-structure helpers accept only the shared artifact contract where artifact-qualified data is expected.
    - Settings chat sends the selected Touch 4 artifact through the typed web boundary and the proxy still rejects missing Touch 4 artifacts.
    - Touch 1-3 deck structure calls remain artifact-free without fake artifact values.
  artifacts:
    - path: apps/web/src/lib/api-client.ts
      provides: Shared web DTO and helper signatures using `ArtifactType | null` for deck-structure data.
    - path: apps/web/src/lib/actions/deck-structure-actions.ts
      provides: Server-action wrappers that preserve the tightened artifact contract.
    - path: apps/web/src/components/settings/chat-bar.tsx
      provides: Typed settings-chat request body for artifact-qualified Touch 4 tabs.
    - path: apps/web/src/app/api/deck-structures/chat/route.ts
      provides: Web proxy validation and forwarding for artifact-qualified chat.
    - path: apps/web/src/lib/__tests__/api-client.deck-structures.test.ts
      provides: Regression coverage for typed deck-structure helper transport.
    - path: apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts
      provides: Regression coverage for typed proxy validation and forwarding.
  key_links:
    - from: apps/web/src/lib/actions/deck-structure-actions.ts
      to: apps/web/src/lib/api-client.ts
      via: Deck-structure actions keep optional artifact threading typed as the shared contract.
      pattern: ArtifactType|getDeckStructure|triggerDeckInference
    - from: apps/web/src/components/settings/chat-bar.tsx
      to: apps/web/src/app/api/deck-structures/chat/route.ts
      via: Touch 4 settings chat POST body carries the selected artifact into the proxy.
      pattern: artifactType|/api/deck-structures/chat
    - from: apps/web/src/app/api/deck-structures/chat/route.ts
      to: apps/agent/src/mastra/index.ts
      via: Proxy forwards the same artifact-qualified contract through `URLSearchParams`.
      pattern: URLSearchParams|artifactType
---

<objective>
Tighten the web-side artifact contract so deck-structure helpers, settings chat, and proxy validation all use the shared `ArtifactType` vocabulary instead of broad `string` seams.

Purpose: Remove compile-time drift on the web side while preserving the locked `:touchType` plus optional `artifactType` route contract introduced in earlier v1.6 phases.
Output: Typed web DTOs and actions, typed settings-chat props, and regression coverage proving artifact-qualified chat and deck-structure requests still work.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.6-v1.6-MILESTONE-AUDIT.md
@.planning/phases/39-artifact-contract-hardening/39-RESEARCH.md
@.planning/phases/37-frontend-ui/37-02-SUMMARY.md
@.planning/phases/38-live-verification-sweep/38-04-SUMMARY.md
@apps/web/src/lib/api-client.ts
@apps/web/src/lib/actions/deck-structure-actions.ts
@apps/web/src/components/settings/chat-bar.tsx
@apps/web/src/app/api/deck-structures/chat/route.ts
@apps/web/src/lib/__tests__/api-client.deck-structures.test.ts
@apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts

<interfaces>
From `apps/web/src/lib/api-client.ts`:

```ts
export interface DeckStructureSummary {
  touchType: string;
  artifactType?: string | null;
}

export async function getDeckStructure(
  touchType: string,
  artifactType?: string,
): Promise<DeckStructureDetail>;
```

From `apps/web/src/components/settings/chat-bar.tsx`:

```ts
interface ChatBarProps {
  touchType: string;
  artifactType?: string;
}
```

From `apps/web/src/app/api/deck-structures/chat/route.ts`:

```ts
const body = z.object({
  touchType: z.string().min(1),
  artifactType: z.enum(ARTIFACT_TYPES).optional(),
  message: z.string().min(1),
});
```

Locked decisions to preserve:
- Import `ArtifactType` from `@lumenalta/schemas`; do not add app-local unions.
- Keep the existing `:touchType` route family with `artifactType` in query params, not a new endpoint family.
- Preserve `URLSearchParams` encoding and null/undefined behavior for non-Touch-4 callers.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten deck-structure helper DTOs to the shared ArtifactType contract</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/lib/actions/deck-structure-actions.ts, apps/web/src/lib/__tests__/api-client.deck-structures.test.ts</files>
  <behavior>
    - Test 1: Deck-structure summary and detail payload types expose `artifactType` as the shared `ArtifactType | null` contract.
    - Test 2: Detail and infer helpers still append `artifactType` only when a valid artifact is present.
    - Test 3: Touch 1-3 callers can omit `artifactType` without adding placeholder values.
  </behavior>
  <action>Update the web deck-structure DTOs and helper signatures to import `ArtifactType` from `@lumenalta/schemas` and use `ArtifactType | null` anywhere artifact-qualified deck data crosses the helper boundary. Keep the helpers compatible with non-Touch-4 callers by allowing omitted or null artifact values, but remove broad `string` typing from these deck-structure-specific paths. Extend the targeted Vitest suite so the typed contract and the existing `URLSearchParams` behavior are both locked in. Do not create app-local aliases or duplicate the artifact tuple.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/lib/__tests__/api-client.deck-structures.test.ts</automated>
  </verify>
  <done>Web deck-structure DTOs and server actions use the shared artifact contract without breaking non-Touch-4 helper calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden settings chat and proxy boundaries to the same typed artifact contract</name>
  <files>apps/web/src/components/settings/chat-bar.tsx, apps/web/src/components/settings/__tests__/touch-type-detail-view.test.tsx, apps/web/src/app/api/deck-structures/chat/route.ts, apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts</files>
  <behavior>
    - Test 1: `ChatBar` only accepts the shared artifact values for Touch 4 settings requests and still sends the chosen artifact in the POST body.
    - Test 2: The web proxy validates the same artifact enum and rejects Touch 4 requests that omit `artifactType`.
    - Test 3: The proxy continues forwarding artifact-qualified requests to the existing deployed chat route without changing the transport shape.
  </behavior>
  <action>Replace broad `string` artifact typing in `chat-bar.tsx` with the shared `ArtifactType` contract, then tighten the proxy request parsing so the compile-time and runtime boundaries agree. Keep the request body shape unchanged (`touchType`, optional `artifactType`, `message`) and keep the proxy forwarding logic rooted in the existing `/deck-structures/:touchType/chat` family. Update the focused settings and proxy tests so invalid artifact strings cannot quietly pass through typed web code, but do not require artifact values for Touch 1-3 paths.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/settings/__tests__/touch-type-detail-view.test.tsx src/app/api/deck-structures/chat/__tests__/route.test.ts</automated>
  </verify>
  <done>Web settings chat and the chat proxy share one typed artifact boundary that preserves the deployed route contract and Touch 4 validation rules.</done>
</task>

</tasks>

<verification>
Run the focused web Vitest suites for deck-structure helpers, settings chat, and the proxy route. All should pass with `ArtifactType` imported from `@lumenalta/schemas` and no new local artifact unions.
</verification>

<success_criteria>
- Web deck-structure DTOs and helper signatures use `ArtifactType | null` where artifact-qualified data is expected.
- Settings chat still sends the active artifact end-to-end for Touch 4 while leaving Touch 1-3 artifact-free.
- Proxy validation and forwarding keep the existing `:touchType` plus query-param contract intact.
</success_criteria>

<output>
After completion, create `.planning/phases/39-artifact-contract-hardening/39-01-SUMMARY.md`
</output>
