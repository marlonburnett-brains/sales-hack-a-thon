---
phase: 39-artifact-contract-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/deck-intelligence/deck-structure-key.ts
  - apps/agent/src/deck-intelligence/infer-deck-structure.ts
  - apps/agent/src/deck-intelligence/chat-refinement.ts
  - apps/agent/src/deck-intelligence/__tests__/deck-structure-key.test.ts
  - apps/agent/src/deck-intelligence/__tests__/infer-deck-structure.test.ts
  - apps/agent/src/deck-intelligence/__tests__/chat-refinement.test.ts
  - apps/agent/src/mastra/index.ts
  - apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts
autonomous: true
requirements:
  - DECK-05
must_haves:
  truths:
    - Agent deck-key, inference, and chat helpers all share the same `ArtifactType | null` contract.
    - Detail, infer, and chat route handlers resolve the same artifact-qualified Touch 4 identity before touching deck-structure state.
    - Invalid or missing Touch 4 artifacts fail at the typed route/key boundary instead of surfacing later inside business logic.
  artifacts:
    - path: apps/agent/src/deck-intelligence/deck-structure-key.ts
      provides: Shared agent-side deck-structure identity contract.
    - path: apps/agent/src/deck-intelligence/infer-deck-structure.ts
      provides: Typed inference helpers for artifact-qualified deck rows.
    - path: apps/agent/src/deck-intelligence/chat-refinement.ts
      provides: Typed artifact-aware refinement and persistence flow.
    - path: apps/agent/src/mastra/index.ts
      provides: Route-level parsing and delegation for deck-structure detail, infer, and chat.
    - path: apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts
      provides: Route-contract regression coverage for typed artifact handling.
  key_links:
    - from: apps/agent/src/mastra/index.ts
      to: apps/agent/src/deck-intelligence/deck-structure-key.ts
      via: Route query parsing resolves a typed `{ touchType, artifactType }` key before handler work.
      pattern: ARTIFACT_TYPES|resolveDeckStructureKey
    - from: apps/agent/src/deck-intelligence/chat-refinement.ts
      to: apps/agent/src/deck-intelligence/infer-deck-structure.ts
      via: Structured refinement fallback preserves the same artifact-qualified key contract.
      pattern: inferDeckStructure|artifactType
    - from: apps/agent/src/deck-intelligence/infer-deck-structure.ts
      to: apps/agent/src/deck-intelligence/deck-structure-key.ts
      via: Inference and hashing normalize through the shared deck key helper.
      pattern: DeckStructureKey|resolveDeckStructureKey
---

<objective>
Harden the agent-side deck-intelligence and route seams so artifact-qualified Touch 4 work is typed end-to-end against the shared `ArtifactType` contract.

Purpose: Remove the remaining broad `string` agent seams identified in the audit without changing the locked `resolveDeckStructureKey()` architecture or the existing route family.
Output: Typed deck-key/inference/chat helpers, typed route parsing, and regression coverage proving Touch 4 artifact scoping still resolves to the correct row.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.6-v1.6-MILESTONE-AUDIT.md
@.planning/phases/39-artifact-contract-hardening/39-RESEARCH.md
@.planning/phases/36-backend-engine-api-routes/36-02-PLAN.md
@.planning/phases/38-live-verification-sweep/38-06-SUMMARY.md
@apps/agent/src/deck-intelligence/deck-structure-key.ts
@apps/agent/src/deck-intelligence/infer-deck-structure.ts
@apps/agent/src/deck-intelligence/chat-refinement.ts
@apps/agent/src/mastra/index.ts
@apps/agent/src/deck-intelligence/__tests__/deck-structure-key.test.ts
@apps/agent/src/deck-intelligence/__tests__/infer-deck-structure.test.ts
@apps/agent/src/deck-intelligence/__tests__/chat-refinement.test.ts
@apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts

<interfaces>
From `apps/agent/src/deck-intelligence/deck-structure-key.ts`:

```ts
export type DeckStructureKey = {
  touchType: (typeof TOUCH_TYPES)[number];
  artifactType: ArtifactType | null;
};

export function resolveDeckStructureKey(
  touchType: string,
  artifactType: string | null = null,
): DeckStructureKey;
```

From `apps/agent/src/deck-intelligence/chat-refinement.ts`:

```ts
export async function streamChatRefinement(
  touchType: string,
  userMessage: string,
  onChunk: (text: string) => void,
  artifactType: string | null = null,
): Promise<ChatRefinementResult>;
```

From `apps/agent/src/mastra/index.ts`:

```ts
const query = z
  .object({ artifactType: z.string().nullable().optional() })
  .parse(c.req.query());

key = resolveDeckStructureKey(touchType, query.artifactType ?? null);
```

Locked decisions to preserve:
- `resolveDeckStructureKey()` remains the single runtime gate for Touch 4 artifact validation.
- Keep the existing `:touchType` route family; do not introduce `/touch_4/:artifactType/...` endpoints.
- Preserve `ArtifactType | null` semantics so Touch 1-3 and `pre_call` do not need fake artifact values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten deck-key and inference helpers to the shared ArtifactType contract</name>
  <files>apps/agent/src/deck-intelligence/deck-structure-key.ts, apps/agent/src/deck-intelligence/infer-deck-structure.ts, apps/agent/src/deck-intelligence/__tests__/deck-structure-key.test.ts, apps/agent/src/deck-intelligence/__tests__/infer-deck-structure.test.ts</files>
  <behavior>
    - Test 1: Deck keys still require a valid Touch 4 artifact while leaving Touch 1-3 and `pre_call` at `null`.
    - Test 2: Inference and hashing stay isolated per artifact-qualified Touch 4 key.
    - Test 3: The typed helper signatures no longer rely on broad `string | null` artifact seams when `ArtifactType | null` is the real contract.
  </behavior>
  <action>Refactor the deck-key and inference helper signatures to use `ArtifactType | null` wherever artifact-qualified deck identity crosses the agent boundary. Keep string-based touch-type inputs only where they are truly untrusted route inputs, and normalize them immediately through the shared key helper. Extend the existing key and inference tests so they assert both the typed contract and the unchanged behavioral guarantees from Phase 36. Do not change the six-key cron/list behavior, and do not weaken the Touch 4 artifact requirement.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/deck-intelligence/__tests__/deck-structure-key.test.ts src/deck-intelligence/__tests__/infer-deck-structure.test.ts</automated>
  </verify>
  <done>Deck-key and inference helpers expose the shared artifact contract cleanly while preserving the existing Touch 4 isolation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align chat refinement and Mastra route parsing to the same typed artifact boundary</name>
  <files>apps/agent/src/deck-intelligence/chat-refinement.ts, apps/agent/src/deck-intelligence/__tests__/chat-refinement.test.ts, apps/agent/src/mastra/index.ts, apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts</files>
  <behavior>
    - Test 1: Chat refinement still updates only the resolved artifact-qualified deck row.
    - Test 2: Detail, infer, and chat route handlers validate or narrow `artifactType` before passing it into deck-intelligence helpers.
    - Test 3: Invalid artifact strings can no longer hide behind broad route/helper typing, while valid Touch 4 artifacts still stream and persist correctly.
  </behavior>
  <action>Update `chat-refinement.ts` and the Mastra route handlers so every artifact-qualified seam uses the shared `ArtifactType | null` contract after request parsing. Prefer a small local helper or shared schema only if it removes duplicate casts without broadening scope; otherwise tighten the existing route schemas in place. Keep the current direct-structure-update refinement flow from Phase 38, keep route registration root-mounted, and keep `resolveDeckStructureKey()` as the one validation gate before Prisma or inference work. Extend the targeted route/chat tests so they fail if artifact typing drifts back to broad strings.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/deck-intelligence/__tests__/chat-refinement.test.ts src/mastra/__tests__/deck-structure-routes.test.ts</automated>
  </verify>
  <done>Agent chat and route seams use one typed artifact boundary, and the existing artifact-scoped refinement behavior remains covered by regression tests.</done>
</task>

</tasks>

<verification>
Run the focused agent Vitest suites for deck keys, inference, chat refinement, and Mastra route contracts. All should pass with the tightened `ArtifactType | null` signatures and unchanged Touch 4 artifact scoping behavior.
</verification>

<success_criteria>
- Agent deck-intelligence helpers use `ArtifactType | null` at artifact-qualified boundaries.
- Mastra route parsing narrows artifact input before business logic and still resolves every Touch 4 operation through `resolveDeckStructureKey()`.
- Chat refinement keeps updating only the intended artifact-specific deck row after the type tightening.
</success_criteria>

<output>
After completion, create `.planning/phases/39-artifact-contract-hardening/39-02-SUMMARY.md`
</output>
