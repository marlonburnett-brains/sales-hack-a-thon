---
phase: 39-artifact-contract-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - 39-01
files_modified:
  - apps/web/src/components/settings/deck-structure-view.tsx
  - apps/web/src/components/settings/touch-type-accordion.tsx
  - apps/web/src/components/settings/__tests__/deck-structure-view.test.tsx
autonomous: true
requirements:
  - DECK-03
  - DECK-04
  - DECK-05
must_haves:
  truths:
    - The legacy deck structure view can render Touch 4 artifact rows without collapsing proposal, talk_track, and faq into one touch-only slot.
    - If the legacy view is reused, each Touch 4 artifact entry loads its own detail row and sends the matching artifact into chat refinement.
    - Non-Touch-4 entries still render once with their existing labels and confidence behavior.
  artifacts:
    - path: apps/web/src/components/settings/deck-structure-view.tsx
      provides: Artifact-aware legacy settings aggregation and detail loading.
    - path: apps/web/src/components/settings/touch-type-accordion.tsx
      provides: Reusable accordion entry that can preserve an artifact-qualified label and chat scope.
    - path: apps/web/src/components/settings/__tests__/deck-structure-view.test.tsx
      provides: Regression coverage for artifact-qualified legacy view reuse.
  key_links:
    - from: apps/web/src/components/settings/deck-structure-view.tsx
      to: apps/web/src/lib/actions/deck-structure-actions.ts
      via: Legacy view loads summaries and detail rows using an artifact-qualified key instead of `touchType` alone.
      pattern: getDeckStructuresAction|getDeckStructureAction|artifactType
    - from: apps/web/src/components/settings/deck-structure-view.tsx
      to: apps/web/src/components/settings/touch-type-accordion.tsx
      via: Reused Touch 4 entries pass both a stable composite identity and the selected artifact label into each accordion item.
      pattern: proposal|talk_track|faq|label
    - from: apps/web/src/components/settings/touch-type-accordion.tsx
      to: apps/web/src/components/settings/chat-bar.tsx
      via: Legacy accordion forwards the active artifact into chat refinement when a Touch 4 artifact row is rendered.
      pattern: artifactType|ChatBar
---

<objective>
Harden the legacy `deck-structure-view.tsx` path so future reuse cannot collapse artifact-qualified Touch 4 structures back into a touch-only view.

Purpose: Close the audit's explicit reuse-risk hotspot without redesigning the current Touch 4 tabbed route or expanding scope into unrelated settings performance work.
Output: Artifact-aware legacy view aggregation, artifact-aware accordion wiring, and regression tests proving proposal/talk_track/faq remain distinct if the view is reused.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.6-v1.6-MILESTONE-AUDIT.md
@.planning/phases/39-artifact-contract-hardening/39-RESEARCH.md
@.planning/phases/37-frontend-ui/37-02-SUMMARY.md
@apps/web/src/components/settings/deck-structure-view.tsx
@apps/web/src/components/settings/touch-type-accordion.tsx
@apps/web/src/components/settings/chat-bar.tsx
@apps/web/src/lib/actions/deck-structure-actions.ts

<interfaces>
From `apps/web/src/components/settings/deck-structure-view.tsx`:

```ts
const [structures, setStructures] = useState<Record<string, DeckStructureDetail>>({});

const detail = await getDeckStructureAction(summary.touchType);
structMap[r.touchType] = r.detail;
```

From `apps/web/src/components/settings/touch-type-accordion.tsx`:

```ts
interface TouchTypeAccordionProps {
  touchType: string;
  structure: DeckStructureDetail | null;
  slideIdToThumbnail: Record<string, string>;
}

<ChatBar touchType={touchType} onStructureUpdate={handleStructureUpdate} />
```

Reuse-risk decisions to honor:
- Harden the legacy view enough that Touch 4 artifacts stay distinct if it is reused; do not simply rely on the current tab route never importing it.
- Keep the current tabbed Touch 4 page as-is; this plan addresses reuse safety, not a page redesign.
- Do not broaden scope into the known Touch 4 double-fetch performance debt unless correctness requires it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a regression test for artifact-qualified legacy view reuse</name>
  <files>apps/web/src/components/settings/__tests__/deck-structure-view.test.tsx</files>
  <behavior>
    - Test 1: The legacy view renders separate Touch 4 entries for proposal, talk_track, and faq instead of one overwritten `touch_4` entry.
    - Test 2: Each Touch 4 artifact entry requests its own detail row with the matching `artifactType`.
    - Test 3: Touch 1-3 still render once each without artifact labels or duplicate rows.
  </behavior>
  <action>Create a focused component test that mounts `DeckStructureView` with summary/detail mocks covering Touch 1-3 plus all three Touch 4 artifact rows. Make the test fail against the current touch-type-only map by asserting that proposal, talk_track, and faq stay visible and distinct, and that the detail loader is called with the matching artifact each time. Keep the test narrow to reuse safety; do not couple it to the current Touch 4 tab page.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/settings/__tests__/deck-structure-view.test.tsx</automated>
  </verify>
  <done>A failing regression test captures the exact Touch 4 reuse bug the audit identified in the legacy settings view.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement composite-key legacy deck structure loading and artifact-aware accordions</name>
  <files>apps/web/src/components/settings/deck-structure-view.tsx, apps/web/src/components/settings/touch-type-accordion.tsx, apps/web/src/components/settings/__tests__/deck-structure-view.test.tsx</files>
  <behavior>
    - Test 1: Touch 4 proposal, talk_track, and faq details no longer overwrite each other in local state.
    - Test 2: Reused accordion rows pass the correct artifact label and artifact-qualified chat scope into `ChatBar`.
    - Test 3: Non-Touch-4 accordion rows keep their existing labels, confidence badges, and empty-state behavior.
  </behavior>
  <action>Refactor the legacy view to key Touch 4 rows by an artifact-qualified identity rather than `touchType` alone. Render one accordion item per logical deck-structure row, using artifact-aware labels for Touch 4 rows and preserving existing labels for Touch 1-3. Update `touch-type-accordion.tsx` so it can accept and forward optional `artifactType` context to `ChatBar` when a Touch 4 artifact row is rendered, while leaving non-Touch-4 behavior unchanged. Do not redesign the current `/settings/deck-structures/touch-4` route and do not turn this task into a broader data-loading overhaul.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run src/components/settings/__tests__/deck-structure-view.test.tsx src/components/settings/__tests__/touch-type-detail-view.test.tsx</automated>
  </verify>
  <done>The legacy deck structure view is safe to reuse with Touch 4 artifact rows because detail state, labels, and chat scope all remain artifact-qualified.</done>
</task>

</tasks>

<verification>
Run the new legacy-view regression test plus the existing typed settings-detail suite to confirm artifact-qualified loading and chat scope still hold after the reuse hardening.
</verification>

<success_criteria>
- Reusing `deck-structure-view.tsx` no longer collapses Touch 4 artifact rows into one `touch_4` bucket.
- Proposal, Talk Track, and FAQ entries each fetch and preserve their own detail/chat context.
- Touch 1-3 accordions continue behaving as they did before the hardening change.
</success_criteria>

<output>
After completion, create `.planning/phases/39-artifact-contract-hardening/39-03-SUMMARY.md`
</output>
