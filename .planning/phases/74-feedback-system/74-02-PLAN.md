---
phase: 74-feedback-system
plan: 02
type: tdd
wave: 1
depends_on:
  - 74-01
files_modified:
  - apps/web/src/components/feedback/FeedbackWidget.tsx
  - apps/web/src/components/feedback/FeedbackWidget.test.tsx
autonomous: true
requirements:
  - FEED-01
  - FEED-04

must_haves:
  truths:
    - "FeedbackWidget renders two tab options: Tutorial feedback and Feature feedback"
    - "Submit button is disabled when comment is empty or exceeds 500 characters"
    - "Character counter appears and reflects current comment length when text is entered"
    - "Submit calls submitFeedbackAction with sourceType, sourceId, feedbackType, and comment"
    - "On success: toast.success fires and form resets to default state (empty textarea, Tutorial feedback tab)"
    - "On failure: toast.error fires and textarea retains its content"
    - "FeedbackWidgetProps interface has JSDoc on sourceType, sourceId, and defaultFeedbackType"
  artifacts:
    - path: "apps/web/src/components/feedback/FeedbackWidget.tsx"
      provides: "Reusable client component with Tabs segmented control, Textarea, char counter, submit logic"
      exports: ["FeedbackWidget", "FeedbackWidgetProps"]
    - path: "apps/web/src/components/feedback/FeedbackWidget.test.tsx"
      provides: "Vitest + RTL unit tests for all 7 FEED-01 behaviors"
      min_lines: 80
  key_links:
    - from: "apps/web/src/components/feedback/FeedbackWidget.tsx"
      to: "apps/web/src/lib/actions/feedback-actions.ts"
      via: "import { submitFeedbackAction }"
      pattern: "submitFeedbackAction"
---

<objective>
Implement the FeedbackWidget client component using TDD: write failing tests first, then implement until all tests pass.

Purpose: Delivers FEED-01 (reusable widget) and FEED-04 (JSDoc documentation). The component is self-contained — all behavior verified by unit tests before slug page integration.
Output: FeedbackWidget.tsx (component) + FeedbackWidget.test.tsx (7 unit tests, all green)
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/74-feedback-system/74-CONTEXT.md
@.planning/phases/74-feedback-system/74-RESEARCH.md
@.planning/phases/74-feedback-system/74-VALIDATION.md

<interfaces>
<!-- Key interfaces the executor needs. From Plan 01 output. -->

From apps/web/src/lib/actions/feedback-actions.ts (created in Plan 01):
```typescript
export async function submitFeedbackAction(payload: {
  sourceType: string;
  sourceId: string;
  feedbackType: "tutorial_feedback" | "feature_feedback";
  comment: string;
}): Promise<void>
```

shadcn/ui components available:
- `import { Tabs, TabsList, TabsTrigger } from "@/components/ui/tabs"`
- `import { Textarea } from "@/components/ui/textarea"`
- `import { Button } from "@/components/ui/button"`
- `import { Loader2 } from "lucide-react"`
- `import { toast } from "sonner"`

Stage approval bar async button pattern (apps/web/src/components/touch/stage-approval-bar.tsx):
```tsx
<Button disabled={isSubmitting} onClick={handleSubmit}>
  {isSubmitting ? (
    <><Loader2 className="h-4 w-4 animate-spin" />Submitting...</>
  ) : (
    "Submit feedback"
  )}
</Button>
```
</interfaces>
</context>

<feature>
  <name>FeedbackWidget</name>
  <files>apps/web/src/components/feedback/FeedbackWidget.tsx, apps/web/src/components/feedback/FeedbackWidget.test.tsx</files>
  <behavior>
    RED phase — write these tests first, all must FAIL before implementation:

    Test 1: renders "Tutorial feedback" and "Feature feedback" tab triggers
    Test 2: submit button is disabled when comment is empty string
    Test 3: submit button is disabled when comment.length > 500
    Test 4: character counter shows "X/500" when comment.length > 0; not shown when empty
    Test 5: submit calls submitFeedbackAction({ sourceType, sourceId, feedbackType: "tutorial_feedback", comment }) with correct args
    Test 6: on successful submit — toast.success("Thanks for your feedback!") is called AND textarea resets to "" AND tab resets to "tutorial_feedback"
    Test 7: on failed submit — toast.error("Failed to submit feedback. Please try again.") is called AND textarea retains its content (does NOT reset)

    Mock strategy:
    - `vi.mock("@/lib/actions/feedback-actions", () => ({ submitFeedbackAction: vi.fn() }))`
    - `vi.mock("sonner", () => ({ toast: { success: vi.fn(), error: vi.fn() } }))`
    - Mock shadcn/ui components if needed to simplify rendering

    GREEN phase — implement FeedbackWidget.tsx:
    - "use client" directive
    - Props interface `FeedbackWidgetProps` with JSDoc on each prop:
      - sourceType: The entity type this feedback is attached to (e.g. "tutorial"). Extend by passing a new string value; the AppFeedback table stores it verbatim.
      - sourceId: The database ID of the entity (not a slug). For tutorials, use tutorial.id.
      - defaultFeedbackType: Optional. Defaults to "tutorial_feedback". Pass "feature_feedback" to pre-select the feature tab on pages where that is more relevant.
    - State: `feedbackType` (default "tutorial_feedback"), `comment` (default ""), `isSubmitting` (default false)
    - MAX_CHARS constant = 500
    - Submit disabled when: `isSubmitting || comment.trim().length === 0 || comment.length > MAX_CHARS`
    - Character counter: render "X/500" only when `comment.length > 0` (Claude's discretion — reduces visual noise on empty form)
    - handleSubmit: set isSubmitting=true, call submitFeedbackAction, on success call toast.success + reset (setComment(""), setFeedbackType(defaultFeedbackType ?? "tutorial_feedback")), on error call toast.error (do NOT reset comment), always set isSubmitting=false in finally
    - Layout: horizontal divider (`<hr>`), section heading ("Leave feedback"), Tabs segmented control, Textarea (rows=4 — Claude's discretion), character counter, Submit button
  </behavior>
  <implementation>
    After RED phase tests fail as expected, implement FeedbackWidget.tsx following the behavior spec above. Run tests after each meaningful addition. Commit RED state, then GREEN state separately.

    REFACTOR: Clean up any duplication, ensure types are tight (no `any`), verify JSDoc is present on props interface.
  </implementation>
</feature>

<verification>
  <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/web && npx vitest run src/components/feedback/FeedbackWidget.test.tsx</automated>
</verification>

<success_criteria>
- All 7 unit tests pass
- `cd apps/web && npx vitest run src/components/feedback/FeedbackWidget.test.tsx` exits 0
- FeedbackWidget.tsx has JSDoc on FeedbackWidgetProps (sourceType, sourceId, defaultFeedbackType)
- Component is in dedicated feedback/ folder signaling reusability
- TypeScript compiles: `cd apps/web && npx tsc --noEmit`
</success_criteria>

<output>
After completion, create `.planning/phases/74-feedback-system/74-02-SUMMARY.md`
</output>
