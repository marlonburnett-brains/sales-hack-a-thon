---
phase: 25-integration-verification-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/lib/__tests__/google-auth.test.ts
  - apps/agent/src/lib/__tests__/request-auth.test.ts
  - apps/agent/src/lib/__tests__/token-cache.test.ts
autonomous: true
requirements: [INTG-01, INTG-02, INTG-03]

must_haves:
  truths:
    - "Auth factories fall back to service account when no options provided"
    - "Auth factories use user OAuth2Client when accessToken provided"
    - "extractGoogleAuth returns accessToken when X-Google-Access-Token header present"
    - "extractGoogleAuth returns empty when no headers present (service account fallback)"
    - "extractGoogleAuth refreshes via token-cache when only X-User-Id present"
    - "getPooledGoogleAuth iterates valid tokens and returns pool source on success"
    - "getPooledGoogleAuth marks failed tokens invalid and creates ActionRequired"
    - "getPooledGoogleAuth falls back to service_account when pool exhausted"
    - "Token cache returns cached token on hit and refreshes on miss"
    - "Pool health warning fires when valid tokens < 3"
  artifacts:
    - path: "apps/agent/src/lib/__tests__/google-auth.test.ts"
      provides: "Auth factory + token pool tests"
      min_lines: 80
    - path: "apps/agent/src/lib/__tests__/request-auth.test.ts"
      provides: "Request auth extraction tests"
      min_lines: 40
    - path: "apps/agent/src/lib/__tests__/token-cache.test.ts"
      provides: "Token cache hit/miss/refresh tests"
      min_lines: 60
  key_links:
    - from: "apps/agent/src/lib/__tests__/google-auth.test.ts"
      to: "apps/agent/src/lib/google-auth.ts"
      via: "vi.mock + dynamic import"
      pattern: "import.*google-auth"
    - from: "apps/agent/src/lib/__tests__/request-auth.test.ts"
      to: "apps/agent/src/lib/request-auth.ts"
      via: "vi.mock + dynamic import"
      pattern: "import.*request-auth"
    - from: "apps/agent/src/lib/__tests__/token-cache.test.ts"
      to: "apps/agent/src/lib/token-cache.ts"
      via: "vi.mock + dynamic import"
      pattern: "import.*token-cache"
---

<objective>
Write vitest smoke tests verifying the v1.3 auth priority chain works correctly across all critical paths: dual-mode auth factories (INTG-01, INTG-02), request header extraction (INTG-01, INTG-02), and token pool with fallback (INTG-03). Tests use mocked Google APIs and Prisma -- no real API calls.

Purpose: Permanent regression suite proving Phases 22-24 auth model works. Final gate before marking v1.3 complete.
Output: Three test files covering google-auth.ts, request-auth.ts, and token-cache.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-integration-verification-cutover/25-CONTEXT.md
@.planning/phases/25-integration-verification-cutover/25-RESEARCH.md

@apps/agent/src/lib/google-auth.ts
@apps/agent/src/lib/request-auth.ts
@apps/agent/src/lib/token-cache.ts
@apps/agent/src/lib/token-encryption.ts
@apps/agent/src/lib/__tests__/token-encryption.test.ts
@apps/agent/src/env.ts
@apps/agent/vitest.config.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/agent/src/lib/google-auth.ts:
```typescript
export interface GoogleAuthOptions {
  accessToken?: string;
  userId?: string;
}

export interface PooledAuthResult {
  accessToken?: string;
  source: 'pool' | 'service_account';
  userId?: string;
}

export function getSlidesClient(options?: GoogleAuthOptions): slides_v1.Slides;
export function getDriveClient(options?: GoogleAuthOptions): drive_v3.Drive;
export function getDocsClient(options?: GoogleAuthOptions): docs_v1.Docs;
export async function getPooledGoogleAuth(): Promise<PooledAuthResult>;
```

From apps/agent/src/lib/request-auth.ts:
```typescript
export interface GoogleAuthResult {
  accessToken?: string;
  userId?: string;
}

interface RequestContext {
  req: { header(name: string): string | undefined; };
}

export async function extractGoogleAuth(c: RequestContext): Promise<GoogleAuthResult>;
```

From apps/agent/src/lib/token-cache.ts:
```typescript
export async function getAccessTokenForUser(userId: string): Promise<string | null>;
```

From apps/agent/src/env.ts:
```typescript
// Uses @t3-oss/env-core createEnv -- validates at import time
// Must be mocked in tests to avoid env validation failures
export const env: {
  GOOGLE_CLIENT_ID: string;
  GOOGLE_CLIENT_SECRET: string;
  GOOGLE_SERVICE_ACCOUNT_KEY: string;
  GOOGLE_TOKEN_ENCRYPTION_KEY?: string;
  // ... other env vars
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: google-auth.test.ts -- Auth factory selection + token pool verification</name>
  <files>apps/agent/src/lib/__tests__/google-auth.test.ts</files>
  <behavior>
    - INTG-01 service account fallback:
      - getSlidesClient() with no args returns slides client using GoogleAuth (service account)
      - getDriveClient(undefined) uses service account auth
      - getDocsClient({}) with empty options but no accessToken falls back to service account
    - INTG-02 user token path:
      - getSlidesClient({ accessToken: "user-token" }) uses OAuth2Client with user credentials
      - getDriveClient({ accessToken: "user-token" }) uses OAuth2Client
      - getDocsClient({ accessToken: "user-token" }) uses OAuth2Client
      - getUserAuth returns null when options.accessToken is undefined
    - INTG-03 token pool:
      - getPooledGoogleAuth returns { source: 'pool', accessToken, userId } when valid token found
      - getPooledGoogleAuth updates lastUsedAt on success (fire-and-forget mock verified via vi.fn)
      - getPooledGoogleAuth marks token isValid=false with revokedAt on failure
      - getPooledGoogleAuth creates ActionRequired record on token failure (if not already existing)
      - getPooledGoogleAuth returns { source: 'service_account' } when all tokens fail
      - getPooledGoogleAuth returns { source: 'service_account' } when no tokens in pool
      - Pool health: console.warn fires when valid count < 3
      - Token rotation: 'tokens' event handler updates encrypted refresh token
  </behavior>
  <action>
Create `apps/agent/src/lib/__tests__/google-auth.test.ts`. Follow the existing pattern from `token-encryption.test.ts` using dynamic `await import()` to load the module under test AFTER mocks are established.

**Critical mocking order (pitfall avoidance):**
1. Mock `../../env` FIRST with `vi.mock("../../env", ...)` providing `{ env: { GOOGLE_CLIENT_ID: "test-id", GOOGLE_CLIENT_SECRET: "test-secret", GOOGLE_SERVICE_ACCOUNT_KEY: '{"type":"service_account","project_id":"test"}' } }` -- this prevents @t3-oss/env-core validation at import time.
2. Mock `googleapis` with `vi.mock("googleapis", ...)` providing a `google` object with `auth.GoogleAuth` constructor, `slides()`, `drive()`, `docs()` as vi.fn() mocks.
3. Mock `google-auth-library` with `vi.mock("google-auth-library", ...)` providing `OAuth2Client` constructor mock that tracks `setCredentials` calls and returns mock `getAccessToken`.
4. Mock `@prisma/client` with `vi.mock("@prisma/client", ...)` providing `PrismaClient` constructor with `userGoogleToken.findMany`, `.update`, `.count` and `actionRequired.findFirst`, `.create` as vi.fn() mocks.
5. Mock `./token-encryption` with `vi.mock("./token-encryption", ...)` providing `decryptToken` returning a predictable "decrypted-refresh-token" and `encryptToken` returning `{ encrypted: "enc", iv: "iv", authTag: "tag" }`.

**Use `vi.resetModules()` + dynamic import in each test** to ensure clean module state (module-level `const prisma = new PrismaClient()` runs fresh each time).

**Test structure:**
- `describe("Dual-mode auth factories")` -- tests for getSlidesClient, getDriveClient, getDocsClient with/without options
- `describe("getPooledGoogleAuth")` -- tests for pool iteration, fallback, marking invalid, ActionRequired creation, pool health warning, token rotation

Use `beforeEach(() => { vi.clearAllMocks(); })` to prevent mock state leakage.

For pool health warning test: use `vi.spyOn(console, 'warn')` and verify it was called with the expected message pattern.

For fire-and-forget operations: the mocked Prisma `.update()` should return a thenable `{ catch: vi.fn() }` so the `.catch(() => {})` chain resolves.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && npx vitest run src/lib/__tests__/google-auth.test.ts --reporter=verbose</automated>
  </verify>
  <done>All tests pass: service account fallback with no options, user OAuth2Client with accessToken, pool iteration with success/failure/exhaustion, ActionRequired creation, pool health warning, token rotation event handler</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: request-auth.test.ts + token-cache.test.ts -- Priority chain and cache paths</name>
  <files>apps/agent/src/lib/__tests__/request-auth.test.ts, apps/agent/src/lib/__tests__/token-cache.test.ts</files>
  <behavior>
    - request-auth extractGoogleAuth:
      - Returns { accessToken: "token", userId: "user-123" } when both headers present
      - Returns { accessToken: "token" } when only X-Google-Access-Token present (userId undefined)
      - Returns { accessToken: "refreshed-token", userId: "user-123" } when only X-User-Id present and token-cache returns a token
      - Returns { userId: "user-123" } when only X-User-Id present but token-cache returns null
      - Returns {} when no headers present (service account fallback)
    - token-cache getAccessTokenForUser:
      - Returns cached token when cache has valid (non-expired) entry
      - Refreshes from DB when cache miss, returns new access token
      - Returns null when no stored token in DB (findUnique returns null)
      - Returns null and marks token invalid when OAuth2Client.getAccessToken fails
      - Returns null and marks token invalid when getAccessToken returns no token
      - Deduplicates concurrent refresh requests for the same userId
  </behavior>
  <action>
**request-auth.test.ts:**

Mock `./token-cache` with `vi.mock("./token-cache", ...)` providing `getAccessTokenForUser` as `vi.fn()`. No other mocks needed -- `request-auth.ts` only imports from `./token-cache`.

Create a helper function `makeCtx(headers: Record<string, string>)` that returns `{ req: { header: (name: string) => headers[name] } }` matching the `RequestContext` interface.

Test the full priority chain: direct access token > userId refresh > empty fallback.

**token-cache.test.ts:**

Critical mocking order (same env pitfall as google-auth):
1. Mock `../env` with `vi.mock("../env", ...)` providing `{ env: { GOOGLE_CLIENT_ID: "test-id", GOOGLE_CLIENT_SECRET: "test-secret" } }`.
2. Mock `@prisma/client` with `vi.mock("@prisma/client", ...)` providing `PrismaClient` with `userGoogleToken.findUnique`, `.update` as vi.fn() mocks.
3. Mock `google-auth-library` with `vi.mock("google-auth-library", ...)` providing `OAuth2Client` constructor mock.
4. Mock `./token-encryption` with `vi.mock("./token-encryption", ...)` providing `decryptToken` returning "decrypted-refresh".

**Use `vi.resetModules()` before each test** to clear the module-level `cache` Map and `inflightRefreshes` Map. Then dynamically import `../token-cache` in each test.

Test structure:
- `describe("getAccessTokenForUser")`:
  - Cache hit: Pre-populate by running a successful refresh, then call again and verify Prisma is NOT queried a second time
  - Cache miss / DB lookup: First call triggers Prisma findUnique + OAuth2Client refresh
  - No stored token: findUnique returns null, function returns null
  - Refresh failure: OAuth2Client.getAccessToken throws, function returns null + marks token invalid
  - No access token returned: getAccessToken resolves with `{ token: null }`, function returns null + marks invalid
  - Concurrent dedup: Start two calls for same userId simultaneously, verify findUnique called only once

For the fire-and-forget `.update().catch()` pattern: mock `.update` to return `Promise.resolve({})` (has `.catch` by default).
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && npx vitest run src/lib/__tests__/request-auth.test.ts src/lib/__tests__/token-cache.test.ts --reporter=verbose</automated>
  </verify>
  <done>All tests pass: extractGoogleAuth priority chain (direct token, userId refresh, empty fallback), token cache hit/miss/refresh/failure/dedup paths verified</done>
</task>

</tasks>

<verification>
Run the full agent test suite to confirm no regressions:

```bash
cd apps/agent && npx vitest run --reporter=verbose
```

All existing tests (token-encryption) plus all new tests (google-auth, request-auth, token-cache) must pass.
</verification>

<success_criteria>
- Three new test files exist and all tests pass
- Auth factory service account fallback verified (INTG-01)
- Auth factory user token path verified (INTG-02)
- extractGoogleAuth priority chain verified (INTG-01, INTG-02)
- Token pool iteration, fallback, and health checks verified (INTG-03)
- Token cache hit/miss/refresh/failure paths verified (INTG-03)
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/25-integration-verification-cutover/25-01-SUMMARY.md`
</output>
