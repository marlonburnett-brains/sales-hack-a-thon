---
phase: 70-high-complexity-tutorials
plan: 04
type: execute
wave: 1
depends_on: ["70-03"]
files_modified:
  - apps/tutorials/fixtures/asset-review/stages/regenerating.json
autonomous: true
gap_closure: true
requirements: [TUT-13, TUT-14, TUT-15, TUT-16, TUT-17]

must_haves:
  truths:
    - "Asset Review regenerating stage renders without TypeError (outputRefs not null)"
    - "All 5 tutorials capture unique screenshots for every step (no placeholder duplicates)"
    - "All 5 MP4 videos contain distinct frames for each tutorial step"
  artifacts:
    - path: "apps/tutorials/fixtures/asset-review/stages/regenerating.json"
      provides: "Non-null outputRefs preventing TypeError in AssetReviewPanel"
      contains: "deckUrl"
    - path: "apps/tutorials/output/touch-1-pager/"
      provides: "15 unique screenshots"
    - path: "apps/tutorials/output/touch-2-intro-deck/"
      provides: "13 unique screenshots"
    - path: "apps/tutorials/output/touch-3-capability-deck/"
      provides: "13 unique screenshots"
    - path: "apps/tutorials/output/touch-4-hitl/"
      provides: "16 unique screenshots"
    - path: "apps/tutorials/output/asset-review/"
      provides: "17 unique screenshots"
    - path: "apps/tutorials/videos/touch-1-pager.mp4"
      provides: "Full-fidelity tutorial video"
    - path: "apps/tutorials/videos/touch-2-intro-deck.mp4"
      provides: "Full-fidelity tutorial video"
    - path: "apps/tutorials/videos/touch-3-capability-deck.mp4"
      provides: "Full-fidelity tutorial video"
    - path: "apps/tutorials/videos/touch-4-hitl.mp4"
      provides: "Full-fidelity tutorial video"
    - path: "apps/tutorials/videos/asset-review.mp4"
      provides: "Full-fidelity tutorial video"
  key_links:
    - from: "apps/tutorials/fixtures/asset-review/stages/regenerating.json"
      to: "apps/web/src/components/touch/asset-review-panel.tsx"
      via: "outputRefs.deckUrl access on line 60"
      pattern: "outputRefs.*deckUrl"
---

<objective>
Fix the Asset Review regenerating.json fixture defect and re-run all 5 tutorial captures, TTS, and renders to produce full-fidelity videos without placeholder duplicate screenshots.

Purpose: Close verification gaps from 70-VERIFICATION.md -- the regenerating.json fixture has `outputRefs: null` causing a TypeError in AssetReviewPanel, and all 5 tutorials have placeholder duplicate screenshots from OOM-terminated Playwright runs on M1 Pro 16GB.
Output: Fixed regenerating.json fixture, 74 unique screenshots across 5 tutorials, 5 re-rendered MP4 videos with distinct frames per step.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-high-complexity-tutorials/70-VERIFICATION.md
@.planning/phases/70-high-complexity-tutorials/70-03-SUMMARY.md

@apps/tutorials/fixtures/asset-review/stages/regenerating.json
@apps/web/src/components/touch/asset-review-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix regenerating.json outputRefs null defect</name>
  <files>apps/tutorials/fixtures/asset-review/stages/regenerating.json</files>
  <action>
The AssetReviewPanel component (apps/web/src/components/touch/asset-review-panel.tsx line 60) reads `outputRefs.deckUrl` unconditionally. The regenerating.json stage fixture has `outputRefs: null` in both:
- `interactions[0].outputRefs` (line 11)
- `assetReview.interaction.outputRefs` (line 27)

Fix by adding valid outputRefs objects matching the reject-artifact.json pattern. In the regenerating stage, the artifact is being regenerated for Touch 1 (int-touch1-001), so use the Touch 1 pager URL:

1. In `interactions[0]`, change `"outputRefs": null` to `"outputRefs": "{\"deckUrl\":\"https://docs.google.com/presentation/d/mock-touch1-pager-meridian/edit\",\"dealFolderId\":\"mock-folder-meridian-001\"}"`
2. In `assetReview.interaction`, change `"outputRefs": null` to `"outputRefs": { "deckUrl": "https://docs.google.com/presentation/d/mock-touch1-pager-meridian/edit", "dealFolderId": "mock-folder-meridian-001" }`

Keep `hitlStage: null` and `stageContent: null` in both places -- the regenerating state is about the workflow being in progress, not the HITL stage. The outputRefs should reflect the previous version of the artifact that is being regenerated.

Do NOT change any other fields in regenerating.json -- the `status: "in_progress"`, `decision: null`, and `complianceResult: { passed: null, warnings: [] }` are correct for the regenerating state.
  </action>
  <verify>
    <automated>node -e "const f = require('./apps/tutorials/fixtures/asset-review/stages/regenerating.json'); const ir = f.interactions[0].outputRefs; const ar = f.assetReview.interaction.outputRefs; if (!ir || !ar) { console.error('outputRefs still null'); process.exit(1); } console.log('outputRefs fixed:', typeof ir === 'string' ? JSON.parse(ir).deckUrl : 'ERROR: should be string', ar.deckUrl); process.exit(0);"</automated>
  </verify>
  <done>regenerating.json has non-null outputRefs in both interactions[0] and assetReview.interaction, preventing TypeError in AssetReviewPanel</done>
</task>

<task type="auto">
  <name>Task 2: Re-run all 5 tutorial captures, TTS, and renders</name>
  <files>apps/tutorials/output/touch-1-pager/, apps/tutorials/output/touch-2-intro-deck/, apps/tutorials/output/touch-3-capability-deck/, apps/tutorials/output/touch-4-hitl/, apps/tutorials/output/asset-review/, apps/tutorials/audio/touch-1-pager/, apps/tutorials/audio/touch-2-intro-deck/, apps/tutorials/audio/touch-3-capability-deck/, apps/tutorials/audio/touch-4-hitl/, apps/tutorials/audio/asset-review/, apps/tutorials/videos/</files>
  <action>
Re-run the full capture-TTS-render pipeline for all 5 tutorials to replace placeholder duplicate screenshots with unique captures and produce full-fidelity videos.

**IMPORTANT: OOM Mitigation** -- The M1 Pro 16GB machine OOM'd Playwright at step 5-12 in prior runs. To mitigate:
- Run each capture individually with `--workers=1` (should already be default)
- Close all other applications before capturing
- If a capture still OOM's partway through, note which step it stopped at. The placeholder screenshot approach from 70-03 is acceptable as a fallback -- the goal is to maximize unique screenshots.

**Step 1: Capture all 5 tutorials (run sequentially, not parallel):**
```bash
cd apps/tutorials
pnpm capture touch-1-pager
pnpm capture touch-2-intro-deck
pnpm capture touch-3-capability-deck
pnpm capture touch-4-hitl
pnpm capture asset-review
```

After each capture, verify screenshot count matches script step count:
- touch-1-pager: 15 screenshots expected
- touch-2-intro-deck: 13 screenshots expected
- touch-3-capability-deck: 13 screenshots expected
- touch-4-hitl: 16 screenshots expected
- asset-review: 17 screenshots expected (this one specifically tests the regenerating.json fix)

**Step 2: Generate TTS audio for all 5 tutorials:**
```bash
pnpm tts touch-1-pager
pnpm tts touch-2-intro-deck
pnpm tts touch-3-capability-deck
pnpm tts touch-4-hitl
pnpm tts asset-review
```

TTS audio was already generated in 70-03. Only re-run if the script.json files changed (they should not have changed -- only regenerating.json was modified). If audio already exists with correct step counts, skip this step.

**Step 3: Render MP4 videos for all 5 tutorials:**
```bash
pnpm render touch-1-pager
pnpm render touch-2-intro-deck
pnpm render touch-3-capability-deck
pnpm render touch-4-hitl
pnpm render asset-review
```

Each video should be >1MB. The asset-review video in particular should no longer have an error frame at the regenerating step.

**If OOM persists:** Create placeholder screenshots (copy last captured screenshot for remaining steps) using the same pattern from 70-03. Document which tutorials achieved full captures vs. partial. The fixture fix is the primary deliverable of this plan; full captures are best-effort given hardware constraints.
  </action>
  <verify>
    <automated>ls -la apps/tutorials/output/touch-1-pager/step-*.png 2>/dev/null | wc -l && ls -la apps/tutorials/output/touch-2-intro-deck/step-*.png 2>/dev/null | wc -l && ls -la apps/tutorials/output/touch-3-capability-deck/step-*.png 2>/dev/null | wc -l && ls -la apps/tutorials/output/touch-4-hitl/step-*.png 2>/dev/null | wc -l && ls -la apps/tutorials/output/asset-review/step-*.png 2>/dev/null | wc -l && ls -la apps/tutorials/videos/*.mp4 2>/dev/null</automated>
  </verify>
  <done>All 5 tutorials have screenshot files matching step counts (15+13+13+16+17=74), and 5 MP4 videos exist >1MB each. Asset Review regenerating step no longer shows TypeError error page.</done>
</task>

</tasks>

<verification>
1. `regenerating.json` has non-null `outputRefs` in both `interactions[0]` and `assetReview.interaction`
2. All 5 output directories contain the expected number of screenshot PNG files
3. All 5 MP4 videos exist and are >1MB
4. Asset Review capture does not produce a blank/error page at the regenerating step
</verification>

<success_criteria>
- regenerating.json fixture defect resolved (outputRefs populated)
- All 5 tutorials have screenshots in output directories (unique captures preferred, placeholders acceptable if OOM persists)
- All 5 MP4 videos rendered successfully (>1MB each)
- Asset Review regenerating step renders a valid page (no TypeError)
</success_criteria>

<output>
After completion, create `.planning/phases/70-high-complexity-tutorials/70-04-SUMMARY.md`
</output>
