---
phase: 70-high-complexity-tutorials
plan: 02
type: execute
wave: 2
depends_on:
  - "70-01"
files_modified:
  - apps/tutorials/fixtures/touch-4-hitl/script.json
  - apps/tutorials/fixtures/touch-4-hitl/overrides.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/idle.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/generating.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/skeleton.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/lowfi.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/lowfi-refining.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/lowfi-refined.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/hifi.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/completed.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/transcript-pasted.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/artifacts-proposal.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/artifacts-talktrack.json
  - apps/tutorials/fixtures/touch-4-hitl/stages/artifacts-faq.json
  - apps/tutorials/fixtures/asset-review/script.json
  - apps/tutorials/fixtures/asset-review/overrides.json
  - apps/tutorials/fixtures/asset-review/stages/review-queue.json
  - apps/tutorials/fixtures/asset-review/stages/compliance-check.json
  - apps/tutorials/fixtures/asset-review/stages/compliance-issues.json
  - apps/tutorials/fixtures/asset-review/stages/reject-artifact.json
  - apps/tutorials/fixtures/asset-review/stages/regenerating.json
  - apps/tutorials/fixtures/asset-review/stages/re-review.json
  - apps/tutorials/fixtures/asset-review/stages/approved.json
  - apps/tutorials/capture/asset-review.spec.ts
autonomous: true
requirements:
  - TUT-16
  - TUT-17

must_haves:
  truths:
    - "Touch 4 tutorial captures 15-20 screenshots showing transcript paste, full 6-phase pipeline, per-artifact review (proposal, talk track, FAQ), and Drive links"
    - "Asset Review tutorial captures 15-20 screenshots showing review queue with artifacts from all 4 touches, compliance check with issues, reject+regenerate flow, and final approval"
    - "Touch 4 script.json completely replaces the existing 6-step pilot with an expanded ~18 step version"
    - "Asset Review stages use the stage-aware asset-review mock route (from Plan 01) to show different compliance/review states"
    - "Both tutorials use deal-001 (Meridian Dynamics) for narrative continuity"
  artifacts:
    - path: "apps/tutorials/fixtures/touch-4-hitl/script.json"
      provides: "Expanded Touch 4 tutorial script replacing 6-step pilot"
      contains: "steps"
    - path: "apps/tutorials/fixtures/touch-4-hitl/stages/transcript-pasted.json"
      provides: "Stage after transcript paste showing submitted state"
      contains: "interactions"
    - path: "apps/tutorials/fixtures/touch-4-hitl/stages/artifacts-proposal.json"
      provides: "Stage showing proposal deck artifact for review"
      contains: "interactions"
    - path: "apps/tutorials/fixtures/asset-review/script.json"
      provides: "Asset Review tutorial script with compliance and reject/regen flow"
      contains: "steps"
    - path: "apps/tutorials/fixtures/asset-review/stages/compliance-issues.json"
      provides: "Stage showing compliance warnings on an artifact"
      contains: "assetReview"
    - path: "apps/tutorials/fixtures/asset-review/stages/reject-artifact.json"
      provides: "Stage showing rejected artifact going back for regeneration"
      contains: "assetReview"
    - path: "apps/tutorials/capture/asset-review.spec.ts"
      provides: "Playwright capture spec for Asset Review"
      contains: "TUTORIAL_ID"
  key_links:
    - from: "apps/tutorials/fixtures/asset-review/stages/*.json"
      to: "apps/tutorials/scripts/mock-server.ts"
      via: "Stage fixtures with assetReview field read by stage-aware mock route"
      pattern: "assetReview"
    - from: "apps/tutorials/capture/asset-review.spec.ts"
      to: "apps/tutorials/fixtures/asset-review/script.json"
      via: "TutorialScriptSchema.parse loading step definitions"
      pattern: "TutorialScriptSchema.parse"
---

<objective>
Replace the existing Touch 4 pilot script with an expanded ~18-step tutorial (TUT-16) covering transcript paste, full 6-phase pipeline, per-artifact review, and Drive links. Author the Asset Review tutorial (TUT-17) with compliance checks, reject/regeneration, and approval workflows.

Purpose: Complete the two most complex tutorials in the series. Touch 4 demonstrates the full transcript-to-proposal pipeline with 3 output artifacts. Asset Review serves as the capstone tutorial showing all 4 touches' artifacts reviewed in one workflow with full brand compliance.

Output: 2 validated tutorial scripts, expanded Touch 4 stage fixtures, new Asset Review stage fixtures with assetReview field for stage-aware mock, 1 new capture spec (asset-review), updated touch-4-hitl capture runs against new script.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-high-complexity-tutorials/70-CONTEXT.md
@.planning/phases/70-high-complexity-tutorials/70-RESEARCH.md
@.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-01-SUMMARY.md
@.planning/phases/70-high-complexity-tutorials/70-01-SUMMARY.md

@apps/tutorials/scripts/mock-server.ts
@apps/tutorials/src/helpers/route-mocks.ts
@apps/tutorials/fixtures/loader.ts
@apps/tutorials/fixtures/types.ts
@apps/tutorials/capture/touch-4-hitl.spec.ts
@apps/tutorials/fixtures/touch-4-hitl/script.json
@apps/tutorials/fixtures/touch-4-hitl/overrides.json
@apps/tutorials/fixtures/touch-4-hitl/stages/idle.json
@apps/tutorials/fixtures/touch-4-hitl/stages/skeleton.json
@apps/tutorials/fixtures/touch-4-hitl/stages/lowfi.json
@apps/tutorials/fixtures/touch-4-hitl/stages/hifi.json
@apps/tutorials/fixtures/touch-4-hitl/stages/completed.json
@apps/tutorials/fixtures/touch-4-hitl/stages/generating.json
@apps/tutorials/fixtures/shared/deals.json
@apps/tutorials/fixtures/shared/companies.json
@apps/tutorials/src/types/tutorial-script.ts
@apps/web/src/lib/api-client.ts
@apps/web/src/app/(authenticated)/deals/[dealId]/asset-review/[interactionId]/page.tsx
@apps/web/src/components/touch/hitl-stage-stepper.tsx

<interfaces>
<!-- Key types and patterns -->

Capture spec pattern (from touch-4-hitl.spec.ts):
- TUTORIAL_ID constant, identical boilerplate for all tutorials
- touch-4-hitl.spec.ts ALREADY EXISTS -- no new spec needed for Touch 4
- Only asset-review.spec.ts is new

Asset review page route:
- URL: /deals/[dealId]/asset-review/[interactionId]
- Calls getAssetReviewAction(interactionId)
- Returns: AssetReviewData { interaction, deal, brief, complianceResult }
- complianceResult: { passed: boolean, warnings: ComplianceWarning[] }

Mock server asset-review route (after Plan 01 extension):
- GET /interactions/:id/asset-review
- Checks loadStageFixtures for "assetReview" field first
- Falls back to hardcoded response if no stage fixture

Touch 4 stage fixture pattern (existing, to be expanded):
- stages/ dir with JSON files per stage
- interaction.stageContent is a JSON STRING
- touchType: "touch_4" in fixtures

AssetReviewData interface (from api-client.ts):
```typescript
interface AssetReviewData {
  interaction: { id, dealId, touchType, status, hitlStage, stageContent, outputRefs, ... }
  deal: { id, name, companyId, ... }
  brief: { ... } | null
  complianceResult: { passed: boolean, warnings: { type, severity, message, slideNumber? }[] }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expanded Touch 4 script and stage fixtures</name>
  <files>
    apps/tutorials/fixtures/touch-4-hitl/script.json
    apps/tutorials/fixtures/touch-4-hitl/overrides.json
    apps/tutorials/fixtures/touch-4-hitl/stages/idle.json
    apps/tutorials/fixtures/touch-4-hitl/stages/generating.json
    apps/tutorials/fixtures/touch-4-hitl/stages/skeleton.json
    apps/tutorials/fixtures/touch-4-hitl/stages/lowfi.json
    apps/tutorials/fixtures/touch-4-hitl/stages/lowfi-refining.json
    apps/tutorials/fixtures/touch-4-hitl/stages/lowfi-refined.json
    apps/tutorials/fixtures/touch-4-hitl/stages/hifi.json
    apps/tutorials/fixtures/touch-4-hitl/stages/completed.json
    apps/tutorials/fixtures/touch-4-hitl/stages/transcript-pasted.json
    apps/tutorials/fixtures/touch-4-hitl/stages/artifacts-proposal.json
    apps/tutorials/fixtures/touch-4-hitl/stages/artifacts-talktrack.json
    apps/tutorials/fixtures/touch-4-hitl/stages/artifacts-faq.json
  </files>
  <action>
    **Touch 4: Transcript-to-Proposal EXPANDED (TUT-16) -- REPLACES existing 6-step pilot entirely:**
    - `script.json`: Replace the existing 6-step script with ~18 steps. Adapted to full pipeline + 3 artifacts focus per CONTEXT.md:
      (1) Navigate to /deals/deal-001/touch/4, mockStage "idle", show transcript input area
      (2) Zoom on transcript text area, callout "Paste your meeting transcript here"
      (3) Click text area, mockStage "transcript-pasted" showing pasted text in the area (stage switch simulates paste)
      (4) Click Submit, mockStage "generating", show processing indicator for field extraction
      (5) mockStage "skeleton", show extracted fields (customer context, outcomes, constraints, stakeholders, timeline, budget)
      (6) Zoom on extracted fields content
      (7) Click Approve, mockStage "lowfi", show draft artifacts overview (proposal outline, talk track draft, FAQ draft)
      (8) Zoom on proposal deck draft section
      (9) Click "Request Changes" for refine demo at lowfi, mockStage "lowfi-refining"
      (10) mockStage "lowfi-refined" showing improved drafts
      (11) Click Approve, mockStage "hifi", show final polished versions of all 3 artifacts
      (12) mockStage "artifacts-proposal", zoom on proposal deck content in detail
      (13) mockStage "artifacts-talktrack", zoom on talk track content in detail
      (14) mockStage "artifacts-faq", zoom on FAQ document content in detail
      (15) Click final Approve, mockStage "completed", show "Saved to Drive" with links for all 3 artifacts
      (16) Zoom on Drive links showing proposal deck URL, talk track URL, FAQ URL
    - Narration: conversational, emphasize this is the most comprehensive touch with 3 output artifacts from a single transcript
    - Refine demo at lowfi gate (most meaningful since it affects all 3 artifacts at once)
    - Visual effects: zoom ~30-40%, callouts ~20-30%, cursor on all click steps

    - Update existing stage fixtures (idle.json, generating.json, skeleton.json, lowfi.json, hifi.json, completed.json) to have richer stageContent reflecting the full pipeline:
      - `skeleton.json`: stageContent with extractedFields object (customerContext, businessOutcomes, constraints, stakeholders, timeline, budget) -- all realistic for Meridian Dynamics
      - `lowfi.json`: stageContent with draft summaries of all 3 artifacts (proposalOutline, talkTrackDraft, faqDraft)
      - `hifi.json`: stageContent with polished versions of all 3 artifacts
      - `completed.json`: outputRefs with Google Docs/Slides URLs for proposal, talk track, and FAQ

    - Create NEW stage fixtures:
      - `transcript-pasted.json`: Shows the text area with pasted transcript content (stageContent with transcriptText field)
      - `lowfi-refining.json`: status "in_progress" during regeneration
      - `lowfi-refined.json`: Updated stageContent with improved drafts
      - `artifacts-proposal.json`: Same as hifi but used to trigger a proposal-focused view (stageContent highlights proposal section)
      - `artifacts-talktrack.json`: Same structure, highlights talk track section
      - `artifacts-faq.json`: Same structure, highlights FAQ section

    **CRITICAL constraints per CONTEXT.md locked decisions:**
    - Touch 4 REPLACES the 6-step pilot entirely -- do NOT try to extend it, write fresh
    - Dedicated steps per artifact (proposal, talk track, FAQ each get own step)
    - Show paste action -- click text area, stage switch simulates pasted text, then submit
    - Final step shows Drive links for ALL 3 artifacts
    - stageContent MUST be a JSON string (stringified JSON)
    - All tutorials use deal-001 (Meridian Dynamics)
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && node -e "
      const fs = require('fs');
      let ok = true;
      const t4 = JSON.parse(fs.readFileSync('fixtures/touch-4-hitl/script.json', 'utf-8'));
      console.log('touch-4-hitl: ' + t4.steps.length + ' steps (was 6, should be 15+)');
      if (t4.steps.length < 12) { console.error('FAIL: Touch 4 not expanded enough'); ok = false; }
      const newStages = ['transcript-pasted','lowfi-refining','lowfi-refined','artifacts-proposal','artifacts-talktrack','artifacts-faq'];
      for (const st of newStages) {
        const p = 'fixtures/touch-4-hitl/stages/' + st + '.json';
        if (!fs.existsSync(p)) { console.error('MISSING: ' + p); ok = false; }
      }
      if (ok) console.log('touch-4-hitl new stages: OK');
      if (!ok) process.exit(1);
      console.log('Task 1 files present');
    "</automated>
  </verify>
  <done>
    - Touch 4 script.json replaced entirely with ~16-18 steps (up from 6-step pilot)
    - Touch 4 has 6 new stage fixtures (transcript-pasted, lowfi-refining, lowfi-refined, artifacts-proposal, artifacts-talktrack, artifacts-faq)
    - Touch 4 existing stage fixtures updated with richer stageContent for 3-artifact pipeline
    - Tutorial uses deal-001 (Meridian Dynamics)
  </done>
</task>

<task type="auto">
  <name>Task 2: Asset Review script, fixtures, and overrides</name>
  <files>
    apps/tutorials/fixtures/asset-review/script.json
    apps/tutorials/fixtures/asset-review/overrides.json
    apps/tutorials/fixtures/asset-review/stages/review-queue.json
    apps/tutorials/fixtures/asset-review/stages/compliance-check.json
    apps/tutorials/fixtures/asset-review/stages/compliance-issues.json
    apps/tutorials/fixtures/asset-review/stages/reject-artifact.json
    apps/tutorials/fixtures/asset-review/stages/regenerating.json
    apps/tutorials/fixtures/asset-review/stages/re-review.json
    apps/tutorials/fixtures/asset-review/stages/approved.json
  </files>
  <action>
    **Asset Review & Approval (TUT-17) fixtures:**
    - `script.json`: ~17 step script covering all-touches review + compliance + reject/regen:
      (1) Navigate to /deals/deal-001/touch/4, mockStage "review-queue" (start from Touch 4's completed state showing review link)
      (2) Navigate to /deals/deal-001/asset-review/int-touch4-001, mockStage "review-queue", show review page with artifact details
      (3) Zoom on artifact overview showing Touch 4 proposal details
      (4) mockStage "compliance-check", show compliance check running
      (5) mockStage "compliance-issues", show compliance results with 2-3 warnings (brand color deviation severity:medium, missing disclaimer severity:high, font inconsistency severity:low)
      (6) Zoom on compliance warnings list, callout "Review flagged issues before approving"
      (7) Zoom on the high-severity issue detail
      (8) Click "Fix Issue" on the missing disclaimer warning, mockStage "regenerating" showing fix in progress
      (9) mockStage "re-review", show updated artifact with disclaimer added, compliance re-check passed
      (10) Zoom on fixed content, callout "Issue resolved -- disclaimer added"
      (11) Click Approve for Touch 4 artifact
      (12) Navigate to /deals/deal-001/asset-review/int-touch1-001, mockStage "compliance-check", show Touch 1 pager review
      (13) Zoom on pager compliance results (all passed)
      (14) mockStage "reject-artifact", click Reject showing rejection dialog/reason
      (15) mockStage "regenerating", show artifact going back for regeneration
      (16) mockStage "re-review", show regenerated pager ready for re-review
      (17) Click Approve on regenerated pager, mockStage "approved", show final approved state
    - Narration: capstone tutorial framing -- "You've seen how each touch generates its content. Now let's review everything in one place."
    - Full compliance walkthrough per CONTEXT.md: flagged issues with severity levels, fix one, re-check
    - Reject + regeneration flow per CONTEXT.md: reject one artifact, regeneration, re-review

    - `overrides.json`: Interactions from ALL 4 touches for deal-001:
      - int-touch1-001: touchType "touch_1", status "completed"
      - int-touch2-001: touchType "touch_2", status "completed"
      - int-touch3-001: touchType "touch_3", status "completed"
      - int-touch4-001: touchType "touch_4", status "completed"
      Each with outputRefs containing Google Docs/Slides URLs

    - Stage fixtures (7 files) -- each uses `assetReview` field (for the stage-aware mock route from Plan 01):
      - `review-queue.json`: assetReview with interaction data showing completed status, outputRefs, complianceResult { passed: true, warnings: [] }
      - `compliance-check.json`: assetReview with complianceResult { passed: null } (check in progress)
      - `compliance-issues.json`: assetReview with complianceResult { passed: false, warnings: [{type:"brand_color",severity:"medium",message:"..."},{type:"missing_disclaimer",severity:"high",message:"..."},{type:"font_inconsistency",severity:"low",message:"..."}] }
      - `reject-artifact.json`: assetReview with interaction status "rejected", rejectionReason field
      - `regenerating.json`: assetReview with interaction status "in_progress" (regenerating after rejection)
      - `re-review.json`: assetReview with updated interaction (regenerated), complianceResult { passed: true, warnings: [] }
      - `approved.json`: assetReview with interaction status "approved", complianceResult { passed: true }

    **CRITICAL constraints per CONTEXT.md locked decisions:**
    - TUT-17: artifacts from ALL 4 touches in review queue
    - TUT-17: full compliance walkthrough with flagged issues + severity levels + fix + re-check
    - TUT-17: reject + regeneration flow -- reject one, regenerate, re-review
    - stageContent MUST be a JSON string (stringified JSON)
    - Asset Review stages use "assetReview" field (not "interactions") for the stage-aware mock route
    - All tutorials use deal-001 (Meridian Dynamics)
    - Asset review page URL: /deals/deal-001/asset-review/[interactionId] -- use stable mock IDs matching overrides
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && node -e "
      const fs = require('fs');
      let ok = true;
      const ar = JSON.parse(fs.readFileSync('fixtures/asset-review/script.json', 'utf-8'));
      console.log('asset-review: ' + ar.steps.length + ' steps');
      const arStages = ['review-queue','compliance-check','compliance-issues','reject-artifact','regenerating','re-review','approved'];
      for (const st of arStages) {
        const p = 'fixtures/asset-review/stages/' + st + '.json';
        if (!fs.existsSync(p)) { console.error('MISSING: ' + p); ok = false; }
      }
      const ci = JSON.parse(fs.readFileSync('fixtures/asset-review/stages/compliance-issues.json', 'utf-8'));
      if (!ci.assetReview) { console.error('FAIL: compliance-issues.json missing assetReview field'); ok = false; }
      else { console.log('asset-review stages with assetReview field: OK'); }
      if (!ok) process.exit(1);
      console.log('Task 2 files present');
    "</automated>
  </verify>
  <done>
    - Asset Review script.json with ~17 steps covering all-touch review, compliance, reject/regen
    - Asset Review has 7 stage fixtures with assetReview field for stage-aware mock route
    - Asset Review overrides.json has interactions from all 4 touches
    - Tutorial uses deal-001 (Meridian Dynamics)
  </done>
</task>

<task type="auto">
  <name>Task 3: Asset Review capture spec + end-to-end verification for Touch 4 and Asset Review</name>
  <files>
    apps/tutorials/capture/asset-review.spec.ts
  </files>
  <action>
    Create the asset-review capture spec following the EXACT pattern from `capture/touch-4-hitl.spec.ts`. Only TUTORIAL_ID ("asset-review") and the test.describe string change.

    The existing touch-4-hitl.spec.ts does NOT need changes -- it already uses the generic capture loop which will work with the new expanded script.json automatically.

    After creating the spec, run both captures to verify:
    ```
    pnpm --filter tutorials capture touch-4-hitl
    pnpm --filter tutorials capture asset-review
    ```

    For touch-4-hitl: Verify it now produces 15-20 screenshots (not 6 like the old pilot). The expanded script.json is loaded automatically by the existing spec.

    For asset-review: This is a new tutorial. Verify it produces 15-20 screenshots.

    **Important for Asset Review:** The script navigates to asset-review URLs like `/deals/deal-001/asset-review/int-touch4-001`. The mock server's stage-aware asset-review route (from Plan 01) must be running for the stage fixtures to take effect. If the asset-review page shows default/fallback data instead of compliance issues, verify:
    1. Plan 01's mock server change is present (assetReview field check in GET /interactions/:id/asset-review)
    2. Stage fixture files have the `assetReview` field at the top level
    3. The mockStage name in script.json matches the stage fixture filename

    If captures fail, diagnose and fix fixture data. Common issues:
    - Asset review page 404: interactionId in URL doesn't match any fixture. Use int-touch4-001 matching overrides.json
    - Compliance issues not showing: assetReview field missing from stage fixture or mock server not reading it
    - Touch 4 stageContent parse error: Ensure all stageContent values are JSON strings, not objects
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && ls capture/asset-review.spec.ts && echo "Asset review capture spec exists" && for t in touch-4-hitl asset-review; do count=$(ls output/$t/*.png 2>/dev/null | wc -l); echo "$t: $count screenshots"; done</automated>
  </verify>
  <done>
    - asset-review.spec.ts exists following the generic loop pattern
    - `pnpm --filter tutorials capture touch-4-hitl` produces 15-20 PNGs (expanded from 6)
    - `pnpm --filter tutorials capture asset-review` produces 15-20 PNGs
    - All screenshots are visually deterministic
    - Asset Review screenshots show compliance warnings and reject/regen flow (not just default passed state)
  </done>
</task>

</tasks>

<verification>
Touch 4 and Asset Review tutorials capture successfully:
- touch-4-hitl: 15-20 screenshots showing full pipeline with 3 artifacts
- asset-review: 15-20 screenshots showing compliance + reject/regen flow
Touch 4 script is a complete replacement (not extension) of the 6-step pilot.
Asset Review stage fixtures use assetReview field for stage-aware mock route.
</verification>

<success_criteria>
- Touch 4 script.json fully replaced with 15-20 step expanded version
- Touch 4 shows transcript paste, per-artifact review, Drive links for all 3 artifacts
- Asset Review script covers all 4 touches' artifacts, compliance walkthrough, reject/regen
- 13 new/updated stage fixtures for Touch 4, 7 stage fixtures for Asset Review
- Asset Review stages use assetReview field consumed by stage-aware mock route
- Both capture specs produce correct screenshot counts
</success_criteria>

<output>
After completion, create `.planning/phases/70-high-complexity-tutorials/70-02-SUMMARY.md`
</output>
