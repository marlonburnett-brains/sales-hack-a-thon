---
phase: 70-high-complexity-tutorials
plan: 03
type: execute
wave: 3
depends_on:
  - 70-01
  - 70-02
files_modified: []
autonomous: true
requirements:
  - TUT-13
  - TUT-14
  - TUT-15
  - TUT-16
  - TUT-17

must_haves:
  truths:
    - "All 5 tutorials have TTS audio files generated for every step"
    - "All 5 tutorials have timing manifests with non-zero durations for every step"
    - "All 5 tutorials render as MP4 videos with synchronized audio and screenshots"
    - "No zero-duration entries in any timing manifest"
  artifacts:
    - path: "apps/tutorials/audio/touch-1-pager/timing.json"
      provides: "Timing manifest for Touch 1 tutorial"
      contains: "durations"
    - path: "apps/tutorials/audio/touch-2-intro-deck/timing.json"
      provides: "Timing manifest for Touch 2 tutorial"
      contains: "durations"
    - path: "apps/tutorials/audio/touch-3-capability-deck/timing.json"
      provides: "Timing manifest for Touch 3 tutorial"
      contains: "durations"
    - path: "apps/tutorials/audio/touch-4-hitl/timing.json"
      provides: "Timing manifest for Touch 4 tutorial"
      contains: "durations"
    - path: "apps/tutorials/audio/asset-review/timing.json"
      provides: "Timing manifest for Asset Review tutorial"
      contains: "durations"
    - path: "apps/tutorials/videos/touch-1-pager.mp4"
      provides: "Final Touch 1 tutorial video"
    - path: "apps/tutorials/videos/touch-2-intro-deck.mp4"
      provides: "Final Touch 2 tutorial video"
    - path: "apps/tutorials/videos/touch-3-capability-deck.mp4"
      provides: "Final Touch 3 tutorial video"
    - path: "apps/tutorials/videos/touch-4-hitl.mp4"
      provides: "Final Touch 4 tutorial video"
    - path: "apps/tutorials/videos/asset-review.mp4"
      provides: "Final Asset Review tutorial video"
  key_links:
    - from: "apps/tutorials/scripts/tts.ts"
      to: "apps/tutorials/fixtures/*/script.json"
      via: "TTS reads narration text from each step in script.json"
      pattern: "narration"
    - from: "apps/tutorials/scripts/render.ts"
      to: "apps/tutorials/audio/*/timing.json"
      via: "Remotion uses timing manifest for frame count calculation"
      pattern: "timing.json"
---

<objective>
Generate TTS narration audio and render MP4 videos for all 5 high-complexity tutorials: Touch 1-4 and Asset Review.

Purpose: Produce the final deliverable MP4 tutorial videos by running the established TTS and Remotion render pipelines against the scripts and screenshots created in Plans 01 and 02.

Output: 5 timing manifests, 5 MP4 tutorial videos (all gitignored artifacts -- no source commits needed).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-high-complexity-tutorials/70-CONTEXT.md
@.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-02-SUMMARY.md

@apps/tutorials/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate TTS audio for all 5 tutorials</name>
  <files></files>
  <action>
    Run the Kokoro TTS pipeline for all 5 tutorials sequentially. Use the established `pnpm --filter tutorials tts` command for each:

    ```
    pnpm --filter tutorials tts touch-1-pager
    pnpm --filter tutorials tts touch-2-intro-deck
    pnpm --filter tutorials tts touch-3-capability-deck
    pnpm --filter tutorials tts touch-4-hitl
    pnpm --filter tutorials tts asset-review
    ```

    Each command reads the tutorial's script.json, generates WAV audio for each step's narration text using Kokoro (af_heart voice), runs ffmpeg loudnorm post-processing, and writes a timing.json manifest with per-step durations.

    After each TTS run, verify:
    1. Audio directory exists with WAV files for every step (count matches script step count)
    2. timing.json exists with non-zero durations
    3. No error output from the TTS pipeline

    If any TTS run fails:
    - Check that narration text in script.json is not empty for any step
    - Check that the Kokoro model files are available (they should be from Phase 64)
    - Retry once -- Kokoro occasionally has transient issues on first load

    All outputs are gitignored (audio/ directory). No source commits for this task.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && for t in touch-1-pager touch-2-intro-deck touch-3-capability-deck touch-4-hitl asset-review; do if [ -f "audio/$t/timing.json" ]; then count=$(ls audio/$t/*.wav 2>/dev/null | wc -l); echo "$t: $count WAV files, timing.json present"; else echo "MISSING: audio/$t/timing.json"; fi; done</automated>
  </verify>
  <done>
    - 5 audio directories with WAV files for every step
    - 5 timing.json manifests with non-zero durations for every step
    - No failed or missing audio files
  </done>
</task>

<task type="auto">
  <name>Task 2: Render MP4 videos for all 5 tutorials</name>
  <files></files>
  <action>
    Run the Remotion render pipeline for all 5 tutorials sequentially. Use the established `pnpm --filter tutorials render` command:

    ```
    pnpm --filter tutorials render touch-1-pager
    pnpm --filter tutorials render touch-2-intro-deck
    pnpm --filter tutorials render touch-3-capability-deck
    pnpm --filter tutorials render touch-4-hitl
    pnpm --filter tutorials render asset-review
    ```

    Each command reads the tutorial's timing.json, screenshots, and audio files, then renders an MP4 via Remotion with --concurrency=2 for M1 Pro memory safety. The composition includes intro/outro slates, cross-fade transitions, zoom/pan effects, callout overlays, and cursor animations -- all driven by the script.json fields.

    After each render, verify:
    1. MP4 file exists in videos/ directory
    2. File size is reasonable (>1MB, indicating actual video content)

    If any render fails:
    - Check that all screenshots exist in output/{tutorial}/ directory
    - Check that timing.json has entries matching step IDs
    - Check Remotion error output for specific frame/composition issues
    - Common issue: missing screenshot for a step -- re-run capture first

    All outputs are gitignored (videos/ directory). No source commits for this task.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && for t in touch-1-pager touch-2-intro-deck touch-3-capability-deck touch-4-hitl asset-review; do if [ -f "videos/$t.mp4" ]; then size=$(stat -f%z "videos/$t.mp4" 2>/dev/null || stat -c%s "videos/$t.mp4" 2>/dev/null); echo "$t.mp4: ${size} bytes"; else echo "MISSING: videos/$t.mp4"; fi; done</automated>
  </verify>
  <done>
    - 5 MP4 files in videos/ directory (touch-1-pager.mp4, touch-2-intro-deck.mp4, touch-3-capability-deck.mp4, touch-4-hitl.mp4, asset-review.mp4)
    - Each file >1MB indicating real video content
    - All 5 tutorials complete end-to-end: script -> capture -> TTS -> render
  </done>
</task>

</tasks>

<verification>
All 5 high-complexity tutorials have complete video outputs:
- touch-1-pager.mp4, touch-2-intro-deck.mp4, touch-3-capability-deck.mp4, touch-4-hitl.mp4, asset-review.mp4
- Each with corresponding timing manifests and audio files
- No source file changes (all outputs gitignored)
</verification>

<success_criteria>
- 5 timing manifests with non-zero durations for every step
- 5 MP4 videos rendered successfully
- All videos >1MB file size
- Zero failed TTS or render steps
</success_criteria>

<output>
After completion, create `.planning/phases/70-high-complexity-tutorials/70-03-SUMMARY.md`
</output>
