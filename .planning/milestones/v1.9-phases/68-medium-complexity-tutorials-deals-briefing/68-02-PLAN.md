---
phase: 68-medium-complexity-tutorials-deals-briefing
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified: []
autonomous: true
requirements:
  - TUT-04
  - TUT-05
  - TUT-06
  - TUT-07

must_haves:
  truths:
    - "TTS audio files exist for all steps of all 4 tutorials"
    - "Timing manifests exist for all 4 tutorials"
    - "MP4 videos exist for all 4 tutorials"
    - "Audio and video durations are reasonable (not empty or corrupted)"
  artifacts:
    - path: "apps/tutorials/audio/deals/timing.json"
      provides: "Duration manifest for Deals tutorial render"
      contains: "durations"
    - path: "apps/tutorials/audio/deal-chat/timing.json"
      provides: "Duration manifest for Deal Chat tutorial render"
      contains: "durations"
    - path: "apps/tutorials/audio/briefing/timing.json"
      provides: "Duration manifest for Briefing tutorial render"
      contains: "durations"
    - path: "apps/tutorials/videos/deals.mp4"
      provides: "Final Deals tutorial video"
    - path: "apps/tutorials/videos/deal-overview.mp4"
      provides: "Final Deal Overview tutorial video"
    - path: "apps/tutorials/videos/deal-chat.mp4"
      provides: "Final Deal Chat tutorial video"
    - path: "apps/tutorials/videos/briefing.mp4"
      provides: "Final Briefing tutorial video"
  key_links:
    - from: "apps/tutorials/audio/*/timing.json"
      to: "apps/tutorials/videos/*.mp4"
      via: "Remotion render reads timing manifest for frame counts"
      pattern: "timing\\.json"
---

<objective>
Generate TTS narration audio and render MP4 videos for all four Phase 68 tutorials using the existing Kokoro TTS and Remotion render pipelines.

Purpose: Transform the captured screenshots and authored narration text into finished tutorial videos. This is pipeline execution only -- no new code.

Output: 4 sets of WAV audio files + timing.json manifests, 4 MP4 tutorial videos (gitignored artifacts).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-01-SUMMARY.md
@.planning/phases/67-low-complexity-tutorials/67-03-SUMMARY.md

@apps/tutorials/scripts/tts.ts
@apps/tutorials/scripts/render.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate TTS audio for all 4 tutorials</name>
  <files></files>
  <action>
    Run the Kokoro TTS pipeline for each tutorial. The pipeline reads script.json narration text, generates per-step WAV files, and produces a timing.json manifest with durations.

    ```bash
    cd apps/tutorials
    pnpm tts deals
    pnpm tts deal-overview
    pnpm tts deal-chat
    pnpm tts briefing
    ```

    After each run, verify:
    1. WAV files exist in `audio/{tutorial-name}/` for each step
    2. `timing.json` exists with duration entries matching step count
    3. No zero-duration entries (would indicate TTS failure)

    If any step produces a zero-duration WAV, check the narration text in the script.json for that step -- empty or very short narration can cause issues.

    All outputs are gitignored (audio/ directory is in .gitignore).
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && for t in deals deal-overview deal-chat briefing; do if [ -f "audio/$t/timing.json" ]; then count=$(ls audio/$t/step-*.wav 2>/dev/null | wc -l); echo "$t: $count WAVs + timing.json"; else echo "MISSING: audio/$t/timing.json"; fi; done</automated>
  </verify>
  <done>
    - WAV files exist for every step of all 4 tutorials
    - timing.json manifests exist for all 4 tutorials
    - No zero-duration entries in any timing manifest
  </done>
</task>

<task type="auto">
  <name>Task 2: Render MP4 videos for all 4 tutorials</name>
  <files></files>
  <action>
    Run the Remotion render pipeline for each tutorial. The pipeline reads screenshots from output/{tutorial-name}/, audio from audio/{tutorial-name}/, timing.json for frame counts, and script.json for visual effect metadata. Produces a final MP4 in videos/.

    ```bash
    cd apps/tutorials
    pnpm render deals
    pnpm render deal-overview
    pnpm render deal-chat
    pnpm render briefing
    ```

    Use `--concurrency=2` for M1 Pro memory safety (this is the default in the render script).

    After each render, verify:
    1. MP4 file exists in `videos/{tutorial-name}.mp4`
    2. File size is reasonable (expect 3-15MB per tutorial depending on step count)
    3. No render errors in output

    All outputs are gitignored (videos/ directory is in .gitignore).
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/tutorials && for t in deals deal-overview deal-chat briefing; do if [ -f "videos/$t.mp4" ]; then size=$(ls -la "videos/$t.mp4" | awk '{print $5}'); echo "$t.mp4: ${size} bytes"; else echo "MISSING: videos/$t.mp4"; fi; done</automated>
  </verify>
  <done>
    - 4 MP4 files exist: deals.mp4, deal-overview.mp4, deal-chat.mp4, briefing.mp4
    - Each file is non-empty and reasonable size (3-15MB)
    - All Phase 68 tutorials are complete: captured, narrated, and rendered
  </done>
</task>

</tasks>

<verification>
All 4 tutorials have complete audio and video:
- audio/deals/ has WAVs + timing.json
- audio/deal-overview/ has WAVs + timing.json
- audio/deal-chat/ has WAVs + timing.json
- audio/briefing/ has WAVs + timing.json
- videos/deals.mp4 exists
- videos/deal-overview.mp4 exists
- videos/deal-chat.mp4 exists
- videos/briefing.mp4 exists
</verification>

<success_criteria>
- 4 timing.json manifests with correct step counts
- 4 MP4 videos with reasonable file sizes
- All 4 tutorials complete end-to-end: script -> capture -> TTS -> render
</success_criteria>

<output>
After completion, create `.planning/phases/68-medium-complexity-tutorials-deals-briefing/68-02-SUMMARY.md`
</output>
