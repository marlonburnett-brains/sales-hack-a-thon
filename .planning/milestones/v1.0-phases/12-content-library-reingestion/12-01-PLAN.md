---
phase: 12-content-library-reingestion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/ingestion/manifest/content-manifest.json
  - apps/agent/src/ingestion/manifest/coverage-report.json
  - apps/agent/src/ingestion/manifest/solution-pillars.json
  - apps/agent/src/ingestion/manifest/image-registry-report.json
autonomous: false
requirements: [CONT-01, CONT-02, CONT-03, CONT-04]
user_setup:
  - service: google-cloud-platform
    why: "Google Docs API needed for brand guidelines extraction"
    env_vars: []
    dashboard_config:
      - task: "Enable Google Docs API"
        location: "https://console.developers.google.com/apis/api/docs.googleapis.com/overview?project=749490525472"
  - service: google-drive
    why: "Service account needs viewer access to shortcut target files"
    dashboard_config:
      - task: "Share target Shared Drives/folders with service account email"
        location: "Google Drive sharing dialog for each target folder"
must_haves:
  truths:
    - "Google Docs API is enabled on GCP project 749490525472"
    - "Drive service account can access shortcut target files (Meet Lumenalta, L2 capability decks, case study decks)"
    - "Discovery scan finds more than 5 presentations (up from previous 5)"
    - "Stale manifest is reset and fresh discovery reflects expanded access"
  artifacts:
    - path: "apps/agent/src/ingestion/manifest/content-manifest.json"
      provides: "Fresh manifest with all discovered slides classified"
      contains: "pending"
    - path: "apps/agent/src/ingestion/manifest/coverage-report.json"
      provides: "Coverage report from expanded discovery"
      contains: "totalPresentations"
  key_links:
    - from: "Google Drive service account"
      to: "Shortcut target files"
      via: "Viewer access grants"
      pattern: "presentations discovered > 5"
    - from: "GCP project 749490525472"
      to: "Google Docs API"
      via: "API enablement"
      pattern: "docs.googleapis.com enabled"
---

<objective>
Grant the Google Drive service account access to shortcut target files, enable Google Docs API on the GCP project, reset stale manifest data, and validate that the expanded discovery set includes the expected presentations before running the full ingestion pipeline.

Purpose: All downstream ingestion (Plan 02) depends on the service account being able to read the actual content files behind Drive shortcuts. This plan ensures access is fixed and validated before committing to a full pipeline run.
Output: Validated discovery manifest showing 13+ presentations (up from 5), fresh coverage report, Google Docs API enabled.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-content-library-reingestion/12-CONTEXT.md
@.planning/phases/12-content-library-reingestion/12-RESEARCH.md
@.planning/phases/02-content-library-ingestion/02-01-SUMMARY.md
@.planning/phases/02-content-library-ingestion/02-02-SUMMARY.md
@.planning/phases/02-content-library-ingestion/02-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Enable Google Docs API and grant Drive service account access</name>
  <action>
    Two human-only actions that Claude cannot perform via CLI/API:

    **Step A: Enable Google Docs API**
    1. Visit: https://console.developers.google.com/apis/api/docs.googleapis.com/overview?project=749490525472
    2. Click "Enable"
    3. Wait 2-3 minutes for propagation

    **Step B: Grant Drive service account viewer access**
    The service account email (from GOOGLE_SERVICE_ACCOUNT_KEY JSON) needs viewer access to each Shared Drive or folder containing the shortcut target files. Known targets from Phase 2:
    - Meet Lumenalta
    - NBCUniversal
    - Bleecker Street Group
    - Master Solutions
    - 2026 GTM Solutions
    - Alaska Airlines
    - MasterControl
    - Encompass
    - WSA
    - Satellite Industries
    - Gravie
    - L2 capability decks
    - 1-2 pager templates
    - Case study decks

    For each: Open the containing Shared Drive or folder in Google Drive, click "Share", add the service account email as "Viewer".

    NOTE: Google Drive shortcuts are references, not copies. The service account needs access to the TARGET file/folder, not just the folder containing the shortcut. If targets live in different Shared Drives, each Shared Drive must be shared separately.

    If some targets remain inaccessible after grants, that is acceptable -- log them as warnings and proceed (per user decision).
  </action>
  <resume-signal>Type "done" when both steps are complete (API enabled + access granted), or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reset stale manifest and run discovery validation</name>
  <files>
    apps/agent/src/ingestion/manifest/content-manifest.json
    apps/agent/src/ingestion/manifest/coverage-report.json
    apps/agent/src/ingestion/manifest/solution-pillars.json
    apps/agent/src/ingestion/manifest/image-registry-report.json
  </files>
  <action>
    After the human confirms access grants are complete, perform the following from the `apps/agent` directory:

    1. **Reset stale manifest artifacts.** Delete existing manifest files to force fresh discovery:
       ```bash
       rm -f src/ingestion/manifest/content-manifest.json
       rm -f src/ingestion/manifest/coverage-report.json
       rm -f src/ingestion/manifest/solution-pillars.json
       rm -f src/ingestion/manifest/image-registry-report.json
       ```
       Rationale: The existing 38-entry manifest is dominated by spike tests and a partial 200A Master Deck extraction from limited-access Phase 2 run. Starting fresh ensures clean coverage reporting. The pipeline's idempotency check in `ingestDocument()` prevents duplicate Google Docs in Drive even without the manifest.

    2. **Run manifest-only discovery** to validate expanded access without committing to full ingestion:
       ```bash
       cd apps/agent && npx tsx --env-file=.env src/ingestion/run-ingestion.ts --manifest-only
       ```
       This runs Phases A-C only (discover, extract, classify) and produces:
       - `content-manifest.json` with all discovered and classified slides
       - `coverage-report.json` with industry breakdown and gap detection

    3. **Verify the discovery results.** Check the output for:
       - Total presentations discovered should be > 5 (ideally 13+)
       - Look for the expected shortcut targets in the discovery log (Meet Lumenalta, Master Solutions, etc.)
       - Check `coverage-report.json` for case study presence (was 0 across all industries previously)
       - Note any "WARNING: Could not scan folder" messages -- these indicate still-inaccessible shortcuts

    4. **Check solution pillars.** If Master Solutions deck was discovered, verify that `solution-pillars.json` is now populated (was empty in Phase 2 due to access issues). If still empty, that is acceptable -- classification still works with Gemini's general knowledge.

    5. **Log the results.** Print a summary of:
       - Total presentations discovered
       - Total slides classified
       - Case study count
       - Any access warnings
       - Whether discovery meets expectations (> 5 presentations)
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && test -f src/ingestion/manifest/content-manifest.json && test -f src/ingestion/manifest/coverage-report.json && node -e "const m = require('./src/ingestion/manifest/content-manifest.json'); console.log('Slides:', m.length); if (m.length <= 38) { console.error('Discovery did not expand beyond previous 38 slides'); process.exit(1); }"</automated>
  </verify>
  <done>Discovery validation produces a content-manifest.json with more than 38 classified slides (expanded from previous limited-access run), and coverage-report.json shows updated industry breakdown. Any inaccessible shortcuts are logged as warnings.</done>
</task>

</tasks>

<verification>
1. Google Docs API is enabled (brand guidelines script will not 403 in Plan 02)
2. Discovery scan finds more than 5 presentations (expanded from Phase 2's limited run)
3. content-manifest.json exists with > 38 classified slide entries
4. coverage-report.json shows updated industry breakdown
5. Any remaining access issues are documented as warnings (not silent failures)
</verification>

<success_criteria>
- Google Docs API enabled on GCP project 749490525472
- Drive service account has viewer access to shortcut target files
- Stale manifest reset and fresh discovery-only run completed
- content-manifest.json has > 38 slides (more than Phase 2's limited run)
- coverage-report.json reflects expanded discovery set
- Plan 02 is unblocked for full ingestion
</success_criteria>

<output>
After completion, create `.planning/phases/12-content-library-reingestion/12-01-SUMMARY.md`
</output>
