---
phase: 52-multi-source-slide-assembler
plan: "03"
type: execute
wave: 3
depends_on:
  - 52-02
files_modified:
  - apps/agent/src/generation/multi-source-assembler.ts
  - apps/agent/src/generation/__tests__/multi-source-assembler.test.ts
  - .planning/phases/52-multi-source-slide-assembler/52-VERIFICATION.md
autonomous: false
requirements:
  - FR-4.4
gap_closure: true
must_haves:
  truths:
    - Secondary-source slides in assembled decks preserve design-bearing content beyond text boxes, including images, tables, and non-text shapes, in the intended order.
    - Automated regression coverage proves the assembler no longer uses a text-only secondary-slide rebuild path.
    - Live Google Slides verification confirms the rebuilt secondary slides visually match their source slides closely enough to approve, or records the exact unsupported element that still blocks approval.
  artifacts:
    - path: apps/agent/src/generation/multi-source-assembler.ts
      provides: Fidelity-preserving secondary-slide reconstruction for supported page element types plus explicit warnings for unsupported ones.
      exports: [assembleMultiSourceDeck, groupSlidesBySource, buildMultiSourcePlan]
    - path: apps/agent/src/generation/__tests__/multi-source-assembler.test.ts
      provides: Regression coverage for images, tables, non-text shapes, grouped children, and unsupported-element warnings in the secondary-slide path.
    - path: .planning/phases/52-multi-source-slide-assembler/52-VERIFICATION.md
      provides: Final pass/fail record for the secondary-slide fidelity gap after automated and live verification.
  key_links:
    - from: apps/agent/src/generation/multi-source-assembler.ts
      to: apps/agent/src/generation/__tests__/multi-source-assembler.test.ts
      via: Regression tests must assert createImage/createTable/createShape-or-equivalent requests replace the old text-only reconstruction path.
      pattern: createImage|createTable|createShape|unsupported element
    - from: apps/agent/src/generation/multi-source-assembler.ts
      to: .planning/phases/52-multi-source-slide-assembler/52-VERIFICATION.md
      via: The verification report must reflect whether the new secondary-slide reconstruction closed FR-4.4 and the design-preservation truth.
      pattern: 11/11 must-haves verified|gaps_found|unsupported element
---

<objective>
Close the Phase 52 verification gap by replacing text-only secondary-slide reconstruction with a fidelity-preserving secondary rebuild path, then re-verifying the result against a real Google Slides assembly.

Purpose: FR-4.4 and the phase goal remain open because the current multi-source path recreates only text boxes for secondary slides. This plan restores design preservation for secondary-source slides instead of silently dropping images, tables, and non-text shapes.

Output: Updated multi-source assembler logic, regression coverage for design-bearing elements, and a refreshed `52-VERIFICATION.md` that either closes the gap or records the exact remaining blocker.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-multi-source-slide-assembler/52-RESEARCH.md
@.planning/phases/52-multi-source-slide-assembler/52-VERIFICATION.md
@.planning/phases/52-multi-source-slide-assembler/52-02-SUMMARY.md
@apps/agent/src/generation/multi-source-assembler.ts
@apps/agent/src/generation/__tests__/multi-source-assembler.test.ts

<interfaces>
From `apps/agent/src/generation/multi-source-assembler.ts` (current gap to replace):

```typescript
const textElements = extractTextElements(sourceSlide);

for (const [textIndex, textElement] of textElements.entries()) {
  const textBoxId = `${targetSlideId}-shape-${textIndex + 1}`;
  requests.push({
    createShape: {
      objectId: textBoxId,
      shapeType: "TEXT_BOX",
      elementProperties: {
        pageObjectId: targetSlideId,
        size: textElement.size,
        transform: textElement.transform,
      },
    },
  });
  requests.push({
    insertText: {
      objectId: textBoxId,
      insertionIndex: 0,
      text: textElement.text,
    },
  });
}
```

From `apps/agent/src/ingestion/extract-elements.ts` (element-type priorities already used elsewhere):

```typescript
function extractElementType(element: slides_v1.Schema$PageElement) {
  if (element.elementGroup) return "group";
  if (element.table) return "table";
  if (element.image) return "image";
  if (element.shape?.text?.textElements?.some((te) => te.textRun?.content?.trim())) {
    return "text";
  }
  return "shape";
}
```

From `apps/agent/src/lib/slide-extractor.ts` (existing recursive page-element traversal pattern):

```typescript
for (const element of pageElements) {
  if (element.shape?.text?.textElements) { /* ... */ }
  if (element.table) { /* ... */ }
  if (element.elementGroup?.children) {
    const groupText = extractTextFromPageElements(element.elementGroup.children);
  }
}
```

Gap to close exactly:
- Stop silently discarding non-text secondary-slide content.
- Preserve or reconstruct supported page element types with original geometry.
- Surface unsupported element types explicitly so verification can approve or reject with concrete evidence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace text-only secondary-slide rebuild with fidelity-preserving reconstruction</name>
  <files>apps/agent/src/generation/multi-source-assembler.ts, apps/agent/src/generation/__tests__/multi-source-assembler.test.ts</files>
  <behavior>
    - Secondary slides containing images emit image-preserving requests using source image data plus original size/transform.
    - Secondary slides containing non-text shapes emit shape-preserving requests instead of dropping those elements.
    - Secondary slides containing tables recreate table geometry and non-empty cell text rather than collapsing the slide to text boxes only.
    - Grouped children are traversed recursively so supported child elements are rebuilt in source order.
    - Unsupported elements no longer disappear silently; the assembler logs a warning and creates a visible placeholder marker tied to the source slide/element.
    - Existing primary copy-and-prune, finalSlideOrder mapping, and temp-copy cleanup behavior stays intact.
  </behavior>
  <action>
    Replace the secondary-source branch in `assembleMultiSourceDeck` so it rebuilds supported page elements, not just text boxes.

    Implementation guidance:
    1. Remove the `extractTextElements()`-only approach from the secondary-slide path.
    2. Add a helper inside `multi-source-assembler.ts` (or a small local extraction helper in the same file) that walks `sourceSlide.pageElements` recursively and produces rebuild requests for supported types: `image`, `table`, `shape` (including non-text shapes and text-bearing shapes), plus `elementGroup.children`.
    3. For images, preserve layout using the source element's size/transform and use the image URL/data exposed by the Slides API request surface. Do not substitute a text placeholder when an image is available.
    4. For shapes, preserve `shapeType`, geometry, and text. Text-bearing shapes should still insert text after shape creation; non-text shapes must remain non-text shapes.
    5. For tables, recreate row/column structure and populate non-empty cell text so the table remains visually recognizable and content-bearing.
    6. For grouped elements, flatten recursively and rebuild supported children in deterministic order.
    7. If an element cannot be recreated faithfully (for example chart/video/WordArt or missing required image source data), log a warning with the source slide id and element id, then create a visible placeholder marker in the target slide instead of silently omitting the element. The placeholder exists to make live verification honest, not to claim fidelity.
    8. Preserve the existing single-source fast path, target slide id mapping, reorder logic, and `finally` cleanup.

    Constraints to keep:
    - No new dependencies (NFR-1 still applies project-wide).
    - No schema or Prisma changes.
    - Do not regress to any global replace-all behavior.
    - Do not silently swallow unsupported secondary content; every unsupported case must be observable in logs or on-slide placeholder output.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && npx vitest run src/generation/__tests__/multi-source-assembler.test.ts</automated>
  </verify>
  <done>
    - Secondary-slide reconstruction covers supported design-bearing element types instead of only text boxes.
    - Regression tests prove images, tables, and non-text shapes are rebuilt or explicitly surfaced as unsupported.
    - Existing primary-source and cleanup behaviors still pass in the same targeted test suite.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Google Slides fidelity check for rebuilt secondary slides</name>
  <files>.planning/phases/52-multi-source-slide-assembler/52-VERIFICATION.md</files>
  <what-built>Secondary-slide reconstruction now attempts to preserve supported design-bearing elements and surfaces unsupported elements explicitly instead of silently dropping them.</what-built>
  <how-to-verify>
    1. Run the updated multi-source assembly against a fixture or real selection that includes at least one secondary slide with an image, one with a non-text brand shape or diagram, and one with a table or chart-like layout.
    2. Open the original secondary source presentation and the assembled target deck side by side in Google Slides.
    3. Confirm each rebuilt secondary slide preserves the original slide's layout, imagery, and non-text visual structure closely enough to be presentation-safe, not just text content.
    4. If any element type is still unsupported, confirm it is surfaced as an explicit placeholder/warning rather than silently disappearing.
    5. Reply `approved` only if the secondary slides are visually acceptable and the gap is truly closed; otherwise report the exact source slide, target slide, missing/distorted element type, and whether the new warning/placeholder appeared.
  </how-to-verify>
  <resume-signal>Type `approved` if the live fidelity check passes, or describe the exact remaining mismatch.</resume-signal>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && npx vitest run src/generation/__tests__/multi-source-assembler.test.ts</automated>
  </verify>
  <done>The plan pauses until a human confirms live secondary-slide fidelity or supplies a precise remaining blocker.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the verification record with the final fidelity verdict</name>
  <files>.planning/phases/52-multi-source-slide-assembler/52-VERIFICATION.md</files>
  <action>
    After the checkpoint response arrives, update `52-VERIFICATION.md` truthfully.

    If the user approved the live fidelity check:
    - Set the frontmatter and report body to a verified/pass state.
    - Update the failed truth about preserved original designs to verified.
    - Record the targeted automated regression command plus the human Google Slides fidelity pass as the closure evidence for FR-4.4.

    If the user reports another mismatch:
    - Keep `status: gaps_found`.
    - Replace the old generic gap summary with the exact remaining unsupported element type(s), source/target slide references, and observed behavior from the checkpoint.
    - Keep the report specific enough that the next gap-closure plan can target the remaining unsupported path directly.
  </action>
  <verify>
    <automated>test -s /Users/marlonburnett/source/lumenalta-hackathon/.planning/phases/52-multi-source-slide-assembler/52-VERIFICATION.md &amp;&amp; rg -n "11/11 must-haves verified|status: verified|gaps_found|unsupported element|preserving original designs" /Users/marlonburnett/source/lumenalta-hackathon/.planning/phases/52-multi-source-slide-assembler/52-VERIFICATION.md</automated>
  </verify>
  <done>`52-VERIFICATION.md` reflects the real final state of the fidelity gap, with either a closed pass record or a concrete remaining blocker trail.</done>
</task>

</tasks>

<verification>
The phase gap is closed only when the targeted multi-source assembler regression suite passes and a human confirms that rebuilt secondary slides preserve original visual design closely enough for real Google Slides output. If live fidelity still fails, the verification record must name the exact unsupported element instead of claiming closure.
</verification>

<success_criteria>
- FR-4.4 is satisfied for supported secondary-slide element types, not just text boxes.
- Automated tests prove the secondary-source path rebuilds design-bearing elements or surfaces explicit unsupported placeholders/warnings.
- Live verification confirms the resulting Google Slides output is visually acceptable for secondary slides, or leaves a precise blocker trail.
- `52-VERIFICATION.md` matches the real final outcome with no hidden ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/52-multi-source-slide-assembler/52-03-SUMMARY.md`
</output>
