---
phase: 49-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/mastra/__tests__/agent-registry.test.ts
  - apps/web/src/lib/api-client.ts
  - apps/agent/src/mastra/index.ts
  - .planning/AUTH-CONTRACT.md
autonomous: true
requirements: []
gap_closure: true

must_haves:
  truths:
    - "agent-registry.test.ts passes in an isolated environment without real env vars"
    - "Auth header contract between web and agent is documented for future developers"
  artifacts:
    - path: "apps/agent/src/mastra/__tests__/agent-registry.test.ts"
      provides: "Env-isolated test with vi.mock for env module"
      contains: "vi.mock"
    - path: ".planning/AUTH-CONTRACT.md"
      provides: "Auth header contract documentation"
      contains: "Bearer"
  key_links:
    - from: "apps/agent/src/mastra/__tests__/agent-registry.test.ts"
      to: "apps/agent/src/lib/agent-executor.ts"
      via: "dynamic import in test"
      pattern: "import.*agent-executor"
---

<objective>
Fix the env-coupled agent-registry test and document the auth header contract between web and agent services.

Purpose: The agent-registry test fails in isolated environments because it dynamically imports agent-executor.ts which eagerly validates env vars. The auth header mismatch (web sends Bearer, agent expects X-API-Key) works via Mastra internals but is undocumented and fragile.
Output: Passing test in isolation + AUTH-CONTRACT.md + inline code comments
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-tech-debt-cleanup/49-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix env-coupled agent-registry test</name>
  <files>apps/agent/src/mastra/__tests__/agent-registry.test.ts</files>
  <action>
Add a `vi.mock` call at the top of the test file (after existing vi.mock calls) to mock the env module that agent-executor.ts imports at the top level. The mock path should be `"../../env"` (relative from the test file to the env module). Provide stub values for all required env vars:

```typescript
vi.mock("../../env", () => ({
  env: {
    DATABASE_URL: "postgresql://test:test@localhost:5432/test",
    DIRECT_URL: "postgresql://test:test@localhost:5432/test",
    GOOGLE_SERVICE_ACCOUNT_KEY: "{}",
    GOOGLE_TEMPLATE_PRESENTATION_ID: "test-template-id",
    GOOGLE_CLOUD_PROJECT: "test-project",
    GOOGLE_CLOUD_LOCATION: "us-central1",
    VERTEX_SERVICE_ACCOUNT_KEY: "{}",
    AGENT_API_KEY: "test-key-that-is-at-least-32-characters-long",
    GOOGLE_CLIENT_ID: "test-client-id",
    GOOGLE_CLIENT_SECRET: "test-client-secret",
    NODE_ENV: "test",
  },
}));
```

This prevents `@t3-oss/env-core createEnv()` from eagerly validating env vars when agent-executor.ts is dynamically imported by the third test case.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && npx vitest run src/mastra/__tests__/agent-registry.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>All three test cases in agent-registry.test.ts pass without requiring real env vars to be set</done>
</task>

<task type="auto">
  <name>Task 2: Document auth header contract</name>
  <files>.planning/AUTH-CONTRACT.md, apps/web/src/lib/api-client.ts, apps/agent/src/mastra/index.ts</files>
  <action>
1. Create `.planning/AUTH-CONTRACT.md` with this content:

## Auth Header Contract (Web <-> Agent)

### Current Behavior
- **Web app** (`apps/web/src/lib/api-client.ts`): Sends `Authorization: Bearer <AGENT_API_KEY>`
- **Agent service** (`apps/agent/src/mastra/index.ts`): Configures `SimpleAuth` with `headers: ["X-API-Key"]`
- **CORS config**: Allows both `Authorization` and `X-API-Key` headers
- **Runtime**: Works because Mastra internally maps Bearer tokens to SimpleAuth validation

### Risk
Fragile to Mastra version upgrades. If Mastra stops accepting Bearer as X-API-Key fallback, all web-to-agent calls will fail with 401.

### Recommended Future Fix
Align on one header. Either:
1. Change web `fetchAgent()` to send `X-API-Key: <key>` header (preferred -- matches SimpleAuth config)
2. Change agent SimpleAuth to `headers: ["Authorization"]`

### Decision Log
- Phase 45: Discovered Mastra auth issue, chose Bearer workaround (see STATE.md)
- Phase 45-07: Gap closure attempted X-API-Key alignment but reverted due to Mastra internals

2. Add a brief inline comment in `apps/web/src/lib/api-client.ts` near the Authorization header line:
   `// AUTH-CONTRACT: Sends Bearer but agent expects X-API-Key. Works via Mastra internals. See .planning/AUTH-CONTRACT.md`

3. Add a brief inline comment in `apps/agent/src/mastra/index.ts` near the SimpleAuth config:
   `// AUTH-CONTRACT: Web sends "Authorization: Bearer" not "X-API-Key". Works via Mastra internals. See .planning/AUTH-CONTRACT.md`
  </action>
  <verify>
    <automated>test -f /Users/marlonburnett/source/lumenalta-hackathon/.planning/AUTH-CONTRACT.md && grep -l "AUTH-CONTRACT" /Users/marlonburnett/source/lumenalta-hackathon/apps/web/src/lib/api-client.ts /Users/marlonburnett/source/lumenalta-hackathon/apps/agent/src/mastra/index.ts && echo "PASS"</automated>
  </verify>
  <done>AUTH-CONTRACT.md exists with full contract documentation. Both api-client.ts and index.ts have inline comments pointing to the doc.</done>
</task>

</tasks>

<verification>
- `cd apps/agent && npx vitest run src/mastra/__tests__/agent-registry.test.ts` passes all 3 tests
- `.planning/AUTH-CONTRACT.md` exists and contains the contract
- Both code files have AUTH-CONTRACT comments
</verification>

<success_criteria>
- agent-registry test passes in isolation (no env vars required)
- Auth header contract is documented in a discoverable location with inline pointers
</success_criteria>

<output>
After completion, create `.planning/phases/49-tech-debt-cleanup/49-01-SUMMARY.md`
</output>
