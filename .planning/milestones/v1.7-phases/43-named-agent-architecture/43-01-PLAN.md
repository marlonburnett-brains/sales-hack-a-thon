---
phase: 43-named-agent-architecture
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/schemas/agent-catalog.ts
  - packages/schemas/index.ts
  - apps/agent/prisma/schema.prisma
  - apps/agent/prisma/migrations/<timestamp>_agent_config_foundation/migration.sql
  - apps/agent/prisma/seed.ts
  - apps/agent/src/lib/agent-catalog-defaults.ts
  - apps/agent/src/lib/__tests__/agent-catalog.test.ts
  - apps/agent/src/lib/__tests__/agent-catalog-defaults.test.ts
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
must_haves:
  truths:
    - "The system has one named-agent roster covering every current prompt-bearing responsibility, including background jobs."
    - "Each catalog entry exposes a plain-language role name and a clear responsibility boundary."
    - "Published prompt defaults can be created from the database without relying on inline workflow prompts as the source of truth."
  artifacts:
    - path: "packages/schemas/agent-catalog.ts"
      provides: "Shared agent ids, labels, families, and responsibility metadata"
    - path: "apps/agent/prisma/schema.prisma"
      provides: "AgentConfig and AgentConfigVersion models with immutable version history"
    - path: "apps/agent/prisma/seed.ts"
      provides: "Published version-1 seed rows for the full agent roster"
  key_links:
    - from: "packages/schemas/agent-catalog.ts"
      to: "apps/agent/prisma/seed.ts"
      via: "shared catalog ids and defaults"
      pattern: "AGENT_CATALOG|agentId"
    - from: "apps/agent/prisma/schema.prisma"
      to: "apps/agent/prisma/seed.ts"
      via: "publishedVersionId and version rows"
      pattern: "publishedVersionId|AgentConfigVersion"
---

<objective>
Define the named-agent source-of-truth contract and database foundation before any runtime migration work starts.

Purpose: Lock the product-facing roster, versioned prompt storage model, and seed shape so every downstream plan builds on the same governed agent system.
Output: Shared agent catalog exports, Prisma models/migration for `AgentConfig` + `AgentConfigVersion`, and idempotent seed defaults for the published roster.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-named-agent-architecture/43-CONTEXT.md
@.planning/phases/43-named-agent-architecture/43-RESEARCH.md

<interfaces>
From `packages/schemas/constants.ts`:
```typescript
export const TOUCH_TYPES = ["touch_1", "touch_2", "touch_3", "touch_4", "pre_call"] as const;
export const ARTIFACT_TYPES = ["proposal", "talk_track", "faq"] as const;
export type ArtifactType = (typeof ARTIFACT_TYPES)[number];
```

From `packages/schemas/index.ts`:
```typescript
export {
  INDUSTRIES,
  FUNNEL_STAGES,
  CONTENT_TYPES,
  SLIDE_CATEGORIES,
  BUYER_PERSONAS,
  TOUCH_TYPES,
  ARTIFACT_TYPES,
  ARTIFACT_TYPE_LABELS,
  SUBSECTORS,
  SOLUTION_PILLARS,
  ACTION_TYPES,
  type ArtifactType,
  type ActionType,
} from "./constants";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish the shared agent catalog contract</name>
  <files>packages/schemas/agent-catalog.ts, packages/schemas/index.ts, apps/agent/src/lib/__tests__/agent-catalog.test.ts</files>
  <behavior>
    - Test 1: The catalog exports plain-language role names plus stable ids for every current prompt-bearing responsibility, including pre-call, Touch 1-4, deck intelligence, Atlus extraction, ingestion, and validation scripts.
    - Test 2: Deck-structure inference and deck-structure chat refinement exist as separate entries.
    - Test 3: Shared jobs such as slide selection stay touch-agnostic families instead of duplicating per touch when the responsibility is the same.
  </behavior>
  <action>Create a shared `AGENT_CATALOG` contract in `packages/schemas/agent-catalog.ts` and export it from `packages/schemas/index.ts`. The catalog must use role-based, product-readable names; include stable `agentId`, user-facing `name`, responsibility summary, family/group metadata, whether the agent is shared vs touch-specific, and source-site notes for every current prompt-bearing file. Honor the locked roster decisions exactly: include background jobs as first-class agents, keep deck-structure analyst and refinement assistant separate, keep Touch 4 sub-roles distinct where the work is already split, and prefer shared job-based families when the responsibility repeats across touches. Do not add `@mastra/editor` coupling or per-agent model selection metadata here.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/lib/__tests__/agent-catalog.test.ts</automated>
  </verify>
  <done>The repo has one shared roster contract that downstream runtime and UI work can import without rediscovering prompt sites, and the contract proves the locked user decisions are represented exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add versioned prompt models, forward-only migration, and published seed defaults</name>
  <files>apps/agent/prisma/schema.prisma, apps/agent/prisma/migrations/&lt;timestamp&gt;_agent_config_foundation/migration.sql, apps/agent/prisma/seed.ts, apps/agent/src/lib/agent-catalog-defaults.ts, apps/agent/src/lib/__tests__/agent-catalog-defaults.test.ts</files>
  <behavior>
    - Test 1: Default prompt builders create one published version-1 row per catalog entry with separate baseline and role prompt layers.
    - Test 2: The schema supports immutable version history plus a published-version pointer on the config row.
    - Test 3: Seed logic is idempotent and does not create duplicate configs or duplicate version-1 rows on rerun.
  </behavior>
  <action>Extend `apps/agent/prisma/schema.prisma` with `AgentConfig` and `AgentConfigVersion` only; keep Phase 43 migration scope limited to these models. Model the config row as the stable agent identity (`agentId`, user-facing name, responsibility, family, status metadata, `publishedVersionId`) and the version row as immutable history (`version`, `baselinePrompt`, `rolePrompt`, optional compiled snapshot, publish metadata, change summary). Generate a forward-only migration via `prisma migrate dev --name agent_config_foundation` and commit the resulting SQL; do not use `prisma db push`, reset flows, or unrelated schema edits. Add a helper that builds the shared Lumenalta baseline prompt plus role-specific defaults from the catalog, then update `apps/agent/prisma/seed.ts` to upsert the full roster with published version 1 as boot-time source of truth.</action>
  <verify>
    <automated>pnpm --filter agent prisma validate && pnpm --filter agent exec vitest run src/lib/__tests__/agent-catalog-defaults.test.ts</automated>
  </verify>
  <done>The database schema can store named agents plus immutable prompt history, and a clean seed run creates one published baseline-backed prompt version per catalog entry without duplicating rows.</done>
</task>

</tasks>

<verification>
Run the catalog and defaults suites, then confirm Prisma validation passes with only the new `AgentConfig`/`AgentConfigVersion` models added for this phase.
</verification>

<success_criteria>
- The shared catalog lists every current prompt-bearing responsibility with clear role-readable naming.
- Prisma schema and migration add only the Phase 43 prompt models.
- Seed defaults produce a published version-1 row for every catalog entry using separate baseline and role prompt layers.
</success_criteria>

<output>
After completion, create `.planning/phases/43-named-agent-architecture/43-01-SUMMARY.md`
</output>
