---
phase: 43-named-agent-architecture
plan: "05"
type: execute
wave: 4
depends_on:
  - 43-03
  - 43-04
files_modified:
  - apps/agent/src/deck-intelligence/infer-deck-structure.ts
  - apps/agent/src/deck-intelligence/chat-refinement.ts
  - apps/agent/src/deck-intelligence/__tests__/named-agent-adoption.test.ts
  - apps/agent/src/lib/__tests__/agent-callsite-coverage.test.ts
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
must_haves:
  truths:
    - "Deck-structure inference and deck-structure refinement are governed as separate named agents with clear role boundaries."
    - "After the final migration pass, every remaining business-logic prompt site in the repo is covered by the formal named-agent architecture."
    - "Future business-logic prompt bypasses are caught automatically before the named-agent system can drift."
  artifacts:
    - path: "apps/agent/src/deck-intelligence/infer-deck-structure.ts"
      provides: "Named deck-structure analyst adoption"
    - path: "apps/agent/src/deck-intelligence/chat-refinement.ts"
      provides: "Named deck-structure refinement assistant adoption"
    - path: "apps/agent/src/lib/__tests__/agent-callsite-coverage.test.ts"
      provides: "Repo-level guardrail against business-logic prompt bypasses"
  key_links:
    - from: "apps/agent/src/deck-intelligence/infer-deck-structure.ts"
      to: "packages/schemas/agent-catalog.ts"
      via: "deck-structure analyst id"
      pattern: "deck-structure-analyst"
    - from: "apps/agent/src/lib/__tests__/agent-callsite-coverage.test.ts"
      to: "apps/agent/src/mastra/workflows"
      via: "prompt-bearing business-file inventory"
      pattern: "generateContent|agent-executor"
---

<objective>
Close the last internal migration gap for deck intelligence and add the final repo-wide guardrail that keeps AGENT-01 and AGENT-02 true after Phase 43 ships.

Purpose: Deck intelligence has its own split role boundary and is a natural final slice; once it lands, the repo can enforce complete named-agent governance without false positives from in-flight migrations.
Output: Separate named-agent adoption in deck intelligence plus a repo-level coverage test that blocks future business-logic prompt bypasses.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-named-agent-architecture/43-CONTEXT.md
@.planning/phases/43-named-agent-architecture/43-RESEARCH.md

<interfaces>
Current deck-intelligence split required by locked decisions:
```text
apps/agent/src/deck-intelligence/infer-deck-structure.ts
apps/agent/src/deck-intelligence/chat-refinement.ts
```

Expected named roles:
```text
deck-structure-analyst
deck-structure-refinement-assistant
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate deck-intelligence prompt sites as separate named agents</name>
  <files>apps/agent/src/deck-intelligence/infer-deck-structure.ts, apps/agent/src/deck-intelligence/chat-refinement.ts, apps/agent/src/deck-intelligence/__tests__/named-agent-adoption.test.ts</files>
  <behavior>
    - Test 1: Deck-structure inference uses the named deck-structure analyst agent.
    - Test 2: Chat refinement uses the separate named deck-structure refinement assistant agent.
    - Test 3: Both files preserve their current structure contracts and streamed refinement behavior while dropping inline prompt authority.
  </behavior>
  <action>Refactor the two deck-intelligence modules to use the shared named-agent executor while preserving the current inference outputs, chat refinement protocol, and state update semantics. Honor the locked decision exactly: inference and refinement must remain separate named agents, not one deck-intelligence generalist. Keep any streaming or diff behavior intact and move only the prompt authority plus version-aware execution onto the formal agent layer.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/deck-intelligence/__tests__/named-agent-adoption.test.ts</automated>
  </verify>
  <done>Deck intelligence is fully governed by the named-agent system with distinct responsibility boundaries for inference vs refinement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the final repo-level named-agent coverage guardrail</name>
  <files>apps/agent/src/lib/__tests__/agent-callsite-coverage.test.ts</files>
  <behavior>
    - Test 1: All known prompt-bearing business files resolve through the named-agent executor and not direct `generateContent` calls.
    - Test 2: The catalog ids referenced by business logic exist in the shared roster.
    - Test 3: Low-level provider plumbing and test mocks are allowed, but new workflow/helper/business prompt sites fail the suite.
  </behavior>
  <action>Add one focused repo-level coverage test after the workflow, extraction/ingestion, validation, and deck-intelligence migrations are complete. Inventory the known prompt-bearing business files and fail if they reintroduce direct `GoogleGenAI` usage instead of the named-agent seam. Keep the allow-list narrow so only low-level execution plumbing or test doubles are exempt. This is the durable regression fence that preserves AGENT-01 and AGENT-02 after the full migration finishes.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run src/lib/__tests__/agent-callsite-coverage.test.ts</automated>
  </verify>
  <done>The repo has an automated guardrail that catches future business-logic prompt bypasses once all named-agent migrations in Phase 43 are complete.</done>
</task>

</tasks>

<verification>
Run the deck-intelligence adoption suite and the repo-level coverage suite, then confirm no prompt-bearing business module remains outside the named-agent seam.
</verification>

<success_criteria>
- Deck-structure inference and refinement stay separate named roles.
- All prompt-bearing business modules are covered by the named-agent system by the end of this plan.
- A repo-level test now prevents future business-logic bypasses.
</success_criteria>

<output>
After completion, create `.planning/phases/43-named-agent-architecture/43-05-SUMMARY.md`
</output>
