---
phase: 48-hitl-stage-revert-route
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/mastra/index.ts
autonomous: true
requirements:
  - TOUCH-06
gap_closure: true
must_haves:
  truths:
    - "POST /interactions/:id/revert-stage returns 200 with success true when given a valid earlier target stage"
    - "Reverting sets hitlStage to the target stage and clears stageContent to null"
    - "Reverting to the same or later stage returns 400 error"
    - "UI revert button triggers the full chain without 404"
  artifacts:
    - path: "apps/agent/src/mastra/index.ts"
      provides: "POST /interactions/:id/revert-stage route handler"
      contains: "revert-stage"
  key_links:
    - from: "apps/web/src/lib/api-client.ts"
      to: "apps/agent/src/mastra/index.ts"
      via: "POST /interactions/:id/revert-stage"
      pattern: "revert-stage"
    - from: "apps/web/src/lib/actions/touch-actions.ts"
      to: "apps/web/src/lib/api-client.ts"
      via: "revertInteractionStage function call"
      pattern: "revertInteractionStage"
---

<objective>
Register the missing POST /interactions/:id/revert-stage agent route so users can navigate back to earlier HITL stages during artifact generation.

Purpose: The entire client-side revert flow is already wired (stepper click -> server action -> api-client -> POST), but the agent never registered the route, so it returns 404. This plan adds the single missing route.
Output: Working revert-stage endpoint on the agent that validates stage ordering, updates hitlStage, and clears stageContent.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/48-hitl-stage-revert-route/48-RESEARCH.md
@apps/agent/src/mastra/index.ts
@apps/web/src/lib/api-client.ts

<interfaces>
<!-- The client already defines the contract. The agent route must match exactly. -->

From apps/web/src/lib/api-client.ts (lines 630-647):
```typescript
export type HitlStage = "skeleton" | "lowfi" | "highfi";

export async function revertInteractionStage(
  interactionId: string,
  targetStage: HitlStage
): Promise<{ success: boolean }> {
  return fetchJSON<{ success: boolean }>(
    `/interactions/${interactionId}/revert-stage`,
    {
      method: "POST",
      body: JSON.stringify({ targetStage }),
    }
  );
}
```

From apps/web/src/lib/actions/touch-actions.ts (line ~240):
```typescript
// revertStageAction calls revertInteractionStage(interactionId, targetStage)
```

Existing route pattern from apps/agent/src/mastra/index.ts (~line 1447):
```typescript
registerApiRoute("/interactions/:id/approve-assets", {
  method: "POST",
  handler: async (c) => {
    const id = c.req.param("id");
    // ... validates body, does Prisma update, returns c.json({ success: true })
  },
}),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register POST /interactions/:id/revert-stage agent route</name>
  <files>apps/agent/src/mastra/index.ts</files>
  <action>
Add a new `registerApiRoute` call for `/interactions/:id/revert-stage` in the "Asset Review API" section of `apps/agent/src/mastra/index.ts`, immediately after the existing `/interactions/:id/approve-assets` route (around line 1480).

The route handler must:
1. Extract `id` from `c.req.param("id")`
2. Parse request body with `z.object({ targetStage: z.enum(["skeleton", "lowfi", "highfi"]) }).parse(body)`
3. Fetch current interaction: `prisma.interactionRecord.findUniqueOrThrow({ where: { id }, select: { hitlStage: true } })`
4. Validate stage ordering using a STAGE_ORDER map: `{ skeleton: 0, lowfi: 1, highfi: 2, ready: 3 }`. If `targetIndex >= currentIndex`, return 400 with `{ error: "Can only revert to an earlier stage" }`
5. Update the record: `prisma.interactionRecord.update({ where: { id }, data: { hitlStage: data.targetStage, stageContent: null } })`
6. Return `c.json({ success: true })`
7. Wrap in try/catch, log errors as `[revert-stage] Error:`, return 500 with `{ error: "Stage revert failed", details: String(err) }`

Follow the exact same pattern as the `/interactions/:id/approve-assets` route. Use the existing `prisma` import already available in the file. Use the existing `z` (zod) import already available.

Do NOT add any UI code. Do NOT add a Prisma migration. Do NOT restart or manipulate Mastra workflow state.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && grep -c "revert-stage" apps/agent/src/mastra/index.ts | grep -q "[1-9]" && echo "PASS: revert-stage route found" || echo "FAIL: route not found"</automated>
  </verify>
  <done>POST /interactions/:id/revert-stage route is registered in the agent with zod validation, stage order check, Prisma update (hitlStage + stageContent null), and proper error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Verify E2E route connectivity with agent build</name>
  <files>apps/agent/src/mastra/index.ts</files>
  <action>
Run the TypeScript compiler on the agent app to confirm the new route has no type errors. Then verify the route path matches exactly what the web client sends to:
1. Run `npx tsc --noEmit` in `apps/agent/` to catch any type errors in the new route code
2. Grep the agent index.ts for the registered path and confirm it matches the client path pattern `/interactions/${interactionId}/revert-stage`
3. Confirm the response shape `{ success: true }` matches what the client expects (`Promise<{ success: boolean }>`)

If tsc fails, fix type errors in the route implementation.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Agent compiles with no type errors. Route path `/interactions/:id/revert-stage` matches client expectation. Response shape `{ success: boolean }` matches client type.</done>
</task>

</tasks>

<verification>
1. `grep "revert-stage" apps/agent/src/mastra/index.ts` shows the route registration
2. `cd apps/agent && npx tsc --noEmit` passes without errors
3. Route path matches client: `/interactions/:id/revert-stage` with POST method
4. Route validates targetStage enum and stage ordering
5. Route clears stageContent on revert
</verification>

<success_criteria>
- POST /interactions/:id/revert-stage is registered and compiles
- Route validates targetStage is one of skeleton/lowfi/highfi
- Route rejects revert to same or later stage with 400
- Route updates hitlStage and nulls stageContent on success
- Agent builds without type errors
</success_criteria>

<output>
After completion, create `.planning/phases/48-hitl-stage-revert-route/48-01-SUMMARY.md`
</output>
