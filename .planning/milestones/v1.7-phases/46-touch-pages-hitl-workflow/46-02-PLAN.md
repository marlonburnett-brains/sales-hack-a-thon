---
phase: 46-touch-pages-hitl-workflow
plan: "02"
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - apps/agent/src/mastra/workflows/touch-1-workflow.ts
  - apps/agent/src/mastra/workflows/touch-2-workflow.ts
  - apps/agent/src/mastra/workflows/touch-3-workflow.ts
  - apps/agent/src/mastra/workflows/touch-4-workflow.ts
  - apps/web/src/lib/actions/touch-actions.ts
  - apps/web/src/lib/api-client.ts
autonomous: true
requirements:
  - TOUCH-02
  - TOUCH-03
  - TOUCH-04
  - TOUCH-05
must_haves:
  truths:
    - "Touch 1 workflow suspends at Skeleton (content outline) and Low-fi (draft text) stages before High-fi (Google Slides)"
    - "Touch 2 workflow suspends at Skeleton (slide selection rationale) and Low-fi (draft slide order + notes) stages before High-fi (Google Slides)"
    - "Touch 3 workflow suspends at Skeleton and Low-fi stages (mirrors Touch 2 pattern)"
    - "Touch 4 workflow maps its existing 3 suspend points to the Skeleton/Low-fi/High-fi stage model"
    - "Server actions exist for transitioning between stages (approve and revert) for all 4 touches"
  artifacts:
    - path: "apps/agent/src/mastra/workflows/touch-1-workflow.ts"
      provides: "3-stage HITL workflow with Skeleton and Low-fi suspend points"
      contains: "await-skeleton-approval"
    - path: "apps/agent/src/mastra/workflows/touch-2-workflow.ts"
      provides: "3-stage HITL workflow for Meet Lumenalta deck"
      contains: "await-skeleton-approval"
    - path: "apps/agent/src/mastra/workflows/touch-3-workflow.ts"
      provides: "3-stage HITL workflow for capability deck"
      contains: "await-skeleton-approval"
    - path: "apps/agent/src/mastra/workflows/touch-4-workflow.ts"
      provides: "Existing 3 suspend points mapped to stage model with hitlStage updates"
      contains: "hitlStage"
    - path: "apps/web/src/lib/actions/touch-actions.ts"
      provides: "Stage transition server actions"
      exports: ["transitionStageAction", "revertStageAction"]
  key_links:
    - from: "touch-*-workflow.ts suspend points"
      to: "InteractionRecord.hitlStage"
      via: "prisma update in workflow steps"
      pattern: "prisma\\.interactionRecord\\.update.*hitlStage"
    - from: "apps/web/src/lib/actions/touch-actions.ts"
      to: "apps/web/src/lib/api-client.ts"
      via: "API client calls for workflow resume"
      pattern: "resumeTouch.*Workflow"
---

<objective>
Add multi-stage suspend points to all four touch workflows and create server actions for stage transitions. This converts the existing single-suspend (or Touch 4's 3-suspend) workflows into the 3-stage HITL model.

Purpose: The touch page UI needs to display stage content and let users approve/revert between stages. The workflows must suspend at each stage boundary and update InteractionRecord.hitlStage.
Output: Modified workflow files with stage-aware suspend points, new server actions for stage transitions.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/46-touch-pages-hitl-workflow/46-CONTEXT.md
@.planning/phases/46-touch-pages-hitl-workflow/46-RESEARCH.md
@.planning/phases/46-touch-pages-hitl-workflow/46-01-SUMMARY.md

@apps/agent/src/mastra/workflows/touch-1-workflow.ts
@apps/agent/src/mastra/workflows/touch-2-workflow.ts
@apps/agent/src/mastra/workflows/touch-3-workflow.ts
@apps/agent/src/mastra/workflows/touch-4-workflow.ts
@apps/web/src/lib/actions/touch-actions.ts
@apps/web/src/lib/api-client.ts

<interfaces>
<!-- From 46-01: HitlStage type -->
type HitlStage = "skeleton" | "lowfi" | "highfi";

<!-- From 46-01: InteractionRecord now has -->
hitlStage        String?  // "skeleton" | "lowfi" | "highfi" | "ready" | null
stageContent     String?  // JSON: content at current stage for display

<!-- Existing Mastra suspend/resume pattern from touch-1-workflow.ts -->
const awaitApproval = createStep({
  id: "await-approval",
  suspendSchema: z.object({ ... }),
  execute: async ({ inputData, resumeData, suspend }) => {
    if (!resumeData) {
      return await suspend({ ... });
    }
    // Resumed with approval
    return { ... };
  },
});

<!-- Existing touch action pattern -->
export async function approveTouch1Action(
  runId: string, stepId: string,
  decision: "approved" | "edited",
  editedContent?: Record<string, unknown>
): Promise<WorkflowRunResult>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 3-stage suspend points to Touch 1-3 workflows</name>
  <files>
    apps/agent/src/mastra/workflows/touch-1-workflow.ts,
    apps/agent/src/mastra/workflows/touch-2-workflow.ts,
    apps/agent/src/mastra/workflows/touch-3-workflow.ts
  </files>
  <action>
    For each Touch 1-3 workflow, restructure the existing workflow to have 3 suspend points. Do NOT rewrite from scratch -- add suspend points around the existing logic.

    **Touch 1 (Pager):**
    - After the existing `generateContent` step, add `awaitSkeletonApproval` suspend step. The skeleton content is the content outline (companyName, headline, valueProposition, keyCapabilities summary). Update `InteractionRecord.hitlStage` to "skeleton" and `stageContent` to JSON of skeleton content via prisma update before suspending.
    - After skeleton approval, add a `generateDraftText` step that produces the full draft text (the existing PagerContent with all fields fleshed out). Then add `awaitLowfiApproval` suspend step. Update hitlStage to "lowfi" and stageContent to draft text JSON.
    - After low-fi approval, the existing `assembleDeck` step runs (produces Google Slides). This is the High-fi stage. Update hitlStage to "highfi" after assembly, then to "ready" after final approval.
    - The existing `awaitApproval` step becomes the High-fi approval checkpoint. Keep the existing resume schema but add hitlStage tracking.
    - Wire the new steps into the workflow chain: generateContent -> awaitSkeletonApproval -> generateDraftText -> awaitLowfiApproval -> assembleDeck -> awaitApproval -> recordInteraction.
    - Each suspend step's `suspendSchema` should include `{ stage: string, content: <StageSchema>, dealId: string, interactionId: string }`.
    - Each suspend step's `resumeSchema` should include `{ decision: "approved" | "refined", refinedContent?: <StageSchema> }`.

    **Touch 2 (Intro Deck) and Touch 3 (Capability Deck):**
    - These currently have NO suspend points (select-slides -> assemble-deck). Add 2 suspend points.
    - After slide selection logic, create skeleton content = slide selection rationale (why these slides were chosen, which templates matched). Add `awaitSkeletonApproval` suspend. Update hitlStage to "skeleton".
    - After skeleton approval, produce low-fi content = draft slide order + notes per slide. Add `awaitLowfiApproval` suspend. Update hitlStage to "lowfi".
    - After low-fi approval, proceed to existing deck assembly (High-fi). Update hitlStage to "highfi" then "ready".
    - Add `recordInteraction` step if not already present.
    - Create the InteractionRecord at the start of the workflow (before first suspend) so we have an interactionId to track.

    For all three workflows:
    - Use `executeNamedAgent` for any new LLM calls (not direct model calls).
    - Import prisma from `../../lib/db` for InteractionRecord updates.
    - Preserve all existing workflow IDs and step IDs to avoid breaking in-flight runs.
    - Add new steps with new IDs (e.g., "await-skeleton-approval", "generate-draft-text", "await-lowfi-approval").
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit --project apps/agent/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>Touch 1-3 workflows each have 3 suspend points (skeleton, lowfi, highfi). Each suspend updates InteractionRecord.hitlStage and stageContent. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Map Touch 4 suspends to stage model and add stage transition actions</name>
  <files>
    apps/agent/src/mastra/workflows/touch-4-workflow.ts,
    apps/web/src/lib/actions/touch-actions.ts,
    apps/web/src/lib/api-client.ts
  </files>
  <action>
    **Touch 4 workflow mapping:**
    Touch 4 already has 3 suspend points: `awaitFieldReview` (field validation), `awaitBriefApproval` (brief review), `awaitAssetReview` (final assets). Map these to the stage model:
    - `awaitFieldReview` -> Skeleton stage: The multi-pillar sales brief extraction (customerContext, businessOutcomes, constraints, stakeholders, timeline, budget). Add `prisma.interactionRecord.update` to set hitlStage="skeleton" and stageContent=JSON of field review data before suspending.
    - `awaitBriefApproval` -> Low-fi stage: The full draft text of proposal deck content, talk track, and FAQ. Add hitlStage="lowfi" and stageContent=JSON of all 3 artifacts (proposal, talkTrack, faq) before suspending.
    - `awaitAssetReview` -> High-fi stage: The final Google Slides deck + Google Docs. Add hitlStage="highfi" before suspending, then "ready" after approval.
    - Preserve all existing step IDs and schemas -- only add the hitlStage/stageContent prisma updates.

    **Stage transition server actions (touch-actions.ts):**
    Add two new server actions:

    1. `transitionStageAction(interactionId: string, runId: string, stepId: string, touchType: string, decision: "approved" | "refined", refinedContent?: unknown)` -- Resumes the suspended workflow step with the decision. Calls the appropriate resume function from api-client based on touchType. Revalidates `/deals`.

    2. `revertStageAction(interactionId: string, targetStage: HitlStage)` -- Updates InteractionRecord.hitlStage to targetStage, clears stageContent for downstream stages (set to null). Does NOT restart the workflow -- the UI will trigger a new generation run when the user navigates back. Per locked decision: "downstream stages regenerate" when user goes back.

    **API client additions (api-client.ts):**
    Add any missing resume functions for Touch 2 and Touch 3 workflows (currently only Touch 1 and Touch 4 have resume functions). Add `resumeTouch2Workflow` and `resumeTouch3Workflow` functions following the existing pattern from `resumeTouch1Workflow`.

    Also add a generic `resumeWorkflowStep(runId: string, stepId: string, data: unknown)` helper if the existing per-touch resume functions share enough structure.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit --project apps/agent/tsconfig.json 2>&1 | head -10 && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -10</automated>
  </verify>
  <done>Touch 4 workflow updates hitlStage at each existing suspend point. Server actions exist for stage transition (approve/resume) and stage reversion (go-back). API client has resume functions for all 4 touches. TypeScript compiles for both agent and web.</done>
</task>

</tasks>

<verification>
- All 4 touch workflows compile with stage-aware suspend points
- `transitionStageAction` and `revertStageAction` exported from touch-actions.ts
- API client has resume functions for all 4 touches
- Both apps/agent and apps/web pass TypeScript checks
</verification>

<success_criteria>
- Touch 1 workflow has 3 suspend points: skeleton (content outline), lowfi (draft text), highfi (Google Slides)
- Touch 2/3 workflows have 2 new suspend points: skeleton (slide rationale), lowfi (draft order + notes)
- Touch 4 workflow's existing 3 suspends now update InteractionRecord.hitlStage
- Server actions enable approving/resuming and reverting stages for all touches
- All stageContent is stored as JSON in InteractionRecord for UI display
</success_criteria>

<output>
After completion, create `.planning/phases/46-touch-pages-hitl-workflow/46-02-SUMMARY.md`
</output>
