---
phase: 45-persistent-ai-chat-bar
plan: 03
type: execute
wave: 5
depends_on: ["45-05"]
files_modified:
  - apps/web/src/components/deals/persistent-deal-chat.tsx
  - apps/web/src/components/deals/deal-chat-thread.tsx
  - apps/web/src/components/deals/__tests__/persistent-deal-chat.test.tsx
  - apps/web/src/components/deals/__tests__/deal-chat-thread.test.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/__tests__/layout-chat-persistence.test.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/briefing/page.tsx
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05

must_haves:
  truths:
    - "The seller can access the same deal chat from Overview, Briefing, and Touch pages without losing the thread"
    - "The default UI is a collapsed bottom dock, but the seller can switch into a side-panel layout within the same deal"
    - "The chat header and suggestion chips stay aware of the current page or touch without auto-posting messages on navigation"
    - "Transcript/note confirmation, knowledge matches, and saved-source chips are visible inline in the chat experience"
  artifacts:
    - path: "apps/web/src/components/deals/persistent-deal-chat.tsx"
      provides: "Persistent route-aware shell with dock and side-panel modes"
      exports: ["PersistentDealChat"]
    - path: "apps/web/src/components/deals/deal-chat-thread.tsx"
      provides: "Message list, streamed meta parsing, confirmation controls, and match cards"
      exports: ["DealChatThread"]
    - path: "apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx"
      provides: "Shared deal layout mount point that keeps chat alive across sub-pages"
      contains: "PersistentDealChat"
  key_links:
    - from: "apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx"
      to: "apps/web/src/components/deals/persistent-deal-chat.tsx"
      via: "layout render"
      pattern: "PersistentDealChat"
    - from: "apps/web/src/components/deals/persistent-deal-chat.tsx"
      to: "/api/deals/[dealId]/chat"
      via: "history load and streamed send"
      pattern: "\/api\/deals\/"
    - from: "apps/web/src/components/deals/deal-chat-thread.tsx"
      to: "apps/web/src/lib/actions/deal-chat-actions.ts"
      via: "binding confirmation and save mutations"
      pattern: "confirmDealChatBinding|save.*Binding"
    - from: "apps/web/src/components/deals/persistent-deal-chat.tsx"
      to: "next/navigation"
      via: "route-aware context cue"
      pattern: "useSelectedLayoutSegments|usePathname"
---

<objective>
Ship the seller-facing persistent chat experience inside the shared deal layout and align the existing briefing page with that single assistant surface.

Purpose: Phase 45 is visible when sellers can move through a deal and keep one assistant thread, one context cue, one set of suggestions, and one confirmation flow instead of separate helper panels per page.
Output: Layout-mounted persistent chat UI with dock/side-panel modes, route-aware suggestions, inline confirmation/match cards, and briefing-page alignment.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-persistent-ai-chat-bar/45-CONTEXT.md
@.planning/phases/45-persistent-ai-chat-bar/45-RESEARCH.md
@.planning/phases/45-persistent-ai-chat-bar/45-05-PLAN.md
@.planning/phases/42-deal-detail-layout/42-01-SUMMARY.md
@.planning/phases/42-deal-detail-layout/42-03-SUMMARY.md
@apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx
@apps/web/src/components/deals/briefing-chat-panel.tsx
@apps/web/src/components/settings/chat-bar.tsx
@apps/web/src/components/ui/dialog.tsx

<interfaces>
From `apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx`:
```typescript
export default async function DealLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ dealId: string }>;
})
```

From `apps/web/src/components/deals/briefing-chat-panel.tsx`:
```typescript
interface BriefingChatPanelProps {
  dealId: string;
  companyName: string;
  industry: string;
}

export function BriefingChatPanel(props: BriefingChatPanelProps)
```

From `apps/web/src/components/settings/chat-bar.tsx`:
```typescript
// Proven client pattern: local message state + streaming text + delimiter parsing.
// Reuse the stream/parser ergonomics, not the deck-structure-specific contract.
export function ChatBar(props: ChatBarProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the persistent dock and side-panel chat UI</name>
  <files>
    apps/web/src/components/deals/persistent-deal-chat.tsx,
    apps/web/src/components/deals/deal-chat-thread.tsx,
    apps/web/src/components/deals/__tests__/persistent-deal-chat.test.tsx,
    apps/web/src/components/deals/__tests__/deal-chat-thread.test.tsx
  </files>
  <behavior>
    - Test 1: a brand-new deal chat boots with a short greeting and 2-3 suggestions, but route changes only refresh suggestions and context cues instead of auto-posting a new assistant message.
    - Test 2: the default UI renders as a collapsed bottom dock teaser and can switch into a side-panel mode.
    - Test 3: streamed `---DEAL_CHAT_META---` payloads render suggestions, confirmation chips, and knowledge-match cards inline.
    - Test 4: confirmation UI allows save-to-touch, save-as-general-note, correction, or refine-before-save without losing the current thread.
  </behavior>
  <action>
    Create `apps/web/src/components/deals/persistent-deal-chat.tsx` as the route-aware client shell mounted once per deal. It should:
    - derive live page context from `useSelectedLayoutSegments()` or `usePathname()` inside the client component, not from the server layout
    - start in a collapsed bottom dock with a visible teaser and context label
    - let the seller switch between dock and side-panel modes; use the existing Radix `Dialog` primitives for the side-panel behavior instead of custom portal/focus logic
    - load chat bootstrap/history from the Plan 45-05 actions and keep the same thread alive across client navigation inside the deal layout

    Create `apps/web/src/components/deals/deal-chat-thread.tsx` for the thread body and composer. Reuse the proven stream parsing ergonomics from `apps/web/src/components/settings/chat-bar.tsx`, but adapt them to the Phase 45 contracts:
    - parse `---DEAL_CHAT_META---` payloads after the assistant text stream
    - render context cue, suggestions, knowledge-match cards, and lightweight saved-binding chips
    - surface pending transcript/note confirmation with quick actions for the guessed touch, alternate touch choices, general deal notes, and a refine-first CTA when the runtime marks the input as messy or partial
    - route confirmation, correction, and save-as-general-note mutations through the Plan 45-05 deal-chat actions so the UI reaches the binding-confirmation path explicitly
    - let the seller review and apply the assistant's refined draft before the save/binding confirmation path continues
    - show concise error feedback with Sonner for failed sends or save retries

    Keep the UI mobile-safe, keyboard-accessible, and visually consistent with the existing deal pages. Do not create separate page-local chat roots.
  </action>
  <verify>
    <automated>pnpm --filter web exec vitest run "src/components/deals/__tests__/persistent-deal-chat.test.tsx" "src/components/deals/__tests__/deal-chat-thread.test.tsx"</automated>
  </verify>
  <done>The repo has a tested persistent chat shell that supports dock and side-panel modes, route-aware suggestions, streamed meta parsing, and inline confirmation UI.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the chat in the shared deal layout and align the briefing page</name>
  <files>
    apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx,
    apps/web/src/app/(authenticated)/deals/[dealId]/__tests__/layout-chat-persistence.test.tsx,
    apps/web/src/app/(authenticated)/deals/[dealId]/briefing/page.tsx
  </files>
  <action>
    Update `apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx` so `PersistentDealChat` is mounted once beside the existing sidebar/content shell. Preserve the Phase 42 full-bleed layout and breadcrumb behavior while ensuring the assistant survives navigation between Overview, Briefing, review pages, and Touch routes.

    Update `apps/web/src/app/(authenticated)/deals/[dealId]/briefing/page.tsx` so the persistent assistant is the primary interactive surface on Briefing too. Do not keep a second divergent chat experience at the top of the page. Replace or reduce the old page-local chat shell so the page focuses on briefing artifacts and clear copy that points sellers back to the shared assistant when relevant.

    Add `apps/web/src/app/(authenticated)/deals/[dealId]/__tests__/layout-chat-persistence.test.tsx` as the explicit cross-route integration harness for this task. Mount the shared deal layout with nested deal sub-pages, navigate between at least two sub-routes, and assert the same chat state instance/history survives while the visible page/touch context cue and suggestion set update for the new route.

    After wiring, verify the page/touch context cue updates on navigation, suggestions refresh by route, and the thread state remains intact while moving between deal sub-pages.
  </action>
  <verify>
    <automated>pnpm --filter web exec tsc --noEmit && pnpm --filter web exec vitest run "src/components/deals/__tests__/persistent-deal-chat.test.tsx" "src/components/deals/__tests__/deal-chat-thread.test.tsx" "src/app/(authenticated)/deals/[dealId]/__tests__/layout-chat-persistence.test.tsx"</automated>
  </verify>
  <done>The shared layout owns one persistent assistant across deal sub-pages, and Briefing no longer exposes a separate conflicting chat surface.</done>
</task>

</tasks>

<verification>
1. Navigate between `/deals/[id]/overview`, `/deals/[id]/briefing`, and `/deals/[id]/touch/1` without losing the thread.
2. Confirm the default state is a collapsed bottom dock and the side-panel toggle still works afterward.
3. Paste transcript-like text from Briefing or Overview and verify the UI offers refine-before-save help when the input is messy, then asks for binding confirmation instead of silently saving.
4. Ask a similar-case question and confirm the UI renders top matches with why-fit cards.
</verification>

<success_criteria>
- The seller can reach the same assistant on any deal sub-page and keep the thread across navigation.
- The UI honors the locked dock-first, side-panel-optional presentation.
- Route-aware suggestions and context labels update without auto-posting messages on every page visit.
- Confirmation chips, refine-before-save affordances, grounded answers, and knowledge-match cards are visible inline in the persistent chat surface.
</success_criteria>

<output>
After completion, create `.planning/phases/45-persistent-ai-chat-bar/45-03-SUMMARY.md`
</output>
