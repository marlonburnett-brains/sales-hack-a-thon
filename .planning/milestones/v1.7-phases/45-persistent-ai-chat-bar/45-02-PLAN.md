---
phase: 45-persistent-ai-chat-bar
plan: 02
type: execute
wave: 3
depends_on: ["45-01", "45-04"]
files_modified:
  - apps/agent/src/deal-chat/context.ts
  - apps/agent/src/deal-chat/assistant.ts
  - apps/agent/src/deal-chat/__tests__/assistant.test.ts
  - apps/agent/src/mastra/index.ts
  - apps/agent/src/mastra/__tests__/deal-chat-routes.test.ts
autonomous: true
requirements:
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05

must_haves:
  truths:
    - "The assistant can answer grounded questions about a deal's data, history, and current page context"
    - "Knowledge-base queries return top matches with explicit why-fit explanations instead of a raw dump"
    - "The assistant can ask for confirmation before saving notes or transcripts and then persist the confirmed result"
  artifacts:
    - path: "apps/agent/src/deal-chat/assistant.ts"
      provides: "Deal-chat orchestration, response shaping, and structured side-effect metadata"
      exports: ["runDealChatTurn"]
    - path: "apps/agent/src/deal-chat/context.ts"
      provides: "Grounding loader for deal, interactions, prior sources, and route context"
      exports: ["loadDealChatContext"]
    - path: "apps/agent/src/mastra/index.ts"
      provides: "Deal-chat history, streaming, and binding-confirmation routes"
      exports: ["GET /deals/:dealId/chat", "POST /deals/:dealId/chat", "POST /deals/:dealId/chat/bindings"]
  key_links:
    - from: "apps/agent/src/deal-chat/assistant.ts"
      to: "apps/agent/src/lib/agent-executor.ts"
      via: "deal-chat-assistant named-agent execution"
      pattern: "agentId:\s*\"deal-chat-assistant\""
    - from: "apps/agent/src/deal-chat/assistant.ts"
      to: "apps/agent/src/lib/atlusai-search.ts"
      via: "knowledge retrieval"
      pattern: "searchSlides"
    - from: "apps/agent/src/mastra/index.ts"
      to: "apps/agent/src/deal-chat/assistant.ts"
      via: "deal chat route handlers"
      pattern: "deals\/:dealId\/chat"
---

<objective>
Implement the agent-side deal-chat orchestration and route handlers that turn the persistence foundation into a working governed assistant.

Purpose: Once the contracts and tables exist, Phase 45 still needs one governed runtime that answers deal questions, retrieves similar cases, and requests transcript/note confirmation through dedicated agent routes before the web layer mirrors that contract.
Output: Deal-chat agent orchestration, route registration, and direct route-level regression coverage.

Execution order inside this plan is strict: finalize the agent runtime and `/deals/:dealId/chat` behavior first, then lock it with dedicated route tests so the downstream web bridge can mirror a verified contract.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-persistent-ai-chat-bar/45-CONTEXT.md
@.planning/phases/45-persistent-ai-chat-bar/45-RESEARCH.md
@.planning/phases/45-persistent-ai-chat-bar/45-01-PLAN.md
@.planning/phases/45-persistent-ai-chat-bar/45-04-PLAN.md
@.planning/phases/43-named-agent-architecture/43-02-SUMMARY.md
@.planning/phases/43-named-agent-architecture/43-05-SUMMARY.md
@apps/agent/src/lib/agent-executor.ts
@apps/agent/src/lib/atlusai-search.ts

<interfaces>
From `apps/agent/src/lib/agent-executor.ts`:
```typescript
export interface ExecuteNamedAgentParams<TOutput = undefined> {
  agentId: AgentId;
  messages: Array<{ role: "user" | "assistant" | "system"; content: string }>;
  pinnedVersionId?: string;
}

export async function executeRuntimeProviderNamedAgent<TOutput = undefined>(
  params: ExecuteNamedAgentParams<TOutput>,
): Promise<NamedAgentExecutionResult<TOutput>>;

export async function streamRuntimeProviderNamedAgent<TOutput = undefined>(
  params: ExecuteNamedAgentParams<TOutput>,
): Promise<{ stream: AsyncIterable<{ text?: string }>; promptVersion: ... }>;
```

From `apps/agent/src/lib/atlusai-search.ts`:
```typescript
export async function searchSlides(params: {
  query: string;
  industry?: string;
  touchType?: string;
  limit?: number;
}): Promise<SlideSearchResult[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the named-agent deal-chat orchestrator and register agent routes</name>
  <files>
    apps/agent/src/deal-chat/context.ts,
    apps/agent/src/deal-chat/assistant.ts,
    apps/agent/src/deal-chat/__tests__/assistant.test.ts,
    apps/agent/src/mastra/index.ts
  </files>
  <behavior>
    - Test 1: deal-history questions use deal/interactions/source context and return short answer first, then supporting bullets, then next steps when useful.
    - Test 2: knowledge queries call `searchSlides()` and return structured top matches with per-match why-fit explanations.
    - Test 3: transcript/note-like inputs return a pending confirmation payload when binding confidence is insufficient or the content is messy, including an explicit `refineBeforeSave` affordance with assistant-generated cleanup guidance before any save action.
    - Test 4: every prompt-bearing assistant turn runs through `deal-chat-assistant`, not inline prompt strings.
  </behavior>
  <action>
    Create `apps/agent/src/deal-chat/context.ts` to load the full grounding payload for a turn: deal metadata, relevant `InteractionRecord`s, saved `DealContextSource` rows, recent visible chat history, optional prompt summary, and the current route context stamped by the web client.

    Create `apps/agent/src/deal-chat/assistant.ts` as the single orchestration entrypoint. It should:
    - route user turns through the named `deal-chat-assistant` runtime
    - answer with the locked format for deal-history questions
    - call `searchSlides()` for knowledge-base queries and transform results into structured cards with why-fit copy
    - detect note/transcript save intent, use the binding heuristics from Plan 45-04, and return a pending confirmation payload instead of silently saving uncertain content
    - when the input looks partial, ambiguous, or messy, append explicit assistant metadata for a refine-before-save path: a short cleanup recommendation, proposed refined text, and a `refineBeforeSave` action the UI can surface before any binding confirmation or persistence call
    - stream assistant text first, then append one final `---DEAL_CHAT_META---` JSON payload containing suggestions, confirmation chips, refine-before-save metadata, match cards, and prompt-version metadata

    Update `apps/agent/src/mastra/index.ts` to register the deal-chat routes without creating a second route family. Add:
    - `GET /deals/:dealId/chat` for thread history plus greeting/suggestions bootstrap
    - `POST /deals/:dealId/chat` for streaming assistant turns
    - `POST /deals/:dealId/chat/bindings` for confirmation, correction, or save-as-general-note actions

    Keep persistence explicit through the Plan 45-04 helpers; do not adopt Mastra Memory as the primary store in this phase.
  </action>
  <verify>
    <automated>MISSING -- Wave 3 must create `apps/agent/src/mastra/__tests__/deal-chat-routes.test.ts` first</automated>
  </verify>
  <done>The agent can load deal context, stream governed answers, retrieve knowledge matches, and return confirmation-aware metadata through dedicated `/deals/:dealId/chat` routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add route-level regression coverage for `/deals/:dealId/chat` and `/deals/:dealId/chat/bindings`</name>
  <files>
    apps/agent/src/mastra/__tests__/deal-chat-routes.test.ts
  </files>
  <behavior>
    - Test 1: `GET /deals/:dealId/chat` returns the persisted thread bootstrap for a deal.
    - Test 2: `POST /deals/:dealId/chat` exercises the streaming assistant path and proves the final `---DEAL_CHAT_META---` payload is appended.
    - Test 3: `POST /deals/:dealId/chat/bindings` accepts confirmation/correction payloads and returns the saved source metadata.
    - Test 4: route handlers invoke the Plan 45-04 persistence helpers rather than bypassing the durable storage path.
  </behavior>
  <action>
    Create `apps/agent/src/mastra/__tests__/deal-chat-routes.test.ts` as the direct route harness for the new agent endpoints. Exercise all three handlers registered in `apps/agent/src/mastra/index.ts` so this plan proves the real runtime contract before the web bridge mirrors it.

    Cover the happy path and the confirmation-required path. Validate the registered `/deals/:dealId/chat` and `/deals/:dealId/chat/bindings` handlers call the deal-chat orchestrator and persistence helpers rather than returning stub data.
  </action>
  <verify>
    <automated>pnpm --filter agent exec vitest run "src/deal-chat/__tests__/assistant.test.ts" "src/mastra/__tests__/deal-chat-routes.test.ts" "src/lib/__tests__/agent-callsite-coverage.test.ts"</automated>
  </verify>
  <done>The agent routes for history, streaming turns, and binding confirmation are covered by direct automated tests, not only by downstream consumers.</done>
</task>

</tasks>

<verification>
1. Agent-side tests prove named-agent usage, grounded answer shaping, and knowledge-result structuring.
2. Agent route tests prove messy transcript-like input produces refine-before-save metadata before any save path.
3. Route-level tests directly exercise `GET /deals/:dealId/chat`, `POST /deals/:dealId/chat`, and `POST /deals/:dealId/chat/bindings`.
4. The new route family is registered under `/deals/:dealId/chat` on the agent.
</verification>

<success_criteria>
- Deal-chat runtime answers deal-history questions with grounded context and the locked response style.
- Similar-case queries return structured top matches with why-fit explanations.
- Messy transcript or notes input yields a refine-first assistant path before save confirmation.
- The entire runtime stays under named-agent governance.
- The agent route family is proven with direct automated coverage before the web bridge is built.
</success_criteria>

<output>
After completion, create `.planning/phases/45-persistent-ai-chat-bar/45-02-SUMMARY.md`
</output>
