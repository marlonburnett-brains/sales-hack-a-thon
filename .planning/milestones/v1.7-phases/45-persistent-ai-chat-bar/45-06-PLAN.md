---
phase: 45-persistent-ai-chat-bar
plan: 06
type: execute
wave: 6
depends_on:
  - 45-02
  - 45-03
  - 45-05
files_modified:
  - packages/schemas/deal-chat.ts
  - apps/agent/src/deal-chat/assistant.ts
  - apps/agent/src/deal-chat/__tests__/assistant.test.ts
  - apps/agent/src/mastra/index.ts
  - apps/agent/src/mastra/__tests__/deal-chat-routes.test.ts
  - apps/web/src/app/api/deals/[dealId]/chat/route.ts
  - apps/web/src/app/api/deals/[dealId]/chat/__tests__/route.test.ts
  - apps/web/src/components/deals/deal-chat-thread.tsx
  - apps/web/src/components/deals/__tests__/deal-chat-thread.test.tsx
autonomous: true
gap_closure: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03
  - CHAT-04
  - CHAT-05
must_haves:
  truths:
    - "The seller can upload a transcript file from the persistent deal chat on any deal sub-page without losing the existing thread or route-aware context cues"
    - "An uploaded transcript reaches the same confirmation-first binding flow as pasted transcript text, including explicit touch confirmation and general-note fallback"
    - "Messy or partial uploaded transcript text still surfaces refine-before-save guidance before any note or transcript is saved"
  artifacts:
    - path: "packages/schemas/deal-chat.ts"
      provides: "Shared deal-chat send contract accepts structured uploaded transcript content alongside existing route context and binding metadata"
      exports: ["dealChatSendRequestSchema"]
    - path: "apps/web/src/components/deals/deal-chat-thread.tsx"
      provides: "Persistent chat composer exposes transcript upload, selected-file state, and JSON payload submission"
      exports: ["DealChatThread"]
    - path: "apps/web/src/app/api/deals/[dealId]/chat/route.ts"
      provides: "Web proxy forwards uploaded transcript payloads through the existing deal-chat route family"
      exports: ["POST"]
    - path: "apps/agent/src/mastra/index.ts"
      provides: "Agent chat route accepts uploaded transcript content and passes it into the governed assistant flow"
      exports: ["POST /deals/:dealId/chat"]
  key_links:
    - from: "apps/web/src/components/deals/deal-chat-thread.tsx"
      to: "apps/web/src/app/api/deals/[dealId]/chat/route.ts"
      via: "client-side file read and structured send payload"
      pattern: "transcriptUpload|FileReader|text\(\)"
    - from: "apps/web/src/app/api/deals/[dealId]/chat/route.ts"
      to: "apps/agent/src/mastra/index.ts"
      via: "typed POST /deals/:dealId/chat proxy"
      pattern: "transcriptUpload"
    - from: "apps/agent/src/mastra/index.ts"
      to: "apps/agent/src/deal-chat/assistant.ts"
      via: "runDealChatTurn upload-aware request"
      pattern: "runDealChatTurn"
    - from: "packages/schemas/deal-chat.ts"
      to: "apps/web/src/app/api/deals/[dealId]/chat/route.ts"
      via: "shared request validation"
      pattern: "dealChatSendRequestSchema"
---

<objective>
Close the remaining Phase 45 verification gap by adding transcript upload support to the persistent deal chat without breaking the existing text, binding, and knowledge-query flow.

Purpose: Phase 45 is not done until sellers can upload transcript files as easily as they can paste notes, and those uploads still go through the same governed confirmation-first chat workflow.
Output: One gap-only follow-up that extends the shared send contract, web and agent chat routes, persistent composer UI, and focused regression coverage for uploaded transcript binding.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-persistent-ai-chat-bar/45-CONTEXT.md
@.planning/phases/45-persistent-ai-chat-bar/45-VERIFICATION.md
@.planning/phases/45-persistent-ai-chat-bar/45-02-SUMMARY.md
@.planning/phases/45-persistent-ai-chat-bar/45-03-SUMMARY.md
@.planning/phases/45-persistent-ai-chat-bar/45-05-SUMMARY.md
@packages/schemas/deal-chat.ts
@apps/web/src/components/deals/deal-chat-thread.tsx
@apps/web/src/app/api/deals/[dealId]/chat/route.ts
@apps/agent/src/mastra/index.ts

<interfaces>
From `packages/schemas/deal-chat.ts`:
```typescript
export const dealChatSendRequestSchema = z.object({
  dealId: z.string(),
  message: z.string().min(1),
  routeContext: dealChatRouteContextSchema,
  pendingBinding: dealChatBindingSchema.nullable().optional(),
  pendingRefineBeforeSave: dealChatRefineBeforeSaveSchema.nullable().optional(),
});
```

From `apps/web/src/components/deals/deal-chat-thread.tsx`:
```typescript
interface DealChatThreadProps {
  dealId: string;
  routeContext: DealChatRouteContext;
  initialMessages: DealChatMessageData[];
  greeting: string | null;
  suggestions: DealChatSuggestion[];
}

const response = await fetch(`/api/deals/${dealId}/chat`, {
  method: "POST",
  headers: { "Content-Type": "application/json" },
  body: JSON.stringify({ dealId, message: trimmed, routeContext }),
});
```

From `apps/web/src/app/api/deals/[dealId]/chat/route.ts`:
```typescript
const body = dealChatSendRequestSchema.safeParse({
  ...(await request.json()),
  dealId,
});
```

From `apps/agent/src/mastra/index.ts`:
```typescript
const parsed = dealChatSendRequestSchema.parse({
  ...body,
  dealId,
});

const result = await runDealChatTurn({
  dealId,
  message: parsed.message.trim(),
  routeContext: parsed.routeContext,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the shared deal-chat contract and route chain for uploaded transcript turns</name>
  <files>
    packages/schemas/deal-chat.ts,
    apps/agent/src/deal-chat/assistant.ts,
    apps/agent/src/deal-chat/__tests__/assistant.test.ts,
    apps/agent/src/mastra/index.ts,
    apps/agent/src/mastra/__tests__/deal-chat-routes.test.ts,
    apps/web/src/app/api/deals/[dealId]/chat/route.ts,
    apps/web/src/app/api/deals/[dealId]/chat/__tests__/route.test.ts
  </files>
  <behavior>
    - Test 1: a send payload can include uploaded transcript content plus filename and mime type without breaking existing text-only turns.
    - Test 2: the web proxy and agent route preserve uploaded transcript payloads end to end and still stream the final `---DEAL_CHAT_META---` response contract.
    - Test 3: uploaded transcript turns reach the same confirmation/refine-before-save logic as pasted transcript text, including messy-upload guidance before save.
  </behavior>
  <action>
    Update `packages/schemas/deal-chat.ts` so the shared send contract accepts a typed `transcriptUpload` payload on the existing chat request instead of creating a second upload-only endpoint. Use a structured object with file metadata plus normalized text content, and keep the current `routeContext`, `pendingBinding`, and `pendingRefineBeforeSave` fields intact. Preserve backward compatibility for plain text-only sends.

    Update `apps/web/src/app/api/deals/[dealId]/chat/route.ts` and `apps/agent/src/mastra/index.ts` to accept the extended request body and forward it unchanged through `POST /api/deals/[dealId]/chat` -> `POST /deals/:dealId/chat`. Do not switch this path to multipart form data; keep the existing JSON streaming route and accept uploaded transcript content after the browser has read the file into text. The response must remain the same text stream plus final `---DEAL_CHAT_META---` JSON payload.

    Update `apps/agent/src/deal-chat/assistant.ts` so uploaded transcript content is treated as transcript-save input inside the existing governed assistant flow. Use the uploaded text as the binding/refinement input, keep answer style rules unchanged, and preserve the current confirmation-first semantics: uploads from Overview or Briefing still ask whether the content belongs to a touch or to general deal notes, and messy uploads still require review before save.

    Add or expand route and assistant regression tests to prove upload-aware parsing, proxy forwarding, streamed meta preservation, and confirmation/refine behavior. Keep the implementation on the existing named-agent route family; do not invent a separate transcript-upload route namespace.
  </action>
  <verify>
    <automated>pnpm --filter agent exec vitest run "src/deal-chat/__tests__/assistant.test.ts" "src/mastra/__tests__/deal-chat-routes.test.ts" && pnpm --filter web exec vitest run "src/app/api/deals/[dealId]/chat/__tests__/route.test.ts"</automated>
  </verify>
  <done>The shared schema, web proxy, and agent route all accept uploaded transcript content and preserve the existing confirmation/refine chat contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transcript upload affordances to the persistent chat composer</name>
  <files>
    apps/web/src/components/deals/deal-chat-thread.tsx,
    apps/web/src/components/deals/__tests__/deal-chat-thread.test.tsx
  </files>
  <behavior>
    - Test 1: the persistent composer exposes an accessible transcript upload control, shows the selected file, and lets the seller clear or replace it before send.
    - Test 2: selecting a supported transcript file reads its text client-side and posts the new `transcriptUpload` payload through the existing `/api/deals/[dealId]/chat` send path.
    - Test 3: uploaded transcripts land in the same inline confirmation and refine-before-save UI as pasted transcript text, while unsupported or unreadable files show clear error feedback.
  </behavior>
  <action>
    Update `apps/web/src/components/deals/deal-chat-thread.tsx` so the composer includes a lightweight transcript upload affordance next to the existing textarea/send controls. Keep the dock-first layout compact and mobile-safe. The control must be keyboard-accessible, must expose the selected filename in the composer, and must let the seller remove or replace a selected file before sending.

    Read supported transcript files client-side into normalized text before posting. Default to text transcript formats (`text/*`, `.txt`, `.md`, `.srt`, `.vtt`) and reject binary/unreadable uploads with clear Sonner feedback rather than silently failing. Allow the seller to add optional typed instructions alongside the uploaded transcript so the assistant can refine or bind with page-aware context.

    When an upload is sent, keep the existing thread continuity: render an intelligible user turn for the upload, post the extended JSON payload to `/api/deals/[dealId]/chat`, and reuse the current streamed response parsing plus inline binding actions. Do not create a page-local upload modal or a separate transcript-only composer path.

    Expand `apps/web/src/components/deals/__tests__/deal-chat-thread.test.tsx` to cover file selection, payload shape, send/reset behavior, and the uploaded transcript confirmation/refine flow.
  </action>
  <verify>
    <automated>pnpm --filter web exec vitest run "src/components/deals/__tests__/deal-chat-thread.test.tsx"</automated>
  </verify>
  <done>The persistent deal chat lets sellers upload supported transcript files and send them through the same route-aware confirmation workflow as pasted transcript text.</done>
</task>

</tasks>

<verification>
1. Select a supported transcript file from the persistent deal chat, send it from Overview or Briefing, and confirm the assistant asks where it should be saved instead of auto-saving.
2. Upload a deliberately messy transcript snippet and confirm the assistant returns refine-before-save guidance before any binding confirmation completes.
3. Verify the existing text-only send flow, streamed metadata parsing, and knowledge-query cards still work after the upload contract change.
</verification>

<success_criteria>
- Sellers can upload transcript files from the persistent chat on any deal sub-page.
- Uploaded transcript content travels through the same shared schema, web proxy, and agent route family as existing text chat turns.
- CHAT-03 is closed without regressing the dock-first persistent chat experience, binding confirmations, or knowledge-query responses.
</success_criteria>

<output>
After completion, create `.planning/phases/45-persistent-ai-chat-bar/45-06-SUMMARY.md`
</output>
