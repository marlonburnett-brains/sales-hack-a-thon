---
phase: 42-deal-detail-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/ui/breadcrumb.tsx
  - apps/web/src/components/deals/deal-sidebar.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/page.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/loading.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/overview/page.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/briefing/page.tsx
  - apps/web/src/app/(authenticated)/deals/[dealId]/touch/[touchNumber]/page.tsx
  - apps/web/src/app/(authenticated)/templates/[id]/slides/page.tsx
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "User sees breadcrumbs (Deals > Company Name > Current Page) on any deal sub-page"
    - "User can click Deals breadcrumb to return to pipeline list"
    - "User sees a deal sidebar with links to Overview, Briefing, Touch 1-4"
    - "Each sidebar link navigates to its own sub-page"
    - "Touch sidebar items show status indicators (empty/dot/checkmark)"
    - "/deals/[id] redirects to /deals/[id]/overview"
    - "Existing review and asset-review sub-routes render correctly within the new layout"
  artifacts:
    - path: "apps/web/src/components/ui/breadcrumb.tsx"
      provides: "Shared reusable Breadcrumb component"
      exports: ["Breadcrumb", "BreadcrumbItem"]
    - path: "apps/web/src/components/deals/deal-sidebar.tsx"
      provides: "Deal-specific sidebar with nav links and touch status"
      exports: ["DealSidebar"]
    - path: "apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx"
      provides: "Nested layout rendering breadcrumbs + deal sidebar + children"
  key_links:
    - from: "deals/[dealId]/layout.tsx"
      to: "getDealAction"
      via: "server action import"
      pattern: "getDealAction"
    - from: "deals/[dealId]/layout.tsx"
      to: "DealSidebar"
      via: "component import"
      pattern: "DealSidebar"
    - from: "deals/[dealId]/layout.tsx"
      to: "Breadcrumb"
      via: "component import"
      pattern: "Breadcrumb"
    - from: "deals/[dealId]/page.tsx"
      to: "/deals/[dealId]/overview"
      via: "redirect()"
      pattern: "redirect.*overview"
---

<objective>
Build the deal detail navigation infrastructure: shared Breadcrumb component, deal-specific sidebar with touch status indicators, nested Next.js layout, route redirect, and placeholder sub-pages.

Purpose: Establishes the structural shell that all deal sub-pages (Overview, Briefing, Touch 1-4) render inside. Without this, no deal content pages can exist.
Output: Working nested layout with sidebar navigation, breadcrumbs, and route structure for all deal sub-pages.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-deal-detail-layout/42-CONTEXT.md
@.planning/phases/42-deal-detail-layout/42-RESEARCH.md
@apps/web/src/app/(authenticated)/deals/[dealId]/page.tsx
@apps/web/src/app/(authenticated)/deals/[dealId]/loading.tsx
@apps/web/src/components/sidebar.tsx
@apps/web/src/app/(authenticated)/templates/[id]/slides/page.tsx
@apps/web/src/lib/actions/deal-actions.ts

<interfaces>
<!-- Key types the executor needs -->

From apps/web/src/lib/api-client.ts:
```typescript
export interface Deal {
  id: string;
  companyId: string;
  company?: Company;
  name: string;
  salespersonName: string | null;
  salespersonPhoto: string | null;
  driveFolderId: string | null;
  status: string; // "open" | "won" | "lost" | "abandoned"
  ownerId: string | null;
  ownerEmail: string | null;
  ownerName: string | null;
  collaborators: string; // JSON string: [{id?, email, name?}]
  createdAt: string;
  updatedAt: string;
  interactions?: InteractionRecord[];
}

export interface InteractionRecord {
  id: string;
  dealId: string;
  touchType: string;
  status: string;
  inputs: string;
  decision: string | null;
  generatedContent: string | null;
  outputRefs: string | null;
  driveFileId: string | null;
  createdAt: string;
  updatedAt: string;
  feedbackSignals?: FeedbackSignal[];
  brief?: BriefRecord | null;
}
```

From apps/web/src/lib/actions/deal-actions.ts:
```typescript
export async function getDealAction(dealId: string): Promise<Deal | null>;
```

From apps/web/src/components/sidebar.tsx:
```typescript
// Global sidebar wraps children in:
// <div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">{children}</div>
// Deal layout renders INSIDE this container.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Breadcrumb component and DealSidebar component</name>
  <files>
    apps/web/src/components/ui/breadcrumb.tsx,
    apps/web/src/components/deals/deal-sidebar.tsx,
    apps/web/src/app/(authenticated)/templates/[id]/slides/page.tsx
  </files>
  <action>
1. Create `apps/web/src/components/ui/breadcrumb.tsx`:
   - Shared reusable Breadcrumb component matching the existing inline pattern from templates slides page.
   - Props: `items: BreadcrumbItem[]` (each with `label` and optional `href`), `current?: string` (non-link final crumb).
   - Uses `Link` from next/link, `ChevronRight` from lucide-react as separator.
   - Styling: `nav` with `aria-label="Breadcrumb"`, flex items-center gap-1, text-sm. Links: `text-slate-500 hover:text-slate-900 transition-colors`. Current: `font-medium text-slate-900`.
   - Export both `Breadcrumb` component and `BreadcrumbItem` interface.

2. Create `apps/web/src/components/deals/deal-sidebar.tsx` as a CLIENT component ("use client"):
   - Props: `deal: Deal` (import from `@/lib/api-client`), `dealId: string`.
   - Uses `usePathname()` from next/navigation to highlight active link.
   - Renders a vertical nav column with:
     - Optional header area: deal company name (truncated) and status badge using existing `Badge` component.
     - Nav items in order: Overview (`/deals/${dealId}/overview`), Briefing (`/deals/${dealId}/briefing`), then Touch 1-4 (`/deals/${dealId}/touch/1` through `/deals/${dealId}/touch/4`).
     - Active link: `bg-slate-100 font-medium text-slate-900`. Inactive: `text-slate-600 hover:bg-slate-50 hover:text-slate-900`. Match global sidebar styling patterns.
     - Touch items show status indicators: derive status from `deal.interactions` using the same logic as deal-card.tsx:
       - `not_started` (no interactions for that touch): no indicator (or faint empty circle)
       - `in_progress` (has interactions but none approved/edited/overridden/delivered): small dot (h-2 w-2 rounded-full bg-amber-400)
       - `completed` (has interaction with approved/edited/overridden/delivered status): checkmark icon (Check from lucide-react, h-3 w-3 text-green-600)
     - Width: `w-[200px]` with border-r border-slate-200, bg-white, full height.
     - Icons: use appropriate lucide icons -- LayoutDashboard for Overview, FileText for Briefing, numbered circles or Hash for touches.
   - Mobile: hidden by default (`hidden md:flex flex-col`). Mobile users navigate via breadcrumbs.

3. Update `apps/web/src/app/(authenticated)/templates/[id]/slides/page.tsx`:
   - Replace the inline breadcrumb HTML (nav element around line 55-64) with the new shared `Breadcrumb` component.
   - Import `{ Breadcrumb }` from `@/components/ui/breadcrumb`.
   - Replace: `<Breadcrumb items={[{ label: "Templates", href: "/templates" }]} current={templateName} />`.
   - Do the same for the second inline breadcrumb occurrence (around line 77 in the main return).
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -40</automated>
  </verify>
  <done>Breadcrumb component exists and is reusable. DealSidebar component exists with touch status indicators. Template slides page uses shared Breadcrumb. All typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create deal layout, redirect, and placeholder sub-pages</name>
  <files>
    apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx,
    apps/web/src/app/(authenticated)/deals/[dealId]/page.tsx,
    apps/web/src/app/(authenticated)/deals/[dealId]/loading.tsx,
    apps/web/src/app/(authenticated)/deals/[dealId]/overview/page.tsx,
    apps/web/src/app/(authenticated)/deals/[dealId]/briefing/page.tsx,
    apps/web/src/app/(authenticated)/deals/[dealId]/touch/[touchNumber]/page.tsx
  </files>
  <action>
1. Create `apps/web/src/app/(authenticated)/deals/[dealId]/layout.tsx`:
   - Server component (NO "use client").
   - Accepts `{ children, params: Promise<{ dealId: string }> }`.
   - Fetches deal via `getDealAction(dealId)`. If null, call `notFound()`.
   - Renders structure: flex container at full height.
     - `DealSidebar` on the left (pass deal and dealId).
     - Right side: `Breadcrumb` at top, then `{children}` below with `px-6 py-4`.
   - Breadcrumb items: `[{ label: "Deals", href: "/deals" }, { label: deal.company?.name ?? "Deal", href: `/deals/${dealId}/overview` }]`.
   - The `current` crumb is NOT set here -- each sub-page will determine its own title. Instead, the layout only provides the first two levels. Sub-pages can add their own page title above their content.
   - IMPORTANT: This layout renders INSIDE the global sidebar's `max-w-7xl px-4 py-6` container. The deal sidebar + content flex sits within that. Use negative margins if needed: `className="-mx-4 -my-6 sm:-mx-6 lg:-mx-8"` on the outer wrapper to reclaim the global padding, then add `min-h-[calc(100vh-3.5rem)]` for full height (3.5rem = mobile header).
   - Add `export const dynamic = "force-dynamic";` at the top.

2. Modify `apps/web/src/app/(authenticated)/deals/[dealId]/page.tsx`:
   - Replace the entire current monolithic page with a simple redirect.
   - Import `{ redirect }` from "next/navigation".
   - Accept `params: Promise<{ dealId: string }>`, await it, then `redirect(\`/deals/${dealId}/overview\`)`.
   - Remove ALL old imports (Badge, Separator, Alert, TouchFlowCard, InteractionTimeline, PreCallSection, etc.).

3. Update `apps/web/src/app/(authenticated)/deals/[dealId]/loading.tsx`:
   - Simplify to a layout-level skeleton: show a sidebar skeleton placeholder on the left and a content area skeleton on the right, rather than the old monolithic skeleton.

4. Create `apps/web/src/app/(authenticated)/deals/[dealId]/overview/page.tsx`:
   - Temporary placeholder: server component that renders a heading "Overview" with `<p>Overview content coming in Plan 02.</p>`.
   - Accept `params: Promise<{ dealId: string }>`.
   - Add `export const dynamic = "force-dynamic";`.

5. Create `apps/web/src/app/(authenticated)/deals/[dealId]/briefing/page.tsx`:
   - Temporary placeholder: server component that renders a heading "Briefing" with `<p>Briefing content coming in Plan 03.</p>`.
   - Accept `params: Promise<{ dealId: string }>`.
   - Add `export const dynamic = "force-dynamic";`.

6. Create `apps/web/src/app/(authenticated)/deals/[dealId]/touch/[touchNumber]/page.tsx`:
   - Placeholder for Phase 46. Server component.
   - Accept `params: Promise<{ dealId: string; touchNumber: string }>`.
   - Validate touchNumber is 1-4, otherwise `notFound()`.
   - Render heading "Touch {touchNumber}" with placeholder text "Touch workflow coming in Phase 46."
   - Add `export const dynamic = "force-dynamic";`.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -40 && echo "---" && cd apps/web && npx next build 2>&1 | tail -30</automated>
  </verify>
  <done>Navigating to /deals/[id] redirects to /deals/[id]/overview. Deal sidebar is visible with all 6 nav links. Breadcrumbs show "Deals > Company Name" on all sub-pages. Touch placeholder pages validate touch number 1-4. Existing review and asset-review routes inherit the new layout. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit --project apps/web/tsconfig.json` passes with no errors
2. `cd apps/web && npx next build` completes successfully
3. Navigate to `/deals/[id]` -- should redirect to `/deals/[id]/overview`
4. Breadcrumbs visible on overview, briefing, and touch pages
5. Deal sidebar shows all 6 links with correct active state highlighting
6. Touch items show status indicators based on interaction data
7. Template slides page still renders correctly with shared Breadcrumb
</verification>

<success_criteria>
- Shared Breadcrumb component exists at `components/ui/breadcrumb.tsx` and is used in both deal layout and template slides
- DealSidebar component exists with Overview, Briefing, Touch 1-4 links and status indicators
- Deal layout wraps all deal sub-pages with sidebar + breadcrumbs
- `/deals/[id]` redirects to `/deals/[id]/overview`
- All route placeholders exist for overview, briefing, and touch/[touchNumber]
- Existing sub-routes (review, asset-review) continue working
- TypeScript compiles clean, Next.js build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/42-deal-detail-layout/42-01-SUMMARY.md`
</output>
