---
phase: 33-slide-intelligence-foundation
plan: 03
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - apps/web/src/components/slide-viewer/classification-panel.tsx
  - apps/web/src/components/slide-viewer/element-map-panel.tsx
  - apps/web/src/lib/actions/slide-actions.ts
  - apps/agent/src/mastra/index.ts
autonomous: false
requirements: [SLI-02]

must_haves:
  truths:
    - "User opens per-template slide viewer and sees AI-generated description for each slide"
    - "Description shows purpose, visual composition, key content, and use cases in a collapsible section"
    - "Description section is above classification tags and expanded by default"
    - "Slides without descriptions show Generating description... placeholder"
    - "Element map data is visible in the slide viewer with shrinkable/expandable layout"
  artifacts:
    - path: "apps/web/src/components/slide-viewer/classification-panel.tsx"
      provides: "Collapsible description section above classification tags"
      contains: "description"
    - path: "apps/web/src/components/slide-viewer/element-map-panel.tsx"
      provides: "Element map viewer with expandable thumbnail layout"
      exports: ["ElementMapPanel"]
    - path: "apps/agent/src/mastra/index.ts"
      provides: "Updated slides endpoint returning description and elements"
      contains: "description"
  key_links:
    - from: "apps/web/src/components/slide-viewer/classification-panel.tsx"
      to: "slide-actions.ts"
      via: "fetches slide data including description"
      pattern: "description"
    - from: "apps/web/src/components/slide-viewer/element-map-panel.tsx"
      to: "agent API /templates/:id/slides"
      via: "element data returned with slide response"
      pattern: "elements"
---

<objective>
Display AI descriptions and element maps in the slide viewer UI.

Purpose: Users need to see the rich AI descriptions to understand slide purpose and content at a glance, and element maps to verify structural data for downstream AI-driven slide manipulation.

Output: Description section in ClassificationPanel, new ElementMapPanel component, updated API responses including description and element data.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-slide-intelligence-foundation/33-CONTEXT.md
@.planning/phases/33-slide-intelligence-foundation/33-RESEARCH.md
@.planning/phases/33-slide-intelligence-foundation/33-01-SUMMARY.md

@apps/web/src/components/slide-viewer/classification-panel.tsx
@apps/web/src/lib/actions/slide-actions.ts
@apps/agent/src/mastra/index.ts

<interfaces>
<!-- From Plan 01 outputs -->

SlideEmbedding now has:
```prisma
description String?  // JSON: { purpose, visualComposition, keyContent, useCases }
```

SlideElement model:
```prisma
model SlideElement {
  id          String @id @default(cuid())
  slideId     String
  elementId   String
  elementType String   // "shape" | "text" | "image" | "table" | "group"
  positionX   Float    // EMU
  positionY   Float    // EMU
  width       Float    // EMU
  height      Float    // EMU
  contentText String   @default("")
  fontSize    Float?
  fontColor   String?
  isBold      Boolean  @default(false)
}
```

Existing slide viewer patterns from classification-panel.tsx:
```typescript
interface ClassificationPanelProps {
  slide: SlideData;
  templateId: string;
  onUpdated: (slide: SlideData) => void;
  onFindSimilar?: (slideId: string) => void;
  isFindingSimilar?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API + description section + element map panel</name>
  <files>
    apps/agent/src/mastra/index.ts,
    apps/web/src/lib/actions/slide-actions.ts,
    apps/web/src/components/slide-viewer/classification-panel.tsx,
    apps/web/src/components/slide-viewer/element-map-panel.tsx
  </files>
  <action>
    1. **Update GET /templates/:id/slides endpoint** in mastra/index.ts:
       - Include `description` in the slide data returned (it's on SlideEmbedding, should come through automatically if using `findMany` without explicit select, but verify)
       - Include element data: for each slide, query `prisma.slideElement.findMany({ where: { slideId: slide.id }, orderBy: { positionY: 'asc' } })` and attach as `elements` array
       - If this makes the query too heavy, add a separate endpoint `GET /templates/:id/slides/:slideId/elements` -- but prefer including inline since the data is small per slide

    2. **Update slide-actions.ts**:
       - Extend `SlideData` type to include `description?: string | null` and `elements?: SlideElementData[]`
       - Define `SlideElementData` interface matching the Prisma model fields
       - Ensure the fetch functions pass this data through to components

    3. **Add description section to classification-panel.tsx**:
       - Add a collapsible section ABOVE the existing classification tags (per user decision: expanded by default)
       - Use shadcn/ui Collapsible component (or a simple disclosure with state)
       - Parse the description JSON string into { purpose, visualComposition, keyContent, useCases }
       - Render each field with a label and the text content:
         ```
         Purpose: [text]
         Visual Composition: [text]
         Key Content: [text]
         Use Cases: [text]
         ```
       - If description is null, show italic placeholder: "Generating description..." with a subtle pulse animation (per user decision)
       - Style with consistent typography: labels in font-medium text-sm text-muted-foreground, content in text-sm

    4. **Create element-map-panel.tsx** -- New component:
       - Export `ElementMapPanel` that receives `elements: SlideElementData[]`
       - Display as a structured list/table showing each element:
         - Type icon (shape/text/image/table/group) using lucide-react icons
         - Element ID (truncated)
         - Content text preview (first 50 chars)
         - Position info (converted from EMU to readable format, e.g., "x: 1.2in, y: 0.5in")
         - Styling info (font size, color swatch, bold indicator)
       - Make the panel shrinkable/expandable (per user decision): use a resizable/collapsible layout where the element list can be expanded to see full details or collapsed to a compact summary
       - Show element count in the header: "Elements (N)"
       - If no elements, show "No element data available"
       - Place this panel below the description section in the slide viewer (could be a tab or a separate collapsible section)

    5. **Wire element-map-panel into the slide viewer page**:
       - Import ElementMapPanel in the parent slide viewer component
       - Pass the slide's elements array to it
       - Position it in the sidebar/panel area alongside the classification panel
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx turbo build --filter=web -- 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Slide viewer shows AI description with 4 aspects in collapsible section above classification tags
    - Description section is expanded by default, shows placeholder for slides without descriptions
    - Element map panel shows structural data per slide with element type, position, content
    - Element map panel is expandable/collapsible
    - API returns description and elements data with slide responses
    - Web app builds without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete slide intelligence flow</name>
  <what-built>
    Complete slide intelligence pipeline and content classification UI:
    1. AI descriptions generated during ingestion (purpose, visual composition, key content, use cases)
    2. Element maps extracted and stored per slide
    3. Content classification (Template/Example) with touch binding
    4. Amber "Classify" badge on unclassified templates
    5. Description and element map display in slide viewer
    6. Backfill detection on agent startup
  </what-built>
  <how-to-verify>
    1. Navigate to Templates page -- verify unclassified templates show amber "Classify" badge
    2. Click a template card dropdown -> "Classify" -> select "Example" -> verify touch type selector appears and is required
    3. Classify a template as "Template" -> verify label appears on card
    4. Ingest a template (or re-ingest existing) -> wait for completion
    5. Open the slide viewer for the ingested template -> verify:
       a. Each slide shows AI description with Purpose, Visual Composition, Key Content, Use Cases
       b. Description section is collapsible and expanded by default
       c. Element map panel shows page elements with type, position, content
    6. For a slide without description yet, verify "Generating description..." placeholder shows
    7. Check agent logs for backfill detection on restart: "[backfill] Queued N templates..."
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Web app builds: `npx turbo build --filter=web`
2. Agent compiles: `cd apps/agent && npx tsc --noEmit`
3. Slide viewer displays description section above classification tags
4. Element map panel renders element data
5. Classification flow works end-to-end (card dropdown -> API -> database -> display)
</verification>

<success_criteria>
- AI descriptions visible in slide viewer with all 4 aspects (purpose, visual composition, key content, use cases)
- Element maps displayed per slide with structural data
- Description section is collapsible, expanded by default, with placeholder for pending descriptions
- All 9 phase requirements (SLI-01 through SLI-05, CCL-01 through CCL-04) satisfied
- User confirms visual verification of complete flow
</success_criteria>

<output>
After completion, create `.planning/phases/33-slide-intelligence-foundation/33-03-SUMMARY.md`
</output>
