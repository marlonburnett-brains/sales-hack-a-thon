---
phase: 33-slide-intelligence-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/lib/template-utils.ts
  - apps/web/src/lib/actions/template-actions.ts
  - apps/web/src/components/template-card.tsx
  - apps/web/src/components/slide-viewer/classification-panel.tsx
  - apps/web/src/components/template-status-badge.tsx
  - apps/agent/src/mastra/index.ts
autonomous: true
requirements: [CCL-01, CCL-02, CCL-03, CCL-04]

must_haves:
  truths:
    - "User can classify a presentation as Template or Example from the template card dropdown"
    - "User can classify a presentation from the template detail/viewer page"
    - "When classifying as Example, user must select at least one touch type before saving"
    - "Unclassified presentations show a Classify amber badge on template cards"
    - "Classification (Template/Example) is visible on template cards and detail views"
  artifacts:
    - path: "apps/web/src/lib/template-utils.ts"
      provides: "Classify status variant and classification helpers"
      contains: "classify"
    - path: "apps/web/src/lib/actions/template-actions.ts"
      provides: "Server action to classify template via agent API"
      contains: "classifyTemplate"
    - path: "apps/web/src/components/template-card.tsx"
      provides: "Classification dropdown item and badge display"
      contains: "Classify"
    - path: "apps/agent/src/mastra/index.ts"
      provides: "POST /templates/:id/classify API endpoint"
      contains: "classify"
  key_links:
    - from: "apps/web/src/components/template-card.tsx"
      to: "apps/web/src/lib/actions/template-actions.ts"
      via: "calls classifyTemplate server action"
      pattern: "classifyTemplate"
    - from: "apps/web/src/lib/actions/template-actions.ts"
      to: "agent API /templates/:id/classify"
      via: "fetch POST to agent"
      pattern: "templates.*classify"
    - from: "apps/agent/src/mastra/index.ts"
      to: "prisma.template.update"
      via: "updates contentClassification and touchTypes"
      pattern: "contentClassification"
---

<objective>
Add content classification (Template/Example) with touch binding UI and backend API.

Purpose: Users need to categorize presentations for Phase 34 (Deck Intelligence) which requires classified examples per touch type to infer deck structures. The "Classify" badge also signals action needed on unclassified content.

Output: New classify API endpoint, classification UI in template cards and detail views, amber "Classify" badge for unclassified presentations.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-slide-intelligence-foundation/33-CONTEXT.md
@.planning/phases/33-slide-intelligence-foundation/33-RESEARCH.md

@apps/web/src/lib/template-utils.ts
@apps/web/src/components/template-card.tsx
@apps/web/src/components/template-status-badge.tsx
@apps/web/src/components/slide-viewer/classification-panel.tsx
@apps/web/src/lib/actions/template-actions.ts
@apps/agent/src/mastra/index.ts

<interfaces>
<!-- Existing patterns the executor needs -->

From apps/web/src/lib/template-utils.ts:
```typescript
export type TemplateStatus = "ready" | "no_access" | "not_ingested" | "stale" | "ingesting" | "queued" | "failed";
export const STATUS_CONFIG: Record<TemplateStatus, { label: string; className: string }>;
export const TOUCH_TYPES = [
  { value: "touch_1", label: "Touch 1" },
  { value: "touch_2", label: "Touch 2" },
  { value: "touch_3", label: "Touch 3" },
  { value: "touch_4", label: "Touch 4+" },
] as const;
```

From apps/agent/src/mastra/index.ts:
```typescript
registerApiRoute("/templates/:id", { method: "GET", handler: ... });
registerApiRoute("/templates/:id/ingest", { method: "POST", handler: ... });
// Pattern: registerApiRoute(path, { method, handler })
```

Template model (from schema.prisma after Plan 01 migration):
```prisma
model Template {
  // ... existing fields ...
  contentClassification String?  // null | "template" | "example"
  touchTypes        String    // JSON array: ["touch_1", "touch_2"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classification API endpoint + status helpers</name>
  <files>
    apps/agent/src/mastra/index.ts,
    apps/web/src/lib/template-utils.ts,
    apps/web/src/lib/actions/template-actions.ts
  </files>
  <action>
    1. **Add POST /templates/:id/classify endpoint** in mastra/index.ts:
       - Register after the existing `/templates/:id/ingest` route
       - Accepts JSON body: `{ classification: "template" | "example", touchTypes?: string[] }`
       - Validation: if classification is "example", touchTypes must be non-empty array
       - If classification is "template", ignore touchTypes (keep existing touchTypes unchanged)
       - Update template: `prisma.template.update({ where: { id }, data: { contentClassification: classification, ...(touchTypes && { touchTypes: JSON.stringify(touchTypes) }) } })`
       - Return 200 with updated template
       - Return 400 if "example" without touchTypes
       - Return 404 if template not found

    2. **Also update GET /templates and GET /templates/:id** to include `contentClassification` in the response if not already returning all fields (check existing query -- Prisma findMany likely returns it automatically after migration, but verify the select/include if present).

    3. **Update template-utils.ts**:
       - Add "classify" to TemplateStatus union type: `| "classify"`
       - Update `getTemplateStatus()`: After checking ingestion states but before returning "ready", check if `template.contentClassification` is null AND the template has been ingested (lastIngestedAt is not null) -- return "classify" in that case
       - The function signature needs to accept `contentClassification?: string | null` in the template parameter
       - Add "classify" entry to STATUS_CONFIG:
         ```typescript
         classify: {
           label: "Classify",
           className: "bg-amber-100 text-amber-800 border-amber-200",
         },
         ```

    4. **Create classifyTemplate server action** in template-actions.ts:
       - Export `classifyTemplateAction(templateId: string, classification: "template" | "example", touchTypes?: string[]): Promise<{ success: boolean; error?: string }>`
       - POST to agent API `/templates/${templateId}/classify`
       - Handle errors, return structured result
       - Revalidate relevant paths after success (`revalidatePath`)
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx turbo build --filter=web -- 2>&1 | tail -20</automated>
  </verify>
  <done>
    - POST /templates/:id/classify endpoint validates and stores classification
    - template-utils.ts returns "classify" status for ingested templates with null contentClassification
    - STATUS_CONFIG has amber "Classify" badge entry
    - classifyTemplateAction server action calls agent API
  </done>
</task>

<task type="auto">
  <name>Task 2: Classification UI in template cards and detail views</name>
  <files>
    apps/web/src/components/template-card.tsx,
    apps/web/src/components/slide-viewer/classification-panel.tsx,
    apps/web/src/components/template-status-badge.tsx
  </files>
  <action>
    1. **Update template-card.tsx** -- Add classification dropdown and display:
       - Add "Classify" item to the existing actions dropdown menu (DropdownMenuItem with Tag icon from lucide-react)
       - Clicking opens a small inline classification dialog/popover (not a full modal -- keep it lightweight):
         - Two options: "Template" and "Example" as radio buttons or toggle group
         - When "Example" is selected, show TOUCH_TYPES as multi-select checkboxes (must select at least 1)
         - "Save" button calls `classifyTemplateAction`
         - Show loading state on save, toast on success/error
       - Display current classification on the card (if not null): small label like "Template" or "Example (Touch 1, 2)" near the status badge area
       - The amber "Classify" badge from STATUS_CONFIG will show automatically via the existing status badge component

    2. **Update classification-panel.tsx** (slide viewer detail page):
       - Add a "Content Classification" section at the top of the panel (before the existing classification tags)
       - Show current template classification state: "Template", "Example (Touch 1, Touch 3)", or "Unclassified"
       - If unclassified, show inline classify controls (same Template/Example + touch selector pattern as the card)
       - If already classified, show with an "Edit" button to reclassify
       - This is template-level (not slide-level), so it applies to the whole presentation

    3. **Update template-status-badge.tsx** (if it exists as a separate component):
       - Ensure it renders the "classify" status correctly from STATUS_CONFIG
       - The amber badge should be visually consistent with existing Ready/Stale/Failed badges
       - If this component doesn't exist separately (status badge is inline in template-card), ensure the template-card badge rendering handles the new "classify" status

    Per user decision: Use amber badge consistent with existing badge patterns. Touch type selector must be required when Example is selected -- can't save without selecting at least one touch.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && npx turbo build --filter=web -- 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Template cards show "Classify" dropdown action and current classification label
    - Amber "Classify" badge appears on unclassified ingested templates
    - Classification panel in slide viewer shows template classification with edit capability
    - Example classification requires touch type selection (cannot save without at least one)
    - Web app builds without errors
  </done>
</task>

</tasks>

<verification>
1. Web app builds: `cd apps/web && npx next build` (or turbo build)
2. Template card dropdown includes "Classify" action
3. STATUS_CONFIG includes "classify" variant with amber styling
4. classifyTemplateAction server action exists with proper error handling
5. Agent API has POST /templates/:id/classify route
</verification>

<success_criteria>
- Users can classify presentations as Template or Example from card dropdown and detail view
- Example classification requires touch type binding (at least one)
- Unclassified ingested presentations show amber "Classify" badge
- Classification is displayed on template cards and detail views
- All code compiles and follows existing UI patterns
</success_criteria>

<output>
After completion, create `.planning/phases/33-slide-intelligence-foundation/33-02-SUMMARY.md`
</output>
