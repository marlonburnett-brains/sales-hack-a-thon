---
phase: 34-deck-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/sidebar.tsx
  - apps/web/src/app/(authenticated)/settings/layout.tsx
  - apps/web/src/app/(authenticated)/settings/page.tsx
  - apps/web/src/app/(authenticated)/settings/deck-structures/page.tsx
  - apps/web/src/app/(authenticated)/settings/integrations/page.tsx
  - apps/web/src/components/settings/integrations-status.tsx
autonomous: true
requirements: [DKI-01, DKI-02]

must_haves:
  truths:
    - "User can see Settings link in the sidebar navigation with a Cog icon"
    - "User clicks Settings and lands on /settings/deck-structures (default sub-section)"
    - "User sees left vertical tabs for Deck Structures and Integrations sub-navigation"
    - "User navigates to Integrations and sees connection status cards for Google Workspace and AtlusAI"
  artifacts:
    - path: "apps/web/src/components/sidebar.tsx"
      provides: "Settings nav link at bottom of sidebar above collapse button"
      contains: "Settings"
    - path: "apps/web/src/app/(authenticated)/settings/layout.tsx"
      provides: "Settings layout with left vertical tabs navigation"
      contains: "settingsTabs"
    - path: "apps/web/src/app/(authenticated)/settings/page.tsx"
      provides: "Root settings page that redirects to deck-structures"
      min_lines: 5
    - path: "apps/web/src/app/(authenticated)/settings/deck-structures/page.tsx"
      provides: "Deck structures page shell (placeholder for Plan 03)"
      min_lines: 10
    - path: "apps/web/src/app/(authenticated)/settings/integrations/page.tsx"
      provides: "Integrations page with connection status cards"
      min_lines: 20
    - path: "apps/web/src/components/settings/integrations-status.tsx"
      provides: "Google Workspace and AtlusAI connection status card components"
      exports: ["IntegrationsStatus"]
  key_links:
    - from: "apps/web/src/components/sidebar.tsx"
      to: "/settings"
      via: "Settings Link nav item"
      pattern: "href.*settings"
    - from: "apps/web/src/app/(authenticated)/settings/layout.tsx"
      to: "/settings/deck-structures"
      via: "Vertical tab navigation links"
      pattern: "deck-structures.*integrations"
    - from: "apps/web/src/app/(authenticated)/settings/page.tsx"
      to: "/settings/deck-structures"
      via: "redirect"
      pattern: "redirect.*deck-structures"
---

<objective>
Create the Settings page shell with sidebar navigation entry and nested sub-navigation. Users can navigate to Settings from the sidebar, see vertical tabs for Deck Structures and Integrations, and view integration connection status cards.

Purpose: Establishes the routing and layout foundation that the deck structure display (Plan 03) will fill with content. Can be built in parallel with Plan 01 since it has no backend dependencies.
Output: Settings route group, sidebar link, vertical tab layout, integrations page with connection cards.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/34-deck-intelligence/34-CONTEXT.md
@.planning/phases/34-deck-intelligence/34-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/web/src/components/sidebar.tsx:
```typescript
// navItems array structure
const navItems = [
  { href: "/deals", label: "Deals", icon: Briefcase },
  { href: "/templates", label: "Templates", icon: LayoutTemplate },
  { href: "/slides", label: "Slide Library", icon: Layers },
  { href: "/discovery", label: "AtlusAI", icon: Brain },
  { href: "/actions", label: "Action Required", icon: AlertTriangle },
];

// Bottom section: collapse button + UserNav
// Settings link should be added BETWEEN nav and bottom section (above collapse button)
```

From apps/web/src/app/(authenticated)/layout.tsx (route group pattern):
```typescript
// All authenticated routes use the (authenticated) route group
// Settings goes under: apps/web/src/app/(authenticated)/settings/
```

Existing UI primitives available: Card, Badge, Progress (all from shadcn/ui)
Icons available: Settings (lucide-react), Layers, Plug
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar Settings link + Settings layout with vertical tabs</name>
  <files>
    apps/web/src/components/sidebar.tsx,
    apps/web/src/app/(authenticated)/settings/layout.tsx,
    apps/web/src/app/(authenticated)/settings/page.tsx
  </files>
  <action>
    1. **Add Settings link to sidebar.tsx:**
       - Import `Settings` icon from lucide-react.
       - Add a Settings link rendered SEPARATELY from the navItems map, positioned in the bottom section ABOVE the collapse button but BELOW the main nav. Use the same link styling as navItems (active state with pathname.startsWith("/settings"), same gap/padding/text classes). When collapsed, show just the icon with title="Settings". Per user decision: bottom of sidebar nav list, above collapse button, Cog/Settings icon.
       - The Settings link should use `onClick={closeMobile}` like other nav items for mobile support.

    2. **Create settings/layout.tsx** (client component):
       - "use client" directive at top.
       - Import Link from next/link, usePathname from next/navigation, Layers and Plug icons from lucide-react.
       - Define `settingsTabs` array: [{ href: "/settings/deck-structures", label: "Deck Structures", icon: Layers }, { href: "/settings/integrations", label: "Integrations", icon: Plug }].
       - Render a flex layout: narrow left column (w-48, shrink-0, border-r border-slate-200, pr-4) with the tab links, and a flex-1 right side for children. Per user decision: left vertical tabs pattern (GitHub/Linear style).
       - Each tab link: flex items-center gap-2, rounded-md px-3 py-2 text-sm. Active state (pathname.startsWith(href)): bg-slate-100 font-medium text-slate-900. Inactive: text-slate-600 hover:bg-slate-50.
       - Add a "Settings" heading (h1, text-2xl font-bold text-slate-900 mb-6) above the flex layout.
       - Ensure keyboard accessibility: focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 on tab links.

    3. **Create settings/page.tsx** (server component):
       - Import `redirect` from next/navigation.
       - Export default function that calls `redirect("/settings/deck-structures")`.
       - This ensures /settings always lands on the default sub-section.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Settings link visible in sidebar with Cog icon, clicking it navigates to /settings/deck-structures, vertical tabs show Deck Structures and Integrations sub-sections</done>
</task>

<task type="auto">
  <name>Task 2: Deck Structures placeholder page + Integrations page with connection cards</name>
  <files>
    apps/web/src/app/(authenticated)/settings/deck-structures/page.tsx,
    apps/web/src/app/(authenticated)/settings/integrations/page.tsx,
    apps/web/src/components/settings/integrations-status.tsx
  </files>
  <action>
    1. **Create deck-structures/page.tsx** (server component):
       - Simple page with heading "Deck Structures" (text-lg font-semibold text-slate-900 mb-4).
       - Placeholder paragraph: "AI-inferred deck structure analysis will appear here once examples are classified." wrapped in a rounded border-dashed border-2 border-slate-200 p-8 text-center text-slate-500 container.
       - This is a shell that Plan 03 will replace with the full deck structure display.

    2. **Create integrations-status.tsx** client component:
       - Import Card, CardContent, CardHeader, CardTitle from shadcn/ui Card. Import Badge from shadcn/ui. Import lucide-react icons (CheckCircle2, XCircle, ExternalLink).
       - `IntegrationsStatus` component that renders two cards side by side (grid grid-cols-1 md:grid-cols-2 gap-4):
         a. **Google Workspace card**: Shows "Google Workspace" title, a green "Connected" badge (if user has Google token) or red "Disconnected" badge. Display user email if connected. Show a "Manage in Google" external link button (opens Google account settings). For now, always show "Connected" status (read-only per user decision).
         b. **AtlusAI card**: Shows "AtlusAI" title, green "Connected" or amber "Not Connected" badge. Display account info if connected. Show "Manage AtlusAI" external link. For now, always show basic status.
       - Both cards are read-only per user decision. No reconnect actions that actually do anything -- just display status.
       - Use consistent card styling: border border-slate-200, rounded-lg, p-4.

    3. **Create integrations/page.tsx** (server component):
       - Import IntegrationsStatus component.
       - Page heading "Integrations" (text-lg font-semibold text-slate-900 mb-4).
       - Subtitle: "Manage external service connections." (text-sm text-slate-500 mb-6).
       - Render `<IntegrationsStatus />`.
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Deck structures page shows placeholder content, integrations page shows two connection status cards for Google Workspace and AtlusAI with appropriate badges</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes in apps/web
- /settings route redirects to /settings/deck-structures
- Sidebar shows Settings link with Cog icon at bottom above collapse button
- Vertical tabs navigate between Deck Structures and Integrations
- Integrations page shows two service status cards
</verification>

<success_criteria>
- Settings link appears in sidebar with conventional placement (bottom, above collapse)
- Settings page has left vertical tabs with Deck Structures and Integrations
- Root /settings redirects to /settings/deck-structures
- Integrations page shows read-only connection status cards for Google Workspace and AtlusAI
- All components follow existing project patterns (Tailwind, shadcn/ui, lucide-react)
</success_criteria>

<output>
After completion, create `.planning/phases/34-deck-intelligence/34-02-SUMMARY.md`
</output>
