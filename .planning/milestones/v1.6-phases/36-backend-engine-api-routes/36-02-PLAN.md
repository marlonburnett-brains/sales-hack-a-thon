---
phase: 36-backend-engine-api-routes
plan: 02
type: execute
wave: 2
depends_on:
  - 36-01
files_modified:
  - apps/agent/src/deck-intelligence/chat-refinement.ts
  - apps/agent/src/mastra/index.ts
  - apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts
  - apps/web/src/lib/api-client.ts
  - apps/web/src/lib/__tests__/api-client.deck-structures.test.ts
  - apps/web/src/app/api/deck-structures/chat/route.ts
  - apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts
autonomous: true
requirements:
  - DECK-05
must_haves:
  truths:
    - "GET /deck-structures exposes seven logical entries: Touch 1, Touch 2, Touch 3, Pre-call, Touch 4 Proposal, Touch 4 Talk Track, and Touch 4 FAQ."
    - "Touch 4 detail, infer, and chat requests fail fast when `artifactType` is missing and return the requested artifact row when present."
    - "Chat history, summaries, and `lastChatAt` remain isolated per `(touchType, artifactType)` row."
    - "Web helpers and the Next.js chat proxy can forward `artifactType` without introducing a second route family."
  artifacts:
    - path: "apps/agent/src/deck-intelligence/chat-refinement.ts"
      provides: "Artifact-aware deck chat lookup, persistence, summarization, and re-inference"
      exports: ["streamChatRefinement"]
    - path: "apps/agent/src/mastra/index.ts"
      provides: "Artifact-qualified seven-entry deck-structure list/detail/infer/chat route handling"
      contains: "registerApiRoute(\"/deck-structures\"")"
    - path: "apps/web/src/lib/api-client.ts"
      provides: "Optional artifactType-aware deck structure request helpers"
      exports: ["getDeckStructures", "getDeckStructure", "triggerDeckInference"]
    - path: "apps/web/src/app/api/deck-structures/chat/route.ts"
      provides: "Streaming proxy that forwards `artifactType` to the agent chat route"
      exports: ["POST"]
  key_links:
    - from: "apps/agent/src/mastra/index.ts"
      to: "apps/agent/src/deck-intelligence/deck-structure-key.ts"
      via: "route boundary validation"
      pattern: "resolveDeckStructureKey|artifactType"
    - from: "apps/agent/src/deck-intelligence/chat-refinement.ts"
      to: "apps/agent/src/deck-intelligence/infer-deck-structure.ts"
      via: "artifact-aware re-inference"
      pattern: "inferDeckStructure\("
    - from: "apps/web/src/app/api/deck-structures/chat/route.ts"
      to: "apps/agent/src/mastra/index.ts"
      via: "query-string passthrough"
      pattern: "artifactType"
---

<objective>
Wire artifact-aware deck-structure routes and chat end to end so the web and agent both operate on the correct Touch 4 artifact row.

Purpose: complete the Phase 36 route contract and per-artifact chat isolation that Phase 37 UI depends on.
Output: validated agent routes, artifact-scoped chat refinement, and web helpers/proxy support for `?artifactType=`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.6-phases/36-backend-engine-api-routes/36-CONTEXT.md
@.planning/milestones/v1.6-phases/36-backend-engine-api-routes/36-RESEARCH.md
@.planning/milestones/v1.5-phases/34-deck-intelligence/34-03-SUMMARY.md
@apps/agent/src/deck-intelligence/chat-refinement.ts
@apps/agent/src/mastra/index.ts
@apps/web/src/lib/api-client.ts
@apps/web/src/app/api/deck-structures/chat/route.ts

<interfaces>
From `apps/agent/src/deck-intelligence/chat-refinement.ts`:
```ts
export interface ChatRefinementResult {
  aiResponse: string;
  updatedStructure: DeckStructureOutput;
  diff: StructureDiff;
}

export async function streamChatRefinement(
  touchType: string,
  userMessage: string,
  onChunk: (text: string) => void,
): Promise<ChatRefinementResult>;
```

From `apps/web/src/lib/api-client.ts`:
```ts
export interface DeckStructureDetail {
  touchType: string;
  structure: { sections: DeckSectionData[]; sequenceRationale: string };
  chatMessages: DeckChatMessageData[];
}

export async function getDeckStructures(): Promise<DeckStructureSummary[]>;
export async function getDeckStructure(touchType: string): Promise<DeckStructureDetail>;
export async function triggerDeckInference(touchType: string): Promise<{ touchType: string; structure: unknown; confidence: number }>;
```

From `apps/web/src/app/api/deck-structures/chat/route.ts`:
```ts
export async function POST(request: NextRequest): Promise<Response>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make agent routes and chat artifact-aware</name>
  <files>apps/agent/src/deck-intelligence/chat-refinement.ts, apps/agent/src/mastra/index.ts, apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts</files>
  <behavior>
    - Test 1: `GET /deck-structures` returns exactly seven entries in this contract: Touch 1, Touch 2, Touch 3, Pre-call, Touch 4 Proposal, Touch 4 Talk Track, and Touch 4 FAQ, with the three Touch 4 summaries carrying `artifactType`.
    - Test 2: Touch 4 detail, infer, and chat route calls without `?artifactType=` return 400 with a clear validation error.
    - Test 3: Touch 4 detail, infer, and chat route calls with `?artifactType=proposal` resolve the proposal row and do not read `artifactType = null`.
    - Test 4: chat refinement reads and updates `chatContextJson`, `lastChatAt`, and chat messages on the matching artifact-specific row only.
  </behavior>
  <action>Update agent route handlers to parse `artifactType` from query params with Zod and the shared key resolver from Plan 01. `GET /deck-structures` must synthesize the exact seven-entry contract locked in `36-CONTEXT.md`: Touch 1, Touch 2, Touch 3, Pre-call, Touch 4 Proposal, Touch 4 Talk Track, and Touch 4 FAQ. `GET /deck-structures/:touchType`, `POST /deck-structures/:touchType/infer`, and `POST /deck-structures/:touchType/chat` must keep the current route family, reject missing `artifactType` for `touch_4`, ignore extra artifact params for non-touch-4, and use artifact-qualified row lookup. In `chat-refinement.ts`, thread `artifactType` through lookup, re-inference, save, summarization, and diff persistence so cross-artifact leakage is impossible.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run apps/agent/src/mastra/__tests__/deck-structure-routes.test.ts</automated>
  </verify>
  <done>The agent exposes the locked seven-entry artifact-aware API contract and all Touch 4 chat state is isolated per artifact row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread artifactType through web helpers and chat proxy</name>
  <files>apps/web/src/lib/api-client.ts, apps/web/src/lib/__tests__/api-client.deck-structures.test.ts, apps/web/src/app/api/deck-structures/chat/route.ts, apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts</files>
  <behavior>
    - Test 1: `getDeckStructure` and `triggerDeckInference` append `artifactType` via `URLSearchParams` when provided and omit it otherwise.
    - Test 2: deck-structure summary/detail typings expose `artifactType` so Phase 37 can distinguish the three Touch 4 rows cleanly.
    - Test 3: the Next.js chat proxy rejects missing `artifactType` for Touch 4 requests and forwards valid `artifactType` queries to the agent chat route while preserving the streaming delimiter protocol.
  </behavior>
  <action>Update the shared web API helpers to accept optional `artifactType`, expose it in relevant summary/detail response types, and construct query strings with `URLSearchParams` instead of manual concatenation. Update the Next.js deck chat proxy to accept `artifactType` in the request body, validate the Touch 4 requirement consistently with the agent contract, append the query string when calling the agent, and keep the existing streaming response passthrough unchanged apart from artifact routing. Do not invent new endpoint shapes; Phase 36 explicitly keeps `:touchType` plus query param.</action>
  <verify>
    <automated>pnpm --filter web exec vitest run apps/web/src/lib/__tests__/api-client.deck-structures.test.ts apps/web/src/app/api/deck-structures/chat/__tests__/route.test.ts</automated>
  </verify>
  <done>Phase 37 has artifact-aware web transport ready: typed summary/detail payloads, optional query threading, and a streaming chat proxy that preserves the existing protocol.</done>
</task>

</tasks>

<verification>
Run the agent and web Vitest commands from this plan, then confirm that `GET /deck-structures` returns exactly the seven named entries from the locked contract and every valid Touch 4 request path carries `artifactType` from web entrypoint to agent row lookup without changing the existing route family.
</verification>

<success_criteria>
- Agent list/detail/infer/chat routes honor `?artifactType=` and reject missing Touch 4 artifact keys.
- Chat persistence and summarization stay isolated per `(touchType, artifactType)` row.
- Web helpers and chat proxy can request the correct artifact-specific deck structure entry without extra route families.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.6-phases/36-backend-engine-api-routes/36-02-SUMMARY.md`
</output>
