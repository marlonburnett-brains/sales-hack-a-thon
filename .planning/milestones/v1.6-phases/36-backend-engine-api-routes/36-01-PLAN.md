---
phase: 36-backend-engine-api-routes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/src/deck-intelligence/deck-structure-key.ts
  - apps/agent/src/deck-intelligence/infer-deck-structure.ts
  - apps/agent/src/deck-intelligence/auto-infer-cron.ts
  - apps/agent/src/deck-intelligence/__tests__/deck-structure-key.test.ts
  - apps/agent/src/deck-intelligence/__tests__/infer-deck-structure.test.ts
  - apps/agent/src/deck-intelligence/__tests__/auto-infer-cron.test.ts
autonomous: true
requirements:
  - DECK-01
  - DECK-02
must_haves:
  truths:
    - "Each Touch 4 artifact produces and updates its own deck structure without mixing proposal, talk track, and FAQ examples."
    - "Cron refreshes Touch 1-3 plus each Touch 4 artifact independently, so one artifact can re-infer without blocking the others."
    - "When an artifact has no matching Touch 4 examples, the system still keeps an empty artifact-specific row instead of falling back to generic touch_4 behavior."
  artifacts:
    - path: "apps/agent/src/deck-intelligence/deck-structure-key.ts"
      provides: "Shared DeckStructureKey resolution, separate API vs cron key builders, and touch_4 validation helpers"
      exports: ["DeckStructureKey", "resolveDeckStructureKey", "getDeckStructureListKeys", "getDeckStructureCronKeys"]
    - path: "apps/agent/src/deck-intelligence/infer-deck-structure.ts"
      provides: "Artifact-aware example filtering, hash computation, and empty-row persistence"
      exports: ["computeDataHash", "inferDeckStructure"]
    - path: "apps/agent/src/deck-intelligence/auto-infer-cron.ts"
      provides: "Six-key inference loop with per-key hash and active-chat protection"
      exports: ["startDeckInferenceCron"]
  key_links:
    - from: "apps/agent/src/deck-intelligence/infer-deck-structure.ts"
      to: "apps/agent/src/deck-intelligence/deck-structure-key.ts"
      via: "shared key resolver"
      pattern: "resolveDeckStructureKey"
    - from: "apps/agent/src/deck-intelligence/auto-infer-cron.ts"
      to: "apps/agent/src/deck-intelligence/infer-deck-structure.ts"
      via: "artifact-qualified hash and inference calls"
      pattern: "computeDataHash\(|inferDeckStructure\("
---

<objective>
Create the artifact-aware backend keying foundation for deck inference and cron so Touch 4 behaves as three independent structures instead of one blocked generic path.

Purpose: satisfy the locked Phase 36 backend decisions before route and chat wiring builds on top.
Output: a shared deck-structure key contract, artifact-aware inference/hash logic, and cron coverage for all required deck keys.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.6-phases/36-backend-engine-api-routes/36-CONTEXT.md
@.planning/milestones/v1.6-phases/36-backend-engine-api-routes/36-RESEARCH.md
@.planning/milestones/v1.6-phases/35-schema-constants-foundation/35-02-SUMMARY.md
@.planning/milestones/v1.5-phases/34-deck-intelligence/34-01-SUMMARY.md
@apps/agent/src/deck-intelligence/infer-deck-structure.ts
@apps/agent/src/deck-intelligence/auto-infer-cron.ts
@packages/schemas/constants.ts

<interfaces>
From `packages/schemas/constants.ts`:
```ts
export const TOUCH_TYPES = ["touch_1", "touch_2", "touch_3", "touch_4", "pre_call"] as const;
export const ARTIFACT_TYPES = ["proposal", "talk_track", "faq"] as const;
export type ArtifactType = (typeof ARTIFACT_TYPES)[number];
```

From `apps/agent/src/deck-intelligence/infer-deck-structure.ts`:
```ts
export function isUnsupportedGenericTouch4(touchType: string, artifactType: string | null = null): boolean;
export async function computeDataHash(touchType: string): Promise<string>;
export async function inferDeckStructure(touchType: string, chatConstraints?: string): Promise<DeckStructureOutput>;
```

From `apps/agent/src/deck-intelligence/auto-infer-cron.ts`:
```ts
export function startDeckInferenceCron(): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared deck key contract</name>
  <files>apps/agent/src/deck-intelligence/deck-structure-key.ts, apps/agent/src/deck-intelligence/__tests__/deck-structure-key.test.ts</files>
  <behavior>
    - Test 1: `resolveDeckStructureKey("touch_4", "proposal")` returns `{ touchType: "touch_4", artifactType: "proposal" }`.
    - Test 2: `resolveDeckStructureKey("touch_4")` throws a clear validation error because Phase 36 locks touch_4 to required `artifactType`.
    - Test 3: `getDeckStructureListKeys()` returns the API/list contract with Touch 1, Touch 2, Touch 3, Pre-call, and three separate Touch 4 artifact entries.
    - Test 4: `getDeckStructureCronKeys()` returns the exact six-key cron contract from DECK-02 and excludes `pre_call`.
  </behavior>
  <action>Create `deck-structure-key.ts` as the shared backend contract for Phase 36. Export a `DeckStructureKey` type plus helpers that normalize `{ touchType, artifactType }`, require `artifactType` for `touch_4`, coerce non-touch-4 requests to `artifactType: null`, and expose two explicit key builders: one for API/list contexts that includes `pre_call`, and one for cron that returns only the six DECK-02 inference keys (Touch 1-3 plus Touch 4 Proposal/Talk Track/FAQ). Use `ARTIFACT_TYPES` from `@lumenalta/schemas`; do not hardcode artifact strings, do not let cron reuse the list/API key builder, and do not revive the generic touch_4 placeholder path.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run apps/agent/src/deck-intelligence/__tests__/deck-structure-key.test.ts</automated>
  </verify>
  <done>The repo has one canonical backend key resolver and tests prove touch_4 requires artifactType while non-touch-4 stays on null-artifact keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make inference and hashing artifact-aware</name>
  <files>apps/agent/src/deck-intelligence/infer-deck-structure.ts, apps/agent/src/deck-intelligence/__tests__/infer-deck-structure.test.ts</files>
  <behavior>
    - Test 1: Touch 4 example selection includes only rows whose `artifactType` exactly matches the requested artifact.
    - Test 2: Touch 4 template selection still allows all Touch 4 templates as secondary variation sources per locked decision.
    - Test 3: When a Touch 4 artifact has zero matching examples, inference persists an empty `(touch_4, artifactType)` row instead of falling back to `artifactType = null`.
    - Test 4: `computeDataHash()` changes when Touch 4 artifact-qualified example inputs differ and stays isolated per artifact.
  </behavior>
  <action>Refactor `inferDeckStructure` and `computeDataHash` to accept a `DeckStructureKey` or equivalent `{ touchType, artifactType }` input from Task 1. Keep current non-touch-4 behavior on `artifactType: null`, but for `touch_4` filter primary examples strictly by the requested artifact, allow all Touch 4 templates as secondary inputs, and persist/update rows with `findFirst` plus update/create semantics. Remove the Phase 35 generic-touch_4 early-return path from normal inference flow; keep only validation helpers for invalid generic requests. Make empty artifact-specific structures persist with stable rows and hashes.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run apps/agent/src/deck-intelligence/__tests__/infer-deck-structure.test.ts</automated>
  </verify>
  <done>Inference, empty persistence, and data hashing all operate on artifact-qualified keys and no valid Touch 4 artifact request can read or write the generic null-artifact row.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand cron to explicit deck keys</name>
  <files>apps/agent/src/deck-intelligence/auto-infer-cron.ts, apps/agent/src/deck-intelligence/__tests__/auto-infer-cron.test.ts</files>
  <behavior>
    - Test 1: cron iterates exactly the six DECK-02 inference keys and does not process `pre_call`.
    - Test 2: active-chat protection is evaluated against the matching `(touchType, artifactType)` row so Proposal activity does not block Talk Track or FAQ.
    - Test 3: one key failure is logged and the loop continues to remaining keys.
  </behavior>
  <action>Replace the touch-type-only cron loop with the cron-specific six-key builder from Task 1. For each key, load the matching deck structure row, compute the artifact-qualified hash from Task 2, respect `lastChatAt` per row, and call artifact-aware inference with existing `chatContextJson`. Keep the existing startup delay, interval, and continue-on-error behavior. Use the cron-specific key builder rather than filtering `TOUCH_TYPES` or reusing the API/list key builder, because DECK-02 excludes `pre_call` while Phase 36 still requires `pre_call` in the route list contract.</action>
  <verify>
    <automated>pnpm --filter agent exec vitest run apps/agent/src/deck-intelligence/__tests__/auto-infer-cron.test.ts</automated>
  </verify>
  <done>Cron processes exactly six independent inference keys instead of legacy touch types, with isolated hashes, active-chat windows, and failure handling per key, while leaving `pre_call` out of the cron loop.</done>
</task>

</tasks>

<verification>
Run the three agent Vitest files from this plan, then confirm the implementation no longer depends on the generic `touch_4` placeholder path for valid artifact-qualified inference or cron work and that the cron key contract is exactly six keys with `pre_call` excluded.
</verification>

<success_criteria>
- `inferDeckStructure()` and `computeDataHash()` resolve artifact-qualified Touch 4 keys.
- Empty `(touch_4, artifactType)` rows are persisted without generic fallback.
- Cron iterates exactly six inference keys, excludes `pre_call`, and isolates hash/chat protection per row.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.6-phases/36-backend-engine-api-routes/36-01-SUMMARY.md`
</output>
