---
phase: 35-schema-constants-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/agent/prisma/schema.prisma
  - apps/agent/prisma/migrations/YYYYMMDDHHMMSS_add_artifact_type_foundation/migration.sql
  - apps/agent/src/deck-intelligence/infer-deck-structure.ts
  - apps/agent/src/deck-intelligence/auto-infer-cron.ts
  - apps/agent/src/deck-intelligence/chat-refinement.ts
  - apps/agent/src/mastra/index.ts
autonomous: true
requirements: [SCHM-01, SCHM-02]
must_haves:
  truths:
    - "Templates can store a nullable artifactType without changing existing rows"
    - "Deck structures can store nullable artifactType and preserve one non-Touch-4 null row per touch type"
    - "The legacy generic touch_4 deck structure row is removed and current runtime code does not recreate it"
  artifacts:
    - path: "apps/agent/prisma/schema.prisma"
      provides: "Nullable artifactType columns and composite deck structure identity"
      contains: "artifactType"
    - path: "apps/agent/prisma/migrations/YYYYMMDDHHMMSS_add_artifact_type_foundation/migration.sql"
      provides: "Forward-only migration with idempotent cleanup and DB hardening"
      contains: "DeckStructure_non_touch4_null_artifact_key"
    - path: "apps/agent/src/deck-intelligence/infer-deck-structure.ts"
      provides: "Legacy non-Touch-4 upserts keyed by touchType + null artifactType"
      contains: "artifactType: null"
    - path: "apps/agent/src/mastra/index.ts"
      provides: "Current deck structure endpoints remain stable without generic touch_4 recreation"
      contains: "touch_4"
  key_links:
    - from: "apps/agent/prisma/migrations/YYYYMMDDHHMMSS_add_artifact_type_foundation/migration.sql"
      to: "DeckStructure"
      via: "idempotent delete + partial unique index"
      pattern: "DeckStructure_non_touch4_null_artifact_key"
    - from: "apps/agent/src/deck-intelligence/infer-deck-structure.ts"
      to: "prisma.deckStructure"
      via: "composite unique key using null artifactType"
      pattern: "touchType_artifactType"
    - from: "apps/agent/src/deck-intelligence/auto-infer-cron.ts"
      to: "touch_4"
      via: "temporary exclusion until artifact-aware inference lands in Phase 36"
      pattern: "tt !== \"touch_4\""
---

<objective>
Add the Phase 35 database contract for artifact-typed Touch 4 structures and keep the current v1.5 runtime from recreating invalid generic data.

Purpose: The schema must support artifact-type classification now, while the existing deck-intelligence engine remains safe until Phase 36 threads `artifactType` through inference and API behavior.
Output: Prisma schema changes, forward-only migration SQL, and compatibility guards for current deck-structure runtime code.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.6-phases/35-schema-constants-foundation/35-CONTEXT.md
@.planning/milestones/v1.6-phases/35-schema-constants-foundation/35-RESEARCH.md
@.planning/milestones/v1.5-phases/34-deck-intelligence/34-01-SUMMARY.md
@.planning/milestones/v1.5-phases/34-deck-intelligence/34-03-SUMMARY.md
@apps/agent/prisma/schema.prisma
@apps/agent/src/deck-intelligence/infer-deck-structure.ts
@apps/agent/src/deck-intelligence/auto-infer-cron.ts
@apps/agent/src/deck-intelligence/chat-refinement.ts

<interfaces>
From apps/agent/prisma/schema.prisma:
```prisma
model Template {
  id                    String   @id @default(cuid())
  touchTypes            String
  contentClassification String?
}

model DeckStructure {
  id              String   @id @default(cuid())
  touchType       String   @unique
  structureJson   String
  exampleCount    Int      @default(0)
  confidence      Float    @default(0)
  chatContextJson String?
  dataHash        String?
  inferredAt      DateTime @default(now())
  lastChatAt      DateTime?
  chatMessages    DeckChatMessage[]
}
```

From apps/agent/src/deck-intelligence/infer-deck-structure.ts:
```typescript
export async function inferDeckStructure(
  touchType: string,
  chatConstraints?: string,
): Promise<DeckStructureOutput>;

await prisma.deckStructure.upsert({
  where: { touchType },
  create: { touchType, ... },
  update: { ... },
});
```

From apps/agent/src/deck-intelligence/auto-infer-cron.ts:
```typescript
const DECK_TOUCH_TYPES = TOUCH_TYPES.filter((tt) => tt !== "pre_call");
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nullable artifactType fields and hardened migration SQL</name>
  <files>apps/agent/prisma/schema.prisma, apps/agent/prisma/migrations/YYYYMMDDHHMMSS_add_artifact_type_foundation/migration.sql</files>
  <action>Update the Prisma schema so `Template` gains nullable `artifactType String?` and `DeckStructure` gains nullable `artifactType String?` while replacing `touchType @unique` with `@@unique([touchType, artifactType])`. Keep these fields as strings, not Prisma enums, because the repo standard is shared TS constants plus string-backed DB fields. Generate the migration with `prisma migrate dev --name add_artifact_type_foundation --create-only`, then inspect and harden the SQL before applying. The migration must be forward-only and idempotent: delete the existing generic `touch_4` row where `artifactType IS NULL`, never seed new Touch 4 rows, never backfill `Template.artifactType`, drop the old single-column uniqueness safely, add the composite uniqueness, add a partial unique index that preserves one non-Touch-4 null-artifact row per touch type, and add DB-level `CHECK` constraints so only `proposal`, `talk_track`, `faq`, or `NULL` are accepted. If migration drift appears, use the project’s forward-only resolve pattern; never reset and never use `prisma db push`.</action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && pnpm exec prisma validate && rg -n 'artifactType|DeckStructure_non_touch4_null_artifact_key|CHECK|DELETE FROM "DeckStructure"' prisma/schema.prisma prisma/migrations/*/migration.sql</automated>
  </verify>
  <done>The Prisma schema exposes nullable artifactType on Template and DeckStructure, and the committed migration removes the legacy generic touch_4 row while hardening the intended uniqueness semantics for null artifact rows.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve current deck-intelligence runtime without recreating generic Touch 4 data</name>
  <files>apps/agent/src/deck-intelligence/infer-deck-structure.ts, apps/agent/src/deck-intelligence/auto-infer-cron.ts, apps/agent/src/deck-intelligence/chat-refinement.ts, apps/agent/src/mastra/index.ts</files>
  <action>Make the minimum code changes required so the current Phase 34 deck-intelligence runtime still compiles and does not recreate a forbidden generic Touch 4 row after the schema change. Update every current `DeckStructure` read/write that assumes unique `touchType` to use the composite key with `artifactType: null` for the legacy non-Touch-4 paths. Exclude `touch_4` from the current cron loop and add an explicit guard anywhere the old runtime would infer or chat against generic Touch 4 data; return the existing placeholder/empty-state shape instead of writing a catch-all row. Keep list/detail route behavior stable for the current web UI by surfacing only null-artifact legacy rows in Phase 35. Do not implement artifact-aware inference, six-key API responses, or Touch 4 artifact tabs here — that belongs to Phase 36/37. This task exists purely to keep the codebase safe between the schema migration and the later artifact-aware backend work.</action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && pnpm exec tsc --noEmit --pretty false && rg -n 'touchType_artifactType|artifactType: null|tt !== "touch_4"|touch_4' src/deck-intelligence src/mastra/index.ts</automated>
  </verify>
  <done>Agent code compiles against the new Prisma model identity, non-Touch-4 deck structures still work through `artifactType: null`, and current runtime paths no longer recreate a generic Touch 4 deck structure row.</done>
</task>

</tasks>

<verification>
- `pnpm exec prisma validate` passes in `apps/agent`
- `pnpm exec tsc --noEmit --pretty false` passes in `apps/agent`
- Migration SQL contains idempotent Touch 4 cleanup plus the partial unique index for non-Touch-4 null artifact rows
- Current deck-intelligence runtime paths use the composite key with `artifactType: null` or explicitly skip generic Touch 4
</verification>

<success_criteria>
- `Template.artifactType` exists as a nullable column with no backfill
- `DeckStructure.artifactType` exists as a nullable column with composite uniqueness on `(touchType, artifactType)`
- The old generic `touch_4` `DeckStructure` row is removed during migration and not recreated by current runtime code
- Existing non-Touch-4 deck-structure behavior stays intact until Phase 36 adds artifact-aware logic
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.6-phases/35-schema-constants-foundation/35-02-SUMMARY.md`
</output>
