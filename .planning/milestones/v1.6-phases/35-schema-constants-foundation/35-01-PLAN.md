---
phase: 35-schema-constants-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/schemas/constants.ts
  - packages/schemas/index.ts
autonomous: true
requirements: [SCHM-03]
must_haves:
  truths:
    - "Both apps can import the canonical artifact type values from @lumenalta/schemas"
    - "Artifact labels are shared with the same Proposal → Talk Track → FAQ order the user locked"
    - "No app needs to hardcode proposal/talk_track/faq strings outside the shared schemas package"
  artifacts:
    - path: "packages/schemas/constants.ts"
      provides: "Canonical artifact type values, labels, and exported ArtifactType type"
      contains: "ARTIFACT_TYPES"
    - path: "packages/schemas/index.ts"
      provides: "Barrel exports for artifact type constants and types"
      contains: "ARTIFACT_TYPES"
  key_links:
    - from: "packages/schemas/index.ts"
      to: "packages/schemas/constants.ts"
      via: "barrel re-export"
      pattern: "ARTIFACT_TYPES"
---

<objective>
Publish the shared artifact-type contract for v1.6 in `@lumenalta/schemas`.

Purpose: Downstream agent and web work must consume one source of truth for stored values and UI labels instead of duplicating strings.
Output: Shared artifact type constants, labels, and type exports.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/milestones/v1.6-phases/35-schema-constants-foundation/35-CONTEXT.md
@.planning/milestones/v1.6-phases/35-schema-constants-foundation/35-RESEARCH.md
@packages/schemas/constants.ts
@packages/schemas/index.ts

<interfaces>
From packages/schemas/constants.ts:
```typescript
export const TOUCH_TYPES = [
  "touch_1",
  "touch_2",
  "touch_3",
  "touch_4",
  "pre_call",
] as const;

export const ACTION_TYPES = {
  REAUTH_NEEDED: 'reauth_needed',
  SHARE_WITH_SA: 'share_with_sa',
  DRIVE_ACCESS: 'drive_access',
  ATLUS_ACCOUNT_REQUIRED: 'atlus_account_required',
  ATLUS_PROJECT_REQUIRED: 'atlus_project_required',
} as const;
```

From packages/schemas/index.ts:
```typescript
export {
  INDUSTRIES,
  FUNNEL_STAGES,
  CONTENT_TYPES,
  SLIDE_CATEGORIES,
  BUYER_PERSONAS,
  TOUCH_TYPES,
  SUBSECTORS,
  SOLUTION_PILLARS,
  ACTION_TYPES,
  type ActionType,
} from "./constants.ts";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical artifact type constants to shared schemas</name>
  <files>packages/schemas/constants.ts</files>
  <action>Add the locked artifact subtype vocabulary to the shared constants package. Export `ARTIFACT_TYPES` exactly as `['proposal', 'talk_track', 'faq'] as const` in that order, keep the stored values in `snake_case`, add `ArtifactType` from the tuple, and add a friendly-label export that maps those raw values to `Proposal`, `Talk Track`, and `FAQ`. Include an ordered export that preserves the canonical UI order if it helps downstream consumption, but do not introduce a Prisma enum, database-specific code, or app-local duplicates. This task exists to satisfy the user decision that raw values and display labels both live in `@lumenalta/schemas`.</action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && pnpm exec tsx -e "import { ARTIFACT_TYPES, ARTIFACT_TYPE_LABELS } from '@lumenalta/schemas'; console.log(JSON.stringify({ ARTIFACT_TYPES, ARTIFACT_TYPE_LABELS }));"</automated>
  </verify>
  <done>`packages/schemas/constants.ts` exports the three canonical artifact values and their friendly labels with the locked order preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Re-export the artifact contract from the schemas barrel</name>
  <files>packages/schemas/index.ts</files>
  <action>Update the package barrel so both apps can import the new artifact constants and types from `@lumenalta/schemas` without reaching into internal file paths. Re-export every new symbol created in Task 1 from `./constants.ts`. Do not move existing exports or rename the package surface; this is a narrow additive contract change for downstream phases.</action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon/apps/agent && pnpm exec tsx -e "import { ARTIFACT_TYPES, ARTIFACT_TYPE_LABELS, type ArtifactType } from '@lumenalta/schemas'; const first: ArtifactType = ARTIFACT_TYPES[0]; console.log(first, ARTIFACT_TYPE_LABELS[first]);"</automated>
  </verify>
  <done>`@lumenalta/schemas` exposes the artifact values, labels, and type aliases through the public barrel exports.</done>
</task>

</tasks>

<verification>
- Importing `ARTIFACT_TYPES` and `ARTIFACT_TYPE_LABELS` from `@lumenalta/schemas` succeeds from the agent workspace.
- The exported raw values are exactly `proposal`, `talk_track`, and `faq` in canonical order.
</verification>

<success_criteria>
- `ARTIFACT_TYPES` is importable from `@lumenalta/schemas`
- Friendly labels are exported from the same package surface
- Downstream phases can type against `ArtifactType` without hardcoding strings
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.6-phases/35-schema-constants-foundation/35-01-SUMMARY.md`
</output>
