---
phase: 16-google-oauth-login-wall
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - apps/web/src/app/login/page.tsx
  - apps/web/src/components/user-nav.tsx
  - apps/web/src/app/(authenticated)/layout.tsx
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05

must_haves:
  truths:
    - "Login page shows briefcase icon, 'Lumenalta Sales' branding, subtitle, and 'Sign in with Google' button"
    - "Domain rejection error message displays on login page for non-@lumenalta.com users"
    - "Nav bar shows user's Google avatar as circular image on the right side"
    - "Clicking avatar reveals dropdown with display name, email, and 'Sign out' button"
    - "Clicking 'Sign out' ends session and redirects to login page"
  artifacts:
    - path: "apps/web/src/app/login/page.tsx"
      provides: "Login page with Google OAuth sign-in"
      min_lines: 30
    - path: "apps/web/src/components/user-nav.tsx"
      provides: "Avatar dropdown with user info and sign-out"
      min_lines: 30
    - path: "apps/web/src/app/(authenticated)/layout.tsx"
      provides: "Authenticated layout with nav bar and UserNav"
  key_links:
    - from: "apps/web/src/app/login/page.tsx"
      to: "apps/web/src/lib/supabase/client.ts"
      via: "signInWithOAuth call"
      pattern: "signInWithOAuth.*google.*hd.*lumenalta"
    - from: "apps/web/src/components/user-nav.tsx"
      to: "apps/web/src/lib/actions/auth.ts"
      via: "signOut server action"
      pattern: "signOut"
    - from: "apps/web/src/app/(authenticated)/layout.tsx"
      to: "apps/web/src/components/user-nav.tsx"
      via: "renders UserNav with user data"
      pattern: "UserNav.*user"

user_setup:
  - service: supabase-auth
    why: "Google OAuth requires Supabase Auth provider configuration"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Settings -> API -> anon/public key (or publishable key)"
    dashboard_config:
      - task: "Enable Google provider in Supabase Auth"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google"
      - task: "Add Google OAuth Client ID and Secret"
        location: "Supabase Dashboard -> Authentication -> Providers -> Google -> Client ID / Secret"
  - service: google-cloud-oauth
    why: "Google OAuth consent screen and credentials must be configured"
    dashboard_config:
      - task: "Create OAuth 2.0 Client ID (Web application)"
        location: "Google Cloud Console -> APIs & Services -> Credentials -> Create Credentials -> OAuth Client ID"
      - task: "Add authorized redirect URI"
        location: "Set redirect URI to: https://<supabase-project-ref>.supabase.co/auth/v1/callback"
      - task: "Configure OAuth consent screen"
        location: "Google Cloud Console -> APIs & Services -> OAuth consent screen"
---

<objective>
Create the login page with Google OAuth sign-in, the UserNav avatar dropdown component, and update the authenticated layout to display user info in the nav bar. Add shadcn dropdown-menu and avatar components.

Purpose: Deliver the user-facing auth experience -- the login gate and authenticated nav bar -- consuming the auth infrastructure from Plan 01.

Output: Login page at /login, UserNav component, enhanced authenticated layout with user avatar.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-google-oauth-login-wall/16-CONTEXT.md
@.planning/phases/16-google-oauth-login-wall/16-RESEARCH.md
@.planning/phases/16-google-oauth-login-wall/16-01-SUMMARY.md

<interfaces>
<!-- Contracts created by Plan 01 that this plan consumes -->

From apps/web/src/lib/supabase/client.ts (created by Plan 01):
```typescript
import { createBrowserClient } from '@supabase/ssr'

export function createClient() {
  return createBrowserClient(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
}
```

From apps/web/src/lib/supabase/server.ts (created by Plan 01):
```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'

export async function createClient(): Promise<SupabaseClient>
```

From apps/web/src/lib/actions/auth.ts (created by Plan 01):
```typescript
'use server'
export async function signOut(): Promise<never>  // redirects to /login
```

From apps/web/src/app/(authenticated)/layout.tsx (created by Plan 01 -- to be enhanced):
```typescript
// Currently has basic nav bar with Briefcase icon + "Lumenalta Sales"
// This plan adds UserNav component to the right side of the nav bar
export default async function AuthenticatedLayout({ children }: { children: React.ReactNode })
```

Existing shadcn components available (no install needed):
- Button (apps/web/src/components/ui/button.tsx)
- Skeleton (apps/web/src/components/ui/skeleton.tsx)

Existing icons available (lucide-react ^0.576.0):
- Briefcase, LogOut -- already used/available

Components to install via shadcn CLI:
- DropdownMenu (@radix-ui/react-dropdown-menu)
- Avatar (@radix-ui/react-avatar)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components, create login page and UserNav, update authenticated layout</name>
  <files>
    apps/web/src/app/login/page.tsx,
    apps/web/src/components/user-nav.tsx,
    apps/web/src/app/(authenticated)/layout.tsx
  </files>
  <action>
1. Install shadcn dropdown-menu and avatar components:
   ```
   cd apps/web && npx shadcn@latest add dropdown-menu avatar --yes
   ```

2. Create `apps/web/src/app/login/page.tsx` (client component):
   - 'use client' directive (needs useSearchParams and onClick handler)
   - Import: createClient from '@/lib/supabase/client', Button from '@/components/ui/button', Briefcase from 'lucide-react', useSearchParams from 'next/navigation'
   - Wrap the component body that uses useSearchParams in a Suspense boundary. The page component itself should use Suspense to wrap the inner content that calls useSearchParams (Next.js 15 requires this).
   - Read `error` and `next` from searchParams. Default `next` to `/deals`.
   - Layout per locked decisions:
     - Full-height centered flex container: `min-h-screen items-center justify-center bg-white`
     - Briefcase icon: `h-8 w-8 text-blue-600` with "Lumenalta Sales" text `text-2xl font-semibold text-slate-900`
     - Subtitle: "Agentic Sales Orchestration" in `text-sm text-muted-foreground`
     - If `error === 'domain'`: show message "Access is restricted to @lumenalta.com accounts. Please sign in with your Lumenalta Google account." in `text-sm text-destructive max-w-sm text-center`
     - If `error === 'auth'`: show generic "Sign-in failed. Please try again." message
     - "Sign in with Google" button: `variant="outline" size="lg"` -- use the shadcn Button
     - Below button: "@lumenalta.com accounts only" in `text-xs text-muted-foreground`
   - handleGoogleSignIn function:
     - Creates browser Supabase client
     - Calls `supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: \`\${window.location.origin}/auth/callback?next=\${encodeURIComponent(next)}\`, queryParams: { hd: 'lumenalta.com' } } })`
     - The `hd` parameter is a UX hint only (filters Google account picker) -- real enforcement is in the callback route (Plan 01)
   - No nav bar on this page (it renders under root layout which has no nav)
   - Add a Google "G" icon or use a plain text button. Per research, a simple outline button with text is sufficient. Include a small inline SVG Google "G" logo before the text for recognition if straightforward, otherwise plain text is acceptable.

3. Create `apps/web/src/components/user-nav.tsx` (client component):
   - 'use client' directive (needs dropdown interaction)
   - Import: Avatar + AvatarFallback + AvatarImage from '@/components/ui/avatar', DropdownMenu + DropdownMenuContent + DropdownMenuItem + DropdownMenuLabel + DropdownMenuSeparator + DropdownMenuTrigger from '@/components/ui/dropdown-menu', LogOut from 'lucide-react', signOut from '@/lib/actions/auth'
   - Props interface: `{ user: { name: string; email: string; avatarUrl: string } }`
   - Avatar trigger: circular button with `<Avatar className="h-8 w-8">`, `<AvatarImage>` with user.avatarUrl, `<AvatarFallback>` with first letter of name uppercased
   - Dropdown content (align="end", w-56):
     - DropdownMenuLabel: display name (text-sm font-medium) + email (text-xs text-muted-foreground)
     - DropdownMenuSeparator
     - DropdownMenuItem with LogOut icon + "Sign out" text, onClick calls signOut()
   - No other items in dropdown per locked decisions (v1.1 scope)
   - Add `cursor-pointer` to the avatar trigger button per UI/UX skill rules

4. Update `apps/web/src/app/(authenticated)/layout.tsx`:
   - Make it an async Server Component
   - Import createClient from '@/lib/supabase/server'
   - At the top of the component, call `const supabase = await createClient()` then `const { data: { user } } = await supabase.auth.getUser()`
   - If no user, this shouldn't happen (middleware redirects), but as a safety net redirect to /login
   - Extract user metadata for UserNav: `user.user_metadata.full_name` (name), `user.email` (email), `user.user_metadata.avatar_url` (avatarUrl)
   - Render nav bar:
     - Same sticky nav styling as current: `sticky top-0 z-50 border-b bg-white/95 backdrop-blur`
     - Left side: Briefcase icon + "Lumenalta Sales" link to /deals
     - Right side: `<UserNav user={{ name, email, avatarUrl }} />`
     - Use `flex items-center justify-between` to space left/right nav items
   - Render `<main className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">{children}</main>`
  </action>
  <verify>
    <automated>cd /Users/marlonburnett/source/lumenalta-hackathon && test -f apps/web/src/app/login/page.tsx && test -f apps/web/src/components/user-nav.tsx && test -f apps/web/src/components/ui/dropdown-menu.tsx && test -f apps/web/src/components/ui/avatar.tsx && pnpm --filter web build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Login page at /login with briefcase branding, subtitle, Google sign-in button, domain-only note
    - Domain rejection error message displays when ?error=domain is present
    - UserNav component renders avatar dropdown with name, email, sign-out
    - Authenticated layout shows nav bar with Briefcase branding (left) and UserNav (right)
    - shadcn dropdown-menu and avatar components installed
    - Build passes cleanly
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete auth flow end-to-end</name>
  <action>
    Human verifies the complete Google OAuth login wall with domain restriction, session persistence, and sign-out. Covers login page, middleware route protection, OAuth callback with @lumenalta.com enforcement, nav bar avatar dropdown, and sign-out.

    **Prerequisites:** Ensure these are configured before testing:
    1. Supabase Dashboard: Google provider enabled with OAuth Client ID and Secret
    2. Google Cloud Console: OAuth Client ID created with redirect URI set to `https://<supabase-project-ref>.supabase.co/auth/v1/callback`
    3. `.env.local` in apps/web has `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_ANON_KEY`
    4. Run `cd apps/web && pnpm dev`

    **Test AUTH-03 (Route protection):**
    1. Open an incognito browser window
    2. Navigate to http://localhost:3000/deals
    3. Expected: Redirected to /login?next=%2Fdeals
    4. Navigate to http://localhost:3000/deals/some-id
    5. Expected: Redirected to /login?next=%2Fdeals%2Fsome-id

    **Test AUTH-01 (Google sign-in):**
    6. On the login page, verify: briefcase icon, "Lumenalta Sales", "Agentic Sales Orchestration" subtitle, "Sign in with Google" button, "@lumenalta.com accounts only" note
    7. Click "Sign in with Google"
    8. Expected: Google account picker appears, showing @lumenalta.com accounts (due to hd parameter)
    9. Sign in with a @lumenalta.com account
    10. Expected: Redirected to /deals (or the original return URL)

    **Test AUTH-02 (Domain rejection):**
    11. Sign out (via avatar dropdown)
    12. Click "Sign in with Google" and choose a non-@lumenalta.com account (if possible via Google picker)
    13. Expected: Redirected to /login with message "Access is restricted to @lumenalta.com accounts..."

    **Test AUTH-04 (Session persistence):**
    14. While signed in, refresh the browser (Cmd+R)
    15. Expected: Still on the same page, still signed in
    16. Open a new tab, navigate to http://localhost:3000
    17. Expected: Lands on /deals without login prompt

    **Test AUTH-05 (Sign out):**
    18. Click the avatar in the top-right corner of the nav bar
    19. Expected: Dropdown shows your display name, email, and "Sign out" button
    20. Click "Sign out"
    21. Expected: Redirected to /login page immediately (no confirmation dialog)
    22. Try navigating to /deals
    23. Expected: Redirected back to /login
  </action>
  <verify>Manual verification -- all 5 AUTH requirements tested in browser per steps above</verify>
  <done>All 5 AUTH requirements (AUTH-01 through AUTH-05) pass manual testing. User types "approved" to confirm.</done>
</task>

</tasks>

<verification>
1. `pnpm --filter web build` succeeds
2. Login page renders at /login with all required branding elements
3. UserNav dropdown shows user info and sign-out
4. Full auth flow works: unauthenticated -> login -> Google OAuth -> callback -> deals
5. Domain rejection works: non-@lumenalta.com -> signed out -> error message on login page
6. Session persists across browser refresh
7. Sign-out clears session and redirects to login
</verification>

<success_criteria>
All 5 AUTH requirements (AUTH-01 through AUTH-05) verified working in browser:
- AUTH-01: @lumenalta.com Google sign-in succeeds
- AUTH-02: Non-lumenalta.com rejected with clear message
- AUTH-03: Unauthenticated users redirected to /login with return URL
- AUTH-04: Session persists across refresh
- AUTH-05: Sign out works and redirects to /login
</success_criteria>

<output>
After completion, create `.planning/phases/16-google-oauth-login-wall/16-02-SUMMARY.md`
</output>
